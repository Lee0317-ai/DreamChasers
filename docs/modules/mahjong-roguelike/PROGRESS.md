# 胡了卜进展

## 2026-08-03

- 补齐 T244 `390x844` production 交互 smoke：真实点击入槽、手动碰牌、首层清台与通关弹窗均通过，游戏控制台无 warn/error。
- 第二层成功触发两组吃牌候选 `4筒 5筒 6筒` / `5筒 6筒 7筒`；选择态刷新后候选原样恢复，选择右侧候选后只结算该 exact candidate，未误删保留的 `4筒`。
- 验收基线提交 `a17a37ee` 已生成 exact-commit production build `a17a37ee7c3b-20260803T042725Z`；最终分支 HEAD 也重新完成 build、授权后的 verify-only 和 5 条 HTTP smoke。最终 build ID 以生成 manifest 为准，T244 验收完成。

## 2026-07-30

- 完成 T246 胡了卜 Cocos v1 M2 三节点完整纵切设计，并获 Lee 书面批准；随后完成 T247 代码级实施计划，尚未开始 M2 实现代码。
- M2 主目标固定为新玩家无需外部说明，在 `8–12` 分钟内完成 `Boot → Title → 碰教学 → 吃/多候选教学 → 奖励三选一 → mini Boss → Result`。
- 采用独立 `Boot / Title / Game / Result` 四 Scene；Game 内奖励、多候选、Boss 介绍、暂停和设置使用正式 Modal Prefab，不采用单场景多层或丰富局外首页。
- mini Boss 强制 `完成一次杠 + 清空牌山`，胡保留为可选高价值操作；强制胡教学和完整十节点章节留到 M3。
- M2 建立真实 AppFlowController、Prefab、checkpoint、AudioService 与 settings 边界；音频先使用 1 首有许可记录的临时 BGM 和 8 类关键 SFX，最终视听品质留到 M4。
- 正式规格已写入 `docs/superpowers/specs/2026-07-30-hulebu-cocos-v1-m2-three-node-vertical-slice-design.md`；可执行计划已写入 `docs/superpowers/plans/2026-07-30-hulebu-cocos-v1-m2-three-node-vertical-slice.md`。计划按 App Flow/Settings/四 Scene、Prefab/薄适配器、三节点内容/Coordinator、恢复/音频、production E2E 五批推进；T244 竖屏 production 完整交互 smoke 是 Batch 1 前置门槛。

## 2026-07-29

- 完成 T245 胡了卜 Cocos 快照真实路径修复：正式构建在校验与物化 source snapshot 时统一使用 `realpath`，symlink worktree 不再产生错误根路径；发布回归测试通过 `189/189`。
- T244 M1 核心架构实现和两轮独立评审已收口：恢复链路会深校验牌山、卡槽、备用槽、牌河、明牌区、道具、历史快照、奖励和事件上下文；v0 存档只迁移一次；奖励、事件、组合选择和多次撤回恢复保持精确状态。
- T244 聚焦验证通过：`hulebu-cocos-domain + mahjong-cocos-project` 共 `158/158`，Cocos domain TypeScript 检查通过，最终评审为 `0 Critical / 0 Important`。
- 精确提交 `1bc4867cf56919b1230307297d9d4600b4f6bb4f` 已完成 Creator 3.8.8 production build 和 verify-only；build id 为 `1bc4867cf569-20260729T150509Z`，源码树摘要为 `69077b638241d0213957351cd7954a09edefb3ac2f92449d6c1fe9f82e9e3194`，产物摘要为 `0124635db2711a0b752c05be796d1232593cc71385fb6e00ed15a2048d3dc5b6`，5 条 HTTP smoke 均为 `200`。
- 正式包浏览器验证确认：桌面与 `390x844` 均非黑屏、无控制台 warning/error；点击牌后刷新可恢复相同局内状态，移动端 HUD、牌山、组合栏、卡槽和工具栏完整可见。
- Lee 已确认正式发布方向按微信小程序常见竖屏体验验收，`1280x720` 横屏不再属于当前目标，不登记横屏适配任务，也不修改 Binder/UI/layout。
- T244 转为待验收：既有 `390x844` production 证据覆盖完整 UI 可见、选牌与刷新恢复；组合、多候选 exact choice 和一次清关仍需在可访问本地正式包的浏览器会话中补做，本轮不提前标记完成。
## 2026-07-01

- 接续 T239 文档：确认 Cocos `pyramid` 牌山的验收口径应从“集中成三角轮廓”改为“真实逐层收拢结构”。底层、中层、顶层需要各自拥有独立 footprint，越上层横向跨度越窄，并由生成器计算出的上层覆盖关系写入 `blockedBy`。
- T239 后续测试建议钉住四点：至少 3 层、底层 x 跨度大于顶层、顶层平均 y 高于底层、低层节点存在来自更高层的 blocker。截图验收则看首局牌山是否呈现底宽顶窄坡面，而不是平面模板点循环堆叠。
- 并发风险已记录：来源 Codex 线程仍在进行 T239 代码修正，本次只续写文档，不改 Cocos 生成器代码，避免覆盖原线程改动。

## 2026-06-28

- 完成 T238：Cocos 首局牌山新增中央堆叠 `pyramid` 模板，前 5 关优先使用更接近目标图的三角牌山，后续关卡继续轮换原有 Graph 模板。
- T238 顺手拆开 HUD 顶部记牌器与余牌进度：记牌器回到关卡牌匾下方只做牌型计数，`余牌 42` 改成独立牌匾，避免文字压在记牌器贴图上。
- T238 已用 Cocos Web Mobile 构建产物和 Playwright 进入真实 `playing` 状态检查竖屏与 390px 移动端，截图为 `tmp-hulebu-cocos-t238c-portrait.png` 和 `tmp-hulebu-cocos-t238c-mobile.png`；Cocos CLI 仍在显示 build finished 后返回退出码 36。
- 完成 T237：Cocos 局内可玩性和观感继续重排；麻将牌进一步缩到 `38x51`，牌山缩放上限收紧，组合按钮缩小并下移，底部槽位压进背景木托盘，右侧工具按钮继续保持顶层但减少侵占。
- T237 使用 `debug=false` Cocos Web Mobile 构建产物截图检查，竖屏和 390px 移动端均已去掉 profiler 干扰，画面从“太丑且玩不了”推进到更干净的可玩局内布局。
- 完成 T236：按反馈继续精修 Cocos 局内比例和 HUD 层级；麻将牌从 `52x70` 收到 `44x59`，底部额外托盘层已隐藏/移除调用，右侧 `洗牌 / 撤回 / 提示` 挂到 Canvas 顶层的 `ToolOverlayRoot`，记牌器移动到关卡牌匾下方。
- T236 已用 Cocos Web Mobile 构建产物和 Playwright 截图检查竖屏与 390px 移动端，四项反馈均已落到画面；debug 构建仍会显示 Cocos profiler 覆盖层。
- 完成 T235：Cocos Web 预览已通过 Playwright 进入真实 `playing` 状态做截图 QA；修正 runtime layout 使用 Cocos `visibleSize` 作为世界尺寸，茶室背景居中，首局牌山缩放收紧，组合动作条改为跟随底部槽位上方，整体从图1的绿色空底/错位状态推进到接近图2的茶桌局内构图。
- T235 验证通过：`npm run test -w packages/shared -- mahjong-cocos-project`、`npx tsc --noEmit -p apps/game/mahjong-roguelike/cocos/hulebu-cocos-3.8.8/tsconfig.json`、Cocos Web Mobile 构建产物 + Playwright 截图检查；Cocos CLI 仍会在显示 build finished 后返回退出码 36。
- 完成 T232：Cocos 正式工程已接入茶室桌面整屏背景，补齐目标概念图里最明显缺失的场景氛围层。
- 背景资源已归档到 `output/hulebu-ui-assets/hulebu-scene-background-v1/`，运行时资源位于 Cocos `assets/resources/ui/v6/backgrounds/`；`GameSceneController` 新增 `SceneBackgroundArt`，加载成功后隐藏旧程序化绿桌底板，加载失败时回退旧底板。
- 完成 T233：Cocos 奖励三选一已从小按钮弹窗调成更大的三张奖励卡呈现，标题和副标题上移，卡牌尺寸提升到 `106x120`。
- 完成 T234：Cocos 右侧工具按钮角标现在会从 runtime `hud.toolText` 动态同步洗牌、撤回和打牌次数，不再固定显示 `3`。
- 完成 T231：Cocos `ui/v6` 资源树已补齐奖励卡、场景皮肤卡、弹层底板和面板资源；顶部牌匾、奖励选择卡和流程弹层会优先加载真实 Sprite。

- 修正 `6条` UI 牌面：原 `tile_bamboo_06.png` 视觉上像内嵌小牌，且和 `4条` 在小尺寸下容易混淆；现在按参考图改为 `4条` 同款竹节元件的三列两排布局，基于 v7 标准空白底牌重组，沿用同一套牌体、底色、竹节笔触和阴影风格。
- 已同步源原型与站内静态 Demo：`apps/game/mahjong-roguelike/prototypes/config-playable/assets/ui-v6/tiles/mahjong/bamboo/tile_bamboo_06.png` 和 `apps/web/public/games/hulebu-demo/assets/ui-v6/tiles/mahjong/bamboo/tile_bamboo_06.png`。
- 完成 T198：Cocos 正式工程已接入关前特殊事件基础流程，第 `6 / 8 / 10 / 14 / 18` 关前会进入事件三选一；第一版事件覆盖 `路遇老雀 / 旧牌匣 / 暗灯牌局 / 封盘押后`，分别对应本关铜钱、撤回补给、禁看山和禁洗牌。
- 完成 T199：Cocos 正式工程已新增长期模式入口基础，配置层提供 `mainline / endless / daily` run profile 和关卡映射 helper；Controller 可启动主线、无尽和每日 run，并把展示层数与实际复用的关卡配置分开。
- 完成 T200：Cocos 正式工程已接入局外成长基础状态，`HulebuMetaUpgradeState` 覆盖备用槽、护符、初始工具、河道扩容、开局铜钱和看山预置；runtime 会把这些加成合入 HUD、槽位容量、牌河容量和工具次数。
- 完成 T201：Cocos 正式工程已接入本局流派基础状态，第一版包含 `顺吃流 / 碰开流 / 开杠流 / 追胡流 / 道具流 / 信息流`；runtime 会把流派加成合入工具、铜钱和组合得分，Controller 提供 `selectRunArchetype()` 供后续开局 UI 调用。
- 完成 T202：Cocos 正式工程已接入开局流派选择 UI，新 run 启动前会弹出六个流派选项；点击后选择本局流派并进入对应模式首关。
- 完成 T203：Cocos 正式工程已接入局外模式入口，默认启动先显示 `主线 / 无尽 / 每日` 三个模式按钮；选择模式后进入本局流派选择，再进入对应模式首关。
- 完成 T204：Cocos 正式工程已接入局外成长 UI，大厅 `升级` 入口可进入六轴成长面板；点击备用槽、护符、工具、牌河、铜钱、看山会更新局外成长状态并影响下一局 runtime。
- 完成 T205：Cocos 正式工程已接入高阶周目入口基础，配置层新增 `advanced` run mode 和东风场、南风场、西风场、北风场四档 profile；大厅 `高阶` 入口可选择风场，再进入本局流派选择。
- 完成 T206：Cocos 正式工程已接入局外铜钱和升级成本闭环，Controller 本地持有铜钱；通关回大厅前发放铜钱，局外成长面板会显示等级和价格，升级会扣费并受上限约束。

## 2026-06-22

- 创建 T185 胡了卜 Web 数值平衡和内容冻结第一轮，并先收“特殊事件后半程断档”这一刀。
- T185 继续把晚段奖励往“兑现而不是解释”收口：主线 `18+` 新增 `胡分 / 杠分 / 工具包` 的收官偏置，高阶第 `16 / 18` 档也从 `看山` 改成 `工具包`，后段更像整理手牌和封口，而不是继续补信息件。
- T185 又继续把每日终章前一拍收进同一口径：每日第 `18` 档也从 `看山` 收成 `工具包`，现在第 `18 / 20` 两档都会更像整手和封口，而不是在终章前再补信息件。
- 主线特殊事件触发已从 `6 / 8 / 10 / 13 / 16 / 19` 扩成 `6 / 8 / 10 / 12 / 14 / 16 / 18 / 20`，让主线后半程持续有事件分叉，而不是只在前半段和尾声各露几次面。
- 每日牌局事件触发同步扩成 `4 / 7 / 10 / 12 / 14 / 16 / 18 / 20`，高阶周目同步扩成 `5 / 8 / 11 / 13 / 15 / 17 / 19 / 20` 对应关前节点。
- 主线后半段新增分段事件偏置：先推信息 / 护路，再推顺手追胡和风险抬注，中段再进挡墙续压，终段再推牌尾收束和北风封尾，后半程内容味道不再混成一团。
- 无尽长跑新增 `chapterIndex >= 5 && chapterStep === 3` 的额外事件点，长期 run 后段从每章 3 次事件抬到 4 次，长跑不会越往后越安静。
- 每日和高阶后半段事件也继续补了分段偏置：每日第 `12 / 14 / 16 / 18 / 20` 关前会更明显地往信息、续压、挡墙、封口和终局封尾推进；高阶第 `11 / 13 / 15 / 17 / 19 / 20` 关前也补进对应偏置，后半程不再只是把前段事件重复播一遍。
- 主线终章前最后一档奖励已从纯容错倾向改成 `胡分 / 看山 / 工具` 的收官包；每日和高阶第 20 档奖励偏置也同步改成这组口味，终局前会更像在补最后一刀，而不是继续发护符和位子。
- 无尽后段奖励继续收口：长跑 `chapterStep === 3` 的奖励改成 `胡分 / 看山 / 铜钱`，高章节第 `6 / 7` 相位的章节偏置也改成更偏 `收口 / 读口 / 整手` 的组合，后段不会一直只给续命件。
- 这轮又把晚段内容再推向收官：主线第 `19` 关、每日第 `20` 关和高阶第 `19` 档的后段事件从 `探牌 / 试口` 压向 `锁门 / 封尾`，无尽第 `7` 章的回账主题奖励把 `洗牌` 换成 `工具包`，终局前后更像 build 落成而不是继续拿信息过渡。
- 每日后段两档主题词缀也继续收口：`挡墙续押` 不再发信息件拖中段，而是改成更明确的 `胡/杠` 收官包；`封尾落锁` 也从 `胡/碰 + 丢弃补刀` 收成 `胡/杠 + 工具包`，让每日后段更像主题终局局。
- 后段路线奖励也继续拉开了：`胡流` 更偏终口兑现，`信息流` 更偏读口转化，`河控流` 更偏控河转压，`道具流` 更偏整理节奏，`救场流` 则回到补位续命；无尽 / 每日 / 高阶三条线的后段 build 味道不再只是换个顺序的同一组奖励。
- 第 `17 / 18 / 19` 关主线牌山压力继续收了一刀：牌量从 `336 / 348 / 360` 收到 `330 / 340 / 348`，字牌权重从 `68 / 74 / 82` 收到 `64 / 70 / 76`，孤张预算也各回落一格，终章前的压力改成持续绷紧而不是最后三关一起顶满。
- `胡了卜王` 终章 Boss 继续收口：朋友试玩终章仍保留复合目标，但把 `碰 2` 收到 `碰 1`、积分目标从 `180` 收到 `170`，让终章更像 build 检查而不是把所有轴同时卡死。
- 无尽后段 Boss 也继续收了一格：后段章节守层的记分线再从 `170 + 章节 * 20` 收到 `168 + 章节 * 18`，同时第 8 章这类本来就自带 `胡 / 杠` 的终局章节不再额外叠第二层 `杠 2`；`吃` 额外检查只留在第 7 相位，不再把第 8 相位做成四种组合一起压桌。
- 每日终局 Boss 同步收口：每日第 20 关仍保留词缀驱动的终章变体，但 `碰` 目标压回 `1`，得分目标上限再压到 `190`，避免当天词缀把终章再抬成比主线更硬的一档。
- 主线终章前两档再继续收了一格：第 `18 / 19` 关牌量进一步从 `340 / 348` 收到 `336 / 344`，字牌权重从 `70 / 76` 收到 `68 / 72`，孤张预算从 `9 / 10` 收到 `8 / 9`，让终章门前和终章本体的压力递进更顺，不再像连续两脚一起踩满。
- 每日终局 Boss 继续去掉机械叠题：第 20 关现在只再挂 `1` 个今日词缀重点组合，不再默认把词缀重点叠成 `2` 个额外目标，终局会更像“当天主题版 Boss”，不是再额外做一张词缀考卷。
- 无尽第 `7 / 8` 相位也继续去掉机械补题：章节主题已经带双组合目标时，不再额外挂通用 `碰 1`，守层压力更多来自章节身份本身。
- 无尽后段章节 Boss 记分线继续从 `168 + 章节 * 18` 收到 `166 + 章节 * 17`，让后段逼分仍在，但不会和章节主题、胡杠目标一起把结尾压到太死。
- 主线第 `17 / 18 / 19` 关再收一刀：当前压成 `328 / 334 / 340` 张、字牌权重 `62 / 66 / 70`、孤张预算 `7 / 8 / 8`，终章门前和终章本体之间的递进更顺。
- 主线和每日终局 Boss 同步再收：`胡了卜王` 现在把 `杠` 目标也压回 `1` 且得分线为 `166`；每日第 20 关最终得分线卡进 `186` 内；每日第 `16` 档奖励改成 `胡钱 / 整手 / 工具` 过渡包。
- 无尽高章节守层分数线继续从 `166 + 章节 * 17` 收到 `164 + 章节 * 16`，让长跑后段仍逼分，但少一点机械补题感。
- 本轮把代码、静态副本和回归也一起重新钉到这组冻结值：朋友试玩主线第 `17 / 18 / 19` 关现为 `328 / 334 / 340` 张、字牌权重 `62 / 66 / 70`、孤张预算 `7 / 8 / 8`；无尽后段 Boss 守层分数线为 `164 + 章节 * 16`。
- 创建 T184 胡了卜路线奖励和长期成长深化。
- 创建 T186 胡了卜局外成长与局内流派开局重构规划。
- 把原先只停留在路线图里的长期成长第二轮正式收口为六轴：`备用槽 / 满槽护符 / 初始道具 / 河道扩容 / 开局铜钱 / 看山预置`。
- `/games/hulebu` 升级页新增路线 `当前收益 / 下一步` 状态卡，右侧预览也同步补了 `路线当前收益 / 路线下一步`，让路线挂载不再只藏在参数和局内偏置里。
- 已确认新的主线产品结构：长期成长依赖局外加点、资源、解锁和携带能力；每一局真正的打法则在开局前通过流派技能选择决定。后续奖励、事件和 Boss 检查都应围绕“本局已选流派”偏置，而不是默认玩家会跨多关长成同一条路线。
- T186 继续落到壳层：`/games/hulebu` 的 `局内流派` 面板已补前 20 关引导式解锁状态，`吃 / 碰` 起步可选，`杠 / 胡 / 道具 / 信息` 会随主线进度逐步开放；20 关后以及无尽、每日、高阶显示自由选择口径。
- T186 内层奖励池也继续收口：`preferredRoute` 的基础偏好池现在只轻补一张协同奖励，不再把中后段路线 bias 直接拼进去；中后段奖励加权继续由本局 `runArchetype` 主导。
- T186 事件和终章 Boss 口径也继续收口：事件池中 `preferredRoute` 只轻补一个基础偏好事件，高阶事件不再额外挂旧局外偏好；终章 Boss 的压力说明改用本局 `runArchetype` 流派标签，不再读旧路线挂载。
- T186 最终术语扫尾已完成：外层自动候选统一改成 `自动偏好`，内层 Boss 阶段目标改成 `本局流派主骨`，避免旧 `自动挂载 / 当前路线主骨` 文案继续暗示路线挂载是局内主轴。
- 失败链路保持最小口径：外层壳直接丢弃旧失败结算，`hulebu:run-failed` 不再生成任何外层失败页；失败只保留在 iframe 内的 `失败 / 重开` 弹框，原型、静态 Demo 和回归测试都已按这个口径锁定。
- 外层 hydration 也补了旧存档兜底：如果浏览器本地还残留历史 `failed` settlement，`/games/hulebu` 不会再把它当成已完成结算恢复出来，刷新后仍只保留 iframe 内的短失败弹框口径。
- 继续把结算里的“解释感”收掉：原型和站内静态 Demo 的通关弹层不再显示 `Boss 复盘` 卡，`/games/hulebu` 外层结算页也压回只读结果指标和一句摘要，不再顺手展开路线、词缀、Boss 或周目说明。
- 继续把局外首页的“说明页感”收掉：主线 / 无尽 / 每日不再铺大块导向和节奏说明，外层只保留开始入口、当前进度和少量必要预览，避免失败口径收短了但局外页还在替玩家讲太多。
- 创建 T183 胡了卜 UI 资产 Cocos 接入说明文档。
- 新增 `UI_ASSETS_COCOS_INTEGRATION.md`，记录 T181 v6 透明 UI / 牌面资源包和 T182 v1 操作演出概念包的可用路径、预览图、Cocos `resources` 目录建议、SpriteFrame key 建议、逻辑牌面映射、导入设置、接入顺序和验收清单。
- 文档明确 T181 v6 可作为 Cocos 后续接入候选资源；T182 的人物 cut-in 仍是程序化占位，不应直接进入正式发布包；T181 v6 暂缺正式 `胡` 按钮，需要后续单独补同风格资源。

## 2026-06-21

- 继续加深 T176 高阶周目结算归因。
- `东风试路·定风 / 南风挡墙·回潮 / 西风试锋·收注 / 北风锁火·熄潮` 这批高阶 identity 已在当时补齐成功/失败读法；当前对外口径已经收回，不再把这些解释性内容挂到失败结算上。
- `/games/hulebu` 外壳高阶配置卡同步补了四条新分支的事件聚光和奖励主骨，避免被大类通用文案吞掉。
- 高阶结算新增 `nextDecision` 路线决策层，按东/南/西/北四档给出“继续深追、先补容错、后段收账、终局封口顺序”等下一局路线判断，原型、站内静态 Demo 和 `/games/hulebu` 结算页已同步。
- 高阶结算继续新增 `tempoRead` 节奏读法层，东/南/西/北四档分别强调三拍开线、呼吸墙、读账收账和封火落锁，让打完一轮后的操作手感差异更明显。
- 高阶失败结算新增 `failureAxisVerdict` 失败轴判定层，会把目标完成度、节奏断档、容错耗尽和构筑失配压成“目标线先塌 / 节奏先断拍 / 容错先见底 / 构筑先散线”的第一眼结论。
- 高阶失败结算继续加深为四段式拆分，新增 `failureGoalBreakdown / failureTempoBreakdown / failureBufferBreakdown / failureBossMismatchVerdict`，让目标、节奏、容错和构筑 / Boss 错位不再挤在同一句里。
- `东风探手·压口` 现在也从东风通用前段里单独拎出来，补了事件身份 / 构筑手感 / 奖励链 / Boss 压力四条判定，让前段压口不再吞进普通东风开线。
- `西风收口·留门 / 北风锁火·终煤 / 北风封尾·闷锁` 也继续补了更偏终局的专属手感句，让后段留门、闷燃封火和闷锁封尾不再靠同一条大类文案兜底。
- 失败反馈也继续往细处拆，`北风锁火·终煤 / 北风封尾·闷锁` 现在各自有专属的构筑手感、Boss 压力和奖励链判定，终局死法不再只剩一条“高压封刀”的大类解释。
- `北风锁火·终煤 / 北风封尾·闷锁` 的失败拆分现在还能继续细到目标完成度、节奏和容错卡点，能更明确地分出火线没闷到最后一拍、尾门没焊死和 Boss 终局逼锁错位这几类失败。
- 高阶成功结算新增 `rewardChainVerdict` 奖励链判定层，按东/南/西/北四档把奖励主骨压成“开线链 / 筑墙链 / 收账链 / 封刀链”，让不同构筑的收益身份更容易一眼看出。
- 高阶事件结算新增 `eventIdentityVerdict` 事件身份判定层，按东/南/西/北四档把事件线压成“试路局 / 换气局 / 读账局 / 落锁局”，让事件差异不再只靠事件名区分。
- 高阶构筑结算新增 `buildIdentityVerdict` 构筑手感判定层，按四档压成“前段开线 / 中段呼吸墙 / 后段读账收口 / 终局封刀”，让结算能直接说清这套 build 的手感身份。
- 高阶 Boss 结算新增 `bossPressureVerdict` Boss 压力判定层，按四档压成“前段催线 / 中段压墙 / 后段查账 / 终局逼锁”，让玩家能看懂这一档 Boss 到底在逼哪条线。
- 高阶奖励池新增 4 张 A1-A4 专属奖励：`东风构筑：三拍开线 / 南风构筑：呼吸墙核 / 西风构筑：读账封口 / 北风构筑：终局封刀`，并接入对应档位池与构筑加权池，让差异实际进入局内抽奖。
- 高阶奖励池和构筑联动继续加厚，东/南/西/北四档又补了一批路线锚点、事件锚点和专属奖励；四档高阶现在不是只有一条主线奖励骨，而是开始有更密的分支词条层。
- 本轮验证通过：`npm run test -w packages/shared -- mahjong-config-playable-prototype`、`npm run test -w apps/web -- hulebu`、`npm run typecheck -w apps/web`、原型 HTML 内联脚本 `node --check`、站内静态 Demo 内联脚本 `node --check`。

## 2026-06-16

- 对 T181 路线奖励和局外能力深化做了第一轮实现。
- `/games/hulebu` 的局外升级已从原来的 `备用槽 / 满槽护符 / 初始道具` 三项，扩到 `备用槽 / 满槽护符 / 初始道具 / 河道扩容 / 开局铜钱 / 看山次数` 六轴长期成长。
- Web 壳层现在会把 `riverBonus / coinBonus / visionBonus` 连同原有三项一起写进 iframe 参数；HTML 原型和站内静态 Demo 已接住这些长期成长信号。
- 登录账号下的 `HulebuProgress` 也同步扩展到 `dailyStreak / lastDailySeed / upgrades / equippedAscensionLoadout / unlockedAscensionPerks`，局外长期成长不再只保存在本地。
- 本轮验证通过：`npm run test -w packages/shared -- mahjong-config-playable-prototype`、`npm run test -w apps/web -- hulebu`、`npm run typecheck -w apps/web`、`npm run build -w apps/web`、`git diff --check`。
- 下一步 Web 完整版内容建议继续留在 T181，补更深的路线奖励联动和结算后的长期选择；Cocos 和音画资源继续后置。
- 对 T176 高阶周目做了一轮后续加深补丁。
- 原型新增更深的高阶 identity：补了 `高阶构筑：河杠连锁 / 收官定局 / 顺手成胡 / 稳压续命` 以及 `封盘护河 / 续战筹码 / 牌尾缓冲` 等北风场专属能力与奖励链。
- 高阶事件池也补了第二轮 identity 差异：新增 `顺手摸牌 / 稳压补墙 / 河杠涨潮 / 收官落灯` 四个更贴构筑的事件，并把 `东风试胆 / 南桌续押 / 西风照听 / 北风断尾` 接入更深层的档位事件轮换。
- 高阶结算 payload 新增 `settlementHighlights`，会把本轮更关键的高阶构筑收益收束成结构化摘要。
- 当时做过一轮高阶外层复盘加深；当前已回退为轻结果口径，不再在外层结算里展示 `构筑回顾 / 关键收益 / 关键失误 / 下一轮建议` 这类解释卡。
- 本轮验证通过：`npm run test -w packages/shared -- mahjong-config-playable-prototype`、`npm run test -w apps/web -- hulebu`、`npm run typecheck -w apps/web`、`npm run build -w apps/web`、两份 HTML inline script `node --check`、`git diff --check`。
- 创建并完成 T180 胡了卜无尽和每日深度化。
- HTML 原型新增 `getEndlessChapterProfile()` 和 `getDailyMutatorProfile()`，无尽第 21 层后开始按每 5 层一个章节推进，并在章节尾部挂 `章节 Boss` 信号。
- 每日牌局第二版新增 `今日词缀 / 今日奖励 / 连续参与` 三个局外和结算信号；原型与 `/games/hulebu` 壳层都会同步显示。
- `/games/hulebu` 的 `无尽` 面板现在会显示 `当前章节 / 章节 Boss / 无尽最高`，`每日` 面板会显示 `今日词缀 / 今日奖励 / 连续参与 / 今日最佳`。
- 结算页新增无尽章节摘要和每日词缀 / 奖励 / streak 文案；站内静态 Demo 已同步到 `/games/hulebu-demo/config/*.json` 绝对资源路径。
- 站内 Web 壳层测试、共享原型测试、类型检查、构建、文档同步和 `git diff --check` 通过。
- 下一步 Web 完整版内容建议进入 T181 路线奖励和局外能力深化；Cocos 和音画资源继续后置。
- 创建并完成 T179 胡了卜成就图鉴扩容。
- `/games/hulebu` 的 `图鉴` 面板已从第一版平铺卡扩成第二版分组图鉴，新增 `图鉴总览 / 分类进度 / 分组列表`。
- 第二版成就表新增 Boss、事件、高阶和路线目标，当前分类覆盖 `主线碑记 / Boss 纪录 / 无尽牌山 / 每日留痕 / 局外积累 / 高阶征途 / 事件见闻 / 路线收束`。
- 高阶 / Boss / 事件相关目标新增 `未揭示目标` 隐藏态；旧存档 hydration 会继续按现有本地字段自动补算新增成就。
- 站内 Web 壳层测试、类型检查、构建和文档同步通过。
- 下一步 Web 完整版内容建议进入 T180 无尽和每日深度化；Cocos 和音画资源继续后置。
- 创建并完成 T178 胡了卜特殊事件池扩容。
- HTML 原型新增 `SPECIAL_EVENT_RARITIES`、`SPECIAL_EVENT_TAGS` 和 `EVENT_BUILD_LINKS`，把特殊事件从第一版固定补给扩到 `常见 / 稀有事件 / 高阶事件` 三层。
- 普通 run 新增 `河灯旧约 / 封盘押后 / 险招翻倍` 等第二版事件；高阶 run 会按 `河控流 / 稳压流 / 封盘流` 等构筑线更优先给对应事件。
- 事件弹层现在会显示稀有度、类型和构筑联动提示；外层结算不再追加 `最近事件` 卡片，避免结果页继续膨胀。
- 站内静态 Demo 已同步，测试、类型检查、构建和脚本检查通过。
- 下一步 Web 完整版内容建议进入 T179 成就图鉴扩容；Cocos 和音画资源继续后置。
- 创建并完成 T177 胡了卜 Boss 试炼第二版。
- HTML 原型新增 `BOSS_TRIAL_PHASES` 和 `BOSS_TRIAL_VARIANTS`，把第 10 关中段试炼、第 20 关终局 Boss、高阶 Boss 变体和无尽 Boss 变体区分开。
- Boss 面板新增 `阶段目标` 和 `Boss 奖励品质`，高阶 Boss 会显示 `高阶 Boss 变体`，且不套普通教程目标。
- 通关和失败事件保留 `bossReview` payload 作为内部运行态数据；外层 `/games/hulebu` 结算页不再展示 `Boss 复盘` 卡片，失败只保留 iframe 内的 `失败 / 重开` 弹框。
- 站内静态 Demo 已同步，桌面端和 390px 移动端已检查高阶 Boss 文案与页面宽度。
- 下一步 Web 完整版内容建议进入 T178 特殊事件池扩容；Cocos 和音画资源继续后置。
- 创建并完成 T176 胡了卜高阶周目完整版。
- `/games/hulebu` 高阶入口已扩展为 `东风场 / 南风场 / 西风场 / 北风场` 四档，局外新增 `高阶配置` 能力面板。
- 内层试玩页已接入高阶能力槽、专属奖励、档位事件、构筑识别、失败复盘和结算复盘。
- 北风场补充 `封盘 / 迟火` 能力与 `封终流` 构筑线；静态 Demo 和测试已同步。
- 普通前几关教程仍只作用普通模式，高阶前几关不套教程。
- 下一步 Web 完整版内容建议进入 T177 Boss 试炼第二版；Cocos 和音画资源继续后置。
- 创建并完成 T175 胡了卜网页版完整版路线重排和缺口拆分。
- 路线正式调整为：先把 `/games/hulebu` 网页版开发到内容、系统、数值和长期目标稳定，再补 Cocos 正式表现层、音乐、美术、动效和发布资源。
- 当前 Web Demo 已具备主线、局外、升级、路线奖励、无尽、每日、成就、高阶和账号进度续层，但相对完整版仍缺高阶周目完整版、Boss 试炼第二版、特殊事件池、成就图鉴扩容、无尽/每日深度、路线奖励和局外能力深化、统一数值平衡与内容冻结。
- 后续建议从 T176 高阶周目完整版开始，之后再拆 Boss、事件、成就、无尽/每日、奖励/能力和平衡冻结；Cocos 和音画资源后置。
- 本任务只更新文档和任务体系，不修改 Web Demo、Cocos 工程、共享包、Prisma 或资源文件。

## 2026-06-15

- 创建并完成 T174 胡了卜账号进度续层。
- `apps/web/prisma/schema.prisma` 已新增 `HulebuProgress` 模型，登录账号现在可以保存局外铜钱、无尽最高层、高阶解锁、每日最佳和成就。
- `/api/games/hulebu/progress` 已新增登录态进度读取与合并写回接口，优先把账号记录和本地旧存档做首登合并。
- `/games/hulebu` 局外壳层已在 hydration 后读取账号进度，并在升级、结算和长期进度变化时同步回写，避免重新打开页面后又从 0 开始。
- 验证通过：`npm run test -w apps/web -- hulebu`；`npm run typecheck -w apps/web`；`npm run build -w apps/web`；`npm run docs:sync`；`git diff --check`；右侧内置浏览器刷新 `/games/hulebu` 检查。
- 创建并完成 T173 胡了卜高阶周目第一版。
- `/games/hulebu` 局外 `高阶` 面板已开放 `东风场 / 南风场` 两档周目入口，并显示当前解锁、下一档提示和进行中摘要。
- 外层本地存档新增 `bestAscensionLevel`，会保存已解锁最高周目，并在高阶通关后推进下一档。
- HTML 原型与站内静态 Demo 已新增 `ascensionLevel / ascensionName` 解析、周目 HUD、重开承接和结算摘要。
- `东风场` 会提高牌山压力并带周期性高压词缀；`南风场` 进一步减少起始工具并禁用洗牌。
- 验证通过：`npm run test -w packages/shared -- mahjong-config-playable-prototype`；`npm run test -w apps/web -- hulebu`；`npm run typecheck -w apps/web`；`npm run build -w apps/web`；源原型与站内静态副本内联脚本 `node --check`；`npm run docs:sync`；占位符扫描；`git diff --check`；桌面端和 390px 移动端 `/games/hulebu` 检查。
- 创建并完成 T172 胡了卜成就图鉴第一版。
- `/games/hulebu` 局外 `图鉴` 面板已从占位改为成就图鉴第一版。
- 外层本地存档新增 `achievements`，会记录首批成就的首次解锁时间。
- 图鉴第一版已接入 8 项成就，覆盖主线、无尽、每日和局外升级四类长期进度。
- 旧存档 hydration 时会按 `bestEndlessLayer`、`dailyBestLevels`、`upgrades` 和历史结算补算首批可解锁成就。
- 创建并完成 T171 胡了卜每日牌局第一版。
- `/games/hulebu` 局外 `每日` 面板已开放 `开始每日`，外层 iframe 使用 `mode=daily&dailySeed=YYYY-MM-DD`。
- 外层本地存档新增 `dailyBestLevels`，会按日期保存当日本地最好关数；局外摘要、进行中摘要和结算面板都能显示。
- HTML 原型与站内静态 Demo 已新增 `runMode: "daily"`、`isDailyMode()`、`getDailyLevelIndex()`、`getDailyDifficultyProfile()` 和固定日 seed 的牌山生成口径。
- 每日第一版继续复用现有 20 关和路线型奖励池，不新增独立配置文件，也不改无尽层数和主线结构。

## 2026-06-14

- 创建并完成 T170 胡了卜无尽牌山第一版。
- `/games/hulebu` 局外 `无尽` 面板已开放 `开始无尽`，无尽 run 从第 21 层起步。
- 外层 iframe URL 已支持 `mode=endless&startLayer=21`，并继续携带 `reserveBonus / shieldBonus / toolBonus` 三项局外升级参数。
- 外层本地存档新增 `bestEndlessLayer`，局外页、进行中摘要、结算面板和最近一轮记录都会显示无尽层数与历史最高层。
- HTML 原型新增 `runMode`、`ENDLESS_START_LAYER`、`isEndlessMode`、`getEndlessLayerOrder`、`getEndlessDifficultyProfile` 和 `advanceAfterEndlessClear` 等无尽 helper。
- 无尽第一版复用后半段难度 profile 和路线型奖励池：每 3 层给奖励，每 5 层进入 Boss 压力，小 Boss 要求杠和积分，大 Boss 额外要求胡。
- 站内静态 Demo 已同步，保留 `/games/hulebu-demo/config/*.json` 绝对配置路径。
- 验证通过：`npm run test -w packages/shared -- mahjong-config-playable-prototype`；`npm run test -w apps/web -- hulebu`；`npm run typecheck -w apps/web`；`npm run build -w apps/web`；源原型与站内静态副本内联脚本 `node --check`；`npm run docs:sync`；占位符扫描；`git diff --check`；右侧内置浏览器桌面端和 390px 移动端 `/games/hulebu` 检查。
- 创建并完成 T169 胡了卜路线型奖励池扩展。
- `apps/game/mahjong-roguelike/config/rewards.json` 已从 10 个基础奖励扩展到 16 个奖励，覆盖 `吃 / 碰 / 杠 / 胡 / 道具 / 信息` 等路线类别。
- 默认 20 关主线的奖励节点已在试玩页侧映射为路线型池：第 6、9、13、16、19 关会出现更明确的吃流、碰流、杠流、胡流、道具流和信息流选择。
- 奖励弹层已新增路线标签；已选奖励区会展示 `奖励名 · 路线`，玩家能直接看出本轮 build 倾向。
- 本轮只复用既有 effect 类型：`reserve_limit_delta`、`shield_delta`、`coin_delta`、`tool_delta`、`combo_score_bonus`，不新增复杂被动系统，不改 `levels.json`。
- 验证通过：`npm run test -w packages/shared -- mahjong-config mahjong-config-playable-prototype`；`npm run test -w apps/web -- hulebu`；源原型与站内静态副本内联脚本 `node --check`；`git diff --check`。
- 创建并完成 T168 胡了卜铜钱资产和 3 项局外升级。
- `/games/hulebu` 局外页的 `备用槽 / 满槽护符 / 初始道具` 已从占位说明升级为真实可购买升级卡。
- 浏览器本地存档已从“局外铜钱 + 最近结算”扩展为“局外铜钱 + 最近结算 + 三项升级等级”。
- 开始新一轮主线时，iframe URL 已附带 `reserveBonus / shieldBonus / toolBonus` 参数；原型源文件和站内静态副本都会读取这些 bonus，并把它们接入 `reserveLimit / shields / tools`。
- 页面行为验证通过：预置 `999` 铜钱后，购买首档 `备用槽` 会扣到 `839`，新开 run 的 iframe URL 已变为 `...reserveBonus=1&shieldBonus=0&toolBonus=0`；390px 移动端升级页仍能显示 3 张升级卡和局外铜钱读数。
- 创建并完成 T167 胡了卜局外首页和结算面板。
- `/games/hulebu` 已从“直接打开 iframe”改为先显示局外首页；局外页会展示 `开始挑战 / 继续本轮 / 升级 / 图鉴 / 无尽 / 每日` 入口。
- 主线 run 继续复用现有 20 关 iframe Demo，但现在可以从外层壳返回局外、继续本轮，并在通关或失败后回到站内结算面板。
- 局外页已显示浏览器本地累计的铜钱；`备用槽 / 满槽护符 / 初始道具` 三项外置升级现已开放真实购买。
- 静态 Demo 与原型源文件已补 `embed=shell` 模式和父页面消息桥接，能向外层壳同步进行中、通关和失败事件。
- 验证通过：`npm run test -w apps/web -- hulebu`；`npm run typecheck -w apps/web`；`npm run build -w apps/web`；源原型与站内静态副本内联脚本 `node --check`；右侧内置浏览器桌面端和 390px 移动端 `/games/hulebu` 检查。

## 2026-06-13

- 创建并完成 T166 胡了卜 20 关完整主线 Demo。
- 默认玩家试玩页已从 10 关扩展到 20 关，并保留第 10 关 `终局试炼`。
- 第 11-19 关已补后半段渐进难度 profile，牌量依次提升到 `252 / 258 / 276 / 288 / 300 / 312 / 324 / 336 / 348`；第 20 关启用 `胡了卜王` 终章 Boss，牌量为 `360`。
- 奖励节点已扩展为第 `3 / 6 / 9 / 13 / 16 / 19` 关；第 20 关 Boss 目标会在默认玩家页和站内静态 Demo 中显示为 6 项复合目标。
- 默认玩家页 `auto` 模板已补多次 seed 重试和“起手不能直接露出完整答案组”的兜底条件，避免后半段大牌量关卡首轮直接给出完整答案。
- 390px 移动端已修正底部固定道具栏遮挡：工具栏贴底，页面底部留白增大，卡槽最后一格不再被遮住。
- 验证通过：`npm run test -w packages/shared -- mahjong-config-playable-prototype`；`npm run test -w packages/shared -- mahjong-config`；`npm run test -w apps/web -- hulebu`；HTML 内联脚本语法检查；右侧内置浏览器桌面端与 390px 移动端 `/games/hulebu-demo/index.html` 验证；`npm run docs:sync`；`git diff --check`。
- 创建并完成 T165 胡了卜 Demo 完整体验版推进方案。
- 经对照完整游戏设计方案和当前站内 Demo，确认当前 Demo 已覆盖核心局内玩法，但仍缺 20 关主线开放、局外首页、铜钱资产、升级消费、路线型奖励、无尽、每日、成就、高阶周目和 Cocos 正式表现层追平。
- 完整体验版路线已收束为“先完整主线，再长期模式”：下一步建议优先开 T166，把默认 `/games/hulebu` Demo 扩为 20 关主线，并让第 20 关成为 `胡了卜王` 终章 Boss。
- 后续任务建议顺序为 T167 局外首页和结算、T168 铜钱资产和 3 项局外升级、T169 路线型奖励池、T170 无尽牌山、T171 每日牌局、T172 成就图鉴、T173 高阶周目、T174 Cocos 正式表现层追平完整体验版。
- T165 只做路线规划和任务拆分，不修改玩法代码或站内静态 Demo。
- 创建并完成 T164 胡了卜 Boss 试炼 Demo 第一版。
- 默认朋友试玩 Demo 第 10 关启用 `终局试炼`，目标为 `杠 1 / 胡 1 / 积分 180`；HUD 显示 `试炼 x/3`，玩家页新增紧凑试炼目标条。
- 清空牌山但试炼目标未完成时会进入失败弹层，并明确提示 `目标未完成` 和具体缺口；击破试炼后一次性发放 `试炼奖励 +180 铜钱`。
- 第 10 关密集牌山生成器已能承接试炼目标包，并启用起手窗口平衡，避免起手直接露出完整答案组。
- 站内静态 Demo `/games/hulebu-demo/index.html` 已同步；390px 移动端牌山宽度收敛为 `330px`，卡槽与底部工具栏保持间距不重叠。
- 验证通过：`npm run test -w packages/shared -- mahjong-config-playable-prototype`；`npm run test -w packages/shared -- mahjong-config`；`npm run test -w apps/web -- hulebu`；HTML 内联脚本语法检查；右侧内置浏览器桌面端和 390px 移动端试炼/布局检查。
- 创建并完成 T163 胡了卜特殊事件 Demo 第一版。
- 默认玩家 Demo 现在会在第 6、8、10 关前弹出一次关前特殊事件选择；第一版事件池包含 `路遇老雀`、`旧牌匣`、`加注一局`、`暗灯牌局`。
- 事件选项覆盖三类结果：立即获得铜钱、补充 `洗牌 / 丢弃` 等救场道具、主动选择下一关词缀换通关额外铜钱。
- 第一版词缀已接入 `禁洗牌`、`禁透视` 和 `高压牌山`。`禁洗牌` 会禁用洗牌按钮并显示状态提示；`高压牌山` 会让当前事件关提高牌量压力并纳入高压模板池；HUD 会显示当前事件词缀。
- 事件状态只作用玩家主动选择后的当前关，下一关会清空，避免词缀串关；站内静态 Demo `/games/hulebu-demo/index.html` 已同步。
- 验证通过：`npm run test -w packages/shared -- mahjong-config-playable-prototype`；`npm run test -w packages/shared -- mahjong-config`；HTML 内联脚本语法检查；`npm run test -w apps/web -- hulebu`；右侧内置浏览器桌面端和 390px 移动端事件/布局检查。
- 创建并完成 T162 胡了卜残局收官和悬台窄腰高压池。
- 默认玩家 Demo 第 8-10 关的 auto 随机池已加入 `suspended-waist / 悬台窄腰`；第 5-7 关继续使用基础模板池，避免过早把高压窄腰结构推给新手。
- 普通关牌桌清空但主槽仍有残张时会进入 `残局收官`，不再直接无成本通关。
- `残局收官` 第一阶段已落地 `弃牌通关` 与 `选作牌引`：弃牌会清掉残张通关；牌引会让玩家选 1 张主槽残张带入下一关，并在 HUD 中显示提示。
- 站内静态 Demo `/games/hulebu-demo/index.html` 已同步，保留静态配置路径；`/games/hulebu` 继续通过 iframe 承载试玩。
- 验证通过：`npm run test -w packages/shared -- mahjong-config-playable-prototype`；`npm run test -w packages/shared -- mahjong-config`；HTML 内联脚本语法检查；`npm run test -w apps/web -- hulebu`；右侧内置浏览器桌面端和 390px 移动端布局检查；`npm run docs:sync`；`git diff --check`。
- Lee 提到的特殊事件不并入 T162，已在交接中标为后续单独任务方向：高阶词缀、随机事件和 Boss 试炼。

## 2026-06-03

- 创建并完成 T105 胡了卜震落牌平铺和遮挡点击修复。
- 根因确认：`shakeLooseMountainTile` 使用本次开山的局部 `index` 计算落点，重复 `杠 / 胡` 会复用同一组坐标；运行态遮挡检测又跳过 `looseMountainTile`，导致震落牌盖住普通牌时下层普通牌仍可点击。
- 修复后震落牌使用全局递增 `looseMountainDropIndex` 进入桌面平铺层，连续多次开山不会互相叠起；震落牌会清理旧 `stackColumn / stackBridge / blockedBy` 元数据，保持自身可点击入槽。
- 普通牌遮挡检测已计入震落牌：如果震落牌视觉上盖住普通下层牌，下层牌不可点击；震落牌自身仍不被阻塞。
- 已同步 `/games/hulebu-demo/index.html` 静态发布副本，并保留 `/games/hulebu-demo/config/*.json` 绝对 fetch 路径。
- 验证通过：`npm run test -w packages/shared -- mahjong-config`；`npm run test -w packages/shared -- mahjong-config-playable-prototype`；`npm run test -w apps/web -- hulebu`；HTML 内联脚本 `node --check`；Kimi WebBridge 行为检查；390px Chrome 截图和 DOM 布局检查。

## 2026-06-02

- 创建 T103 胡了卜立体窄腰牌山模板设计。根据 Lee 提供的高堆叠参考图，确认只借鉴结构关系，不复制原图美术、牌面、颜色、文案或具体布局。
- T103 设计稿推荐后续新增 `悬台窄腰 / suspended-waist` 模板：上层大平台制造压迫感，中段 1-2 条窄腰形成释放瓶颈，底部 2-3 个支撑柱作为后期目标，左右侧向散牌提供干扰和恢复入口。实现顺序建议为调牌器验证、再进入第 8-10 关随机池、最后评估是否同步发布副本和 Cocos 共享生成器。
- 完成 T104 胡了卜悬台窄腰模板调牌器实现。`suspended-waist` 已加入 HTML 原型和站内静态调牌器，中文名为 `悬台窄腰`，结构区域包含 `top-platform / waist / support-column / side-scatter`。
- T104 暂不把 `suspended-waist` 加入默认玩家页 auto 随机池；当前只通过 `/games/hulebu-demo/tuner.html?template=suspended-waist&level=10&seed=waist-check` 进行调牌器验证。Kimi WebBridge 桌面验证生成 240 张牌、首轮可点 8 张、同组最多露出 2 张；390px 截图保存到 `/tmp/hulebu-t104-suspended-waist-mobile.png`。
- 完成 T098 胡了卜朋友 Demo 第 5-10 关渐进难度曲线。
- 默认玩家 Demo 前 4 关教学保持不变，第 5-10 关新增 difficulty profile，牌量从 `72 / 96 / 132 / 168 / 210 / 240` 逐步上升，堆叠深度从 `3` 提高到 `6`，字牌权重从 `12` 提高到 `40`。
- 第 5 关标题直接显示“正式入门”，不再从教学关后直接进入 240 张高压牌山；第 10 关在朋友 Demo 中作为“综合高压”压力关，不叠正式 Boss 目标和胡包，调牌器和正式配置仍保留原 Boss 配置。
- Kimi WebBridge 验证：第 5 关生成 72 张、首轮可点 8 张、最大同组 2；第 10 关生成 240 张、首轮可点 7 张、最大同组 2。
- 完成 T099 胡了卜试玩页卡槽满槽显示修复和记牌器。
- 默认朋友 Demo 不再使用隐藏备用槽自动救场，`reserveLimit` 为 0；第 8 张入槽后保留在可见主槽，满槽且无组合时提示“请先发动组合，或使用丢弃救场”。
- 玩家页记牌器移到牌桌和卡槽之间，首屏可见，按 `万 / 条 / 筒 / 字` 展示剩余未移除牌总数和点数数量，并随入槽、组合、丢弃和洗牌刷新。
- Kimi WebBridge 满槽采样验证：`slotLength=8`、`reserveLength=0`、第 8 张位置为 `slot`、无失败弹层；截图保存到 `/tmp/hulebu-t098-t099-after.png`。
- 新增/更新 `mahjong-config-playable-prototype` 与 `mahjong-config` 回归测试，覆盖渐进 profile、首轮同组上限、玩家页记牌器、记牌器刷新和第 8 张入槽不被隐藏备用槽吞掉。
- 完成 T100 胡了卜有限牌河、补杠和胡牌奖励核心玩法设计。
- 新规格确认后续核心玩法为 `有限牌河 + 明碰区 + 补杠孤张出口 + 明杠开山 + 胡牌强奖励 + 听牌提示 + 孤张预算生成器`。
- 补杠定位为低收益孤张出口，不触发震山；明杠才触发震山开牌；胡牌作为局内最大爆发，清槽、强开山并处理牌河。后续建议先拆 HTML Demo 实现任务验证该规则。
- 完成 T101 胡了卜有限牌河、补杠和胡牌奖励试玩 Demo。
- 默认玩家 Demo 新增明牌区和 `3` 格有限牌河；`丢弃` 改为进入选择模式，由玩家点击任意槽位牌打入牌河，牌河容量会在 HUD 中显示。
- `碰` 后组合进入明牌区；后续第 4 张同牌可触发 `补杠`，只升级明牌区并清掉孤张，不触发强开山。直接 `杠` 会执行明杠开山，震落 1 张压顶牌为可选桌面牌；`胡` 会清槽、震落 3 张压顶牌并清理牌河 1 张。
- 满槽失败判定已纳入牌河容量：槽满但牌河未满时提示先发动组合或打牌到牌河；槽满、无组合、牌河满且无救场时才弹失败层。
- 验证通过：`npm run test -w packages/shared -- mahjong-config-playable-prototype`；`npm run test -w packages/shared -- mahjong-config`；HTML 脚本 `node --check`；Kimi WebBridge 桌面 smoke test，截图保存到 `/tmp/hulebu-t101-smoke.png`。
- 根据 Lee 第 5 关试玩反馈补充生成器修正：第 5-10 关自然明杠包统一提高到 `2` 个；明杠目标牌面会从普通填充和覆盖组中保留出来，避免玩家看到的同一牌面被拆成多个 3 张组合。Kimi WebBridge 第 5 关实测存在 `wan-6` 和 `wan-3` 两条干净明杠路线，且按释放步骤推进到第 3 步会出现真实 `杠` 候选。
- 补齐简化听牌提示和孤张预算生成器检查：默认玩家页会在槽内距离 `3 + 3 + 2` 胡牌差 1-2 张时显示 `听/差` 文案，并在记牌器中高亮牌山仍有的目标牌；目标牌已不在牌山时提示降级为“牌山暂无”。第 5-10 关 profile 已加入孤张预算，生成器会在产出后统计未归类解法组风险并校验不超过预算。Kimi WebBridge 已验证第 5 关孤张风险 `1/1`、自然明杠路线仍为 2 条且无同面 3 张组干扰。
- 按 Lee 对 `杠开山` 的理解修正开山反馈：明杠和胡牌开山不再把压顶牌直接移出局，而是把压顶牌震落到桌面平铺层，保留为可点击入槽的 `board` 牌，并解除它们对下层牌的阻挡。Kimi WebBridge 已验证震落牌按钮可点击，点击后进入主槽。
- 按 Lee 继续试玩反馈修正记牌器和动作栏：默认玩家页记牌器现在只统计牌山 `board` 牌，卡槽、牌河、明牌区和移除区不计入；震落到桌面的牌仍计入，点入卡槽后立即扣除。`吃 / 碰 / 杠 / 补杠 / 胡` 已拆到卡槽上方独立动作栏，卡槽行只保留 8 格槽；390px 移动端补底部留白，固定道具栏不遮盖卡槽。
- 根据 Lee 发布前反馈继续修正开山强度和可读性：直接 `杠` 震落 1 张，`胡` 震落 3 张；默认玩家页隐藏内部标题栏，修正旧 grid 行撑高记牌器的问题，牌面回到 390px 下约 `40x54`，记牌器改为上方牌面、下方余牌数量。T102 静态发布副本已同步，390px Playwright 截图保存到 `/tmp/hulebu-t102-mobile-counter-split-final.png`。
- 完成 T102 胡了卜 Demo 站内网页小游戏发布接入。新增 `/games/hulebu` 路由和 `apps/web/src/modules/games/hulebu/` iframe 容器；静态 Demo、调牌器和配置复制到 `apps/web/public/games/hulebu-demo/`；游戏站麻将卡片和搜索入口指向 `/games/hulebu`。

## 2026-06-01

- 完成 T097 胡了卜教学关必须发动对应组合才通关。
- 默认玩家 Demo 前 4 关新增教学动作门槛：第 1 关必须点击 `碰`，第 2 关必须点击 `吃`，第 3 关必须点击 `杠`，第 4 关必须点击 `胡`，单纯把牌全部放入卡槽不会通关。
- 教学关候选组合会过滤为本关目标动作，第 4 关 `胡` 教学不会暴露 `吃 / 碰` 候选；前 3 关教学牌山收敛为最小教学包，避免 6 格教学槽被干扰牌提前占满。
- 该教学门槛只作用于默认玩家试玩页，调牌器和配置联调不受影响。
- 完成 T096 胡了卜玩家页布局、牌面放大和模板随机调参。
- 默认玩家页主栏收敛为 `480px`、右侧道具栏 `72px`，顶部 HUD、牌桌、卡槽和道具栏在 1512x682 桌面视口下不再产生整页滚动。
- 密集牌山规则牌尺寸从 `45x60` 放大到 `52x70`，玩家页实测牌面约 `37x49`，第 5 关 240 张牌中实际渲染约 43 张可见牌，首轮可点保持 8 张。
- 默认普通密集关 `auto` 模板改为按 seed/重开随机选择，并增加安全回退：候选模板生成失败或首轮可点超过 8 时会尝试下一个 auto 候选，避免朋友试玩刷出过多入口。
- 牌山主堆权重从 `1.8` 提高到 `2.45`，让多数牌更集中在几个主牌山中，减少大画面空旷感；调牌器仍可手动指定完整 8 个模板。
- Kimi WebBridge 验证第 5 关 `seed=epsilon`：1512x682 视口无横向/纵向溢出，牌桌约 `396x452`，牌面约 `37x49`，首轮可点 8 张，标题显示随机回退后的 `环形` 模板。
- 完成 T095 胡了卜混合窗口牌山生成器。
- 根据 Lee 反馈，确认 T093/T094 之后仍存在“顺序答案”问题：密集牌山会把当前最上层可点击牌直接切成 3 张一组，并分配同一个 `碰 / 吃 / 杠` 答案，玩家无需记忆和取舍。
- `prototypes/config-playable/index.html` 已把密集牌山可解路径拆成答案组和释放窗口：`solutionGroup` 继续表示可消组合，`solutionStep` 表示释放时机，首轮窗口会混入不同答案组的 `hold / lure / finish` 牌。
- 默认普通密集关首轮实际可点击窗口会重平衡，保证任一答案组最多露出 2 张；第 1 关运行态抽样首轮 8 张可点、最大同组 2 张，第 5 关首轮 7 张可点、最大同组 2 张。
- 胡牌重点关和 Boss 目标关暂不做运行态重平衡，避免破坏胡牌包和 Boss 目标可解性；后续如果要增强 Boss 选择压力，需要单独设计目标包混合规则。
- 更新 `mahjong-config-playable-prototype` 和 `mahjong-config` 回归测试，覆盖混合窗口函数、`mixedWindowRole`、首轮同组上限、拆分答案组和朋友试玩第 5 关首轮窗口。
- 完成 T094 胡了卜残局收官与试玩反馈设计。
- 根据 Lee 试玩反馈，确认 T093 仍需修正：前 4 关不能只把牌点入卡槽就结束，必须真实发动 `碰 / 吃 / 杠 / 胡`；玩家页牌面和点击热区需要放大；`丢弃` 应进入槽位选择模式；玩家页需要可见记牌器。
- 确认普通关不把 `卡槽必须清空` 作为默认胜利条件。牌桌清空但卡槽仍有残张时进入 `残局收官`，把孤张转为关末决策，而不是直接失败或无成本过关。
- T094 设计规格定义了 `弃牌通关 / 选作牌引 / 收入牌河` 三个收官方向。Demo 第一阶段建议先落 `弃牌通关` 与 `选作牌引`，`收入牌河` 先作为长期构筑方向保留。
- 新增设计规格 `docs/superpowers/specs/2026-06-01-hulebu-endgame-settlement-design.md`，后续实现应据此另开任务，允许修改 HTML 原型和共享测试。
- 完成 T093 胡了卜 10 关朋友试玩 Demo。
- `prototypes/config-playable/index.html` 默认玩家页现在只展示 10 关小 run：第 1 关教学 `碰`，第 2 关教学 `吃`，第 3 关教学 `杠`，第 4 关教学 `胡`。
- 前 3 关使用 6 格主槽，第 3 关通关后奖励固定为 `卡槽 +2`，第 4 关起进入 8 格主槽；前 4 关使用小牌量教学牌山，第 5 关开始恢复 240 张密集牌山高压模式。
- 右侧道具从 `洗山 / 回手 / 看山` 改为 `洗牌 / 撤回 / 丢弃`；丢弃会移除主槽末尾牌并消耗次数，主槽已满且无组合时如果还有丢弃次数会提示可用丢弃救场。
- 移动端默认玩家页在 390 CSS 像素下改为底部横排三道具，CDP 验证 `scrollWidth=390`、卡槽完整、底部三道具完整可见，截图保存到 `tmp/hulebu-t093-mobile-cdp.png`。
- 新增/更新 `mahjong-config-playable-prototype` 与 `mahjong-config` 回归测试，锁定 10 关试玩编排、前 4 关教学组合、6/8 卡槽、第三关固定奖励、第 5 关密集牌山和丢弃道具行为。
- 完成 T092 胡了卜玩家页正式一屏 HUD 重排。
- `prototypes/config-playable/index.html` 默认玩家页新增顶部 `play-hud`，动态显示关卡、目标、余牌、积分和铜钱；右侧完整信息侧栏在玩家页隐藏，只保留 `洗山 / 回手 / 看山` 道具栏。
- 移动端保持主栏 + 64px 右侧道具栏双列结构，不再把侧栏推到下方滚动；底部 8 格卡槽继续位于首屏。
- 调牌器视图继续保留调参面板、余牌统计、已选奖励、工具、重开和下一关控制；顶部新 HUD 在调牌器视图隐藏。
- Kimi WebBridge 桌面验证通过：1512x682 视口无整页滚动，牌桌 `307x351`，HUD `460x61`，卡槽 `460x114`，右侧道具栏 `76x204`，第 1 关 41 张可见、8 张可点、无横向溢出，截图保存到 `/tmp/hulebu-t092-desktop.png`。
- 390x844 受控移动检查通过：页面无横向溢出且不滚动，牌桌 `276x315`，卡槽底部 `719px`，右侧道具栏 `64x204` 位于首屏，首轮仍为 8 张可点，截图保存到 `/tmp/hulebu-t092-mobile.png`。
- 新增/更新 `mahjong-config-playable-prototype` 回归测试，锁定顶部 HUD、右侧道具栏、玩家页隐藏完整侧栏、移动端双列结构和调牌器分离。
- 根据 Lee 后续验收反馈完成 T092 密集牌山调参补丁：轻微遮挡阈值统一为 `8%`，低于 8% 可点击，达到 8% 才阻塞；规则牌尺寸调为 `45x60`，CSS 宽度调为 `8.0357142857%`，桌面第 1 关实测牌面约 `24.5x32.7`。
- 牌山布局改为模板锚点向中心收拢，并给前 4 个主堆增加权重；第 1-8 关默认首轮可点稳定在 5-8 张，符合当前起手 3-8 张目标，同时继续覆盖 34 个具体牌面。
- Kimi WebBridge 验收补丁验证：1512x682 默认页第 1 关 41 张可见、8 张可点、无横向溢出，截图保存到 `/tmp/hulebu-t092-8pct-larger-tiles.png`；运行态遮挡探针验证 `0.9%` 视觉覆盖不阻塞、`11.1%` 视觉覆盖阻塞；390x844 受控移动检查牌桌 `254x290`、卡槽首屏可见。
- 新增/更新 `mahjong-config-playable-prototype` 与 `mahjong-config` 回归测试，锁定 `8%` 遮挡阈值、`45x60` 规则尺寸、8 模板轮换、3-8 张起手范围、主堆集中度和桥接堆单入口/多入口随机变化。

## 2026-05-31

- 完成 T091 胡了卜玩家页正式 HUD 空间压缩。
- `prototypes/config-playable/index.html` 将密集牌山坐标高度从 `560x720` 压到 `560x640`，玩家页牌桌使用 `430px / 48vh` 高度预算，给顶部关卡目标、剩余牌统计、底部卡槽和右侧道具按钮预留正式界面空间。
- 玩家页顶部条、牌桌容器、卡槽、组合按钮、工具按钮和右侧面板间距已收紧；桌面右侧面板限制在视口内滚动，避免撑高整页。
- Kimi WebBridge 桌面验证通过：3031 页面无整页滚动，牌桌 `332x379`，顶部 `69px`，卡槽 `130px`，右侧面板在视口内滚动，第 1 关 41 张可见、8 张可点、无横向溢出，截图保存到 `/tmp/hulebu-t091-desktop.png`。
- 390x844 受控移动检查通过：牌桌 `344x393`，占视口高度约 `46.6%`，卡槽在首屏可见，无横向溢出，截图保存到 `/tmp/hulebu-t091-mobile.png`。移动端右侧信息面板仍位于下方滚动，正式移动 HUD 重排应另开任务。
- 新增/更新 `mahjong-config-playable-prototype` 与 `mahjong-config` 回归测试，锁定 `560x640` 坐标系、玩家页 `430px / 48vh` 牌桌预算、移动端单列覆盖和首轮 8-12 张可点规则。
- 本地 3031 静态服务已恢复为项目根目录服务，默认玩家页可继续通过 `http://127.0.0.1:3031/apps/game/mahjong-roguelike/prototypes/config-playable/index.html` 访问。
- 完成 T090 胡了卜失败提示弹层。
- `prototypes/config-playable/index.html` 新增统一失败入口 `failLevel` / `showLevelFailed`，主槽满且无可发动组合或救场资源时会弹出“本关失败”overlay。
- 失败提示会说明原因，默认文案为“主槽已满，且当前没有可发动的组合或可用救场。”，并提供“重开本关”按钮。
- Boss 目标未完成导致失败时复用同一失败弹层；失败后牌面、组合按钮和工具按钮保持禁用。
- 新增 `mahjong-config` VM 回归测试，模拟 8 格主槽满且无组合，验证 `phase=failed`、失败 overlay 打开、标题为“本关失败”且出现“重开本关”按钮；`mahjong-config-playable-prototype` 静态测试保护失败弹层函数和文案。
- Kimi WebBridge 真实浏览器验证通过：模拟失败后 `phase=failed`、overlay 打开、标题为“本关失败”、按钮为“重开本关”，截图保存到 `/tmp/hulebu-failure-feedback.png`。
- 完成 T089 胡了卜原型随机组合堆遮挡。
- `prototypes/config-playable/index.html` 的密集牌山新增随机组合/桥接堆，桥接顶牌会显式压住多个堆顶入口；移走桥接顶牌后，下层可点击入口会增加。
- 同一局内混合 `full / partial / loose` 三类堆叠遮挡形态，遮挡比例覆盖 5%-100%，保留“只看到一张”“轻微露出”“错位预览”的不同读牌压力。
- 默认玩家页第 1 关最终实测为 41 张可见、9 张可点、1 个桥接顶牌；桥接顶牌按实际释放数显示压住 2 个入口。
- 根据 Lee 反馈继续收紧桌面空旷感：密集牌山坐标系调整为 `560x720`，规则牌尺寸调整为 `42x56`，桌面玩家页主栏收敛到 `500px`，山形棋盘使用 `width: min(100%, 64vh)`。
- 桥牌生成已新增避让，运行态点击判定会按实际渲染后的视觉矩形过滤，避免视觉上已经露出的顶牌仍不可点击。
- Kimi WebBridge 最终桌面验证：玩家页左栏 `500px`，棋盘 `442x569`，第 1 关 41 张可见、9 张可点、露出但 disabled 的顶牌 0、桥牌真实压住 2 个入口，截图保存到 `/tmp/hulebu-visual-click-rule-final.png`。
- 调牌器第 1 关实测为 43 张可见、8 张可点、31 张下层预览全部 disabled；遮挡比例为 `0.05 / 0.33 / 0.41 / 0.75 / 1.00`，遮挡模式覆盖 `full / partial / loose`。
- 新增/扩展 `mahjong-config-playable-prototype` 与 `mahjong-config` 回归测试，覆盖桥接堆常量、桥接 DOM 标记、遮挡模式、遮挡比例、预览牌不可点和桥接牌移走后解锁多个入口。
- Kimi WebBridge 桌面验证通过：默认玩家页保持首轮 9 张可点，预览牌保持不可点，视觉露出但 disabled 的顶牌为 0；调牌器截图保存到 `/tmp/hulebu-t089-tuner.png`。
- 临时 headless Chrome 390x844 移动端截图验证通过：牌山竖向可见，主槽和牌桌没有互相压住；截图保存到 `/tmp/hulebu-t089-mobile.png`。

## 2026-05-30

- 完成 T088 胡了卜原型散乱可见压叠层。
- `prototypes/config-playable/index.html` 的密集牌山渲染从“只显示堆叠顶牌”改为“顶牌 + 4 张下层预览牌”，下层预览牌保持真实牌面、错位露出、blocked/disabled，不会被点击入槽。
- 默认玩家页第 1 关实测为 50 张可见牌、10 张可点、40 张 `stack-preview` 全部 disabled，预览深度覆盖 1-4；首轮入口仍维持约 10 张。
- 默认玩家页的栈深数字已降噪为小圆点，避免满屏数字干扰玩家读牌；调牌器仍显示数字深度，便于调关判断堆叠厚度。
- 新增/扩展 `mahjong-config-playable-prototype` 与 `mahjong-config` 回归测试，覆盖预览深度常量、预览 DOM 标记、生成器侧可见预览数量、预览牌被 blocked。
- Kimi WebBridge 桌面验证通过：默认玩家页和调牌器均为 50 张可见、10 张可点、40 张下层预览不可点；调牌器深度数字仍显示。
- 临时 headless Chrome 390x844 移动端截图验证通过：牌桌完整显示，无横向截断；截图保存到 `/tmp/hulebu-t088-mobile.png`。
- 完成 T087 胡了卜原型模板随机、全牌种覆盖和竖屏牌桌。
- `prototypes/config-playable/index.html` 的密集牌山坐标系从 920x520 改为 640x860 竖屏基准，桌面和 390px 移动端都能完整容纳牌桌。
- 配置试玩原型新增 8 个本地模板：中心塔、双翼、十字、环形、长墙、群岛、峡谷、阶梯；默认按关卡稳定轮换，调牌器可通过 `template` 参数或下拉框指定模板。
- 默认 240 张牌仍保持首轮约 10 张可点，栈顶显示 `data-stack-depth` 角标，深堆不会一次性展开过多可选牌。
- 密集牌山牌面分配从四类花色覆盖升级为 34 个具体牌面覆盖，保证 `万1-9 / 条1-9 / 筒1-9 / 东南西北中发白` 都至少出现。
- 已修正首轮栈顶牌面过于单一的问题，覆盖队列和填充牌型都会洗牌，首屏视觉更混合。
- 新增/扩展 `mahjong-config-playable-prototype` 与 `mahjong-config` 回归测试，覆盖模板常量、竖屏比例、模板 URL 参数、自动模板轮换、34 牌面覆盖和首轮 8-12 张可点。
- Kimi WebBridge 桌面验证通过：第 1 关默认玩家页显示 640/860 竖屏牌桌、10 张可点、10 个栈深角标；第 4 关调牌器 `template=ring` 可显示环形模板且模板下拉生效。
- 已修正调牌器固定模板的验收误导：从 `template=ring` 页面切到其他关卡时恢复 `auto`，避免所有关都继续显示环形；重开本关仍保留当前调参。
- 临时 headless Chrome 390x844 移动端截图验证通过：竖屏牌桌完整显示，无横向截断；截图保存到 `/tmp/hulebu-t087-mobile-fixed.png`。
- 完成 T086 胡了卜数百张小牌密集牌山原型。
- `prototypes/config-playable/index.html` 的密集牌山默认牌量从约 50 张提高到 240 张，调参范围改为 120-420 张。
- 密集牌山牌面规则尺寸从 72x96 缩小到 38x52，CSS 可见牌面约 44x60 像素，牌桌坐标系扩大到 920x520。
- 牌山位置生成改为 6 条压叠牌流加 4 个竖堆入口，默认同列深度为 6，首轮可点牌从 58 张压到约 10 张；保留现有可解组合包、遮挡计算和手动组合规则。
- 牌型分配加入 `万 / 条 / 筒 / 字` 四类保底覆盖，避免数百张牌只集中在少数花色。
- `mahjong-config-playable-prototype` 新增静态回归测试，覆盖默认 240 张、小牌尺寸、调参范围、首轮入口常量和 CSS 小牌比例。
- 旧 `tiles=58` URL 调参测试已更新为 `tiles=360`，与新的数百张牌量范围保持一致。
- `mahjong-config` VM 回归测试覆盖默认 240 张、首轮可点 8-12 张、四类牌至少各 12 张。
- Kimi WebBridge 已恢复健康并完成浏览器验证：默认调牌器生成 240 张牌，首轮可点 10 张，余牌统计为 `万 56 / 条 12 / 筒 38 / 字 134`；`tiles=420` 上限 VM 压测生成 420 张牌且首轮入口为 9；截图保存到 `/private/tmp/hulebu-t086-ten-open.png`。
- 完成 T085 胡了卜玩家试玩页和调牌器分离。
- `prototypes/config-playable/index.html` 默认进入玩家试玩页和密集牌山模式，隐藏顶部配置切换、关卡标签和调参面板。
- 玩家页右上保留“调牌器”新窗口入口，链接会带上当前关卡和模式参数。
- 新增 `prototypes/config-playable/tuner.html`，作为独立调牌器入口跳转到 `index.html?view=tuner&mode=mountain`，并保留原 URL 中的调参参数。
- 移动端玩家页不再把右侧信息面板排在牌桌前，首屏优先展示牌桌和主流程控件。
- 新增 `mahjong-config-playable-prototype` 回归测试，覆盖默认玩家入口、独立调牌器入口和调参面板只在 tuner 视图展示。
- Kimi WebBridge 当前 daemon 健康检查异常，浏览器自动截图和移动端复核需要在 WebBridge 恢复后补做；本轮未再使用系统 Chrome 或临时 headless 兜底。

## 2026-05-29

- 完成 T084 胡了卜 Graph-based 牌山生成器 Cocos 接入。
- `HulebuLevelConfig.ts` 已从本地随机柱式 `createHulebuRandomMountainLevelConfig` 切换为共享层 `generateHulebuMountain`。
- Cocos 默认 20 关现在按 `center-tower`、`two-wings`、`cross`、`ring`、`long-wall`、`islands`、`canyon`、`staircase` 8 个核心模板轮换生成牌山。
- 共享生成器输出的 `levelTiles` 已映射为 Cocos 当前 `HulebuLevelTileConfig`，现有 Cocos 渲染、点击、8 格槽、组合按钮、通关提示和奖励节点流转继续复用。
- Cocos 工程 `tsconfig.json` 已补 ES2019 lib，保证直接 import 共享生成器时 Cocos 工程 typecheck 可通过。
- `mahjong-cocos-project` 测试已新增 Graph-based 接入保护，覆盖模板轮换、多层遮挡、被 blocker 移走后解锁和首关铺开跨度。

## 2026-05-28

- 完成 T083 胡了卜模板注册表和 8 个核心模板共享实现。
- `packages/shared/src/mahjong-mountain-generator.ts` 已从模板分支升级为模板 definition 注册表，提供 `listHulebuMountainTemplateDefinitions`、`getHulebuMountainTemplateDefinition` 和 `normalizeHulebuMountainGeneratorConfig`。
- 第一批 8 个核心模板已可生成：`center-tower`、`two-wings`、`cross`、`ring`、`long-wall`、`islands`、`canyon`、`staircase`。
- `ExperienceReport` 已新增模板摘要、参数快照、通用校验器、窗口曲线和释放事件，方便后续调关卡解释“为什么这局有压力”。
- 本任务只修改共享层生成器和文档，不接 Cocos；后续需要另起任务把 Graph-based `levelTiles` 转成 Cocos 当前关卡配置。
- 完成 T079 胡了卜 Graph-based 牌山生成器地基设计。
- 确认当前 Cocos 随机柱式牌山不适合作为长期地基，后续应以 `牌山骨架图 -> 理论解法路径 -> 牌面发牌 -> 难度/体验评估` 为主线。
- 完成 T080 胡了卜 Graph-based 牌山生成器共享实现第一版。
- 新增 `packages/shared/src/mahjong-mountain-generator.ts`，支持 seed 稳定的中心塔/双翼模板、5% 遮挡图、理论解法路径、`碰 / 吃 / 杠 / 胡` 牌面分配、干扰节点、体验报告和 Cocos 后续可消费的 `levelTiles`。
- 完成 T081 胡了卜地图模板语法系统设计。
- 用户已确认采用“模板语法系统”路线，并选择“8 个核心模板 + 第二批 backlog 预留”。第一期核心模板为中心塔、双翼、十字、环形、长墙、岛屿、峡谷、阶梯；第二批 backlog 为花瓣、堡垒、棋盘、迷雾外圈。
- T081 只做正式设计落档，不修改生成器代码和 Cocos；后续建议先做 T082 模板注册表和参数系统，再做 T083 8 个核心模板实现。
- 完成 T082 胡了卜模板注册表和参数系统实施计划。
- 新增 `docs/superpowers/plans/2026-05-28-hulebu-template-registry-parameter-system.md`，将后续共享生成器重构拆为注册表类型、参数归一化、现有两模板迁移、ExperienceReport 扩展、导出与文档、验证六段。
- T082 只写实施计划，不修改 `packages/shared` 代码、不接 Cocos；后续进入实现时应先保留 `center-tower` 和 `two-wings` 行为，再扩 8 个核心模板。

## 2026-05-21

- 完成 T029 框架调研和规划。
- 补齐模块文档目录。
- 明确推荐路线：共享规则与配置优先，GDevelop 可做 Web H5 原型，Cocos Creator 作为正式小游戏主线，Next.js 只做站内嵌入和事件接收。
- 玩法细节尚未定稿，后续需要讨论 `FRAMEWORK_PLAN.md` 中列出的关键问题。

## 2026-05-22

- 完成 T030 手动组合和成长系统规划。
- 新增 `GAMEPLAY_PLAN.md`。
- 确认消除不自动触发，改为玩家手动点击 `吃 / 碰 / 杠` 按钮发动。
- 明确按钮默认灰色，满足条件后切换为金红冒火状态。
- 将槽位定义为可成长属性，规划铜钱、槽位容量、备用槽、槽位净化、局内奖励和道具体系。
- 完成 T031 最终模式结构定稿。
- 确认最终结构为：`闯关模式`、`无尽牌山`、`高阶周目`、`每日牌局`、`成就图鉴`。
- 完成 T032 高阶挑战系统规划。
- 确认高阶挑战由牌山层数增长、词缀系统、随机事件、卡槽压缩和 Boss 试炼组成。
- 已补充第一批 12 个高阶词缀、12 个随机事件和 5 个 Boss 试炼。
- 完成 T033 组合提示和牌堆生成规则。
- 确认吃碰杠出现合法组合即提示并展示消除内容；多组合可选择；后续高阶挑战支持手动选择 3-4 张再发动。
- 确认牌堆采用组合包受控随机生成，不要求完整麻将牌组，低关可以只用一种花色。
- 补充杠包被碰后产生孤张的处理机制。
- 完成 T034 牌谱记牌器规划。
- 确认顶部显示 `余牌`，默认展示花色总数，展开显示 1-9 点数剩余数量。
- 明确记牌器显示数量，透视显示位置，两者分工不同。
- 完成 T035 局内 Roguelike 能力池规划。
- 已按孤张/补牌/换牌、杠流、吃流、碰流、花色流、槽位流、道具流、信息流 8 类规划 44 个局内能力。
- 已明确直接移除牌、补牌、换牌属于强能力，需要稀有度和出现数量限制。
- 开始 T038 永久固化能力和卡槽系统规划。
- 明确 `手牌槽位` 和 `能力卡槽` 分开管理，透视归入道具强化，不等于扩槽。
- 已规划永久能力四层结构：基础成长、固化能力、起局能力、道具强化。
- 已确认普通闯关/东风场基线为固化能力槽 3、起局能力槽 1、道具强化槽 1，高阶周目逐步压缩能力卡槽。
- 完成 T040 完整牌局规则、经济体力和失败救场规划。
- 明确可点击牌只看上层遮挡，入槽动画期间锁定输入，入槽后只能用 `撤回` 道具撤销。
- 明确多个 `吃 / 碰 / 杠` 组合由玩家自由选择，点击发动后不做二次确认。
- 将组合收益改为 `局内积分`，用于本轮购买洗牌、撤回、透视等局内资源；`铜钱` 用于过关、无尽、成就和永久成长。
- 新增体力系统口径：每局消耗 5 点，体力上限 100 点，按时间恢复，可用铜钱或付费购买。
- 明确失败前救场顺序：组合检测、备用槽自动救场、满槽护符、救场道具窗口、首败保护、失败结算。
- 明确孤张只做内部判定，不直接在牌面标记给玩家。
- 将教学口径改为 `新手引导`，不是独立关卡类型。
- 完成 T041 团队评审版玩法方案整理。
- 新增 `GAMEPLAY_REVIEW_PLAN.md`，用于团队评审和后续人工维护。
- 新增 `GAMEPLAY_REVIEW.html`，用于更直观浏览玩法循环、核心系统、MVP 边界和待评审问题。

## 2026-05-23

- 完成 T042 MVP 玩法验证计划。
- 新增 `MVP_VALIDATION_PLAN.md`，用于定义核心假设、最小验证闭环、测试样本、观察指标和通过标准。
- 新增 `MVP_VALIDATION_PLAN.html`，用于更直观展示验证目标、验证场景、观察标准和 MVP 冻结线。
- 明确下一步不是直接扩完整玩法，而是先做最小可玩闭环，验证手动吃碰杠、槽位压力、余牌价值、局内奖励和失败救场是否成立。
- 完成 T043 最小可玩闭环和 MVP 开发拆分计划。
- 新增 `MVP_BUILD_PLAN.md`，用于承接 T042，明确验证原型功能、5 个验证场景、受控随机组合包、验证观察项、正式 MVP 开发拆分和冻结线。
- 新增 `MVP_BUILD_PLAN.html`，用于团队快速浏览后续执行路径。
- 明确推荐下一步是新增 `T044：麻将 Roguelike 最小可玩验证原型`，先做 3-5 个验证场景；验证通过后再回到 T017 做规则模型、配置生成器和 Cocos 核心场景。
- 完成 T044 最小可玩验证原型。
- 新增 `PLAYABLE_VALIDATION_PROTOTYPE.html`，用单文件 HTML/CSS/JS 实现 5 个验证场景，覆盖点击、入槽、手动 `吃 / 碰 / 杠`、候选选择、余牌、局内积分、奖励三选一、道具和满槽救场。
- 已通过浏览器桌面端检查：第一场手动 `碰`、奖励弹层、多组合候选和危局备用槽救场可用。
- 已通过浏览器移动端检查，并加入牌山坐标缩放，避免窄屏牌面溢出。
- 完成 T045 胡了卜命名落档和规则模型第一版。
- 确认游戏显示名为 `胡了卜`，模块 slug 继续使用 `mahjong-roguelike`，避免目录和历史任务大范围迁移。
- 新增 `packages/shared` 包壳和 `mahjong-game` 规则模型。
- 规则模型覆盖吃、碰、杠候选检测，满槽前组合检测，执行组合结算，余牌统计，遮挡可点击判断和 Roguelike 奖励效果。
- 已通过 `npm run test -w packages/shared -- mahjong` 和 `npm run typecheck -w packages/shared`。
- 完成 T046 胡了卜验证场景配置草案。
- 新增 `apps/game/mahjong-roguelike/config/tiles.json`、`levels.json`、`rewards.json`。
- 新增 `apps/game/mahjong-roguelike/README.md` 和 `docs/rules.md`，说明配置和共享规则模型的关系。
- 已将 T044 的入门、顺子、杠冲突、多组合、危局 5 个验证场景沉淀为引擎无关配置。
- 已通过 JSON 解析检查、配置引用检查、`npm run docs:sync` 和 `git diff --check`。
- 完成 T047 胡了卜 MVP 10 关和 10 奖励配置草案。
- 已在 `levels.json` 中追加 5 个主线关卡草案：三门初会、留杠一手、连吃试手、看余牌、小胡收官。
- 已在 `rewards.json` 中追加 2 个奖励：碰上开花、洗山，使局内奖励达到 10 个。
- 新增 `apps/game/mahjong-roguelike/docs/content-plan.md`，说明 MVP 关卡曲线和奖励路线。
- 已通过 MVP 配置数量和引用检查、`npm run docs:sync` 和 `git diff --check`。
- 完成 T048 胡了卜配置加载验证。
- 新增 `packages/shared/src/mahjong-config.test.ts`，读取真实 `levels.json` 和 `rewards.json`，验证 10 关和 10 奖励能被共享规则模型承接。
- 配置加载测试覆盖基础契约、ID 唯一性、奖励和遮挡引用、初始状态创建、可点击牌、余牌统计、组合候选样本和奖励 effect 应用。
- 已通过 `npm run test -w packages/shared -- mahjong` 和 `npm run typecheck -w packages/shared`。
- 完成 T049 胡了卜配置驱动试玩原型。
- 新增 `apps/game/mahjong-roguelike/prototypes/config-playable/index.html`，直接读取 10 关配置和奖励配置。
- 原型支持关卡切换、牌面渲染、遮挡点击、槽位、候选组合、手动 `吃 / 碰 / 杠`、余牌、积分、铜钱、工具和奖励选择。
- 根据反馈增强牌山视觉：加宽牌面、层级偏移、交错偏移、厚度阴影和牌桌背景，让上层牌更明显压住下层牌。
- 已确认当前仍是配置联调页，不是最终“羊了个羊”式密集牌山；下一步需要牌山生成器和更高牌量布局。
- 已通过桌面端浏览器检查：指定 3 张 9筒后 `碰` 亮起，消除后下层 2万解锁，第二次 `碰` 后奖励三选一出现。
- 已通过移动端 390px 检查：无横向溢出，牌山、关卡、槽位可显示。
- 完成 T050 胡了卜牌山生成器和密集堆叠布局。
- 在配置试玩页新增 `配置关卡 / 密集牌山` 模式切换，保留原 10 关配置联调能力。
- 密集牌山模式按关卡组合素材生成 50 张左右的牌，使用 4 层坐标模板和几何重叠自动计算 `blockedBy`。
- 已修正高牌量下的 z-index 层级命中问题，保证顶层牌真正覆盖下层牌并可优先点击。
- 已通过浏览器检查：第 1 关密集模式生成 50 张牌，其中 42 张被压住；点击顶层三张 `2万` 后 `碰 1` 亮起，可点击牌从 8 张增加到 11 张。
- 已通过窄宽度检查：当前 546px 浏览器宽度无横向溢出，顶部模式切换、牌桌和槽位可显示。

## 2026-05-24

- 开始 T051 胡了卜麻将牌面 UI 参考图。
- 使用 `pptoken-imagegen` 生成 3 张牌面风格参考 sheet：
  - `output/imagegen/hulebu-tile-ui-reference-clean-readable.png`
  - `output/imagegen/hulebu-tile-ui-reference-warm-jade.png`
  - `output/imagegen/hulebu-tile-ui-reference-fantasy-roguelike.png`
- 三张参考图均为 `1536 x 1024` PNG，用于比较清爽高可读、温润玉牌和轻幻想 Roguelike 三种方向。
- 本次只生成参考图，不切 sprite atlas，不接入 Cocos/GDevelop，不修改现有玩法原型。
- 用户确认偏好清爽高可读方向，但指出 AI 生成图中 1-9 牌面不准确且有重复。
- 已改为程序化绘制准确版 v2：
  - 总览图：`output/imagegen/hulebu-tile-ui-reference-clean-readable-accurate-v2.png`
  - 单牌目录：`output/imagegen/hulebu-tiles-v2/`
  - 清单文件：`output/imagegen/hulebu-tiles-v2/manifest.json`
- 已校验准确版 v2 共 27 张单牌，`wan / tong / tiao` 各 1-9，单牌尺寸为 `256 x 360` PNG。
- 用户继续反馈 `7条` 和 `7筒` 观感不对。
- 已重新生成准确版 v3：
  - 总览图：`output/imagegen/hulebu-tile-ui-reference-clean-readable-accurate-v3.png`
  - 单牌目录：`output/imagegen/hulebu-tiles-v3/`
  - 清单文件：`output/imagegen/hulebu-tiles-v3/manifest.json`
- 已校验 v3 共 27 张单牌，`wan / tong / tiao` 各 1-9，`7筒` 和 `7条` 单牌均为 `256 x 360` PNG。
- 用户继续指定重点观察 `5条 / 6条 / 7条 / 8条` 和 `6筒 / 7筒`。
- 已重新生成准确版 v4：
  - 总览图：`output/imagegen/hulebu-tile-ui-reference-clean-readable-accurate-v4.png`
  - 单牌目录：`output/imagegen/hulebu-tiles-v4/`
  - 清单文件：`output/imagegen/hulebu-tiles-v4/manifest.json`
- 已校验 v4 共 27 张单牌，`wan / tong / tiao` 各 1-9，重点抽查的 `5条 / 6条 / 7条 / 8条 / 6筒 / 7筒` 均为 `256 x 360` PNG。
- 用户提供 `1条 / 2条 / 6条 / 8条` 新参考图，要求按新图重写。
- 已重新生成准确版 v5：
  - 总览图：`output/imagegen/hulebu-tile-ui-reference-clean-readable-accurate-v5.png`
  - 条子重点检查图：`output/imagegen/hulebu-tiao-reference-v5-focus.png`
  - 单牌目录：`output/imagegen/hulebu-tiles-v5/`
  - 清单文件：`output/imagegen/hulebu-tiles-v5/manifest.json`
- 已校验 v5 共 27 张单牌，`wan / tong / tiao` 各 1-9，重点抽查的 `1条 / 2条 / 6条 / 8条` 均为 `256 x 360` PNG。
- 用户提供完整 `1-9条` 参考图，要求按完整参考重改条子。
- 已重新生成准确版 v6：
  - 总览图：`output/imagegen/hulebu-tile-ui-reference-clean-readable-accurate-v6.png`
  - 条子完整检查图：`output/imagegen/hulebu-tiao-reference-v6-focus.png`
  - 单牌目录：`output/imagegen/hulebu-tiles-v6/`
  - 清单文件：`output/imagegen/hulebu-tiles-v6/manifest.json`
- 已校验 v6 共 27 张单牌，`wan / tong / tiao` 各 1-9，`1条` 到 `9条` 均为 `256 x 360` PNG。
- 用户继续提供 `8条` 单独参考图，要求修正为上下两个大号结形。
- 已重新生成准确版 v7：
  - 总览图：`output/imagegen/hulebu-tile-ui-reference-clean-readable-accurate-v7.png`
  - `8条` 检查图：`output/imagegen/hulebu-tiao-08-v7-focus.png`
  - 单牌目录：`output/imagegen/hulebu-tiles-v7/`
  - 清单文件：`output/imagegen/hulebu-tiles-v7/manifest.json`
- 已校验 v7 共 27 张单牌，`wan / tong / tiao` 各 1-9，`8条` 为 `256 x 360` PNG。
- 用户反馈 `8条` 仍然需要完全照参考图画，不要弯曲；同时要求后续先只生成完整总览图，确认后再拆分。
- 已重新生成准确版 v8：
  - 总览图：`output/imagegen/hulebu-tile-ui-reference-clean-readable-accurate-v8.png`
- v8 仅重画 `8条` 为直线 `W / M` 结构，未拆分单牌目录。
- 已检查 v8 总览图为 `1536 x 1024` PNG。
- 完成 T052 胡了卜 Boss 目标配置化第一版。
- 第 10 关新增 `bossGoals`，目标为 `吃 1 / 碰 1 / 杠 1 / 积分 80`。
- 配置试玩页已从单一 `bossGoal` 改为多目标 `bossGoals`，Boss 目标栏会显示多目标进度，清空牌山时必须全部目标完成。
- 密集牌山生成器已按 Boss 组合目标优先插入目标包，并在生成后校验组合次数和积分目标。
- 配置加载测试新增 Boss 目标契约校验，避免后续 Boss 关缺少目标或使用未知目标类型。
- 完成 T053 胡了卜 Boss 牌型目标第一版。
- 新增 `suit_set` 目标类型，第 10 关现在要求 `万 / 筒 / 条` 都至少完成 1 次组合。
- 配置试玩页会展示 `三门齐 万 0/1 · 筒 0/1 · 条 0/1`，并把该目标纳入 Boss 通关校验。
- 密集牌山生成器会为第 10 关 Boss 目标包分配 `wan / tong / tiao` 花色队列，保证三门齐目标理论可完成。
- 完成 T054 胡了卜 Boss 目标反馈和通关提示优化。
- Boss 目标栏已从纯文本改为目标标签，支持未完成、完成和刚推进三种状态。
- 执行组合后会短暂高亮对应组合目标、花色目标和积分目标。
- 清空牌山但 Boss 目标未完成时，提示改为 `牌山已清空，但 Boss 目标未完成`，并列出缺少的目标。
- 用户选中 `hulebu-eight-bamboo-celadon-candidate-2.png` 的宋代青瓷风格，并要求新增 `东 / 南 / 西 / 北 / 中 / 发 / 白板`。
- 已生成完整青瓷风牌面总览图：`output/imagegen/hulebu-complete-celadon-mahjong-face-v1.png`。
- 总览图包含 `万 / 筒 / 条` 1-9 和 `东 / 南 / 西 / 北 / 中 / 发 / 白板`，暂未拆分单牌。
- 已检查完整青瓷风总览图为 `1600 x 1220` PNG。
- 用户继续反馈 `1条 / 2条 / 3条 / 4条 / 7条` 排布需按参考图修正：`1条` 使用小鸟，`2条` 上下两根，`3条` 上一根下两根，`4条` 上二下二，`7条` 顶部竖条放在最上方。
- 已生成青瓷风条子 `1-9` 校正版完整预览图：`output/imagegen/hulebu-bamboo-celadon-corrected-layout-v1.png`。
- 本轮只生成完整预览图，暂未拆分单牌。
- 已检查校正版预览图为 `2304 x 760` PNG。
- 用户确认后续改为单张审核流，并以 `hulebu-eight-bamboo-celadon-candidate-2.png` 作为 `8条` 基准稿。
- 已按该风格生成 `1条` 小鸟单牌：`output/imagegen/hulebu-one-bamboo-celadon-single-v1.png`。
- 已检查 `1条` 单牌为 `1024 x 1024` PNG。
- 用户确认 `1条` 可用，继续生成 `2条` 单牌：`output/imagegen/hulebu-two-bamboo-celadon-single-v1.png`。
- 已检查 `2条` 单牌为 `1024 x 1024` PNG。
- 用户确认 `2条` 可用，继续生成 `3条` 单牌：`output/imagegen/hulebu-three-bamboo-celadon-single-v1.png`。
- 已检查 `3条` 单牌为 `1024 x 1024` PNG。
- 根据用户反馈，重新生成下方两根间距更宽的 `3条` v2：`output/imagegen/hulebu-three-bamboo-celadon-single-v2.png`。
- 已检查 `3条` v2 为 `1024 x 1024` PNG。
- 用户确认 `3条` v2 可用，继续生成 `4条` 单牌：`output/imagegen/hulebu-four-bamboo-celadon-single-v1.png`。
- 已检查 `4条` 单牌为 `1024 x 1024` PNG。
- 完成 T056 胡了卜固定 8 格主槽和胡牌基础支持。
- 主槽默认从 7 格调整为 8 格，旧扩槽奖励和旧 effect 会被限制在 8 格以内，不再把主槽推到 9 格或更多。
- 新增 `胡` 组合类型：主槽 8 张能拆成两个 3 张组合和一个对子时，可以点击 `胡` 一次消除 8 张。
- 备用槽继续作为救场，不参与 `胡` 的 `3 + 3 + 2` 判定。
- 通关条件继续以牌山清空为准，槽位中残留孤张不阻止过关。
- 配置试玩页已新增 `胡` 按钮、候选展示、分数和铜钱结算，并保留 `吃 / 碰 / 杠` 原有逻辑。

## 2026-05-25

- 用户确认 `4条` 可用，继续单张审核流生成 `5条`。
- 已生成 `5条` 单牌：`output/imagegen/hulebu-five-bamboo-celadon-single-v1.png`。
- 已检查 `5条` 单牌为 `1024 x 1024` PNG。
- 用户要求继续生成 `6条`，首次接口请求超时后已重试成功。
- 已生成 `6条` 单牌：`output/imagegen/hulebu-six-bamboo-celadon-single-v1.png`。
- 已检查 `6条` 单牌为 `1024 x 1024` PNG。
- 用户要求继续生成 `7条`。
- 已生成 `7条` 单牌：`output/imagegen/hulebu-seven-bamboo-celadon-single-v1.png`。
- 已检查 `7条` 单牌为 `1024 x 1024` PNG。
- 用户跳过重新生成 `8条`，要求继续生成 `9条`。
- 已生成 `9条` 单牌：`output/imagegen/hulebu-nine-bamboo-celadon-single-v1.png`。
- 已检查 `9条` 单牌为 `1024 x 1024` PNG。
- 用户确认条子按单张审核流生成版本使用，开始筒子单张审核流。
- 已生成 `1筒` 单牌：`output/imagegen/hulebu-one-dot-celadon-single-v1.png`。
- 已检查 `1筒` 单牌为 `1024 x 1024` PNG。
- 用户确认 `1筒` 可用，并要求继续设计 `2筒 / 3筒 / 4筒`。
- 已生成 `2筒` 单牌：`output/imagegen/hulebu-two-dot-celadon-single-v1.png`；首次接口请求超时后重试成功。
- 根据用户纠正，`3筒` 应为左上到右下三颗斜线排列，而不是上一下二。
- 已生成斜线排布 `3筒` 单牌：`output/imagegen/hulebu-three-dot-celadon-single-v1.png`；首次接口请求超时后重试成功。
- 已检查 `2筒 / 3筒` 单牌均为 `1024 x 1024` PNG。
- 已生成 `4筒` 单牌：`output/imagegen/hulebu-four-dot-celadon-single-v1.png`。
- 已检查 `4筒` 单牌为 `1024 x 1024` PNG。
- 用户要求继续生成 `5筒`。
- 已生成 `5筒` 单牌：`output/imagegen/hulebu-five-dot-celadon-single-v1.png`。
- 已检查 `5筒` 单牌为 `1024 x 1024` PNG。
- 用户要求继续生成 `6筒`。
- 已生成 `6筒` 单牌：`output/imagegen/hulebu-six-dot-celadon-single-v1.png`。
- 已检查 `6筒` 单牌为 `1024 x 1024` PNG。
- 用户要求继续生成 `7筒`；首次接口返回不完整后已重试成功。
- 已生成 `7筒` 单牌：`output/imagegen/hulebu-seven-dot-celadon-single-v1.png`。
- 已检查 `7筒` 单牌为 `1024 x 1024` PNG。
- 根据用户反馈，`7筒` 上方三颗应为左上到右下斜排，而不是横排。

## 2026-05-26

- 完成 T067 胡了卜 Cocos 首屏目标图视觉壳。
- Cocos 运行时首屏已从黑底占位推进为目标图方向的视觉壳：绿色牌桌、顶部关卡/分数/进度牌、右侧洗牌/撤回/提示按钮、底部木质 8 格槽。
- 修复 Cocos Web Preview 只显示默认启动图的问题：`GameSceneController` 启动时会自动创建 `RuntimeCamera`，绑定到 `Canvas.cameraComponent`，并设置正交投影、清屏色和可见层。
- 已在 Chrome 的 Cocos Web Preview 手机视口中确认首屏显示胡了卜牌山、绿色桌面、右侧工具按钮和 8 格卡槽。
- 用户已安装 Cocos Dashboard 和 Cocos Creator 3.8.8。
- 完成 T062 胡了卜 Cocos Creator 3.8.8 工程接入。
- 已确认本机编辑器路径：`/Applications/Cocos/Creator/3.8.8/CocosCreator.app`。
- 已新增 `apps/game/mahjong-roguelike/cocos/hulebu-cocos-3.8.8/` 工程壳，可在 Cocos Dashboard 中添加/打开。
- 工程壳基于 Creator 3.8.8 自带 `empty-2d` 模板结构，包含 `package.json`、`tsconfig.json`、`.creator/`、`settings/`、`profiles/`。
- 已新增 Cocos 首场景脚本边界：`GameSceneController`、`BoardLayerBinder`、`SlotLayerBinder`、`ComboBarBinder`、`HudBinder` 和 `HulebuSceneModel` DTO。
- 已新增 `assets/scenes/README.md` 和 `assets/resources/config/README.md`，说明真实 `HulebuGameScene.scene` 创建、脚本挂载和配置导入边界。
- 已新增 `packages/shared/src/mahjong-cocos-project.test.ts`，用测试保护工程壳关键文件。
- 真实 `.scene`、prefab、资源 atlas、动画和配置同步仍需后续任务在 Creator 中完成。
- 已重新生成斜排上三颗的 `7筒` v2：`output/imagegen/hulebu-seven-dot-celadon-single-v2.png`；首次接口返回不完整后已重试成功。
- 已检查 `7筒` v2 为 `1024 x 1024` PNG。
- 完成 T060 胡了卜 Cocos/GDevelop 正式表现层桥接第一版。
- 新增 `packages/shared/src/mahjong-presentation.ts`，把 `MahjongGameState` 转换为引擎无关的表现层快照。
- 表现层快照覆盖牌山可点/遮挡状态、8 格主槽、备用槽、`胡 / 杠 / 碰 / 吃` 按钮、余牌和 HUD。
- 新增 `packages/shared/src/mahjong-presentation.test.ts`，用测试保护 Cocos/GDevelop 可消费的数据契约。
- 新增 `apps/game/mahjong-roguelike/docs/formal-presentation-bridge.md`，说明 Cocos 场景结构、GDevelop 对象变量映射、输入回传和本任务边界。
- 完成 T057 胡了卜胡牌节奏配置和密集牌山胡牌包。
- 新增 `featuredCombos` 关卡重点组合字段，第 6 关和第 10 关已标记 `胡`。
- 配置试玩页已展示“本关重点”，密集牌山模式会优先生成一个可胡的 `3 + 3 + 2` 8 张组合包。
- 已修正密集牌山生成器遮挡写入口径，超过 5% 遮挡才写入 `blockedBy`，与点击禁用规则保持一致。
- 已通过共享配置测试、脚本语法检查、桌面端第 6 关胡牌包试玩和 390px 移动端检查。
- 完成 T058 胡了卜 20 关节奏骨架和第二 Boss。
- `levels.json` 已扩展到 20 关，新增第 11-20 关二阶段草案。
- 奖励节点已固定为第 3/6/9/13/16/19 关，Boss 节点为第 10/20 关。
- 第 20 关 Boss 已加入 `吃 / 碰 / 杠 / 胡 / 万筒条字 / 积分` 复合目标。
- 配置试玩页 Boss 目标和密集牌山解法校验已支持 `胡` 目标。
- 已修复第 20 关密集牌山中 `featuredCombos: ["hu"]` 与 Boss `胡 1` 目标重复占用 8 张可用牌的问题；现在重点胡牌包会复用为 Boss 胡牌目标。
- 配置试玩页已支持 `?level=20&mode=mountain` 深链接，方便直接验收第 20 关密集牌山。
- 完成 T059 胡了卜随机牌山调参面板。
- 配置试玩页密集牌山模式已新增开发用 `牌山调参` 面板，支持调整随机种子、牌量、同列堆叠深度、`胡` 包数量和字牌权重，并可一键重新生成当前关卡牌山。
- 密集牌山生成器已支持 URL 参数：`seed`、`tiles`、`stack`、`hu`、`honor`；例如 `?level=20&mode=mountain&seed=calibrate&tiles=58&stack=6&hu=2&honor=90`。
- 已补充共享配置回归测试，确认调参参数能进入生成器并改变牌量、同列堆叠、胡包数量和字牌数量。
- 完成 T061 胡了卜 Cocos 场景骨架第一版。
- 新增 `packages/shared/src/mahjong-cocos-scene.ts`，把表现层快照转换为 Cocos 友好的节点、控件和 HUD 视图模型。
- 新增 `packages/shared/src/mahjong-cocos-scene.test.ts`，用测试保护 Cocos 绑定数据契约。
- 新增 `apps/game/mahjong-roguelike/cocos/`，包含场景节点结构、脚本边界和 prefab key 绑定清单。

## 2026-05-26

- 继续 T051 胡了卜麻将牌面 UI 参考图。
- 根据用户新参考图，先生成 `1万` 样稿，作为后续 `2-9万` 的风格起点：
  - `output/imagegen/hulebu-one-wan-celadon-single-v1.png`
- 已统一检查该样稿为 `1024 x 1024` PNG；本轮仍只生成图片参考资产，不接入 Cocos/GDevelop 工程。
- 继续 T051 胡了卜麻将牌面 UI 参考图。
- 根据用户提供的筒子参考图，补齐青瓷风 `7筒 / 8筒 / 9筒` 单牌：
  - `output/imagegen/hulebu-seven-dot-celadon-single-v1.png`
  - `output/imagegen/hulebu-eight-dot-celadon-single-v1.png`
  - `output/imagegen/hulebu-nine-dot-celadon-single-v1.png`
- 已统一检查三张新筒子单牌均为 `1024 x 1024` PNG；本轮只生成图片参考资产，不接入 Cocos/GDevelop 工程。
- 完成 T063 胡了卜 Cocos 首屏自动渲染。
- 新增 `assets/scripts/bootstrap/HulebuSampleSceneModel.ts`，提供 Creator 播放验证用的占位 scene model。
- `GameSceneController` 默认加载测试 scene model，并在需要时自动补 `Canvas` / `UITransform`。
- `BoardLayerBinder` 可自动生成占位牌节点并绘制牌面、可点态和暗化态。
- `SlotLayerBinder` 可自动绘制 8 格主槽。
- `ComboBarBinder` 可自动绘制 `胡 / 杠 / 碰 / 吃` 按钮。
- `HudBinder` 可按节点名自动查找或创建 HUD Label。
- Cocos 工程 `tsconfig.json` 已补 `ignoreDeprecations` 和 `skipLibCheck`，便于本地 `npx tsc` 对项目脚本做检查。
- 完成 T065 胡了卜 Cocos 手机竖屏首屏适配。
- Cocos 工程设计分辨率已设置为 390x844。
- 测试牌山、8 格主槽、组合按钮和 HUD 已按手机竖屏重排，解决 iPhone 预览时牌山过小且靠下的问题。
- 完成 T066 胡了卜 Cocos 真实可见尺寸自适应。
- `HulebuSampleSceneModel` 新增运行时布局解析：读取 `game.canvas.clientWidth/clientHeight`、`view.getVisibleSize()` 和 DPR，输出统一布局尺寸。
- `GameSceneController`、`BoardLayerBinder`、`SlotLayerBinder`、`ComboBarBinder`、`HudBinder` 已统一使用运行时尺寸和缩放值，避免 Cocos Web Preview 的物理像素尺寸把首屏内容缩小。
- 处理了 Cocos 预览中新增 `utils` 目录导致的资产缓存残留问题，最终把布局工具收回既有脚本，浏览器预览不再出现新的 `Missing class`。
- 已在 iPhone 预览中确认 HUD 顶部显示、牌山居中、组合按钮和 8 格主槽位于底部且可读。
- 开始并完成 T068 胡了卜麻将 UI 图片资源归档和切图。
- 已新增 Cocos UI 图片资源目录：`apps/game/mahjong-roguelike/cocos/hulebu-cocos-3.8.8/assets/resources/ui/mahjong-tiles/`。
- 已按 `牌面 / 字牌 / 参考图 / 中间稿` 分类：
  - 27 张数牌单图：`tiles/numbered/wan/`、`tiles/numbered/tiao/`、`tiles/numbered/tong/`。
  - 7 张字牌单图：`tiles/honors/`，从 `output/imagegen/hulebu-honor-tiles-celadon-reference-v1.png` 切出。
  - 2 张参考图：`references/`。
  - 7 张中间稿：`drafts/`。
- 已新增 `manifest.json` 和 `README.md`，记录每张图的来源、分类、尺寸、`tileKey` 和后续绑定口径。
- 完成 T069 胡了卜 Cocos 首条点击可玩链路。
- 已为 Cocos 首屏测试牌增加触摸回调，可点击牌会进入 8 格主槽。
- 已为 `胡 / 杠 / 碰 / 吃` 组合按钮增加触摸回调，候选满足时按钮可执行基础消除。
- `GameSceneController` 已持有测试用主槽状态，点击和消除后会刷新牌山、槽位、HUD、分数和组合按钮。
- 本任务只跑通 Cocos 测试 scene model 的首条互动链路，真实关卡配置、最终图片 prefab、动画、奖励和关卡流转仍留给后续任务。
- 已通过 `npm run test -w packages/shared -- mahjong-cocos-project`。
- 已通过 `npx tsc --noEmit -p apps/game/mahjong-roguelike/cocos/hulebu-cocos-3.8.8/tsconfig.json`。
- 已通过 `npm run test -w packages/shared -- mahjong`。
- 已通过 `npm run typecheck -w packages/shared`。
- 已通过 `npm run docs:sync`，同步 35 个任务分片和 35 个领取分片。
- 已通过 `git diff --check`。
- 已在 Cocos Web Preview 手机视口手动验证点击入槽和 `7条 / 8条 / 9条 -> 吃` 消除链路。
- 完成 T070 胡了卜 Cocos 点击后遮挡解锁和槽位牌名显示。
- `GameSceneController` 已在每次刷新测试首屏时按剩余牌重新计算遮挡状态，超过 5% 面积被更高层剩余牌遮挡时保持不可点。
- 点击移走上层牌后，下层牌会重新变亮并恢复可点，避免“上面拿走了，下面还是点不了”的反馈断层。
- `SlotLayerBinder` 已为每个槽位稳定创建直接子级 `Label`，入槽牌会显示当前牌名。
- 已修正 Cocos `resources.meta` 的 Asset Bundle 配置，避免 Web Preview 构建 settings 查询报错。
- 已通过 Cocos Web Preview 手机视口手动验证：`7条 / 8条 / 9条` 入槽显示牌名，`吃` 消除后中层 `西` 重新解锁并可入槽。

## 2026-05-27

- 完成 T072 胡了卜 Cocos 真实配置首关接入。
- Cocos 工程新增 `HulebuLevelConfig`、`HulebuRuntimeState` 和 `HulebuConfiguredSceneModel`，默认从真实第 1 关 `validation_intro_peng` 创建首屏 scene model。
- `GameSceneController` 新增 `loadConfiguredLevelOnStart`，启动时优先加载真实配置；点击牌和点击 `胡 / 杠 / 碰 / 吃` 组合按钮会通过 runtime state 刷新牌山、卡槽、HUD 和按钮状态。
- 旧测试 `HulebuSampleSceneModel` 作为 fallback 保留，方便后续定位表现层问题。
- 已补充 Cocos 工程结构测试，校验 Cocos 内嵌首关配置与 `apps/game/mahjong-roguelike/config/levels.json` 第 1 关关键字段一致。
- 已在 Cocos Web Preview 手机视口手动验证：真实第 1 关显示 3 张上层 `9筒` 和 3 张下层 `2万`；三张 `9筒` 入槽后 `碰 1` 激活；执行 `碰` 后卡槽清空，下层 `2万` 继续可点。
- 当前仍使用程序化占位牌面，尚未绑定最终 SpriteFrame prefab、奖励三选一、Boss 目标进度或关卡流。
- 完成 T073 胡了卜 Cocos 牌面 SpriteFrame 绑定第一版。
- 新增 `HulebuTileSpriteCatalog`，把 T068 归档的 27 张数牌和 7 张字牌映射到 Cocos `resources.load(.../spriteFrame)` 路径，并加入缓存和 pending callback 合并。
- `BoardLayerBinder` 新增 `TileArt` 图片层，优先按 `model.prefabKey` 加载青瓷麻将 SpriteFrame；图片加载成功后隐藏文字标签，缺图或加载失败时继续显示程序化占位牌和文字 fallback。
- 已在 Cocos Web Preview 手机视口手动验证：真实第 1 关上层 `9筒` 和下层 `2万` 显示图片牌面；三张 `9筒` 仍可点击入槽，`碰 1` 激活后可消除；消除后下层 `2万` 图片牌面保留并可继续点击。
- 当前图片源为 1024x1024 生成图，牌体四周仍有背景留白，第一版只验证资源绑定链路；后续需要做正式裁切、Tile prefab、图集、槽位图片和动画表现。
- 完成 T074 胡了卜无边框麻将牌面资源。
- 已从 T068 的带框青瓷牌面中派生 34 张透明无边框 PNG：27 张数牌位于 `tiles/borderless/numbered/`，7 张字牌位于 `tiles/borderless/honors/`。
- 无边框 PNG 保持 1024x1024 SpriteFrame 尺寸，但边缘 alpha 清空，只保留牌面符号，便于叠在 Cocos 自绘牌体或后续 Tile prefab 底板上。
- `manifest.json` 已升级记录 `borderlessNumberedTiles` 和 `borderlessHonorTiles`；`README.md` 与 `scene-binding.md` 已明确运行时优先使用无边框资源，原带框资源只做回退和美术复核。
- `HulebuTileSpriteCatalog` 已把 34 个 `tileKey` 的加载路径切换到 `tiles/borderless/**/spriteFrame`。
- 完成 T075 胡了卜新牌面 UI 重新应用。
- 已从 `tiles/borderless/` 透明来源图派生 34 张运行时留白版 PNG：27 张数牌位于 `tiles/refreshed/numbered/`，7 张字牌位于 `tiles/refreshed/honors/`。
- 根据目标概念图反馈，留白版 v2 将符号有效内容高度控制在约 61%-62% 画布，保留牌面四周空白，避免麻将符号贴边。
- `manifest.json` 已记录 `assetSetVersion: cocos-refreshed-ui-v2-whitespace-2026-05-27` 和 `runtimeTileSet: refreshed`，并新增 `refreshedNumberedTiles` / `refreshedHonorTiles`。
- `HulebuTileSpriteCatalog` 已把 34 个 `tileKey` 的加载路径切换到 `tiles/refreshed/**/spriteFrame`，Cocos 牌山当前优先显示留白版新牌面。
- 完成 T076 胡了卜 Cocos 通关提示和下一关流转。
- Cocos runtime 已新增 20 关轻量流程配置，`GameSceneController` 持有当前关卡索引和 `playing / cleared / reward` 状态。
- 牌山清空后会弹出通关提示；点击继续进入下一关；第 3/6/9/13/16/19 关在继续时进入奖励三选一；选择奖励后进入下一关；20 关后显示本轮通关。
- 顶部关卡牌匾会按关卡序号显示 `1-1` 到 `2-10`。
- 牌、组合按钮和 overlay 按钮均同时绑定 `TOUCH_END` 与 `Button.EventType.CLICK`，便于浏览器鼠标和移动端触摸验证。
- 当前奖励选择只推进流程，尚未真正应用奖励效果；Boss 目标进度和完整 20 关平衡留给后续任务。
- 完成 T077 胡了卜 Cocos 随机堆叠牌山恢复。
- Cocos 默认 20 关已从 T076 的 6 张流程关恢复为确定性随机堆叠牌山：首关 42 张起步，后续递增到 60 张，支持 9-16 个随机列、4-6 层同列完全覆盖、字牌权重和 5% 遮挡阈值。
- `BoardLayerBinder` 新增 `StackDepthHint` 横条提示，同一列下层牌不再错位露出整张，只通过顶部横条表达下面还有层数。
- 已保留通关提示、继续下一关、奖励节点三选一和 20 关通关流程；新增回归测试确保默认首关不少于 36 张且初始可点牌大于 6，避免再次退化为低牌量流程关。
- 已通过 `npm run test -w packages/shared -- mahjong-cocos-project`、Cocos `tsc` 和 Cocos Web Preview 手机视口目检，预览刷新后首屏显示随机堆叠牌山和堆叠横条提示。
- 完成 T078 胡了卜 Cocos 牌山铺开和遮挡点击一致性。
- 已将随机列布局从紧凑网格改为横向 68、纵向 88 的铺开布局，首关配置跨度从约 `210x102` 扩大到 `300x186`，Cocos 手机预览中牌山不再挤成小团。
- `applyStackBlockers` 已修正为任意更高层牌只要覆盖低层牌超过 5% 就写入 blocker，同列所有上层牌都会阻挡下层牌。
- `HulebuRuntimeState` 已做测试环境可加载的轻量解耦，新增回归测试验证被盖住的下层牌不可入槽，移走 blocker 后才恢复可选。
- 已通过 `npm run test -w packages/shared -- mahjong-cocos-project`，当前 1 个测试文件、11 个测试通过；已通过 Cocos `tsc`；已通过 Cocos Web Preview 手机视口目检，顶层牌可入槽，被同列完全覆盖的下层牌连点不入槽。
- `/games/hulebu` 的 Web 壳层已开始把 T186 从规划落到实现：`局内流派` 现在是每局开局前的明确主轴，原来的 `路线挂载` 文案已收成 `局外偏好`，只保留长期倾向和轻协同。
- `apps/game/mahjong-roguelike/prototypes/config-playable/index.html` 与站内静态 Demo 现已真正解析 `runArchetype` 参数，并让它主导开局加成、奖励偏置、事件偏置和主线终章 Boss 的本局打法口径；`preferredRoute / routeMasteryLevel` 仍保留，但只负责局外轻协同。
- 又继续收了一刀旧路线残影：局外偏好现在只会轻补 1 档起手协同和少量后段偏置，不再像旧结构那样把奖励池、事件池和起手一起重新拉成“跨多关养同一路线”的读法；主线问题正式回到“每局开局前先选这局流派”。
- T186 奖励池口径继续明确：局外基础偏好池不再吃后段路线奖励 bias，只保留一张轻协同奖励；后半程奖励链由本局流派选择承担。
- T186 事件池和 Boss 说明也已同步到同一方向：旧 `preferredRoute` 不再连续塞事件，也不再出现在终章 Boss 主轴文案里；后续新事件或 Boss 校验优先挂到本局流派、长期模式或高阶构筑。
- T186 最终术语扫尾已完成。外层自动候选统一读作 `自动偏好`，内层 Boss 阶段目标统一读作 `本局流派主骨`，当前实现进入待验收；后续不要再恢复旧 `自动挂载 / 当前路线主骨` 文案。
- T187 已补刷新后继续当前本轮：`/games/hulebu` 外层会持久化未完成 `activeRun`，刷新后主线、每日、高阶恢复到当前关开局，无尽恢复到当前层开局；失败或通关后清空该快照。
- T188 已补账号级当前本轮存档：`HulebuProgress.activeRun` 会保存同一份恢复快照，登录同步时本地和账号快照按 `updatedAt` 选择更新的一份，让继续本轮不再只依赖本机缓存。
- T189 已开始并完成旧 Cocos 工程复用评估立项。现有 `apps/game/mahjong-roguelike/cocos/hulebu-cocos-3.8.8/` 可作为正式 Cocos 承接基座，复用项目壳、场景 Binder、runtime 起点、牌面资源目录和共享测试；后续追平应先同步 Web v6 视觉资源和全量牌面映射，再补当前 Web 的有限牌河、明牌组、补杠、震落、满槽救场、Boss、事件、无尽、每日、高阶和局外成长。
- T190 已完成 Cocos 首轮 v6 视觉资源接入。Cocos resources 新增 `ui/v6/` 资源树，包含 Web 当前 v6 牌面、组合按钮、工具按钮、HUD 和槽位；`HulebuTileSpriteCatalog` 已从旧 `refreshed` 牌面切到 v6 牌面，`ComboBarBinder` 会优先加载 v6 操作按钮图片并保留文字 fallback；共享测试已锁定 34 张牌面资源和 `6条` 路径。
- T191 已完成 Cocos v6 HUD、槽位和工具按钮绑定。`SlotLayerBinder` 现在会加载 `hand_slots_8` 作为主槽背板；`HudBinder` 会加载 `tile_counter_wide / level_badge / score_badge` 背板；`GameSceneController` 的 `洗牌 / 撤回 / 提示` 工具按钮会加载 v6 图片。所有新绑定都保留程序化 fallback，并由 `mahjong-cocos-project` 测试覆盖。
- T192 已完成 Cocos 有限牌河、明牌组和补杠基础迁移。`HulebuCocosSceneModel` 已新增 `openMeldNodes / riverNodes`，`HulebuRuntimeState` 已新增 `river / riverLimit / openMelds` 和 `bugang` candidate，`MeldRiverLayerBinder` 会渲染明牌区与牌河；执行 `碰 / 杠 / 补杠 / 胡` 会维护基础明牌和牌河状态。
- T193 已完成 Cocos 丢弃工具和牌河交互。`SlotLayerBinder` 现在支持主槽点击回调；`GameSceneController` 新增丢弃选择态，右侧 `打牌` 工具可进入选择态，点击主槽牌会调用 runtime `discardSlotTile()` 并刷新牌河。
- T194 已完成 Cocos 基础震落和满槽救场。`HulebuRuntimeState` 现在会在 `杠 / 补杠` 后震落最多 2 张压顶牌、在 `胡` 后震落最多 3 张并清牌河；被震落的牌清除 blocker、回到可点 board 状态；主槽满且牌河可用时状态文本提示 `可打牌入河`。
- T195 已完成 Cocos 洗牌和撤回真实交互。`HulebuRuntimeState` 新增历史快照栈和工具次数状态，`useShuffleTool()` 会确定性重排 board 牌面并消耗洗牌次数，`useUndoTool()` 会恢复最近快照并消耗撤回次数；`GameSceneController` 的右侧 `洗牌 / 撤回` 按钮已接入对应 runtime 方法。
- T196 已完成 Cocos Boss 目标基础接入。`HulebuLevelConfig` 已支持 `bossGoals`，第 10/20 关配置了组合次数、花色集合和积分目标；`HulebuRuntimeState` 会记录 Boss 进度并输出 HUD 摘要；`GameSceneController` 已改为牌山清空且 Boss 目标完成才进入通关流程。
- T197 已完成 Cocos 奖励效果基础接入。Cocos runtime 新增本轮奖励状态，三选一奖励现在能影响后续关卡的备用槽、护符、首败保护标记、开局铜钱、工具次数和吃/碰/杠分数；`GameSceneController` 会在选择奖励后把累计状态传入下一关。

- T207 已补 Cocos 高阶风场压力基础。`HulebuLevelConfig` 新增 `HULEBU_ADVANCED_RUN_PRESSURES` 和压力读取 helper；`GameSceneController` 会在每关启动时把当前高阶风场压力与关前事件合并后传入 runtime。东风会扣看山，南风禁洗牌，西风继续压撤回/看山，北风禁洗牌和看山并扣撤回/打牌。

- T208 已补 Cocos 高阶专属奖励基础。`HulebuLevelConfig` 新增 `HULEBU_ADVANCED_REWARD_POOLS` 和 `getHulebuRewardChoicesForRun()`；`GameSceneController` 的奖励弹层在高阶 run 中会优先展示当前风场奖励，并由普通奖励补足 3 个选项。高阶奖励效果已接入 runtime，可影响工具、护符、备用槽、开局铜钱和组合分数。

- T209 已补 Cocos 高阶能力槽基础。`HulebuLevelConfig` 新增 `HULEBU_ADVANCED_ABILITIES` 和 `getHulebuAdvancedAbilityChoices()`；`GameSceneController` 新增 `advancedAbility` phase，高阶风场选择后会先显示高阶能力 3 选 1，再进入本局流派选择。第一版能力效果通过本轮奖励状态和每关修饰器进入 runtime。

- T210 已补 Cocos 高阶事件池基础。`HulebuLevelConfig` 新增 `HULEBU_ADVANCED_SPECIAL_EVENT_POOLS`，`getHulebuSpecialEventChoices()` 现在可按 run profile 返回事件；高阶 run 会优先展示当前风场事件，再用普通事件补足 3 个选项。高阶事件复用现有事件 effect 并通过本关 modifier 生效。

- T211 已补 Cocos 事件元信息基础。`HulebuSpecialEventConfig` 新增 `rarity / tags / dangerLevel`；普通事件和高阶事件都已有稀有度、标签和风险等级；`GameSceneController` 的关前事件弹层会在事件名下展示元信息，为后续事件卡美术、构筑权重和每日/无尽事件变体预留稳定字段。

- T212 已补 Cocos 无尽/每日事件变体。`HulebuLevelConfig` 新增 `HULEBU_ENDLESS_SPECIAL_EVENTS`、`HULEBU_DAILY_SPECIAL_EVENTS` 和 `getHulebuModeSpecialEventPool()`；无尽事件节点优先给出长线补给与尾盘容错，每日事件节点优先给出今日补钱与禁看山词缀压力，再用普通事件补足 3 个选项。

- T213 已补 Cocos 本局流派事件偏置。`HulebuLevelConfig` 新增 `HULEBU_ARCHETYPE_SPECIAL_EVENT_POOLS` 和 `getHulebuArchetypeSpecialEventPool()`；`GameSceneController` 会把当前 `runArchetype.archetypeId` 传入事件选择 helper。事件节点现在优先展示本局流派相关事件，再展示高阶/无尽/每日等模式事件，最后用普通事件补足。

- T214 已补 Cocos Boss 变体基础。配置层新增 `HULEBU_BOSS_VARIANTS`、`getHulebuBossVariantForRun()` 和 `createHulebuRuntimeLevelForRun()`；主线第 10/20 关会区分 `中段试炼 / 胡了卜王`，高阶、无尽、每日 run 会按 profile 获得对应 Boss 变体和轻量目标补丁。Cocos HUD 会显示变体名称，通关判断继续复用 Boss 目标完成逻辑。

- T215 已补 Cocos 当前本轮继续基础。`GameSceneController` 新增 `HulebuActiveRunSnapshot` 和本地存储 helper；进入关卡时会持久化当前 run 的 mode、当前关序、本局流派、本轮奖励、局外成长、局外铜钱和高阶能力；大厅存在“继续本轮”按钮和运行中摘要，点击后可恢复到当前关开局。

- T216 已补 Cocos 最近一轮结算摘要基础。`GameSceneController` 新增 `HulebuSettlementSnapshot` 和本地存储 helper；run 通关回大厅时会记录上一轮模式、关序、铜钱奖励和奖励数；大厅副标题在无 active run 时会展示最近一轮摘要。

- T217 已补 Cocos 本地长期进度基础。`GameSceneController` 新增 `HulebuMetaProgressSnapshot` 和本地存储 helper；无尽 run 完成后会刷新最高层、每日 run 会刷新对应 seed 的最佳关序、高阶 run 会刷新最高已完成风场；大厅模式按钮会显示长期进度副文案。

- T218 已补 Cocos 局外生涯总览基础。大厅新增 `生涯` 入口；`GameSceneController` 新增 `collection` phase 和生涯总览弹层，可集中展示当前本轮、最近一轮、局外铜钱、六轴成长等级，以及主线、无尽、每日和高阶的本地累计状态。

- T219 已补 Cocos 成就图鉴最小版基础。`GameSceneController` 新增本地 `achievements` 快照、首批 8 项成就配置和读写 helper；主线、无尽、每日、升级和高阶推进会解锁对应成就；`生涯` 面板新增图鉴总览、下一项目标和首批图鉴摘要。

- T220 已补 Cocos 主线独立长期进度基础。`HulebuMetaProgressSnapshot` 新增 `bestMainlineLevel`，主线 run 完成后会写回最高已到关序；大厅主线按钮和生涯面板现在优先展示主线长期进度，不再只依赖最近一轮摘要。
- T221 已补 Cocos 局外档案本地持久化基础。`GameSceneController` 新增独立 `meta profile` 本地快照，局外铜钱和六轴成长会在启动大厅、回大厅、局外升级和通关发钱后稳定读写。
- T222 已补 Cocos 当前关中局恢复基础。`HulebuRuntimeState` 已提供运行态快照导出/恢复接口，`HulebuActiveRunSnapshot` 现会携带当前关 runtime 快照；大厅“继续本轮”存在 runtime 快照时可直接恢复中局牌桌、卡槽、牌河、明牌区、震落牌、分数和工具次数。
- T223 已补 Cocos 关间 phase 恢复基础。`HulebuActiveRunSnapshot` 已新增 `resumablePhase`，`cleared / reward / event` 三类节点进入时会写回 active run；大厅“继续本轮”现在也可恢复清关继续、奖励三选一和关前事件 overlay。
- T224 已补 Cocos 开局前 flow 恢复基础。`HulebuActiveRunSnapshot` 现会记录 `pendingRunProfile`，`resumablePhase` 覆盖 `advancedAbility / archetype`；高阶能力选择和本局流派选择节点也会写回 active run，继续本轮可恢复到对应开局前 overlay。
- T225 已补 Cocos 每日牌局第二版信号基础。配置层新增每日词缀 helper 和 8 档每日主题；每日 run 的奖励与事件会按当日 seed 稳定偏置；`metaProgress` 新增 `dailyStreak / lastDailySeed`，大厅每日按钮和生涯总览可展示今日词缀、今日奖励、今日最佳与连续参与。
- T226 已补 Cocos 账号进度桥接基础。`GameSceneController` 现可在浏览器环境下尝试调用 `/api/games/hulebu/progress`；大厅启动和回到局外时会尝试合并账号档，本地 `metaProfile / metaProgress / achievements / activeRun` 变化时会做轻量推送；未登录或接口失败时继续回退本地档。
- T227 已补 Cocos 明牌区与槽位真实牌面。`MeldRiverLayerBinder` 现在会优先把明牌区和牌河渲染成真实麻将牌面，`SlotLayerBinder` 也会优先把主槽和备用槽渲染成真实麻将牌面；四块牌区继续保留文字 fallback，并已通过 Cocos TypeScript 编译与共享静态测试。
- T228 已补 Cocos 候选组合选择弹层基础。`HulebuRuntimeState` 现在可按组合类型返回全部候选，`GameSceneController` 在多组同类候选时会先弹出选择面板；候选项优先显示真实小牌面并保留文字 fallback，选择后按点中的候选 key 结算，单候选仍直接执行。
- T229 已修复 Cocos 预览跨工作区生成器导入。`HulebuLevelConfig.ts` 不再直接 import 工程外 `packages/shared/src/mahjong-mountain-generator`，Cocos 工程内新增 `HulebuMountainGenerator.ts` 承接预览所需的 graph-based levelTiles 生成口径；共享测试已扫描 Cocos 脚本，防止跨工程源文件 import 复发。
- T230 已修正 Cocos 移动端布局缩放口径。runtime 布局不再把 `devicePixelRatio` 当作额外放大因子，小屏视口现在允许按可视区域缩小；`HulebuSampleSceneModel` 与 `HulebuRuntimeState` 统一改为可收缩的 `layoutScale`，共享测试已锁定这一口径，防止再次把整屏放大。
- T231 已补 Cocos UI 素材接入。`ui/v6` 资源树补齐奖励卡、场景皮肤卡、弹层底板和 Buff 面板；`GameSceneController` 的顶部牌匾、奖励选择卡和流程弹层现在优先加载真实 Sprite，顶部 HUD 也收敛到视觉壳牌匾，减少早期占位壳和双 HUD 重叠感。
- T239 遗留修复已接手并完成：`BoardLayerBinder` 不再依赖每张牌自己的 Button 点击状态，而是给 BoardRoot 增加全屏 `UITransform`，统一接 `TOUCH_END` 后按当前 model 屏幕矩形和更高层覆盖率做手动 hit-test；最新口径已收紧为 `0.001` 顶层-only，灰化或被任意更高层实际覆盖的牌不会触发选牌。`enforceCocosInitialFreeMaximum()` 继续用同位置更高层 blocker 压牌，但限制在当前最大层以内，首关可点目标保持 5-6 张。
- T240 已补 Cocos 记牌器展开挂件和首关牌山观感。`HulebuHudModel.tileCounter` 现在提供结构化四门计数，`CounterPlaque` 点击可展开 `CounterExpandedPanel` 浮层并展示每张牌小图计数，浮层用 `BlockInputEvents` 阻断底层牌堆点击；首关最终切到 `long-wall` graph 模板，保持 30 张牌、6 张初始可点，并把牌山从分散环形堆叠收敛为更接近 demo 的横向压叠牌山。
- T239/T240 已补 Cocos Web Mobile 非 debug 构建和 Playwright 验证：构建日志显示 `build Task (web-mobile) Finished`；手机视口下点击可点牌后主槽 0->1、余牌 30->29，直接调用灰化被压牌点击入口主槽保持不变；展开记牌器时 `CounterExpandedPanel` 位于 `ToolOverlayRoot`，层级高于 BoardRoot。
- T239/T240 点击错位回归已修正：上一轮把 `UITransform.getBoundingBoxToWorld()` 误当成触摸坐标口径，导致视觉牌和命中 tileId 偏移。`BoardLayerBinder` 现在使用基于 model position 的 `getTileEventRect()` 对齐 `EventTouch.getUILocation()`；Playwright 手机视口实测 3 张代表性可点牌入槽后的 `tileId / prefabKey` 均与点击目标一致，灰化被压牌入口保持 no-op。
- T239/T240 最新收口为 demo 风格横向压叠牌山：首关模板从 `ring` 改为 `long-wall`，覆盖阈值统一收紧到 `0.001`，只要有更高层实际覆盖就灰化并禁点；被压牌 Sprite 增加灰色 tint。`long-wall` 列距二次调整后不再是 6 个孤立小塔，也不缩成一团，手机视口验收首关 30 张、6 张亮牌、24 张灰牌、运行时 `xSpan: 152 / ySpan: 16`、覆盖规则 0 违例；灰牌不入槽，亮牌入槽 tileId/prefabKey 一致，最终截图为 `.codex-tmp/hulebu-final-wall-before-click.png`。
- T239/T240 已补旧 active run 迁移门禁：`HulebuActiveRunSnapshot` 新增 `boardRevision`，当前版本为 `top-only-long-wall-2026-07-05`；本地 active run 或账号 `cocosSnapshot` 缺失/不匹配该版本时会自动作废，避免普通打开继续恢复旧 cross/高塔式牌山。发布包复测中预置旧 snapshot 后启动回到 lobby 且本地 active run 被清空，再进入首关为 30 张、6 张亮牌、24 张锁牌、`hasCrossIds=false`、`hasLongWallIds=true`。
- T239/T240 根据 Lee 最新反馈再次收口：首关目标改为 15 张总牌、3 张顶层可点、12 张锁牌；`HULEBU_BOARD_REVISION` bump 为 `top-only-three-card-2026-07-05`。BoardLayer 牌面尺寸从 `38x51` 收到 `32x43`，顶层缩放从 `1.14` 收到 `1.04`，锁层颜色和透明度更暗。Kimi WebBridge 真实 Chrome 验证新构建：启动 lobby active run 为空，进入首关 `余牌 15`、3 张可点；底牌边缘点 no-op，三张顶层牌点击后 slot0 的 tileId 均与目标一致。截图为 `.codex-tmp/kimi-three-card-after-fix.png`。
- T239/T240 入口和点击链路按 Lee 反馈再次修正：`GameSceneController` 启动时不再默认停在 lobby，读完本地状态后无有效 active run 直接进入教程首关；`HULEBU_BOARD_REVISION` bump 为 `tutorial-pointer-2026-07-08`，旧 active run 会失效，避免普通刷新继续看到旧复杂局。`BoardLayerBinder` 在原 Cocos `TOUCH_END / MOUSE_UP` 基础上新增 canvas `pointerup / mouseup / touchend` 兜底，把 DOM client 坐标换算到现有 hit-test UI 坐标后再走最高层命中判断，修复桌面点击无效。验证：共享测试 46 个通过、Cocos TS 通过、web-mobile 非 debug 构建完成；Kimi WebBridge 真实 Chrome 启动态为 `playing / 15 总牌 / 3 可点 / 12 锁牌`，截图 `.codex-tmp/kimi-tutorial-pointer-final-start.png`；Playwright 鼠标点击中间顶层牌后 slot0=`graph_long-wall_node-010`，总牌 15->14。
- T243 已建立正式 Cocos 源码与 production 构建溯源基线。正式运行时和 v6 UI 已进入可复现提交；构建会拒绝脏/未跟踪/索引异常输入，从受保护的精确提交快照调用固定 Creator 3.8.8，并把源码、完整 Creator bundle、TypeScript、产物和 HTTP smoke 证据写入 schema 6 manifest。原子 owner marker、promotion journal 与可重试 tombstone 覆盖失败/崩溃恢复；干净 worktree 已通过发布测试 `189/189`、工程测试 `32/32` 和真实 build/verify-only。下一步 T244 回到 `GameSession / RunStateMachine / ContentRepository / SaveService` 的产品架构迁移。
- T249 正式 UI 视觉样稿已获 Lee 确认，青绿玉石主场景、动作按钮三态和工具按钮三态成为正式资源基线。
- T251 已完成正式 UI Batch A+B。资源包新增 `780×1688` 青绿干净背景、5 个 HUD/牌槽资源、15 个动作三态和 15 个工具三态，共 36 个 manifest 资源；设计视口固定为 `390×844`，resourceScale 为 2。
- T251 新增可重复构建脚本、主场景组合预览、透明棋盘三态预览和 validation report；36 个 key 唯一、全部 RGBA、同组三态画布一致，校验状态为 `passed`。
- T252 已完成正式 UI Batch C：新增 4 张无文字卡片、5 个无文字弹窗和 35 张统一麻将牌，formal v1 manifest 扩展到 80 项。
- T252 卡片/弹窗由 PPTOKEN 新站生成母版并在本地完成洋红隔离底抠图；八条从旧字母状图案修正为 2×4 标准竹节，其他牌面沿用 v7 已清理牌体。
- Batch C validation report 已通过：Batch A+B 36 项完整保留、80 个 key 唯一、全部 RGBA、透明角合格、麻将牌统一 `272×384`。
