# 胡了卜决策

## D001：使用双工程 + 共享配置路线

- 日期：2026-05-21
- 状态：建议，待玩法讨论后确认
- 决策：规则和配置保持引擎无关；GDevelop 可用于 Web H5 原型；Cocos Creator 用于正式小游戏工程；Next.js 只做站内嵌入和事件接收。
- 原因：避免 Web 原型、正式小游戏和站点入口三套逻辑割裂。
- 影响：T017 实现前应优先建立 `packages/shared` 规则模型和 JSON 配置。

## D002：第一版不实现完整麻将算法

- 日期：2026-05-21
- 状态：已确认
- 决策：第一版只做 `碰 / 吃 / 杠 / 清一色 / 胡牌目标` 的轻量化消除规则。
- 原因：降低用户学习成本和实现复杂度。
- 影响：复杂番型、听牌、算番、真实麻将局流程全部后置。

## D003：消除必须由玩家手动发动

- 日期：2026-05-22
- 状态：已确认
- 决策：槽位中的牌不会自动消除。系统只检测 `吃 / 碰 / 杠` 是否可用，并把对应按钮从灰色切换为金红冒火状态；玩家点击按钮后才发动消除。
- 原因：手动发动更有麻将手感，也能形成选择压力和策略判断，避免游戏退化成普通自动合成。
- 影响：规则模型需要区分“检测可用组合”和“执行组合”；失败判定需要在槽位满且没有可发动组合时才触发。

## D004：槽位作为局外成长核心

- 日期：2026-05-22
- 状态：建议，待数值验证
- 决策：槽位不只是容量，而是局外养成核心。玩家通过软货币升级槽位容量、备用槽和槽位净化等属性。
- 原因：槽位成长可以自然降低难度，给玩家长期目标，同时不破坏第一阶段免费原则。
- 影响：奖励、道具和关卡难度需要围绕槽位压力重新平衡。

## D005：最终模式结构

- 日期：2026-05-22
- 状态：已确认
- 决策：最终结构采用 `闯关模式`、`无尽牌山`、`高阶周目`、`每日牌局`、`成就图鉴`。
- 原因：20 关首通只能解决新手目标，不能承载长期挑战；无尽层数、高阶周目、每日固定牌局和成就收集可以分别提供挑战、深度、回访和收集目标。
- 影响：关卡配置需要支持固定 20 关、无尽层生成、周目词缀、每日种子和成就条件；UI 需要预留模式入口和通关后解锁反馈。

## D006：高阶挑战由多种压力组成

- 日期：2026-05-22
- 状态：已确认
- 决策：高阶挑战采用 `牌山层数增长 + 词缀系统 + 随机事件 + 卡槽压缩 + Boss 试炼`，不只靠增加牌数或堆叠层数。
- 原因：单纯增加堆叠会让游戏变长但不一定更好玩；限制能力、改变规则和加入选择事件能提供更稳定的挑战感和新鲜感。
- 影响：后续配置需要支持词缀、事件、试炼和能力禁用；UI 需要清楚展示当前词缀、事件选择和被限制的能力。

## D007：组合提示和牌堆生成规则

- 日期：2026-05-22
- 状态：已确认
- 决策：`吃 / 碰 / 杠` 只要出现合法组合就提示，并展示对应消除内容；多个组合时允许玩家选择具体组合。后续高阶挑战支持玩家手动选择 3-4 张牌再发动。牌堆使用组合包受控随机生成，不要求完整麻将牌组，可以同花色重复。
- 原因：组合提示保证基础可读性，组合选择保留策略；受控随机保证牌局有解但不死板；不使用完整麻将牌组可以服务关卡节奏和难度曲线。
- 影响：规则模型需要区分组合检测、组合候选列表、玩家选择和执行；关卡生成器需要支持吃包、碰包、杠包、目标包和干扰包。

## D008：孤张必须有消化机制

- 日期：2026-05-22
- 状态：已确认
- 决策：如果玩家提前 `碰` 掉本可 `杠` 的牌，导致剩余 1 张孤张，系统不直接判错，但需要提供后续补牌、备用槽、弃牌符、换牌、杠后补牌或特定关卡豁免等消化方式。
- 原因：保留 `碰` 和 `杠` 的策略冲突，但避免玩家觉得游戏制造无法处理的死牌。
- 影响：奖励、道具和关卡目标需要预留孤张处理能力；可解性校验只保证初始至少一条可解路径，不保证任意操作都能过。

## D009：顶部牌谱记牌器

- 日期：2026-05-22
- 状态：已确认
- 决策：游戏顶部新增 `余牌/牌谱记牌器`。默认显示 `万 / 条 / 筒` 剩余总数，展开后显示 1-9 点数剩余数量；消除后实时减少。
- 原因：记牌器能让玩家判断是否等待 `杠`、是否继续追顺子、孤张是否还有补牌机会，从而减少盲猜并提高麻将策略感。
- 影响：UI 需要预留顶部简版和展开层；规则状态需要维护剩余牌计数；透视仍保留，因为记牌器只显示数量，不显示位置。

## D010：局内能力池按流派分层

- 日期：2026-05-22
- 状态：已确认
- 决策：局内 Roguelike 能力池按孤张/补牌/换牌、杠流、吃流、碰流、花色流、槽位流、道具流、信息流 8 类组织。
- 原因：每类能力解决不同问题，能让一轮牌局形成不同构筑；孤张和补牌能力优先级最高，因为它们负责处理手动策略带来的残牌后果。
- 影响：后续能力配置需要带 `type`、`rarity`、`effect`、`tags`、`conflicts` 等字段；直接移除牌、补牌、换牌能力需要受稀有度和数量限制。

## D011：永久能力分层与局内能力分离

- 日期：2026-05-22
- 状态：已确认
- 决策：永久能力分为 `基础成长`、`固化能力`、`起局能力`、`道具强化` 四层；其中基础成长自动生效，不占能力卡槽，其余三层都需要装备。局内能力仍保持每过关 3 选 1，只在当前一轮牌局内生效。
- 原因：把局外成长、开局构筑、工具强化和局内奖励拆开后，玩家更容易理解每种能力的用途，也更容易后续平衡。
- 影响：能力配置需要明确 `slotType` 和 `scope`；UI 需要区分永久成长页、开局装备页和局内奖励选择。

## D012：手牌槽位与能力卡槽分开管理

- 日期：2026-05-22
- 状态：已确认
- 决策：`手牌槽位` 负责底部放牌容量和容错，`能力卡槽` 负责装备永久能力。高阶周目默认压缩能力卡槽，不直接等同于减少手牌槽位。
- 原因：手牌槽位承载基础难度，能力卡槽承载构筑难度；两者分离后，玩家能理解高阶挑战到底是在压容量、压 build，还是压信息。
- 影响：高阶词缀、Boss 试炼和周目规则需要写清限制对象；`透视` 归入道具强化，不等于扩槽。

## D013：完整牌局输入状态机

- 日期：2026-05-22
- 状态：已确认
- 决策：只有未被上层牌覆盖的牌可点击；牌入槽动画期间锁定输入；入槽后不能手动撤销，只能用 `撤回` 道具；玩家点击合法 `吃 / 碰 / 杠` 按钮后立即发动，不做二次确认。
- 原因：输入边界明确后，才能避免满槽、动画、撤回和组合发动之间出现状态歧义。
- 影响：规则模型需要有 `idle`、`tile_entering`、`combo_selecting`、`danger_check`、`resolving` 等状态；UI 需要在动画和危局时禁用不合法操作。

## D014：组合候选由玩家自由选择

- 日期：2026-05-22
- 状态：已确认
- 决策：多个合法组合同时存在时，系统展示候选组合，但不替玩家选择最优解；玩家可以选择候选组合，也可以手动选择 3-4 张槽内牌，只有满足条件时对应按钮可用。
- 原因：`吃 / 碰 / 杠` 的冲突选择是麻将手感和策略感的来源，不能被系统自动消掉。
- 影响：规则模型需要输出候选组合列表；UI 需要支持候选选择、槽内手动选择和按钮可用态联动。

## D015：双层收益和体力系统

- 日期：2026-05-22
- 状态：已确认
- 决策：组合收益先给 `局内积分`，用于本轮购买洗牌、撤回、透视等局内资源；过关、无尽层数、成就、每日和后续排行奖励给 `铜钱`，用于永久升级。每局正式牌局消耗 5 点体力，体力上限 100 点，可按时间恢复，也可用铜钱或付费购买。
- 原因：局内积分负责当局决策，铜钱负责长期养成；体力承接回访和商业化，但不应限制新手引导和低成本试玩。
- 影响：旧规划中“组合直接给铜钱”的口径需要改成局内积分；局外成长和体力购买使用铜钱。

## D016：失败前救场顺序

- 日期：2026-05-22
- 状态：已确认
- 决策：主槽满后按 `组合检测 -> 备用槽自动救场 -> 满槽护符 -> 救场道具窗口 -> 首败保护 -> 失败结算` 的顺序处理。不允许复活；失败后局内能力和局内积分清空。
- 原因：先保留玩家手动组合选择，再消耗低破坏性的容错资源，最后才清牌或失败，能减少误判挫败。
- 影响：备用槽、满槽护符、撤回、弃牌符、换牌、首败保护都要进入同一套危局判定流程。

## D017：孤张只做内部判定

- 日期：2026-05-22
- 状态：已确认
- 决策：系统内部可以定义 `暂时孤张` 和 `死孤张`，但不直接在牌面上标记给玩家；玩家通过记牌器、候选组合和道具提示判断风险。
- 原因：直接标记会削弱记牌器和判断空间；内部判定又是满槽护符、备用槽自动救场和孤张能力的必要基础。
- 影响：实现时需要保留内部孤张评分，但 UI 默认不显示 `孤张/死孤张` 标签。

## D018：先做最小可玩验证原型，再进入正式 MVP

- 日期：2026-05-23
- 状态：已确认
- 决策：T042 和 T043 之后不直接进入完整 T017 实现，先新增并完成最小可玩验证原型。原型只覆盖 3-5 个场景，验证手动 `吃 / 碰 / 杠`、槽位压力、余牌价值、局内奖励和失败救场是否成立；验证通过后再进入规则模型、配置生成器和 Cocos 核心场景。
- 原因：玩法规划内容已经较完整，如果跳过验证直接开发完整 MVP，容易把 20 关、无尽、高阶、每日、成就和长期成长一起塞进第一版，导致周期失控。
- 影响：后续建议先拆 `T044：麻将 Roguelike 最小可玩验证原型`；T017 正式 MVP 范围只冻结 10 关、10 个局内奖励、3 个基础道具和 3 个局外升级。

## D019：正式显示名使用胡了卜

- 日期：2026-05-23
- 状态：已确认
- 决策：游戏对外显示名使用 `胡了卜`，代码和文档目录 slug 暂时保持 `mahjong-roguelike`。
- 原因：`胡了卜` 更有记忆点，适合后续游戏站展示；保留现有 slug 能避免历史文档、任务记录和后续工程路径大范围迁移。
- 影响：后续 Web 入口、游戏标题、Cocos/GDevelop 工程展示名和配置 displayName 应统一为 `胡了卜`；目录名如需调整，应单独开任务处理迁移。

## D020：第一版规则模型先保持引擎无关

- 日期：2026-05-23
- 状态：已确认
- 决策：T045 已在 `packages/shared/src/mahjong-game.ts` 中建立纯 TypeScript 规则模型，优先覆盖吃碰杠、槽位、余牌、遮挡点击和基础奖励，不绑定 Cocos 节点、GDevelop 对象或 Next.js 组件。
- 原因：共享规则模型可以作为正式工程、Web 试玩和后续配置生成器的共同基础，避免把 HTML demo 的临时 DOM 状态复制进多个表现层。
- 影响：后续 Cocos/GDevelop 表现层应读取配置并调用共享规则概念；新增规则时优先补测试，再接 UI。

## D021：验证场景先配置化

- 日期：2026-05-23
- 状态：已确认
- 决策：T046 已将 T044 HTML demo 的 5 个验证场景沉淀到 `apps/game/mahjong-roguelike/config/levels.json`，并补齐 `tiles.json` 与 `rewards.json`。
- 原因：配置化可以让后续 Cocos/GDevelop/Web 试玩复用同一批验证关卡，减少表现层重复写死关卡和奖励逻辑。
- 影响：后续新增关卡和奖励应优先写配置；表现层只负责加载、渲染和转发输入。

## D022：MVP 内容先冻结 10 关和 10 奖励草案

- 日期：2026-05-23
- 状态：已确认
- 决策：T047 已将配置扩展到 10 个关卡和 10 个局内奖励，作为表现层接入前的 MVP 内容草案。
- 原因：10 关足够覆盖吃、碰、杠、多组合、余牌和危局救场，比直接做 20 关更适合快速调试和试玩验证。
- 影响：后续表现层优先加载这 10 关；第 11-20 关、无尽、每日和高阶周目继续后置。

## D023：配置必须进入共享规则测试保护

- 日期：2026-05-23
- 状态：已确认
- 决策：T048 已在 `packages/shared/src/mahjong-config.test.ts` 中读取真实关卡和奖励 JSON，作为表现层接入前的自动化保护。
- 原因：只检查 JSON 语法和引用不足以证明配置可用；配置必须能创建规则状态、产生可点击牌、输出余牌统计、形成基础组合样本，并能应用奖励 effect。
- 影响：后续新增或调整关卡、奖励时，应先跑 `npm run test -w packages/shared -- mahjong`，避免 Cocos/GDevelop/Web 表现层接入后才发现配置破坏规则模型。

## D024：配置试玩页不是最终牌山表现

- 日期：2026-05-23
- 状态：已确认
- 决策：T049 的 `config-playable` 原型定位为配置和表现层联调页，不作为最终“羊了个羊”式密集牌山效果。
- 原因：当前 10 关配置使用手写 `x/y/layer/blockedBy`，牌量少、遮挡关系显式，适合验证读取、点击、组合和奖励流程；真正接近“羊了个羊”需要牌山生成器、更多牌量、密集重叠、视觉层压和组合包铺牌算法。
- 影响：后续不要把 T049 原型的稀疏牌山误认为最终体验。下一步若继续游戏表现，应新增任务做牌山生成器和高密度堆叠布局，再接 Cocos/GDevelop。

## D025：密集牌山先作为 HTML 原型生成模式验证

- 日期：2026-05-23
- 状态：已确认
- 决策：T050 先在 `config-playable` 试玩页中加入 `密集牌山` 模式，而不是立即创建 Cocos/GDevelop 工程。
- 原因：当前最需要验证的是牌量、遮挡、解锁节奏和组合包铺牌是否成立。把生成器先放在已有试玩页中，可以快速和 `配置关卡` 模式对比，同时避免过早投入正式引擎工程成本。
- 影响：后续正式工程应复用 T050 的生成思路，但不要直接复制 HTML DOM 状态逻辑；Cocos/GDevelop 仍需要独立实现渲染、动画、输入和资源管理。

## D026：Boss 关目标采用配置化多目标

- 日期：2026-05-24
- 状态：已确认
- 决策：Boss 关不再使用写死的单一目标，而是通过关卡配置中的 `bossGoals` 表达多个目标。第一版支持 `combo_count`、`suit_set` 和 `score_target`，第 10 关目标为 `吃 1 / 碰 1 / 杠 1 / 三门齐 / 积分 80`。
- 原因：Boss 关需要成为阶段记忆点，不能只是普通关卡加一个“杠几次”的条件；组合次数加积分目标能同时要求玩家规划吃碰杠顺序和结算收益。
- 影响：后续第 20 关、无尽 Boss 和高阶周目 Boss 可以沿用 `bossGoals` 扩展；密集牌山生成器需要根据目标包保证目标组合可完成。

## D027：东南西北中发白按字牌进入基础牌池

- 日期：2026-05-24
- 状态：已确认
- 决策：用户口头称为“花牌”的 `东 / 南 / 西 / 北 / 中 / 发 / 白板`，实现口径按麻将常用分类处理为字牌。字牌进入基础牌库和密集牌山生成器，可参与同张 `碰 / 杠`，不参与连续 `吃`。
- 原因：字牌能显著提升麻将辨识度和牌池复杂度，但如果允许 `东南西` 这类顺子会违背玩家直觉，也会污染现有吃牌目标。
- 影响：后续关卡、奖励和 Boss 目标需要重新平衡字牌比例；如要加入春夏秋冬梅兰竹菊等真正花牌，应另开任务，不和本次字牌混用。

## D028：主槽固定 8 格并加入轻量胡牌

- 日期：2026-05-24
- 状态：已确认
- 决策：主槽固定为 8 格，不再通过奖励扩到 9 格或更多。`胡` 作为第四个手动组合按钮进入第一版规则：主槽正好 8 张，且能拆成 `3 + 3 + 2` 时，可以一次消除 8 张。备用槽不参与 `胡` 判定。
- 原因：8 格刚好能承载两个 3 张组合加一个对子，既有麻将记忆点，也能保留槽位压力；扩到 9 格会明显降低难度，让备用槽从救场变成常规容量。
- 影响：后续奖励池不应再提供主槽扩容能力，槽位成长应转向备用槽、满槽护符、撤回、透视、洗牌或孤张处理；正式工程需要把 `胡` 做成和 `吃 / 碰 / 杠` 同级的手动动作。

## D029：胡牌节奏先用关卡重点字段控制

- 日期：2026-05-25
- 状态：已确认
- 决策：第一版不做复杂听牌或番型生成器，而是在关卡配置中新增 `featuredCombos` 重点组合字段。带 `hu` 的关卡会在试玩页显示“本关重点”，密集牌山模式优先生成一个 `3 + 3 + 2` 的 8 张胡牌包。
- 原因：当前需要验证 8 格主槽下 `胡` 的爽点和压力，不需要立刻重写全关卡平衡或引入完整麻将算法。重点字段能让内容侧明确哪些关卡承担教学或压力验证。
- 影响：后续如果 `胡` 出现太稳定，应先把 `featuredCombos` 扩展为权重或数量配置，而不是把备用槽纳入胡牌判定，也不要恢复主槽扩容。

## D030：20 关骨架先锁奖励和 Boss 节点

- 日期：2026-05-25
- 状态：已确认
- 决策：第一版 20 关先按 `3 / 6 / 9 / 13 / 16 / 19` 做奖励节点，按 `10 / 20` 做 Boss 节点。第 20 关 Boss 使用 `吃 / 碰 / 杠 / 胡 / 万筒条字 / 积分` 复合目标，先验证终章压力，不把数值视为最终平衡。
- 原因：用户已经明确奖励和 Boss 节奏。先锁骨架能让后续讨论集中在关卡难度、奖励强度和 Boss 目标是否清楚，而不是继续停留在 10 关原型。
- 影响：后续扩展奖励池和正式工程时，应保留这些节点语义；如果第 20 关目标过多，优先调整目标数量和展示层级，不改回单一 Boss 条件。

## D031：密集牌山参数先在原型层可调

- 日期：2026-05-25
- 状态：已确认
- 决策：密集牌山的随机种子、目标牌量、同列堆叠深度、`胡` 包数量和字牌权重先通过配置试玩页的开发用调参面板和 URL 参数调整，不在当前阶段写死为最终数值。
- 原因：用户希望牌山轮廓保持随机，具体配置后续进入正式游戏构建后继续调。先提供调参入口可以快速观察不同密度和字牌比例下的槽位压力，而不必反复改代码。
- 影响：调参面板不是正式玩家 UI；后续 Cocos/GDevelop 正式工程应把这些参数沉淀为关卡配置、生成器配置或编辑器字段，而不是照搬 HTML 面板。

## D032：Cocos/GDevelop 表现层先消费共享快照契约

- 日期：2026-05-25
- 状态：已确认
- 决策：正式表现层承接不直接复制 HTML 原型 DOM 状态，而是在 `packages/shared/src/mahjong-presentation.ts` 中输出引擎无关的 `MahjongPresentationSnapshot`。Cocos Creator 和 GDevelop 都只消费这份快照并回传输入意图。
- 原因：Cocos 是正式发布主线，GDevelop 是 H5 原型通道；如果两个表现层各自实现牌山、槽位、组合按钮和 HUD 状态，后续规则和配置会快速分叉。
- 影响：后续 Cocos 场景骨架应从 `createMahjongPresentationSnapshot` 开始接入；GDevelop 事件表只映射对象变量，不重新实现 `吃 / 碰 / 杠 / 胡` 判定。

## D033：Cocos 场景骨架先不依赖编辑器资源

- 日期：2026-05-25
- 状态：已确认
- 决策：Cocos 正式工程第一步先建立可测试的 `createMahjongCocosSceneModel` 和 `apps/game/mahjong-roguelike/cocos/` 节点绑定文档，不直接生成 `.scene`、`.prefab` 或 `.meta` 文件。
- 原因：当前环境未确认安装 Cocos Creator。先把 snapshot 到 Cocos 节点模型的转换放在共享包里测试，可以避免在编辑器资源和规则逻辑之间来回耦合。
- 影响：后续打开 Cocos Creator 时，应按 `cocos/scene-binding.md` 创建真实场景资源，并让组件脚本消费 `createMahjongCocosSceneModel`，不要把规则判断写进节点脚本。

## D034：Cocos Creator 工程壳先按 empty-2d 模板落地

- 日期：2026-05-25
- 状态：已确认
- 决策：用户安装 Cocos Creator 3.8.8 后，先在 `apps/game/mahjong-roguelike/cocos/hulebu-cocos-3.8.8/` 创建基于 Creator 自带 `empty-2d` 模板结构的工程壳，并补充可挂载的脚本边界；真实 `.scene` 和 prefab 仍由编辑器生成和维护。
- 原因：这样能让 Cocos Dashboard 直接接手项目，同时避免手写复杂 Cocos 序列化资源格式导致后续编辑器冲突。
- 影响：下一步应在 Creator 中创建 `HulebuGameScene.scene`、绑定节点和脚本，再接共享 scene model。配置同步、资源 atlas、动画和发布构建需要继续拆任务。

## D035：Cocos 首屏先用运行时占位 UI 验证

- 日期：2026-05-26
- 状态：已确认
- 决策：`HulebuGameScene.scene` 仍由 Cocos Creator 编辑器生成和维护；代码侧先通过 `HulebuSampleSceneModel` 和 Binder 自动建节点能力，在运行时生成占位牌、8 格槽、组合按钮和 HUD。
- 原因：用户已经手工搭好节点结构，但手摆几十张测试牌和按钮细节成本高，且后续容易被脚本覆盖。运行时占位 UI 能先证明 Cocos 表现层接通，又不锁死最终美术和 prefab 结构。
- 影响：后续接真实资源时应把自动占位节点替换为 Tile prefab 池和按钮皮肤；接真实玩法前保留 `autoLoadSampleScene` 作为调试开关。

## D036：Cocos 表现层优先按手机竖屏设计

- 日期：2026-05-26
- 状态：已确认
- 决策：胡了卜 Cocos 工程第一版设计分辨率使用 390x844，首屏 UI 按手机竖屏优先布局，而不是沿用桌面横屏 1280x720 基准。
- 原因：用户明确后续主要在手机上游玩。先按手机竖屏确定牌山、主槽、按钮和 HUD 的空间关系，可以避免在桌面坐标中调出的布局到了真机上不可读。
- 影响：后续真实牌山、点击热区、HUD、奖励弹层和 Boss 目标展示都应先以手机竖屏为基准，再考虑桌面/横屏预览。

## D037：Cocos 首屏布局按运行时可见尺寸计算

- 日期：2026-05-26
- 状态：已确认
- 决策：Cocos 首屏占位 UI 不再只依赖固定 390x844 设计分辨率，而是读取运行时 `game.canvas.clientWidth/clientHeight`、`view.getVisibleSize()` 和 DPR，统一计算牌山、8 格主槽、组合按钮和 HUD 的尺寸与位置。
- 原因：Cocos Web Preview 在 iPhone 机型下 canvas 的 CSS 尺寸约为 390x741，但内部渲染尺寸为 780x1482。只用设计分辨率或 `view.getVisibleSize()` 会让首屏内容看起来过小、位置偏下。
- 影响：后续正式 UI、真实牌山、奖励弹层和工具按钮都应沿用运行时可见尺寸口径；新增 Cocos 脚本目录时要注意 `.meta` 和编辑器资产缓存，避免再次触发 `Missing class`。

## D038：地图模板采用语法系统而不是孤立模板函数

- 日期：2026-05-28
- 状态：已确认
- 决策：胡了卜地图模板后续采用 `TemplateArchetype -> Footprint -> LayerProfile -> OcclusionGrammar -> MountainSkeleton -> SolutionTrace -> FaceAssignment -> ExperienceReport` 的模板语法系统。第一期实现 8 个核心模板：中心塔、双翼、十字、环形、长墙、岛屿、峡谷、阶梯；第二批花瓣、堡垒、棋盘、迷雾外圈先进入 backlog。
- 原因：胡了卜目标是完整游戏，地图系统需要长期产关和调难度能力，不能只在生成器里堆几个写死模板分支。
- 影响：后续 T082 应先重构模板注册表和参数系统，再由 T083 实现 8 个核心模板；每个模板都必须带体验标签和校验指标，并继续复用 T080 的理论解法、发牌和 `ExperienceReport`。

## D039：模板注册表实现先保留现有两模板行为

- 日期：2026-05-28
- 状态：已确认
- 决策：模板注册表和参数系统实现时，第一步只迁移 `center-tower` 与 `two-wings`，并保留 T080 当前 seed 稳定性、理论解法、发牌、5% 遮挡和 `levelTiles` 输出。十字、环形、长墙、岛屿、峡谷、阶梯不混入注册表重构任务，由后续 T083 统一实现。
- 原因：先把架构换稳，再扩模板数量，可以避免一次性同时改变模板形状、参数系统和体验报告，降低回归定位难度。
- 影响：后续实现任务必须先写注册表、参数归一化和行为保留测试；只有现有两模板迁移通过后，才能进入 8 个核心模板扩展。

## D040：普通关残局收官替代强制清槽

- 日期：2026-06-01
- 状态：已确认
- 决策：普通关默认不要求 `牌桌清空 + 卡槽清空`。牌桌清空但卡槽仍有残张时进入 `残局收官`，玩家从 `弃牌通关 / 选作牌引 / 收入牌河` 中处理残张。教学关必须发动指定教学组合；Boss 关和特殊契约可以额外要求清槽或胡牌，但必须显式配置。
- 原因：强制清槽会把孤张变成生成器死局和负反馈；残局收官能把孤张转成关末决策、牌引构筑和长期牌河资源，更符合 Roguelike 的选择感。
- 影响：下一轮 Demo 实现应新增收官弹层、牌引状态、玩家可见记牌器和槽位选择式丢弃；正式工程后续再扩展牌河兑换、清场契约和 Boss 清槽目标。

## D041：核心玩法采用有限牌河、补杠和胡牌强奖励

- 日期：2026-06-02
- 状态：已确认
- 决策：胡了卜后续核心规则采用 `有限牌河 + 明碰区 + 补杠孤张出口 + 明杠开山 + 胡牌强奖励 + 听牌提示 + 孤张预算生成器`。补杠只负责处理碰后的第 4 张，低收益、不震山；手牌直接凑齐 4 张的明杠才触发震山开牌；胡牌不是普通关通关条件，而是清槽、强开山和处理牌河的最大局内奖励。
- 原因：严格要求所有牌都完美吃碰杠胡会放大孤张问题；只减少牌型又会削弱麻将乐趣。有限牌河和补杠提供可理解的恢复路线，明杠和胡牌提供微信小游戏需要的明确爽点。
- 影响：后续 Demo 实现应先落地牌河 3 格、明碰区、补杠、明杠开山、胡牌清槽和简化听牌提示；生成器需要从“有解路径”升级到“孤张预算 + 2-3 条恢复路线”校验。

## D042：先完成 Web 版完整内容，再追平 Cocos 和音画资源

- 日期：2026-06-16
- 状态：已确认
- 决策：T174 之后不优先让 Cocos 追平当前 Demo，也不优先补音乐、美术和动效资源。后续先把 `/games/hulebu` 网页版开发到内容、系统、数值和长期目标稳定，再按冻结规格追平 Cocos，最后补音画和发布资源。
- 原因：当前 Web Demo 仍在扩高阶、Boss、事件、成就、无尽、每日、奖励和局外能力。如果 Cocos 与音画资源现在追平，会跟随仍变化的规则和 UI 反复返工。
- 影响：T175 替代此前“下一步做 Cocos 追平”的建议。T176 起优先拆 Web 完整版内容任务；Cocos 仍是正式发布主线，但应等待 Web 内容和数值冻结后承接。

## D043：高阶周目先做四档和构筑线，不并入普通教程

- 日期：2026-06-16
- 状态：已确认
- 决策：T176 将高阶周目扩展为 `东风场 / 南风场 / 西风场 / 北风场` 四档，并用局外 `高阶配置`、高阶能力槽、专属奖励、档位事件和构筑识别承接长期压力。普通前几关教程只服务普通模式；高阶前几关不重复套普通教程。
- 原因：高阶玩家已经理解基础规则，继续弹普通教程会稀释周目压力；高阶真正需要的是构筑、限制、复盘和更明确的档位差异。
- 影响：后续 Boss 试炼第二版、特殊事件池和数值冻结应优先围绕高阶构筑线做深，不要把高阶入口降回普通教学节奏；Cocos 追平时也应按冻结后的高阶四档规格实现。

## D044：Boss 试炼第二版先内建在 Web 原型，不抽共享配置

- 日期：2026-06-16
- 状态：已确认
- 决策：T177 的 Boss 阶段、变体、奖励品质和复盘 payload 先放在 HTML 原型与 `/games/hulebu` Web 壳层内，不改 `levels.json`、`rewards.json` 或 Cocos 工程。
- 原因：当前 Web 完整版内容仍在迭代，Boss 第二版需要先验证阶段目标、奖励品质和复盘表达是否有用；过早抽成共享配置会让 Cocos、资源和数值一起追着变化。
- 影响：Cocos 后续追平时应以 Web 内容冻结后的 Boss 规格为准；T178 特殊事件池扩容可以继续先在 Web 原型验证，再决定哪些规则进入共享配置。
