# 胡了卜

**模块 slug**：`mahjong-roguelike`  
**显示名称**：`胡了卜`
**当前状态**：框架规划、玩法评审稿、MVP 验证计划、后续构建拆分、最小可玩验证原型、规则模型、验证配置、20 关内容骨架、配置加载验证、配置驱动试玩原型、密集牌山生成器原型、Boss 目标配置化、Boss 牌型目标、Boss 目标反馈、字牌基础支持、固定 8 格主槽和 `胡` 牌型基础支持、胡牌节奏配置和密集牌山胡牌包、随机牌山调参面板第一版、Cocos/GDevelop 正式表现层桥接第一版、Cocos 场景骨架第一版、Cocos Creator 3.8.8 工程壳、Cocos 首屏自动渲染、Cocos 手机竖屏首屏适配、Cocos 真实可见尺寸自适应、Cocos 首条点击可玩链路、真实首关配置接入、牌面 SpriteFrame 绑定、无边框麻将牌面资源、新牌面 UI 留白版、Cocos 最小通关闭环、Cocos 随机堆叠牌山恢复、Cocos 牌山铺开和遮挡点击一致性、Graph-based 牌山生成器共享实现、地图模板语法系统设计、模板注册表和参数系统实施计划、模板注册表和 8 个核心模板共享实现、Cocos Graph-based 生成器接入、玩家试玩页和调牌器分离、数百张小牌密集牌山原型、原型模板轮换和竖屏牌桌、原型散乱可见压叠层、原型随机组合堆遮挡和桌面密度调优、失败提示弹层、玩家页正式 HUD 空间压缩、玩家页正式一屏 HUD 重排、8% 轻遮挡验收调参、10 关朋友试玩 Demo、残局收官设计、混合窗口牌山生成器、玩家页布局放大和 auto 模板随机、教学关动作通关判定、朋友 Demo 渐进难度曲线、满槽显示修复、玩家页记牌器、有限牌河/补杠/胡牌奖励核心玩法设计、有限牌河/补杠/胡牌奖励试玩 Demo、站内网页试玩接入已完成、悬台窄腰调牌器模板与高压 auto 池接入、震落牌平铺和遮挡点击修复、残局收官第一阶段、特殊事件第一版、终局试炼 Demo 第一版、完整体验版推进方案已完成、20 关完整主线 Demo 已完成、局外首页和结算壳已完成、局外铜钱资产和 3 项外置升级已完成、路线型奖励池第一版已完成、无尽牌山第一版已完成、每日牌局第一版已完成、成就图鉴第一版已完成、高阶周目第一版已完成、登录账号长期进度续层已完成、网页版完整版优先路线已确认、UI 资产 Cocos 接入说明已补充、Cocos 本局流派基础已接入、Cocos 开局流派选择 UI 已接入、Cocos 局外模式入口已接入、Cocos 局外成长 UI 已接入、Cocos 高阶周目入口基础已接入、Cocos 局外铜钱和升级成本闭环已接入、Cocos 高阶风场压力基础已接入、Cocos 高阶专属奖励基础已接入、Cocos 高阶能力槽基础已接入、Cocos 高阶事件池基础已接入、Cocos 事件元信息基础已接入、Cocos 无尽/每日事件变体已接入、Cocos 本局流派事件偏置已接入、Cocos Boss 变体基础已接入、Cocos 当前本轮继续基础已接入、Cocos 最近一轮结算摘要基础已接入、Cocos 本地长期进度基础已接入、Cocos 局外生涯总览基础已接入、Cocos 成就图鉴最小版基础已接入、Cocos 主线独立长期进度基础已接入、Cocos 局外档案本地持久化基础已接入、Cocos 当前关中局恢复基础已接入、Cocos 关间 phase 恢复基础已接入、Cocos 开局前 flow 恢复基础已接入、Cocos 每日牌局第二版信号基础已接入、Cocos 账号进度桥接基础已接入、Cocos 明牌区与槽位真实牌面已接入、Cocos 候选组合选择弹层基础已接入、Cocos 预览跨工作区生成器导入已修复
**对应任务**：T017, T020, T029-T066, T068-T070, T072-T105, T162-T175, T189-T229

## 0. UI 资源接入手册

- v3 全量资源（T287，当前推荐）：`UI_ASSETS_V3_COCOS_INTEGRATION.md`
- 历史 v6 资源（T181/T182，仅对照旧风格时参考）：`UI_ASSETS_COCOS_INTEGRATION.md`

## 1. 模块定位

`胡了卜` 是游戏站第一阶段核心小游戏，用“羊了个羊”式堆叠消除作为基础交互，用麻将的 `碰 / 吃 / 杠 / 清一色 / 胡牌目标` 提供规则记忆点，再用 Roguelike 奖励提高复玩价值。

核心目标：

- 提升游戏站停留时长和回访。
- 做出比普通三消更有辨识度的麻将消除体验。
- 第一版验证玩法节奏、关卡难度和奖励构筑，不追求完整麻将规则。

## 2. 第一版范围

包含：

- `万 / 条 / 筒` 三类数牌，以及 `东 / 南 / 西 / 北 / 中 / 发 / 白` 字牌。
- 点击可选牌进入槽位。
- 主槽固定为 8 格，不再通过奖励扩到 9 格或更多。
- 系统检测 `碰 / 吃 / 杠 / 胡` 候选，玩家手动点击按钮后消除。
- `胡` 的第一版定义为主槽 8 张可拆成 `3 + 3 + 2`，即两个 3 张组合加一个对子；备用槽不参与判定。
- 槽位满且无组合/牌河/救场失败；普通关清空牌桌但槽内有残张时进入残局收官，可弃牌通关或选 1 张作下一关牌引。
- 每过一关从 3 个随机奖励里选择 1 个。
- 20 个关卡配置。
- 20 个 Roguelike 奖励配置。
- Web 站内试玩入口占位或原型嵌入。
- 正式小游戏工程规则文档。

不包含：

- 完整麻将听牌算法。
- 复杂番型结算。
- 多人游戏。
- 排行榜。
- 复杂养成。
- 真钱激励。

## 3. 技术路线

推荐路线：

1. 先把规则、关卡、奖励抽成共享配置和 TypeScript 规则模型。
2. 用 GDevelop 或轻量 Web 原型快速验证“堆叠点击 + 槽位消除 + 奖励选择”的手感。
3. 用 Cocos Creator 做正式小游戏工程主线，面向微信小游戏和抖音小游戏发布。
4. Next.js 游戏站只负责详情页、站内试玩 iframe、埋点和入口，不承载核心游戏逻辑。

当前已完成第一版共享规则模型：`packages/shared/src/mahjong-game.ts`，用于承接 HTML demo 的核心吃碰杠、槽位、余牌和奖励规则。

当前已完成 MVP 内容配置草案：`apps/game/mahjong-roguelike/config/`，用于承接 20 个关卡骨架、基础牌定义和 10 个局内奖励草案。

当前已完成配置加载验证：`packages/shared/src/mahjong-config.test.ts` 会读取真实 JSON 配置，校验关卡、奖励、初始状态、可点击牌、余牌统计和组合候选样本。

当前已完成配置驱动试玩原型：`apps/game/mahjong-roguelike/prototypes/config-playable/index.html`，用于本地 HTTP 服务下试玩 10 关配置。

当前已完成密集牌山生成器原型：同一试玩页可切换 `密集牌山` 模式，基于当前关卡组合素材生成更多牌、多层坐标和自动遮挡关系，用于验证接近“羊了个羊”式牌山的密度与解锁节奏。

当前已完成 Boss 目标配置化第一版：第 10 关通过 `bossGoals` 配置 `吃 1 / 碰 1 / 杠 1 / 三门齐 / 积分 80`，试玩页会展示多目标进度，并要求清空牌山时全部目标完成。

当前已完成 Boss 目标反馈第一版：目标栏会以标签展示完成态，组合推进后短暂高亮对应目标；清空牌山但目标未完成时，会明确提示缺哪些目标。

当前已完成字牌基础支持第一版：共享规则模型、配置牌库和配置试玩页都支持 `东 / 南 / 西 / 北 / 中 / 发 / 白`；字牌可参与 `碰 / 杠`，不会参与 `吃`。

当前已完成固定 8 格主槽和 `胡` 基础支持：共享规则模型、配置默认值和配置试玩页都使用 8 格主槽；旧扩槽效果会被限制在 8 格以内；`胡` 可一次消除符合 `3 + 3 + 2` 的 8 张主槽牌。

当前已完成胡牌节奏配置第一版：关卡可通过 `featuredCombos` 标记重点组合，第 6 关和第 10 关已标记 `胡`；配置试玩页会展示“本关重点”，密集牌山模式会优先生成一个可手动消除的 `3 + 3 + 2` 胡牌包。

当前已完成 20 关主线骨架第一版：第 3/6/9/13/16/19 关作为奖励节点，第 10/20 关作为 Boss 节点；第 20 关 Boss 已加入 `吃 / 碰 / 杠 / 胡 / 四类牌型 / 积分` 复合目标。

当前已完成随机牌山调参面板第一版：配置试玩页的密集牌山模式支持通过 URL 和侧栏面板调整随机种子、目标牌量、同列堆叠深度、`胡` 包数量和字牌权重，用于后续试玩调难度。

当前已完成玩家试玩页和调牌器分离：`prototypes/config-playable/index.html` 默认是干净的玩家试玩页，隐藏顶部配置切换和调参面板；`tuner.html` 作为独立调牌器入口进入 `view=tuner`，保留调关参数和密集牌山调参能力。

当前已完成 10 关朋友试玩 Demo 的教学动作通关判定：默认玩家页前 4 关分别教学 `碰 / 吃 / 杠 / 胡`，必须成功点击对应动作按钮才算过关；单纯把牌全部放进卡槽只会提示教学目标，不会跳关。教学关候选组合只显示本关目标动作，避免 `胡` 教学被 `吃 / 碰` 候选提前打断。

当前已完成数百张小牌密集牌山原型：`view=tuner&mode=mountain` 默认生成 240 张小牌，调参范围为 120-420 张；最新玩家页布局放大后牌山坐标系为 560x640 竖屏基准，规则牌尺寸为 52x70，桌面玩家页主栏为 480px，牌桌高度受 `420px / 58vh` 约束。调牌器仍可手动指定 8 个本地模板，默认玩家页 `auto` 模板会按 seed/重开随机并带安全回退，避免候选模板生成失败或首轮可点超过 8。密集牌山继续强制覆盖 `万1-9 / 条1-9 / 筒1-9 / 东南西北中发白` 全部 34 个具体牌面，用于评估接近“羊了个羊”式多层高密度读牌压力。

当前已完成原型散乱可见压叠层：默认玩家页和调牌器会在每个堆叠显示顶牌加 4 张下层预览牌；下层预览牌显示真实牌面、错位露出，但保持 blocked/disabled，不扩大首轮 3-8 张可点击入口。默认玩家页把栈深数字降噪为小圆点，调牌器仍保留数字深度提示。

当前已完成原型随机组合堆遮挡：默认玩家页和调牌器会在密集牌山中混合生成完全覆盖堆、轻微遮挡堆、错位预览堆和桥接组合堆；遮挡比例覆盖 5%-100%，运行态判定允许低于 8% 的轻微视觉遮挡继续点击，达到 8% 才阻塞。桥接顶牌移走后可解锁一个或多个下层入口，首轮可点击入口控制在 3-8 张区间；桥牌生成会避让彼此重叠，运行态点击判定会按实际渲染后的视觉矩形过滤，避免视觉上已经露出的顶牌仍不可点击。

当前已完成失败提示弹层：配置试玩原型在主槽满且没有可发动组合或可用救场时会弹出“本关失败”提示，说明失败原因并提供“重开本关”入口；Boss 目标未完成导致失败时也复用同一失败弹层。失败后牌面、组合按钮和工具按钮保持不可操作。

当前已完成玩家页正式 HUD 空间压缩：默认玩家页收紧顶部条、牌桌、卡槽、右侧面板和按钮间距；桌面 Kimi WebBridge 实测牌桌 `332x379`，整页不再滚动；390x844 移动受控检查中牌桌 `344x393`、占视口高度约 `46.6%`，卡槽在首屏可见且无横向溢出。

当前已完成玩家页正式一屏 HUD 重排：默认玩家页新增顶部 HUD，显示关卡、目标、余牌、积分和铜钱；右侧完整信息侧栏在玩家页隐藏，只保留 `洗山 / 回手 / 看山` 道具栏；调牌器视图继续保留调参面板、余牌、奖励和完整侧栏。桌面 Kimi WebBridge 实测牌桌 `307x351`、HUD `460x61`、卡槽 `460x114`、右侧道具栏 `76x204`，整页不滚动。验收补丁后牌面约 `24.5x32.7`，运行态遮挡探针验证 `0.9%` 覆盖可点、`11.1%` 覆盖阻塞；390x844 移动受控检查牌桌 `254x290`、卡槽首屏可见且无横向溢出。

当前已完成 10 关朋友试玩 Demo：默认玩家页只展示 10 关小 run；第 1-4 关分别教学 `碰 / 吃 / 杠 / 胡`，前 3 关使用 6 格主槽，第 3 关后固定奖励 `卡槽 +2`，第 4 关起使用 8 格主槽；第 5-10 关使用渐进 profile，牌量为 `72 / 96 / 132 / 168 / 210 / 240`。第 5 关标题为“正式入门”，第 10 关在朋友 Demo 中是“综合高压”压力关，不叠正式 Boss 目标和胡包；调牌器和正式配置仍保留原 Boss 配置。右侧道具改为 `洗牌 / 撤回 / 丢弃`，丢弃可移除主槽末尾牌并作为满槽救场资源。390 CSS 像素移动端改为底部横排三道具，卡槽和道具完整可见且无横向溢出。

当前已完成残局收官设计和 Demo 第一阶段实现：普通关不再把 `卡槽必须清空` 作为默认硬条件；牌桌清空但槽内有残张时进入 `残局收官`，玩家可选择 `弃牌通关`，或从主槽选 1 张 `选作牌引` 带入下一关并在 HUD 中提示。`收入牌河` 暂不做，保留为正式工程后续构筑方向。

当前已完成混合窗口牌山生成器：密集牌山不再把每个可点击窗口直接切成 3 张完整答案；`solutionGroup` 继续表示答案组，`solutionStep` 表示释放窗口，默认普通密集关首轮会混入不同答案组的牌并保证同组最多露出 2 张。第 1 关和朋友试玩第 5 关运行态抽样均已确认首轮不是直接三张同组答案。

当前已完成玩家页布局放大、auto 模板随机、满槽显示修复和记牌器：默认玩家页进一步压缩顶部 HUD、卡槽和外层留白，在 1512x682 桌面视口下无横向/纵向溢出；牌面实测约 37x49，首轮可点仍控制在 3-8 张。牌山主堆权重提高到 2.45，让多数牌集中在几个主牌山中，减少空旷感。玩家页记牌器位于牌桌和卡槽之间，显示 `万 / 条 / 筒 / 字` 剩余数量；朋友 Demo 不再把满槽牌静默移入隐藏备用槽，第 8 张会保留在可见主槽。

当前已完成有限牌河、补杠和胡牌奖励核心玩法设计：后续核心规则收束为 `有限牌河 + 明碰区 + 补杠孤张出口 + 明杠开山 + 胡牌强奖励 + 听牌提示 + 孤张预算生成器`。补杠只做低收益孤张出口；明杠才触发震山开牌；胡牌不是普通关通关条件，而是清槽、强开山和处理牌河的最大局内奖励。后续不应继续只靠减少牌型或增加牌量调难度。

当前已完成有限牌河、补杠和胡牌奖励试玩 Demo：默认玩家页新增 `牌河 0/3` 和明牌区；右侧 `丢弃` 进入选择模式，由玩家点击任意槽位牌打入牌河；`碰` 会写入明牌区，后续第 4 张同牌可触发 `补杠` 并升级为 4 张明杠；直接 4 张 `杠` 会开山震落 1 张压顶牌，震落牌会平铺回桌面并保持可点击入槽；`胡` 会清槽、震落 3 张压顶牌并清理牌河 1 张。满槽判定已调整为牌河未满时提示打牌，牌河满且无组合/救场时才失败。根据 Lee 第 5 关试玩反馈，正式关自然明杠包已提高到每关 2 个，且明杠目标牌面会从普通 3 张填充组中保留出来，避免玩家感觉系统只生成 `碰`。默认玩家页也已补简化 `听/差` 胡牌提示，记牌器只统计牌山 `board` 牌并高亮牌山仍有的目标牌；卡槽、牌河、明牌区和移除区不计入。记牌器已改为每个牌面格上下两层显示，上方是牌面、下方是余牌数量；`吃 / 碰 / 杠 / 补杠 / 胡` 已拆到卡槽上方独立动作栏，移动端底部道具栏不再遮盖卡槽；第 5-10 关生成器会检查孤张风险预算。

当前已完成站内网页试玩接入：`/games/hulebu` 会以 iframe 加载静态 Demo `/games/hulebu-demo/index.html`，并在游戏站麻将卡片和搜索入口中指向该页面。静态副本已同步有限牌河、补杠、胡牌震落、震落牌平铺遮挡、高压 auto 池、残局收官第一阶段和特殊事件第一版；调牌器保留为 `/games/hulebu-demo/tuner.html`。

当前已完成特殊事件 Demo 第一版：默认玩家 Demo 会在第 6、8、10 关前弹出一次关前事件选择，事件池包含 `路遇老雀`、`旧牌匣`、`加注一局`、`暗灯牌局`。选项覆盖立即铜钱、道具补给和主动词缀换奖励；第一版词缀支持 `禁洗牌`、`禁透视` 和 `高压牌山`，HUD 会显示当前事件词缀，词缀只作用玩家选择后的当前关。

当前已完成终局试炼 Demo 第一版：默认朋友试玩 Demo 第 10 关启用 `终局试炼`，目标为 `杠 1 / 胡 1 / 积分 180`；HUD 显示 `试炼 x/3`，玩家页会在牌桌上方显示紧凑目标条。试炼未达标时清空牌山会进入失败弹层并列出缺口；击破试炼后一次性发放 `试炼奖励 +180 铜钱`。第 10 关生成器已承接试炼目标包，并启用起手窗口平衡，避免开局直接露出完整答案组。

当前已完成完整体验版推进方案：T165 已盘点当前 10 关站内 Demo 与完整游戏方案的差距，确认下一阶段不直接把无尽、每日、成就和高阶周目塞入单个补丁，而是先做 `20 关主线 + 局外升级壳 + 第 20 关 Boss`。后续推荐任务顺序为 T166 20 关完整主线 Demo、T167 局外首页和结算、T168 铜钱资产和 3 项局外升级、T169 路线型奖励池、T170-T173 长期模式，最后由 T174 让 Cocos 正式表现层追平稳定后的完整体验版。

当前已完成 20 关完整主线 Demo：T166 已把默认玩家试玩页从 10 关扩展到 20 关，保留第 10 关 `终局试炼`，并为第 11-19 关补齐后半段渐进难度 profile。第 20 关现已启用 `胡了卜王` 终章 Boss，奖励节点扩展为第 `3 / 6 / 9 / 13 / 16 / 19` 关；默认玩家页 `auto` 模板还补上多次 seed 重试和“起手不能直接露出完整答案组”的兜底条件。站内静态 Demo 已同步，390px 移动端底部固定道具栏也已修正为不遮盖卡槽最后一格。

当前已完成局外首页和结算壳：T167 已把 `/games/hulebu` 从“直接打开 iframe”改成先进入局外首页。局外页现在会展示 `开始挑战 / 继续本轮 / 升级 / 图鉴 / 无尽 / 每日` 壳层入口，并预留 `备用槽 / 满槽护符 / 初始道具` 三项外置升级说明。主线 run 继续复用现有 20 关 iframe Demo，但壳层已能在当前页面生命周期内支持 `返回局外`、`继续本轮`，并在通关或失败后展示站内结算面板和局外铜钱累计预览。静态 Demo 与原型源文件也已补上 `embed=shell` 父页面消息桥接。

当前已完成局外铜钱资产和 3 项外置升级：T168 已把 `/games/hulebu` 的局外铜钱从预览数字升级为真实可消费资产，并开放 `备用槽 / 满槽护符 / 初始道具` 三项升级。升级等级和局外铜钱会持久化到浏览器本地存档；开始新一轮主线时，外层壳会通过 `reserveBonus / shieldBonus / toolBonus` 参数把成长带入 iframe Demo。当前价格为：备用槽 `80 / 240`、满槽护符 `160 / 480`、初始道具 `120 / 360`。

当前已完成路线型奖励池第一版：T169 已把奖励配置扩展到 16 个，并在默认 20 关主线奖励节点中覆盖 `吃流 / 碰流 / 杠流 / 胡流 / 道具流 / 信息流`。奖励弹层会显示路线标签，已选奖励区也会以 `奖励名 · 路线` 展示，让 run 内 build 倾向更容易被玩家读出来。本轮只使用既有 effect 类型，不新增被动状态机，也不改 `levels.json`。

当前已完成无尽牌山第一版：T170 已开放 `/games/hulebu` 局外 `无尽` 面板的 `开始无尽` 入口。无尽从第 21 层开始，iframe 使用 `mode=endless&startLayer=21`，内层原型会按层数循环后半段压力 profile、每 3 层给奖励、每 5 层给 Boss 压力，并向外层同步当前层数。外层本地存档新增 `bestEndlessLayer`，局外页、进行中摘要和结算面板都会显示无尽最高层与本次到达层。

当前已完成 `悬台窄腰 / suspended-waist` 调牌器模板和高压池接入：T103 确认只借鉴高堆叠参考图的“上层平台、中段窄腰、底部支撑柱、侧向散牌”结构，不复制美术、颜色、牌面或文案；T104 已把该模板接入 HTML 原型和 `/games/hulebu-demo/tuner.html`；T162 已把该模板纳入默认玩家页第 8-10 关高压 auto 随机池，第 5-7 关仍使用基础模板池。

当前已完成震落牌平铺和遮挡点击修复：T105 修复了连续多次 `杠 / 胡` 后震落牌复用同一批落点的问题。震落牌现在按全局递增序号进入桌面平铺层，旧堆叠列、桥接和 blocker 元数据会被清理；震落牌自身仍可点击入槽，但如果它视觉上盖住普通下层牌，下层牌会按运行态遮挡规则禁用点击。站内静态副本已同步。

当前已完成 Cocos/GDevelop 正式表现层桥接第一版：`packages/shared/src/mahjong-presentation.ts` 会把共享规则状态转换为引擎无关快照，包含牌山可点/遮挡状态、8 格主槽、备用槽、`胡 / 杠 / 碰 / 吃` 按钮、余牌和 HUD；`apps/game/mahjong-roguelike/docs/formal-presentation-bridge.md` 说明 Cocos/GDevelop 如何消费这份快照。

当前已完成 Cocos 场景骨架第一版：`packages/shared/src/mahjong-cocos-scene.ts` 会把通用快照转换为 Cocos 友好的节点、控件和 HUD 模型；`apps/game/mahjong-roguelike/cocos/` 已补充场景节点结构、脚本边界和绑定清单。

当前已完成 Cocos Creator 3.8.8 工程壳：`apps/game/mahjong-roguelike/cocos/hulebu-cocos-3.8.8/` 基于 Creator 自带 `empty-2d` 模板结构，包含可在 Cocos Dashboard 中添加/打开的项目壳、首场景脚本边界和配置导入占位。

当前已完成 Cocos 首屏自动渲染：`GameSceneController` 可默认加载本地测试 scene model，Board/Slot/Combo/HUD Binder 可自动创建占位牌、8 格槽、组合按钮和 HUD 文案，用于 Creator 播放验证。

当前已完成 Cocos 手机竖屏首屏适配：工程设计分辨率为 390x844，首屏占位牌山、8 格主槽、组合按钮和 HUD 已按手机竖屏基准重排。

当前已完成 Cocos 真实可见尺寸自适应：首屏布局会读取运行时 canvas CSS 尺寸、Cocos 可见尺寸和 DPR，统一缩放牌山、8 格主槽、组合按钮和 HUD；iPhone 预览中 HUD 位于顶部、牌山居中、底部槽位可读。

当前已完成 Cocos 首条点击可玩链路：测试首屏中可点击牌可以进入 8 格主槽，HUD 和组合按钮会刷新；满足 `胡 / 杠 / 碰 / 吃` 候选后可以点击按钮执行基础消除。

当前已完成 Cocos 牌面资源接入、无边框资源派生和运行时留白版牌面：`assets/resources/ui/mahjong-tiles/` 中保留原始带框牌面，`tiles/borderless/` 保留透明牌面符号来源，`tiles/refreshed/` 作为当前 Cocos 优先加载的留白版运行时 SpriteFrame；`HulebuTileSpriteCatalog` 当前优先加载 `refreshed` 路径，让 Cocos 自绘牌体承接牌底并保留符号四周呼吸空间。

当前已完成 Cocos 最小关卡流闭环：`GameSceneController` 已能在牌山清空后弹出通关提示，点击继续进入下一关；第 3/6/9/13/16/19 关进入下一关前会出现 3 选 1 奖励；20 关后显示本轮通关。该版本先保证整局流程走通，奖励效果、Boss 目标进度和最终 UI 美术继续后置。

当前已完成 Cocos 随机堆叠牌山恢复：Cocos 默认 20 关不再使用 T076 的 6 张流程关，而是通过确定性随机生成 42-60 张牌、多列随机轮廓、同列完全覆盖和 4-6 层堆叠；同列下层只露出顶部横条提示层数，遮挡判定继续使用 5% 阈值。Cocos Web Preview 手机视口已目检首屏随机堆叠牌山可显示。

当前已完成 Cocos 牌山铺开和遮挡点击一致性：首关随机牌山配置跨度已扩大到约 `300x186`，Cocos 预览手机视口中牌山不再挤成小团；`applyStackBlockers` 会把任意更高层牌超过 5% 的覆盖写入 `blockedBy`，runtime 测试已验证被盖住的下层牌不可入槽，移走 blocker 后才恢复可选。

当前已完成模板注册表、8 个核心模板共享实现和 Cocos 接入：`packages/shared/src/mahjong-mountain-generator.ts` 已提供模板注册表、参数归一化、参数边界、8 个核心模板、通用校验器和扩展后的 `ExperienceReport`；Cocos 默认 20 关已通过 `generateHulebuMountain(...).levelTiles` 生成牌山，并映射为现有 `HulebuLevelTileConfig`。

当前已完成登录账号长期进度续层：T174 已为 `/games/hulebu` 新增登录账号进度同步。`apps/web/prisma/schema.prisma` 已增加 `HulebuProgress`，`/api/games/hulebu/progress` 可读写局外铜钱、无尽最高层、高阶解锁、每日最佳和成就；局外壳层会优先读取账号记录，并把本地旧存档和账号进度做首次合并，后续升级与结算会同步回写账号，避免重新打开页面后又从 0 开始。

当前已完成 Cocos 账号进度桥接基础：T226 已让 `GameSceneController` 在浏览器环境下尝试调用 `/api/games/hulebu/progress`，并把 Cocos 的局外铜钱、六轴成长、无尽最高层、每日最佳/连续参与、成就与当前本轮快照桥接到现有账号口径。当前仍保留 `lastSettlement / bestMainlineLevel` 作为本地字段，后续如需更完整跨设备恢复，应继续扩服务端模型。

当前已完成 Cocos 明牌区与槽位真实牌面补齐：T227 已让 `MeldRiverLayerBinder` 和 `SlotLayerBinder` 在明牌区、牌河、主槽和备用槽优先显示真实麻将牌面，并继续保留文字 fallback。当前 Cocos 局内最常盯着的四块牌区，已经不再停留在纯文字调试态。

当前已完成刷新后继续当前本轮：T187 在 `/games/hulebu` 外层本地 shell 状态中保存未完成 `activeRun`，刷新后会恢复 `继续本轮` 并从当前关或无尽当前层开局。该版本不恢复中局牌桌、卡槽、牌河或事件弹窗，完整中局云存档仍后置。

当前已完成账号级当前本轮存档：T188 在 `HulebuProgress` 中新增 `activeRun` JSON 快照，登录账号会同步保存当前未完成 run；换浏览器或设备登录后，外层会在本地和账号快照之间选择更新时间更新的一份继续。

当前已完成 Cocos Boss 目标基础接入：Cocos 第 10/20 关已支持 `combo_count / suit_set / score_target` 多目标，runtime 会在组合成功后记录目标进度，HUD 显示 Boss 摘要；牌山清空但目标未完成时不会进入通关流程。

当前已完成 Cocos 奖励效果基础接入：奖励三选一会累计到 Cocos 本轮状态，并影响后续关卡的备用槽、护符、首败保护、开局铜钱、工具次数和吃/碰/杠分数加成。

当前已完成 Cocos 特殊事件基础接入：第 `6 / 8 / 10 / 14 / 18` 关前会弹出关前事件三选一，事件效果通过本关修饰器传入 runtime，支持本关铜钱、撤回补给、禁看山和禁洗牌，进入下一关会自动清空本关事件状态。

当前已完成 Cocos 长期模式入口基础：配置层提供 `mainline / endless / daily` run profile 和关卡映射 helper；Controller 可启动主线、无尽和每日 run。主线仍按 20 关结束，无尽从第 21 层循环第 11-20 关配置，每日按 seed 稳定轮换配置。

当前已完成 Cocos 局外成长基础接入：runtime 支持 `HulebuMetaUpgradeState`，可在 run 启动时合入备用槽、护符、初始工具、河道扩容、开局铜钱和看山预置六轴加成。该层仍只是 Cocos 内部状态，账号同步和局外升级 UI 仍需后续任务接入。

当前已完成 Cocos 局外成长 UI 接入：大厅新增 `升级` 入口，进入后展示备用槽、护符、工具、牌河、铜钱、看山六轴成长；点击会更新 controller 持有的成长状态并刷新等级，下一局启动时继续通过既有 runtime 参数链路生效。

当前已完成 Cocos 高阶周目入口基础：配置层新增 `advanced` run mode 和东风场、南风场、西风场、北风场四档 profile；大厅新增 `高阶` 入口，选择风场后进入本局流派选择并启动对应高阶 run。

当前已完成 Cocos 局外铜钱和升级成本闭环：Controller 持有本地局外铜钱，通关回大厅前发放铜钱；局外成长面板展示铜钱、等级和价格，升级会扣除铜钱并受上限约束。

当前已完成 Cocos 高阶风场压力基础：东风、南风、西风、北风四档不再只是入口 profile，开局后每一关会自动合并对应风场修饰器；南风及以上会逐步禁用洗牌、禁用看山或扣减撤回/打牌次数。

当前已完成 Cocos 高阶专属奖励基础：配置层新增东/南/西/北四档高阶奖励池，奖励节点在高阶 run 中会优先展示当前风场专属奖励，并用普通关卡奖励补足 3 个选项；第一版奖励效果覆盖工具补偿、护符、备用槽、开局铜钱和组合分数加成。

当前已完成 Cocos 高阶能力槽基础：高阶 run 在选择风场后会先进入高阶能力 3 选 1，再进入本局流派选择；第一版能力通过本轮奖励状态和每关修饰器生效，可影响护符、工具、铜钱等已有 runtime 状态。

当前已完成 Cocos 高阶事件池基础：高阶事件节点会按东/南/西/北风场优先展示对应专属事件，再用普通事件补足 3 个选项；第一版事件复用现有 `coin / tool / forbid_tool` 效果并通过本关修饰器进入 runtime。

当前已完成 Cocos 事件元信息基础：普通事件和高阶事件配置已带 `rarity / tags / dangerLevel`，关前事件弹层会展示稀有度、风险等级和事件标签，后续可直接承接事件卡美术、构筑权重和更多事件分支。

当前已完成 Cocos 无尽/每日事件变体：无尽 run 会优先出现 `长山补给 / 深山留尾`，每日 run 会优先出现 `今日手气 / 今日变招`，再用普通事件补足 3 个选项；事件效果继续复用现有本关 modifier。

当前已完成 Cocos 本局流派事件偏置：事件选择 helper 支持传入本局流派 id，事件节点会按 `本局流派事件 -> run mode 事件 -> 普通事件` 的顺序补足 3 个选项，让 `吃 / 碰 / 杠 / 胡 / 道具 / 信息` 六类打法在事件节点上也能先读出来。

详细方案见 `FRAMEWORK_PLAN.md`。

## 4. 文档索引

- `FRAMEWORK_PLAN.md`：框架调研和技术规划。
- `GAMEPLAY_PLAN.md`：手动组合、槽位成长、货币、奖励和道具体系。
- `GAMEPLAY_REVIEW_PLAN.md`：团队评审用玩法方案 Markdown 版。
- `GAMEPLAY_REVIEW.html`：团队评审用可视化 HTML 版。
- `MVP_VALIDATION_PLAN.md`：MVP 玩法验证计划 Markdown 版。
- `MVP_VALIDATION_PLAN.html`：MVP 玩法验证计划可视化 HTML 版。
- `MVP_BUILD_PLAN.md`：最小可玩闭环和 MVP 开发拆分计划 Markdown 版。
- `MVP_BUILD_PLAN.html`：最小可玩闭环和 MVP 开发拆分计划可视化 HTML 版。
- `PLAYABLE_VALIDATION_PROTOTYPE.html`：最小可玩验证原型，可直接通过本地 HTTP 服务打开试玩。
- `apps/game/mahjong-roguelike/prototypes/config-playable/index.html`：配置驱动玩家试玩页，默认读取关卡配置并以密集牌山模式展示干净玩家视角。
- `apps/web/src/app/games/hulebu/page.tsx`：站内网页试玩入口，挂载静态 HTML Demo。
- `apps/web/public/games/hulebu-demo/index.html`：站内发布用静态 Demo 副本。
- `apps/game/mahjong-roguelike/prototypes/config-playable/tuner.html`：独立调牌器入口，会进入 `index.html?view=tuner&mode=mountain` 并显示开发调参面板。
- `apps/game/mahjong-roguelike/docs/tile-mountain-generator.md`：密集牌山生成器原型说明。
- `apps/game/mahjong-roguelike/docs/formal-presentation-bridge.md`：Cocos/GDevelop 正式表现层桥接说明。
- `apps/game/mahjong-roguelike/cocos/README.md`：Cocos 场景骨架说明。
- `apps/game/mahjong-roguelike/cocos/scene-binding.md`：Cocos 节点和资源 key 绑定清单。
- `apps/game/mahjong-roguelike/cocos/hulebu-cocos-3.8.8/README.md`：Cocos Creator 3.8.8 工程壳打开说明。
- `UI_ASSETS_COCOS_INTEGRATION.md`：T181/T182 UI、透明牌面和操作演出资源的 Cocos 接入说明。
- `../../tasks/items/T189-hulebu-cocos-reuse-catchup-plan.md`：Cocos 旧工程复用结论和 Web 冻结追平计划。
- `IMPLEMENTATION_PLAN.md`：后续实现阶段计划。
- `PROGRESS.md`：模块进展。
- `DECISIONS.md`：模块决策。
- `HANDOFF.md`：交接说明。

## 5. 下一步重点

- T175 已确认路线：先把 `/games/hulebu` 网页版做成完整版，再补 Cocos、音乐、美术、动效和发布资源。
- T176 已完成高阶周目完整版第一大块。当前 `/games/hulebu` 已具备四档高阶入口、局外高阶配置、高阶能力槽、高阶专属奖励、档位事件和构筑识别；普通前几关教程仍只作用普通模式，高阶前几关不再套教程。失败只保留在 iframe 内的简短弹框，不再打开外层失败结算。
- T177 已完成 Boss 试炼第二版。当前 Boss 已有阶段目标池、普通/终局/高阶/无尽变体和 `Boss 奖励品质`；高阶 Boss 会显示 `高阶 Boss 变体`，且不套普通教程目标。相关 payload 仍可供内部调试，但不再往外层结算页扩解释卡片。
- T178 已完成特殊事件池第二版第一轮扩容。当前事件已补 `稀有事件 / 构筑联动 / 高阶事件` 三层信号，普通 run 新增 `河灯旧约 / 封盘押后 / 险招翻倍`，高阶 run 也会按构筑线优先出现更贴路数的事件；结算层不再额外展开事件复盘，只保留核心结果摘要。
- T179 已完成成就图鉴第二版第一轮扩容。当前 `/games/hulebu` 图鉴已从 8 张平铺卡扩成 `图鉴总览 / 分类进度 / 分组列表` 结构，新增 Boss、事件、高阶和路线目标，并为部分高阶 / Boss / 事件目标补了 `未揭示目标` 隐藏态。
- T184 已把路线奖励和长期成长第二轮正式收口。当前局外升级明确扩成 `备用槽 / 满槽护符 / 初始道具 / 河道扩容 / 开局铜钱 / 看山预置` 六轴；但这套系统的后续方向已在 T186 中重新定义为“长期成长在局外、每局开局前主动选流派”，旧路线挂载后续将降级成局外偏好和轻协同，而不再作为跨多关 build 身份。
- 下一步实现应继续停留在 Web 完整版内容层，先把 T184 的长期成长深化验收收口；随后进入 20 关/长期模式数值平衡与内容冻结。Cocos、音乐、美术和动效仍继续后置。
- T186 已新增主线结构重构规划。后续胡了卜的主线应改成：局外负责资源、升级、解锁与流派携带能力；局内负责每次开局前选择这局的 `吃 / 碰 / 杠 / 胡 / 道具 / 信息` 流派，并围绕该流派在本局获利。20 关后以及长期模式开始前都应支持选择对应流派技能开局。
- T187 已补刷新恢复当前本轮：外层会保存未完成 run 的当前关/层、模式、流派和摘要，刷新后继续本轮不再从第 1 关重来；后续若要恢复牌桌每张牌和卡槽，需要另开完整中局存档任务。
- T188 已把当前本轮恢复快照同步到账号进度。后续验收跨设备/跨浏览器时，重点看登录后是否能恢复账号里更新的 `activeRun`；完整中局云存档仍不在本任务范围。
- 继续调关时用 `tuner.html` 或 `index.html?view=tuner&mode=mountain`；评估发布体验时用 `/games/hulebu`。当前默认 Demo 已适合朋友体验完整 20 关主线和第 21 层起的无尽第一版，重点观察第 11-19 关压力曲线、第 20 关 `胡了卜王` 目标负担、无尽层数增长是否过快，以及特殊事件、残局收官和终局试炼在长 run 中的节奏是否过密。
- T189 已确认旧 Cocos 工程可复用，不需要重建。后续进入 Cocos 代码任务时，第一步建议只做视觉资源同步和全量牌面检查，特别确认 `6条` 等 Web 曾修过的牌面资源没有误映射；再追平牌河、明牌组、补杠、震落、满槽救场、Boss、事件、无尽、每日、成就和高阶。
- T190 已完成 Cocos 首轮 v6 视觉资源接入：Web v6 的 34 张麻将牌面、组合按钮、工具按钮、HUD 和槽位 PNG 已复制到 Cocos `assets/resources/ui/v6/`，`HulebuTileSpriteCatalog` 已切到 v6 牌面路径，组合按钮会优先加载 v6 图片并保留文字 fallback。共享测试已锁定 `tile.tiao.6 -> tile_bamboo_06`，避免 `6条` 再误映射为 `4条`。
- T191 已完成 Cocos v6 HUD、槽位和工具按钮绑定：`SlotLayerBinder` 会加载 `hand_slots_8` 主槽背板，`HudBinder` 会加载 `tile_counter_wide / level_badge / score_badge` HUD 背板，`GameSceneController` 的右侧 `洗牌 / 撤回 / 提示` 会优先加载 v6 工具按钮图片。程序化绘制仍保留为 fallback。
- T192 已完成 Cocos 有限牌河、明牌组和补杠基础迁移：scene model 新增 `riverNodes / openMeldNodes`，runtime 新增 `river / riverLimit / openMelds` 和 `bugang` candidate，`MeldRiverLayerBinder` 会渲染明牌区和牌河。当前仍未接完整工具按钮交互和震落/满槽救场。
- T193 已完成 Cocos 丢弃工具和牌河交互：`SlotLayerBinder` 支持主槽点击回调，`GameSceneController` 的右侧 `打牌` 工具会进入丢弃选择态，选择态下点击主槽牌会调用 `discardSlotTile()` 打入牌河并刷新场景。
- T194 已完成 Cocos 基础震落和满槽救场：`杠 / 补杠` 会震落最多 2 张压顶牌，`胡` 会震落最多 3 张并沿用清牌河；主槽满且牌河有空间时 HUD 会提示 `可打牌入河`。
- T195 已完成 Cocos 洗牌和撤回真实交互：runtime 新增历史快照栈，入槽、打牌、组合和洗牌前会记录快照；右侧 `洗牌 / 撤回` 按钮已调用 runtime 方法并刷新场景。
- 音乐、美术、图集、最终 Tile prefab、奖励卡、Boss/事件视觉、动效和发布包放到 Web 冻结与 Cocos 追平之后，避免音画资源跟随玩法反复返工。
- GDevelop 侧继续作为快速协作通道，只映射 snapshot 对象变量，不重新实现规则判定。
