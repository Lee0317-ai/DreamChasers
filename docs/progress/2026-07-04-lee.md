# 2026-07-04 Lee 进展

## T239 胡了卜 Cocos 牌山遗留修复

- Codex 接手 T239 遗留项，先确认 `git pull origin main` 因本地分支与远端分叉且未配置 pull 策略失败，本次不强行 merge/rebase。
- 修复被压牌仍可点击风险：`BoardLayerBinder` 在复用牌节点时会清掉当前组件注册过的旧事件，并在点击回调里再次检查当前 model 的 `interactable / dimmed` 状态，避免灰化牌或旧闭包把 tileId 送入 controller。
- 收紧首轮可点上限修正：`enforceCocosInitialFreeMaximum()` 继续采用“用另一张 free tile 原位盖住目标 free tile”的策略，但不再冒出当前最大层边界，避免通过新高层制造异常视觉层级。
- 更新 T239 任务分片和领取分片，补充实际涉及的 `BoardLayerBinder.ts` 与 `HulebuLevelConfig.ts` 范围。
- 验证通过：`npm run test -w packages/shared -- mahjong-cocos-project`，31 个测试通过。
- 验证通过：`npx tsc --noEmit -p apps/game/mahjong-roguelike/cocos/hulebu-cocos-3.8.8/tsconfig.json`。
- 继续补强：`BoardLayerBinder` 现在维护当前牌节点快照，BoardRoot 统一接 `TOUCH_END`，使用 `EventTouch.getUILocation()` 与当前 model 的屏幕矩形做手动 hit-test；若任意更高 `zIndex` 牌对当前牌覆盖超过 5%，即使 Cocos Button 状态异常也不会触发选牌。
- Cocos Web Mobile 非 debug 构建通过：`/Applications/Cocos/Creator/3.8.8/CocosCreator.app/Contents/MacOS/CocosCreator --project ... --build "platform=web-mobile;debug=false;buildPath=.codex-tmp/cocos-web-mobile-build;outputName=web-mobile"`，Cocos CLI 返回既有成功码 36，日志显示 `build Task (web-mobile) Finished`。
- Playwright 手机视口验证通过：首关 30 张、6 张可点、24 张灰化；点击一张可点牌后主槽从 0 变 1、余牌从 30 变 29；直接调用灰化被压牌 `handleTileClick()` 后主槽保持 1。

## T240 胡了卜 Cocos 记牌器和首关牌山补齐

- 结构化记牌器数据：`HulebuHudModel` 新增 `tileCounter`，`HulebuRuntimeState` 会按 board 剩余牌输出四门总数、每张牌 prefabKey 和剩余数量；Sample Scene 与旧刷新路径同步补齐默认计数。
- 记牌器挂件升级：`GameSceneController` 的 `CounterPlaque` 现在显示四门摘要，点击后展开 `CounterExpandedPanel`，浮层展示四门小牌面计数，再点击浮层收起；浮层加 `BlockInputEvents`，避免展开时点击漏到牌堆。
- 首关牌山视觉更贴近参考：`validation_intro_peng` 显式使用 `ring` graph 模板，实测仍为 30 张、6 张初始可点、最大层 4，布局跨度约 `298x178`，比原 `center-tower` 更分散。
- 验证通过：`npm run test -w packages/shared -- mahjong-cocos-project`，31 个测试通过。
- 验证通过：`npx tsc --noEmit -p apps/game/mahjong-roguelike/cocos/hulebu-cocos-3.8.8/tsconfig.json`。
- Cocos Web Mobile 非 debug 构建和 Playwright 截图检查已补跑：展开 `CounterExpandedPanel` 后面板挂在 `ToolOverlayRoot`，`ToolOverlayRoot` siblingIndex 9、`BoardRoot` siblingIndex 2，确认浮层在牌山上方；展开态仍保持 6 张可点、24 张灰化。
