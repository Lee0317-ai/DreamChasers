# 2026-06-04 Lee 进展

- 新增 T117 TimePick 文件夹新增和重命名 API 切换任务，目标是把 `FolderDialog` 的文件夹读取、查重、新建和编辑从 Supabase 切到 DreamChasers API。
- 完成 T117 TimePick 文件夹新增和重命名 API 切换。
- DreamChasers folders API 新增 `POST /api/timepick/folders` 和 `PATCH /api/timepick/folders/[folderId]`，校验 owner、同级重名、空名称、父级归属和循环父级。
- TimePick `FolderDialog` 的文件夹列表读取、新建、编辑和前端同级重名提示已改用 DreamChasers API。
- Kimi WebBridge 真实浏览器联调通过：在 TimePick 首页新建 `T117 根文件夹` 返回 201 并显示；重复同名触发“该位置已存在同名文件夹”；重命名为 `T117 已重命名` 返回 200 并刷新页面；测试文件夹已删除清理。
- T117 验证通过：`npm run test -w apps/web -- timepick account`；`npm run typecheck -w apps/web`；TimePick 定向 ESLint；Kimi WebBridge 真实浏览器联调。
- 新增 T118 TimePick 子文件夹卡片 API 切换任务，目标是把 `SubFolderCard` 的统计、删除和拖拽移动资源从 Supabase 切到 DreamChasers API。
- 完成 T118 TimePick 子文件夹卡片 API 切换。
- TimePick `SubFolderCard` 已移除直接 Supabase 调用，统计改用 DreamChasers resource view API，删除改用 folder delete API，拖拽资源移动改用 resource patch API。
- Kimi WebBridge 真实浏览器联调通过：`bootstrap/folders/resources` 均为 200；子文件夹卡片统计从 DreamChasers API 显示 `1 个文件夹`；拖拽临时资源触发 `PATCH /api/timepick/resources/t118-resource-move` 200 并更新统计为 `1 个文件夹 / 1 个资源`；删除临时文件夹触发 `DELETE /api/timepick/folders/...` 200 并回到空状态；临时测试数据已清理。
- T118 验证通过：静态回归检查 `SubFolderCard` 不含 Supabase 调用；TimePick 定向 ESLint；`npm run test -w apps/web -- timepick account`；`npm run typecheck -w apps/web`；`npm run build -w apps/web`；TimePick `npm run build`；Kimi WebBridge 真实浏览器联调。
- 新增 T119 TimePick 资源卡片删除 API 切换任务，目标是把 `ResourceCard` 的资源删除从 Supabase 切到 DreamChasers API；自动识别、缩略图上传和编辑仍留到后续任务。
- 完成 T119 TimePick 资源卡片删除 API 切换。
- DreamChasers 新增 `DELETE /api/timepick/resources/[resourceId]`，资源删除会校验当前用户只能删除自己的资源。
- TimePick `ResourceCard` 删除资源改用 DreamChasers API client；自动识别、缩略图上传和编辑链路仍保留 Supabase，后续单独拆任务。
- Kimi WebBridge 真实浏览器联调通过：临时资源 `T119 待删除资源` 出现在列表中；点击删除确认后触发 `DELETE /api/timepick/resources/t119-resource-delete` 200；列表刷新后回到空状态；临时测试数据已清理。
- T119 验证通过：TDD 红绿测试；静态回归检查 `ResourceCard` 不再用 Supabase delete；`npm run test -w apps/web -- timepick account`；`npm run typecheck -w apps/web`；`npm run build -w apps/web`；TimePick 定向 ESLint；TimePick `npm run build`；Kimi WebBridge 真实浏览器联调。
- 新增 T120 TimePick 资源录入编辑 API 切换任务，目标是把 `ResourceDialog` 的基础新增/编辑保存、folders 读取和 sections 读取切到 DreamChasers API；上传、自动识别和 Storage 不在本任务内迁移。
- 完成 T120 TimePick 资源录入编辑 API 切换。
- DreamChasers 新增 `GET /api/timepick/sections`、`POST /api/timepick/resources`，并扩展 `PATCH /api/timepick/resources/[resourceId]` 支持基础资源编辑；保留资源移动的旧 PATCH 用法。
- TimePick `ResourceDialog` 已改用 DreamChasers API 读取 folders/sections 并保存基础资源 metadata；上传文件、自动识别 Edge Function 和识别图片上传仍保留 Supabase 链路。
- Kimi WebBridge 真实浏览器联调通过：新增 `T120 临时资源` 触发 `POST /api/timepick/resources` 201；编辑为 `T120 临时资源 已编辑` 触发 `PATCH /api/timepick/resources/cmpyqjcjk0009e6i8y9vmo264` 200；删除清理触发 `DELETE /api/timepick/resources/cmpyqjcjk0009e6i8y9vmo264` 200，最后资源列表响应 `resources: []`。
- T120 验证通过：TDD 红绿测试；`npm run test -w apps/web -- timepick account`；`npm run typecheck -w apps/web`；`npm run build -w apps/web`；TimePick 定向 ESLint；TimePick `npm run build`；Kimi WebBridge 真实浏览器联调。
- 新增 T121 TimePick 资源卡片自动识别更新 API 切换任务，目标是把 `ResourceCard` 自动识别后的资源 metadata 写回从 Supabase `resources.update` 切到 DreamChasers API；Supabase Edge Function 和 Storage 不在本任务内迁移。
- 完成 T121 TimePick 资源卡片自动识别更新 API 切换。
- TimePick API client 新增 `buildTimePickResourcePayload`，用于从现有 `Resource` 合成完整更新 payload，保留 section、folder、url、notes、tags、source inspiration 和 file size 等字段。
- TimePick `ResourceCard.handleAutoRecognize` 的最终写回改用 `updateTimePickResource`；`auto-recognize` Edge Function、识别图片下载和 Supabase Storage 上传仍保留旧链路。
- T121 浏览器联调记录：临时资源 `T121 自动识别临时资源` 创建成功；Lee 确认旧识别能力依赖的 Coze 工作流已关闭，因此不继续要求旧 `auto-recognize` 成功或触发后续 DreamChasers PATCH。临时资源 `cmpys15ie0004l3i8qe3kmbn4` 已通过 DreamChasers DELETE 清理，返回 200。
- 新增待评估想法 IDEA-20260604-06：TimePick 自动识别后续重做为平台系统 AI 能力，图片生成能力单独做 skill。
- T121 验证通过：静态红绿检查；TimePick 定向 ESLint；TimePick `npm run build`。
- 新增 T123 TimePick 资源预览心得保存 API 切换任务，目标是把 `ResourcePreview` 预览弹窗里的保存心得从 Supabase update 切到 DreamChasers API。
- 完成 T123 TimePick 资源预览心得保存 API 切换。
- TimePick `ResourcePreview.handleSaveNotes` 已改用 `updateTimePickResource` 和 `buildTimePickResourcePayload(resource, { notes })`，保存时保留资源其他 metadata，只覆盖心得；组件不再导入 Supabase。
- Kimi WebBridge 真实浏览器联调通过：临时资源 `T123 心得临时资源` 预览弹窗保存心得，最终有效 `PATCH /api/timepick/resources/cmpyslay20004wxi8bv44n3lm` 返回 200 且响应体包含保存后的 `notes`；刷新后列表显示心得；临时资源已通过 `DELETE /api/timepick/resources/cmpyslay20004wxi8bv44n3lm` 200 清理。
- T123 验证通过：静态红绿检查；TimePick 定向 ESLint；TimePick `npm run build`；Kimi WebBridge 真实浏览器联调。
- 新增 T124 TimePick 标签读取和管理 API 切换任务，目标是把 `TagCloud`、`TagTree` 和 `TagManageDialog` 从 Supabase 资源标签读取/RPC 切到 DreamChasers resources API。
- 完成 T124 TimePick 标签读取和管理 API 切换。
- TimePick API client 新增 `getTimePickTagStats`，并让 `buildTimePickResourcePayload` 支持覆盖 `tags`；标签统计从 DreamChasers `Resource[]` 统一计算。
- `TagCloud`、`TagTree` 和 `TagManageDialog` 已移除 Supabase import；标签新增、重命名和删除改为批量调用 `updateTimePickResource` 写回资源 tags。
- Kimi WebBridge 真实浏览器联调通过：创建临时资源 `T124 标签临时资源` 后标签树显示 `t124-old`；标签管理新增 `t124-new`、重命名 `t124-old` 为 `t124-renamed`、删除 `t124-renamed` 均触发 DreamChasers `PATCH /api/timepick/resources/cmpyta6ic00046ai8cvj4oc8z` 200；最终临时资源通过 `DELETE /api/timepick/resources/cmpyta6ic00046ai8cvj4oc8z` 200 清理，资源列表和标签页回到空状态。
- T124 验证通过：静态红绿检查；TimePick 定向 ESLint；TimePick `npm run build`；Kimi WebBridge 真实浏览器联调。
- 新增 T125 TimePick 搜索页 API 切换任务，目标是把 `SearchPage` 的资源搜索和搜索历史从 Supabase `resources` / `search_history` 切到 DreamChasers API。
- 完成 T125 TimePick 搜索页 API 切换。
- DreamChasers 新增 `GET /api/timepick/search`、`GET/POST /api/timepick/search/history` 和 `DELETE /api/timepick/search/history/[historyId]`，搜索和历史操作均校验当前 DreamChasers 登录用户。
- TimePick API client 新增搜索资源、读取历史、写入历史和删除历史方法；`SearchPage` 已移除 Supabase import 和 `.from('resources')` / `.from('search_history')` 调用。
- Kimi WebBridge 真实浏览器联调通过：创建临时资源 `T125 搜索临时资源` 后搜索 `t125-search-tag` 显示 1 条结果；网络确认历史写入 201、历史读取 200、搜索 200；清空关键词后历史显示 `t125-search-tag`；删除历史触发 `DELETE /api/timepick/search/history/cmpyu31sv00093si83x9pxfrj` 200 并清空历史；临时资源 `cmpyu2pnb00083si8zsgiv7rg` 已删除清理。
- T125 验证通过：TDD 红绿测试；静态红绿检查；`npm run test -w apps/web -- timepick account`；`npm run typecheck -w apps/web`；`npm run build -w apps/web`；TimePick 定向 ESLint；TimePick `npm run build`；Kimi WebBridge 真实浏览器联调。
- 新增 T126 TimePick 灵感抽屉 API 切换任务，目标是把 `InspirationDrawer` 和 `RecentInspirations` 从 Supabase `inspirations` 切到 DreamChasers API。
- 完成 T126 TimePick 灵感抽屉 API 切换。
- DreamChasers 新增 `GET/POST /api/timepick/inspirations` 和 `PATCH/DELETE /api/timepick/inspirations/[inspirationId]`，灵感新增、编辑、状态更新和删除均校验当前 DreamChasers 登录用户。
- TimePick API client 新增灵感读取、创建、更新和删除方法；`InspirationDrawer` 和 `RecentInspirations` 已移除 Supabase import 和 `.from('inspirations')` 调用。
- Kimi WebBridge 真实浏览器联调通过：灵感抽屉 GET 200；浏览器 fetch 创建临时灵感 `T126 灵感临时内容` 返回 201，DOM 文本确认 UI 渲染 `我的灵感 (1)`；编辑内容/位置 PATCH 200；标记 `converted` PATCH 200；删除临时灵感 DELETE 200；最终列表 GET 返回空。
- T126 验证通过：TDD 红绿测试；静态红绿检查；`npm run test -w apps/web -- timepick account`；`npm run typecheck -w apps/web`；`npm run build -w apps/web`；TimePick 定向 ESLint；TimePick `npm run build`；Kimi WebBridge 真实浏览器联调。
- 新增 T127 TimePick 角色选择 API 切换任务，目标是把 `RoleSelect` 和 `Home` 的角色读取、默认初始化和切换从 Supabase `user_roles` 切到 DreamChasers API。
- 完成 T127 TimePick 角色选择 API 切换。
- DreamChasers 新增 `GET/PATCH /api/timepick/role`，角色只允许 `collector` 和 `searcher`，无角色时返回 `null`，写入使用当前 DreamChasers 登录用户。
- TimePick API client 新增 `fetchTimePickRole` 和 `setTimePickRole`；`RoleSelect` 和 `Home` 已移除 Supabase import 和 `.from('user_roles')` 调用，保留 `localStorage userRole` 缓存。
- Kimi WebBridge 真实浏览器联调通过：打开 `http://localhost:8080/home` 后同页 fetch 首次 GET role 返回 `null`；PATCH `searcher` 返回 200；非法角色返回 400；PATCH `collector` 返回 200；刷新首页后 `localStorage.userRole` 为 `collector`。
- T127 验证通过：TDD 红绿测试；静态红绿检查；`npm run test -w apps/web -- timepick account`；`npm run typecheck -w apps/web`；`npm run build -w apps/web`；TimePick 定向 ESLint；TimePick `npm run build`；Kimi WebBridge 真实浏览器联调。
- 新增 T128 TimePick 学习重点 API 切换任务，目标是把 `LearningFocusDialog` 的学习重点读取、新增、同义词更新和删除从 Supabase `learning_focus` 切到 DreamChasers API。
- 完成 T128 TimePick 学习重点 API 切换。
- DreamChasers 新增 `GET/POST /api/timepick/learning-focus` 和 `PATCH/DELETE /api/timepick/learning-focus/[focusId]`，学习重点操作均校验当前 DreamChasers 登录用户。
- TimePick API client 新增学习重点读取、新增、更新同义词和删除方法；`LearningFocusDialog` 已移除 Supabase import 和 `.from('learning_focus')` 调用；批量优先级 Edge Function fetch 保持原状。
- Kimi WebBridge 真实浏览器联调通过：浏览器 fetch 创建临时学习重点 `T128 学习重点临时` 返回 201，名称 trim、同义词去重；PATCH 同义词返回 200；空名称 POST 返回 400；DELETE 临时学习重点返回 200，最终列表不再包含临时记录。
- T128 验证通过：TDD 红绿测试；静态红绿检查；`npm run test -w apps/web -- timepick account`；`npm run typecheck -w apps/web`；`npm run build -w apps/web`；TimePick 定向 ESLint；TimePick `npm run build`；Kimi WebBridge 真实浏览器联调。
- 新增 T129 TimePick 任务清单主链路 API 切换任务，目标是把 `/todo` 主页面的任务列表、新增、开始/继续、完成/暂缓/放弃和删除从 Supabase `try_queue_links` 切到 DreamChasers API；未挂路由的 Simple 文件、批量导入和批量优先级 Edge Function 不在本任务范围内。
- 完成 T129 TimePick 任务清单主链路 API 切换。
- DreamChasers 新增 `GET/POST /api/timepick/todos` 和 `PATCH/DELETE /api/timepick/todos/[todoId]`，任务清单操作均校验当前 DreamChasers 登录用户。
- TimePick API client 新增任务清单读取、新增、更新和删除方法；`TodoPage`、`AddTodoDialog`、`CompleteTodoDialog` 已移除 Supabase import 和 `.from('try_queue_links')` 调用；转换资源复用既有 DreamChasers folders/resources API。
- Kimi WebBridge 真实浏览器联调通过：创建临时任务 `T129 临时任务` 返回 201，列表读取包含该任务；开始任务 PATCH 返回 200 并写入 `start_time`；无评分完成返回 400；带评分完成返回 200 并写入 rating/notes/complete_time；删除临时任务返回 200，最终列表不再包含临时任务。
- T129 验证通过：TDD 红绿测试；静态红绿检查；`npm run test -w apps/web -- timepick account`；`npm run typecheck -w apps/web`；`npm run build -w apps/web`；TimePick 定向 ESLint；TimePick `npm run build`；Kimi WebBridge 真实浏览器联调。
- 新增 T130 TimePick Profile 页面 API 切换任务，目标是把 Profile 页面的资料读取、资源统计和出生日期保存从 Supabase 切到 DreamChasers API；旧修改密码入口改为跳转 DreamChasers 账号安全页，不在本任务中实现密码修改。
- 完成 T130 TimePick Profile 页面 API 切换。
- DreamChasers 新增 `GET/PATCH /api/timepick/profile`，profile 读取会确保当前登录邮箱有 `TimePickProfile`，并返回资料信息、资源数、存储统计；出生日期更新拒绝空值、非法日期和未来日期。
- TimePick API client 新增 profile 读取和出生日期更新方法；`Profile` 页面已移除 Supabase `profiles`、`resources` 和 Supabase Auth 修改密码直连，改用 DreamChasers API 与 `/account/security` 安全页入口。
- 真实联调通过：`GET /api/timepick/profile` 返回 200，临时生日 PATCH 返回 200，未来日期 PATCH 返回 400；测试后已把 `lee@example.com` 的 `TimePickProfile.birthDate` 恢复为 `NULL`。
- 浏览器检查通过：`http://localhost:8080/profile` 显示昵称 `lee`、账号 `lee@example.com`、出生日期 `未设置` 和资源统计；点击账号安全设置后 `location.href` 为 `http://localhost:3000/account/security`，页面显示 DreamChasers `安全设置`。
- 新增 T131 TimePick 首页每日抽签弹窗 API 切换任务，目标是把 `FortuneDrawDialog` 的出生日期检查、保存生日、每日抽签和同日缓存从 Supabase `profiles` / `draw-fortune` Edge Function 切到 DreamChasers API；`/fortune` 运势聊天页、上传/Storage、自动识别和模块树不在本任务范围内。
- 完成 T131 TimePick 首页每日抽签弹窗 API 切换。
- DreamChasers 新增 `POST /api/timepick/fortune/draw`，复用 `TimePickProfile.birthDate` 和 `TimePickFortuneDraw`；未设置出生日期返回 409，首次抽签创建无模型每日签文和内联签图，同日重复抽签返回缓存。
- TimePick API client 新增 `drawTimePickFortune`；`FortuneDrawDialog` 已移除 Supabase import，不再读取/更新 Supabase `profiles`，不再调用 Supabase Edge Function `draw-fortune`。
- 浏览器/API 联调通过：未设置生日抽签返回 409；保存临时生日 `1990-01-02` 返回 200；首次抽签返回 201 且 `cached: false`；第二次抽签返回 200 且 `cached: true`，两次 draw id 一致；首页点击抽签按钮显示每日运势弹窗。
- T131 临时 session、今日抽签记录已删除，`lee@example.com` 的 `TimePickProfile.birthDate` 已恢复为 `NULL`。
- 新增 T132 TimePick 剩余 Supabase 直连清零任务，目标是清理 `/fortune`、上传/Storage、自动识别、批量导入、模块树、旧 Simple todo 和 Supabase client 入口；不新增 Prisma schema，不接真实 AI/Storage。
- 完成 T132 TimePick 剩余 Supabase 直连清零。
- DreamChasers 新增 `/api/timepick/fortune/chat` 无模型占位接口；TimePick `/fortune` 运势聊天页改走 DreamChasers API，不再调用 Supabase Edge Function `fortune-agent`。
- `ResourceDialog` / `ResourceCard` 已移除 Supabase Storage 和 `auto-recognize` Edge Function；上传暂降级为本地 data URL，自动识别暂降级为本地 metadata 占位。
- `BatchImportDialog` 改为逐条调用 DreamChasers todo API；旧 `TodoSimple` / `TodoPageSimple` 复用主 `TodoPage`。
- `ModuleDialog` / `ResourceTree` 已移除 Supabase `modules`、`sections`、`module_sections` 和 `resources` 直连；模块写入/拖拽明确提示后续在 DreamChasers 中重建。
- 已删除 TimePick `src/integrations/supabase/client.ts` 和 `types.ts`；全局静态扫描 `rg "integrations/supabase|supabase\\.|\\.from\\('|\\.rpc\\(|functions\\.invoke|storage\\." /Users/lee/Desktop/Lee/TimePick/src` 无结果。
- 新增 T133 账号统一中心页面体系重规划任务，基于 Open Design 项目 `9bf531c6-e521-4b0c-b23e-430e44751483` 和现有 T108/T110-T113 账号规划与代码能力，目标是形成完整账号中心页面信息架构。
- 完成 `docs/superpowers/specs/2026-06-04-account-center-redesign-design.md` 评估稿：明确第一阶段先用邮箱验证登录，账号中心按概览、资料、安全、设备、AI 积分、充值、订阅、LLM 配置、API Key 和产品接入拆分；第二阶段 LLM 配置承接 T108 AI Gateway 的 `platform_pool`、`user_ephemeral_key`、`external_gateway_byok`、`user_encrypted_vault` 和 `local_connector` 五类模型来源，不直接保存用户 provider key 明文。
- 新增 T134 账号统一中心页面体系重规划实施计划任务。
- 完成 `docs/superpowers/plans/2026-06-04-account-center-redesign.md` 实施计划：将第一阶段拆为导航模型、账号视图模型、账号壳与样式、邮箱登录与概览、资料/安全/设备页、AI 积分/充值/订阅/LLM 配置页、API Key/产品接入页、浏览器 QA 和文档收尾 8 组任务；真实支付、订阅变更、AI Gateway 调用、外部 Gateway BYOK 持久化、Key Vault、本地连接器、密码/短信/OAuth/MFA/实名和设备强制下线均拆为后续任务。
- 新增 T135 账号统一中心页面体系第一阶段实现任务，已在隔离 worktree `.worktrees/account-center-redesign` 创建分支 `codex/account-center-redesign`，基线测试 `npm run test -w apps/web` 通过。
- 新增 T137 账号密码登录 Prisma migration 落档任务，目标是为 T135/T136 已完成的 `User.passwordHash` 字段补正式 migration，避免只依赖 `db push`。
- 完成 T137：新增 `apps/web/prisma/migrations/20260604194500_add_user_password_hash/migration.sql`，使用 `ALTER TABLE "User" ADD COLUMN IF NOT EXISTS "passwordHash" TEXT;`，可在当前已同步数据库上重复执行。
- T137 验证通过：在 `apps/web` 目录临时注入原 checkout `.env` 后，`npm exec prisma validate` 通过；`npx prisma db execute --file prisma/migrations/20260604194500_add_user_password_hash/migration.sql` 执行成功。
- T137 遗留：当前项目历史上仍没有完整初始 Prisma migration，后续如要完全切换到 `prisma migrate deploy`，建议单独做 baseline 任务。
- 新增 T138 账号找回密码、修改密码和重发验证邮件任务，目标是补齐邮箱密码账号体系的基础自助恢复闭环。
- 完成 T138：新增 recovery helper、重置密码邮件、`requestPasswordReset`、`completePasswordReset`、`changeCurrentPassword` 和 `resendVerificationEmail` server actions；新增 `/forgot-password`、`/reset-password` 页面；登录页增加忘记密码入口；登录错误页增加重发验证邮件表单；安全页增加修改密码和未验证账号重发验证入口。
- T138 验证通过：`npm run test -w apps/web -- recovery`；`npm run test -w apps/web -- email-login`；`npm run test -w apps/web -- auth`；`npm run test -w apps/web -- auth account`；`npm run typecheck`；`npm run lint`；临时注入 `.env` 后 `npm run build`；页面 HTTP 烟测 `/forgot-password`、`/reset-password`、`/login/error?reason=email-not-verified`、`/login/check-email?mode=password-reset`。
