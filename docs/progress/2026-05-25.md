# 2026-05-25 进展

## T045：实现 AI 修图工具边框功能第一版

- 负责人：Codex / 开发 B
- 状态：待验收
- 完成内容：新增 AI 修图工具边框预设数据；边框面板支持无边框、极简白边、细黑边、拍立得、胶片边、圆角相框、阴影卡片；支持边框宽度、圆角、颜色实时预览。
- 修改文件：`apps/web/src/lib/tools/photo/photo-borders.ts`, `apps/web/src/components/tools/photo/PhotoEditorWorkspace.tsx`, `apps/web/src/components/tools/photo/PhotoEditorWorkspace.module.css`, `docs/tasks/**`
- 验证结果：`npm run lint -w apps/web` 通过但保留既有 Prisma generated 警告；`npm run typecheck -w apps/web` 通过；`npx next build` 在 `apps/web` 通过；页面 `http://localhost:3001/tools/ai-photo-editor` 返回 200。
- 遗留问题：完整 `npm run build -w apps/web` 在 `prisma generate` 阶段失败，错误为 Prisma 依赖 `zeptomatch` 的 CommonJS/ESM 加载问题，需单独处理；本次不做导出合成链路。

## T045：边框四角缝隙修复

- 负责人：Codex / 开发 B
- 状态：待验收
- 完成内容：根据验收反馈调整边框渲染结构，把画布拆成外层相框和内层图片窗口；图片窗口按边框宽度内缩并单独裁切圆角，消除拍立得、圆角相框、阴影卡片四角缝隙；文字和贴纸仍保留在外层相框坐标系，可放置到相框区域。
- 修改文件：`apps/web/src/components/tools/photo/PhotoEditorWorkspace.tsx`, `apps/web/src/components/tools/photo/PhotoEditorWorkspace.module.css`
- 验证结果：`npm run typecheck -w apps/web` 通过；`npm run lint -w apps/web` 通过但保留既有 Prisma generated 警告；`npx next build` 在 `apps/web` 通过。

## T045：实现 AI 修图撤销重做和导出图片

- 负责人：Codex / 开发 B
- 状态：待验收
- 完成内容：顶部“撤销”“重做”“导出图片”按钮接入实际功能；新增编辑快照和历史栈，覆盖调整、滤镜、裁剪、文字、贴纸、边框等编辑；无历史或无图片时按钮禁用；新增 Canvas PNG 导出，合成当前图片、边框、文字和贴纸。
- 修改文件：`apps/web/src/components/tools/photo/PhotoEditorWorkspace.tsx`, `apps/web/src/components/tools/photo/PhotoEditorWorkspace.module.css`, `apps/web/src/components/tools/photo/useCanvasPan.ts`, `apps/web/src/components/tools/photo/useCropBox.ts`, `docs/tasks/**`
- 验证结果：`npm run typecheck -w apps/web` 通过；`npm run lint -w apps/web` 通过但保留既有 Prisma generated 警告；`npx next build` 在 `apps/web` 通过。
- 遗留问题：PNG 导出第一版尽量贴近预览，CSS 滤镜、字体渲染和 SVG 贴纸可能有轻微差异；完整 `npm run build -w apps/web` 仍受 Prisma generate 依赖加载问题阻塞。

## T045：导出结果必须与预览一致修复

- 负责人：Codex / 开发 B
- 状态：待验收
- 完成内容：修复 SVG foreignObject 导出触发浏览器安全错误的问题，改回安全 Canvas 导出；导出尺寸强制使用当前相框预览 DOM 尺寸，并按编辑状态重绘图片、裁剪、滤镜/调整、旋转缩放平移、边框、文字和贴纸；彩色渐变文字改用 Canvas 渐变绘制。
- 修改文件：`apps/web/src/components/tools/photo/PhotoEditorWorkspace.tsx`
- 验证结果：`npm run typecheck -w apps/web` 通过；`npm run lint -w apps/web` 通过但保留既有 Prisma generated 警告；`npx next build` 在 `apps/web` 通过。

## T051：胡了卜麻将牌面 UI 参考图

- 负责人：Codex / 开发 B
- 状态：待验收
- 进展：
  - 用户确认 `4条` 可用，继续单张审核流生成 `5条`。
  - 已生成 `5条` 单牌：`output/imagegen/hulebu-five-bamboo-celadon-single-v1.png`
  - 已检查 `5条` 单牌为 `1024 x 1024` PNG。
  - 用户要求继续生成 `6条`，首次接口请求超时后已重试成功。
  - 已生成 `6条` 单牌：`output/imagegen/hulebu-six-bamboo-celadon-single-v1.png`
  - 已检查 `6条` 单牌为 `1024 x 1024` PNG。
  - 用户要求继续生成 `7条`。
  - 已生成 `7条` 单牌：`output/imagegen/hulebu-seven-bamboo-celadon-single-v1.png`
  - 已检查 `7条` 单牌为 `1024 x 1024` PNG。
  - 用户跳过重新生成 `8条`，要求继续生成 `9条`。
  - 已生成 `9条` 单牌：`output/imagegen/hulebu-nine-bamboo-celadon-single-v1.png`
  - 已检查 `9条` 单牌为 `1024 x 1024` PNG。
  - 用户确认条子按单张审核流生成版本使用，开始筒子单张审核流。
  - 已生成 `1筒` 单牌：`output/imagegen/hulebu-one-dot-celadon-single-v1.png`
  - 已检查 `1筒` 单牌为 `1024 x 1024` PNG。
  - 用户确认 `1筒` 可用，并要求继续设计 `2筒 / 3筒 / 4筒`。
  - 已生成 `2筒` 单牌：`output/imagegen/hulebu-two-dot-celadon-single-v1.png`；首次接口请求超时后重试成功。
  - 根据用户纠正，`3筒` 应为左上到右下三颗斜线排列，而不是上一下二。
  - 已生成斜线排布 `3筒` 单牌：`output/imagegen/hulebu-three-dot-celadon-single-v1.png`；首次接口请求超时后重试成功。
  - 已检查 `2筒 / 3筒` 单牌均为 `1024 x 1024` PNG。
  - 已生成 `4筒` 单牌：`output/imagegen/hulebu-four-dot-celadon-single-v1.png`
  - 已检查 `4筒` 单牌为 `1024 x 1024` PNG。
  - 用户要求继续生成 `5筒`。
  - 已生成 `5筒` 单牌：`output/imagegen/hulebu-five-dot-celadon-single-v1.png`
  - 已检查 `5筒` 单牌为 `1024 x 1024` PNG。
  - 用户要求继续生成 `6筒`。
  - 已生成 `6筒` 单牌：`output/imagegen/hulebu-six-dot-celadon-single-v1.png`
  - 已检查 `6筒` 单牌为 `1024 x 1024` PNG。
  - 用户要求继续生成 `7筒`；首次接口返回不完整后已重试成功。
  - 已生成 `7筒` 单牌：`output/imagegen/hulebu-seven-dot-celadon-single-v1.png`
  - 已检查 `7筒` 单牌为 `1024 x 1024` PNG。
  - 根据用户反馈，`7筒` 上方三颗应为左上到右下斜排，而不是横排。
  - 已重新生成斜排上三颗的 `7筒` v2：`output/imagegen/hulebu-seven-dot-celadon-single-v2.png`；首次接口返回不完整后已重试成功。
  - 已检查 `7筒` v2 为 `1024 x 1024` PNG。
- 验证：
  - 待运行 `npm run docs:sync` 和 `git diff --check`。

## T057：胡了卜胡牌节奏配置和密集牌山胡牌包

- 负责人：Codex / 开发 B
- 状态：待验收
- 进展：
  - 新增 `featuredCombos` 关卡重点组合字段，第 6 关和第 10 关已标记 `胡`。
  - 配置试玩页已展示“本关重点”，密集牌山模式会优先生成一个可胡的 `3 + 3 + 2` 8 张组合包。
  - 已修正密集牌山生成器遮挡写入口径，超过 5% 遮挡才写入 `blockedBy`，与点击禁用规则保持一致。
- 验证：
  - 已通过 `npm run docs:sync`。
  - 已通过 `npm run test -w packages/shared -- mahjong`，2 个测试文件、25 个测试通过。
  - 已通过 `npm run typecheck -w packages/shared`。
  - 已通过 `node --check /private/tmp/hulebu-config-playable-script.js`。
  - 已通过桌面端第 6 关配置模式和密集牌山模式检查，以及 390px 移动端检查。
  - 已通过 `git diff --check`。

## T058：胡了卜 20 关节奏骨架和第二 Boss

- 负责人：Codex / 开发 B
- 状态：待验收
- 进展：
  - `levels.json` 已扩展到 20 关，新增第 11-20 关二阶段草案。
  - 奖励节点已固定为第 3/6/9/13/16/19 关，Boss 节点为第 10/20 关。
  - 第 20 关 Boss 已加入 `吃 / 碰 / 杠 / 胡 / 万筒条字 / 积分` 复合目标。
  - 配置试玩页 Boss 目标和密集牌山解法校验已支持 `胡` 目标。
  - 已修复第 20 关密集牌山中重点 `胡` 包与 Boss `胡 1` 目标重复占用的问题，并补充回归测试。
  - 配置试玩页已支持 `?level=20&mode=mountain` 深链接，方便直接验收第 20 关密集牌山。
- 验证：
  - 已通过 `npm run test -w packages/shared -- mahjong`，2 个测试文件、28 个测试通过。
  - 已通过 `npm run typecheck -w packages/shared`。
  - 已通过 `node --check /private/tmp/hulebu-config-playable-script.js`。
  - 已通过浏览器第 20 关配置模式检查：20 个关卡标签、Boss 目标、8 格主槽、无横向溢出。
  - 已通过浏览器第 20 关密集牌山检查：`?level=20&mode=mountain` 直达后显示 54 张生成牌、37 张被压住、11 张可点击、8 格主槽、Boss 目标和本关重点 `胡`。
  - 已通过当前窄屏浏览器检查：视口宽 472px，无横向溢出。
  - 已通过 `npm run docs:sync` 和 `git diff --check`。

## T059：胡了卜随机牌山调参面板

- 负责人：Codex / 开发 B
- 状态：待验收
- 进展：
  - 新增密集牌山开发用 `牌山调参` 面板。
  - 支持通过 URL 和面板调整 `seed` 随机种子、`tiles` 牌量、`stack` 同列堆叠深度、`hu` 胡包数量和 `honor` 字牌权重。
  - `重新生成牌山` 会用当前面板参数刷新当前关卡。
  - 生成器默认值仍保持当前密集牌山体验，调参面板不作为正式玩家 UI。
- 验证：
  - 已通过 `npm run test -w packages/shared -- mahjong`，2 个测试文件、29 个测试通过。
  - 待运行完整类型检查、脚本语法检查、桌面/移动浏览器检查、`npm run docs:sync` 和 `git diff --check`。

## T060：胡了卜 Cocos/GDevelop 正式表现层桥接

- 负责人：Codex / 开发 B
- 状态：待验收
- 进展：
  - 已新增共享表现层快照契约：`packages/shared/src/mahjong-presentation.ts`。
  - 快照会输出牌山、8 格主槽、备用槽、`胡 / 杠 / 碰 / 吃` 组合按钮、余牌和 HUD 状态。
  - 已新增 `packages/shared/src/mahjong-presentation.test.ts`，测试 Cocos/GDevelop 可消费的数据结构。
  - 已新增 `apps/game/mahjong-roguelike/docs/formal-presentation-bridge.md`，说明 Cocos 场景结构、GDevelop 对象变量映射和输入回传。
- 验证：
  - 已通过红灯测试：缺少 `mahjong-presentation` 时测试失败。
  - 已通过 `npm run test -w packages/shared -- mahjong-presentation`。
  - 已通过 `npm run test -w packages/shared -- mahjong`，3 个测试文件、30 个测试通过。
  - 已通过 `npm run typecheck -w packages/shared`。
  - 待运行文档同步和 `git diff --check`。

## T061：胡了卜 Cocos 场景骨架第一版

- 负责人：Codex / 开发 B
- 状态：待验收
- 进展：
  - 已新增 Cocos 场景视图模型：`packages/shared/src/mahjong-cocos-scene.ts`。
  - 已新增 `packages/shared/src/mahjong-cocos-scene.test.ts`，测试牌节点、槽位节点、组合按钮和 HUD 绑定数据。
  - 已新增 `apps/game/mahjong-roguelike/cocos/README.md`、`scene-binding.md` 和 `scripts/README.md`，说明 Cocos 节点结构、脚本分工和 prefab key。
  - 当前不生成 `.scene`、`.prefab`、`.meta`，避免在未确认 Cocos Creator 编辑器环境前写不可验证资源。
- 验证：
  - 已通过红灯测试：缺少 `mahjong-cocos-scene` 时测试失败。
  - 已通过 `npm run test -w packages/shared -- mahjong-cocos-scene`。
  - 已通过 `npm run test -w packages/shared -- mahjong`，4 个测试文件、31 个测试通过。
  - 已通过 `npm run typecheck -w packages/shared`。
  - 待运行文档同步和 `git diff --check`。

## T062：胡了卜 Cocos Creator 3.8.8 工程接入

- 负责人：Codex / 开发 B
- 状态：已完成
- 进展：
  - 已确认用户安装 Cocos Dashboard 和 Cocos Creator 3.8.8。
  - 已确认本机编辑器路径：`/Applications/Cocos/Creator/3.8.8/CocosCreator.app`。
  - 已找到 Creator 3.8.8 自带 `empty-2d` 模板，并按该结构创建 `apps/game/mahjong-roguelike/cocos/hulebu-cocos-3.8.8/` 工程壳。
  - 已新增首场景脚本边界：`GameSceneController`、`BoardLayerBinder`、`SlotLayerBinder`、`ComboBarBinder`、`HudBinder` 和 `HulebuSceneModel` DTO。
  - 已新增场景创建说明和配置导入占位，真实 `.scene` 仍建议由 Cocos Creator 编辑器生成。
  - 已新增 `packages/shared/src/mahjong-cocos-project.test.ts` 保护工程壳关键文件。
- 验证：
  - 已通过红灯测试：工程壳不存在时 `mahjong-cocos-project` 测试失败。
  - 已通过 `npm run test -w packages/shared -- mahjong-cocos-project`。
  - 已通过 `npm run test -w packages/shared -- mahjong`。
  - 已通过 `npm run typecheck -w packages/shared`。
  - 已通过 `npm run docs:sync`。
  - 已通过 `git diff --check`。

## T064：打工人弹射解压模块文档落档

- 负责人：Codex / 开发 B
- 状态：已完成
- 进展：
  - 已完成 6 阶段完整方案设计（思维挖掘 → 对立分析 → 专家会诊 → 市场调研 → 方案整合 → 执行路径）。
  - 已确认完整产品方案：Roguelike 物理弹射解压游戏，面向打工人，微信小程序平台。
  - 已创建 `docs/modules/angry-worker/` 独立模块文档目录。
  - 已写入 `README.md`：产品定位、核心循环、特性概览、技术栈、模块状态、文档索引。
  - 已写入 `IMPLEMENTATION_PLAN.md`：完整功能清单、开发计划（Week 1-4）、关卡生成算法、Buff 系统、Boss 设计、分数与排行榜、局外成长、游戏模式、广告接入点、成功指标、后续迭代路线。
  - 已写入 `DECISIONS.md`：9 项关键决策（Roguelike 生成、微信小程序、本地图片、目标替换、全功能保留、Matter.js、IAA 变现、3 人团队、分享图规范）。
  - 已写入 `HANDOFF.md`：团队配置、文档索引、技术要点、关键代码片段、下一步行动、风险提醒。
  - 已写入 `PROGRESS.md`：当前状态、已完成/进行中/待开始清单。
  - 已创建任务分片 `docs/tasks/items/T064-angry-worker-integration.md`。
  - 已创建领取分片 `docs/tasks/claims/T064-codex.md`。
  - 已同步更新 `docs/tasks/TASK_BOARD.md` 和 `docs/status/CURRENT_STATUS.md`。
- 验证：
  - 已通过 `npm run docs:sync`。
  - 待运行 UTF-8 无 BOM 检查和 `git diff --check`。
