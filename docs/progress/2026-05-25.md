# 2026-05-25 进展

## T045：实现 AI 修图工具边框功能第一版

- 负责人：Codex / 开发 B
- 状态：待验收
- 完成内容：新增 AI 修图工具边框预设数据；边框面板支持无边框、极简白边、细黑边、拍立得、胶片边、圆角相框、阴影卡片；支持边框宽度、圆角、颜色实时预览。
- 修改文件：`apps/web/src/lib/tools/photo/photo-borders.ts`, `apps/web/src/components/tools/photo/PhotoEditorWorkspace.tsx`, `apps/web/src/components/tools/photo/PhotoEditorWorkspace.module.css`, `docs/tasks/**`
- 验证结果：`npm run lint -w apps/web` 通过但保留既有 Prisma generated 警告；`npm run typecheck -w apps/web` 通过；`npx next build` 在 `apps/web` 通过；页面 `http://localhost:3001/tools/ai-photo-editor` 返回 200。
- 遗留问题：完整 `npm run build -w apps/web` 在 `prisma generate` 阶段失败，错误为 Prisma 依赖 `zeptomatch` 的 CommonJS/ESM 加载问题，需单独处理；本次不做导出合成链路。

## T045：边框四角缝隙修复

- 负责人：Codex / 开发 B
- 状态：待验收
- 完成内容：根据验收反馈调整边框渲染结构，把画布拆成外层相框和内层图片窗口；图片窗口按边框宽度内缩并单独裁切圆角，消除拍立得、圆角相框、阴影卡片四角缝隙；文字和贴纸仍保留在外层相框坐标系，可放置到相框区域。
- 修改文件：`apps/web/src/components/tools/photo/PhotoEditorWorkspace.tsx`, `apps/web/src/components/tools/photo/PhotoEditorWorkspace.module.css`
- 验证结果：`npm run typecheck -w apps/web` 通过；`npm run lint -w apps/web` 通过但保留既有 Prisma generated 警告；`npx next build` 在 `apps/web` 通过。

## T045：实现 AI 修图撤销重做和导出图片

- 负责人：Codex / 开发 B
- 状态：待验收
- 完成内容：顶部“撤销”“重做”“导出图片”按钮接入实际功能；新增编辑快照和历史栈，覆盖调整、滤镜、裁剪、文字、贴纸、边框等编辑；无历史或无图片时按钮禁用；新增 Canvas PNG 导出，合成当前图片、边框、文字和贴纸。
- 修改文件：`apps/web/src/components/tools/photo/PhotoEditorWorkspace.tsx`, `apps/web/src/components/tools/photo/PhotoEditorWorkspace.module.css`, `apps/web/src/components/tools/photo/useCanvasPan.ts`, `apps/web/src/components/tools/photo/useCropBox.ts`, `docs/tasks/**`
- 验证结果：`npm run typecheck -w apps/web` 通过；`npm run lint -w apps/web` 通过但保留既有 Prisma generated 警告；`npx next build` 在 `apps/web` 通过。
- 遗留问题：PNG 导出第一版尽量贴近预览，CSS 滤镜、字体渲染和 SVG 贴纸可能有轻微差异；完整 `npm run build -w apps/web` 仍受 Prisma generate 依赖加载问题阻塞。

## T045：导出结果必须与预览一致修复

- 负责人：Codex / 开发 B
- 状态：待验收
- 完成内容：修复 SVG foreignObject 导出触发浏览器安全错误的问题，改回安全 Canvas 导出；导出尺寸强制使用当前相框预览 DOM 尺寸，并按编辑状态重绘图片、裁剪、滤镜/调整、旋转缩放平移、边框、文字和贴纸；彩色渐变文字改用 Canvas 渐变绘制。
- 修改文件：`apps/web/src/components/tools/photo/PhotoEditorWorkspace.tsx`
- 验证结果：`npm run typecheck -w apps/web` 通过；`npm run lint -w apps/web` 通过但保留既有 Prisma generated 警告；`npx next build` 在 `apps/web` 通过。
