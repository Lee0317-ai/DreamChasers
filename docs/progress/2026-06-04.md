# 2026-06-04 进展

## T135：账号统一中心页面体系实现

- 负责人：Lee
- 状态：已完成，等待提交
- 完成内容：
  - 按 Open Design 和 T133/T134 规划完成账号中心 shell、导航、概览、资料、安全、设备、AI 积分、充值、订阅、LLM 配置、API Key 和产品接入页面。
  - 根据 Lee 的需求修正，把认证入口从 magic-link 日常登录改为邮箱注册验证 + 邮箱密码登录。
  - 新增 `/register`，注册时设置密码并发送验证邮件；`/login` 改为邮箱和密码登录。
  - `User` 增加 `passwordHash`，新增 scrypt 密码哈希/校验工具，Auth.js 增加 Credentials provider 并使用 JWT session。
  - LLM 配置页保持 T108/T133 模型来源策略，不保存 provider 明文 key。
- 验证：
  - `npm run test -w apps/web`：通过，14 个测试文件、78 个测试。
  - `npm run typecheck`：通过。
  - `npm run lint`：通过，保留 Prisma 生成文件既有 warning。
  - `set -a; source /Users/lee/Desktop/Lee/DreamChasers/.env; set +a; npm run build`：通过。
  - Playwright 桌面/移动截图检查 `/login` 和 `/register` 通过；Kimi WebBridge 快照检查已登录账号页通过。

## T136：账号注册登录数据库同步和真实链路联调

- 负责人：Lee
- 状态：已完成
- 完成内容：
  - 在 `apps/web` 目录执行 `npx prisma db push`，同步 `.env` 指向 PostgreSQL 的 `User.passwordHash` 字段。
  - 用测试邮箱完成注册流程，开发终端打印注册验证链接。
  - 确认注册后密码以 `scrypt` 哈希保存，不包含明文。
  - 确认未验证账号无法通过邮箱密码登录。
  - 打开验证链接后进入账号中心，退出后再用邮箱密码登录成功。
  - 清理测试用户 `lee+t136-20260604181929@example.com`。
- 验证：
  - 数据库字段只读查询通过：`passwordHash` 为 nullable `text`。
  - Kimi WebBridge 真实浏览器联调通过：`/register`、`/login/check-email`、`/login/error`、`/account`。
