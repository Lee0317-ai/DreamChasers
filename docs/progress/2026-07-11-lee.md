# 2026-07-11 Lee Progress

## T242 胡了卜 Cocos v1 M0 production build 基线

- 建立 `hulebu-v1.release.json`，固定 Cocos Creator 3.8.8、`web-mobile`、`debug=false`、内容版本、存档 schema、必需产物与 HTTP smoke 路径。
- 新增可测试的发布库与 CLI，覆盖安全产物读取、原子 manifest、临时 HTTP 服务器、Creator 日志判定、输出锁、失败清理和 verify-only 不变性。
- 根命令新增 `npm run game:hulebu:build` 与 `npm run game:hulebu:verify-build`。
- Cocos 项目 README 已改为唯一正式运行时操作说明；配置原型和 Web Demo 已标记为 Legacy Reference，禁止继续双写产品功能。
- 2026-07-12 真实 production build 验证成功：Creator 原始退出码 `36` 经 Finished 标记、零产物错误和 5 条 HTTP smoke 归一化；产物 `566` 个文件、`135266376` 字节、目录约 `131M`。
- 发布与现有 Cocos 契约测试 `139/139` 通过，Cocos TypeScript 通过；verify-only 未改变 manifest 与 Creator 日志的哈希和 mtime。
- 当前站点入口仍指向旧 Web Demo；M1 进入会话/流程架构拆分，UI、音效、宿主切换与上线 QA 在后续里程碑完成。
