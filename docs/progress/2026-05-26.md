# 2026-05-26 进展

# 2026-05-26 进展

## T045：AI 修图增加装饰贴纸集合

- 负责人：Codex / 开发 B
- 状态：待验收
- 完成内容：将贴纸选择区从带文字的大卡片改为紧凑图标网格；从用户提供的 6 组贴纸 JPG 中裁切、去白底、放大和锐化生成 112 个 PNG，其中非气泡组为透明 PNG。根据验收反馈，`bubble-pack` 22 个气泡贴纸已使用 `imagegen` 一比一重绘为外部透明、心形气泡内部实心白底、浅灰柔和边框和 emoji 内容的 PNG，替换原路径。新增贴纸全部接入“装饰”分类，并按生成批次细分为基础装饰、手绘日常、粉色甜心、气泡表情、黑白猫咪、粉彩涂鸦、动物问候。新增贴纸复用现有添加、拖拽、缩放、旋转、删除和导出链路。修复贴纸画布层外层 `<button>` 包含删除 `<button>` 导致的控制台和 hydration 警告，将外层改为非按钮容器；默认新增贴纸尺寸从 72 调整为 36；贴纸选择区缩略图预览从铺满卡片改为保留内边距，避免盆栽、花瓶等竖向素材贴边。
- 修改文件：`apps/web/public/stickers/decor-pack/**`, `apps/web/public/stickers/love-pack/**`, `apps/web/public/stickers/bubble-pack/**`, `apps/web/public/stickers/cat-pack/**`, `apps/web/public/stickers/pastel-pack/**`, `apps/web/public/stickers/animal-pack/**`, `apps/web/src/lib/tools/photo/photo-stickers.ts`, `apps/web/src/components/tools/photo/PhotoEditorWorkspace.tsx`, `apps/web/src/components/tools/photo/PhotoEditorWorkspace.module.css`, `docs/tasks/CHANGE_INTAKE.md`, `docs/progress/2026-05-26.md`, `docs/completion/2026-05-26-task-45-ai-photo-editor-mvp.md`
- 验证结果：`npm run typecheck -w apps/web` 通过；`npm run lint -w apps/web` 通过但保留既有 Prisma generated 警告；`npx next build` 在 `apps/web` 通过；现有本地服务 `http://127.0.0.1:3001/tools/ai-photo-editor` 返回 200；新增贴纸资源如 `/stickers/decor-pack/girl-ponytail.png`、`/stickers/love-pack/headphone-bunny.png`、`/stickers/bubble-pack/bubble-party.png` 和 `/stickers/animal-pack/xo-kangaroo.png` 返回 200 image/png；抽查 `bubble-pack` PNG 角落为透明，气泡区域保留大量不透明白色像素；灰底接触表检查边缘顺滑、无方形白底；本次控制台错误修复后再次执行 `npm run typecheck -w apps/web`、`npm run lint -w apps/web` 和 `npx next build` 均通过；本次缩略图边距调整后再次执行 `npm run typecheck -w apps/web` 和 `npm run lint -w apps/web` 均通过；本次装饰贴纸批次分组后再次执行 `npm run typecheck -w apps/web` 和 `npm run lint -w apps/web` 均通过。
- 遗留问题：Codex 内置浏览器访问本地地址被 `ERR_BLOCKED_BY_CLIENT` 拦截，未完成真实桌面/移动截图检查；本地未安装 Playwright，未额外安装依赖。贴纸来自多张 JPG，透明边缘可能有轻微白边。

## T045：导出状态一致性补充修复

- 负责人：Codex / 开发 B
- 状态：待验收
- 完成内容：根据验收反馈明确导出必须强制生效所有编辑项；导出链路补充应用裁剪区域、滤镜和调整、旋转缩放平移、边框、文字、贴纸；恢复顶部撤销/重做按钮入口，并给滑杆和边框切换补入历史记录；修复滤镜和边框颜色导出不一致，改为整张导出画布像素级应用调整和滤镜，让边框、文字、贴纸与照片一起匹配预览。
- 修改文件：`apps/web/src/components/tools/photo/PhotoEditorWorkspace.tsx`, `apps/web/src/lib/tools/photo/photo-filters.ts`, `docs/progress/2026-05-25.md`, `docs/progress/2026-05-26.md`, `docs/completion/2026-05-26-task-45-ai-photo-editor-mvp.md`, `docs/completion/2026-05-26-task-45-ai-photo-editor-mvp.md`
- 验证结果：`npm run typecheck -w apps/web` 通过；`npm run lint -w apps/web` 通过但保留既有 Prisma generated 警告；`npx next build` 在 `apps/web` 通过；浏览器打开 `http://127.0.0.1:3001/tools/ai-photo-editor`，页面标题、撤销、重做、导出按钮存在且控制台无 error。
- 遗留问题：完整 `npm run build -w apps/web` 仍受 `prisma generate` 的依赖加载问题阻塞，需单独处理。

## T051：胡了卜麻将牌面 UI 参考图

- 负责人：Codex / 开发 B
- 状态：待验收
- 进展：
  - 根据用户新参考图，先生成 `1万` 样稿，作为后续 `2-9万` 的风格起点。
  - 已保存 `1万`：`output/imagegen/hulebu-one-wan-celadon-single-v1.png`。
  - 已统一检查样稿为 `1024 x 1024` PNG。
  - 根据用户提供的筒子参考图，继续生成青瓷风 `7筒 / 8筒 / 9筒` 单牌。
  - 已保存 `7筒`：`output/imagegen/hulebu-seven-dot-celadon-single-v1.png`。
  - 已保存 `8筒`：`output/imagegen/hulebu-eight-dot-celadon-single-v1.png`。
  - 已保存 `9筒`：`output/imagegen/hulebu-nine-dot-celadon-single-v1.png`。
  - 本轮只生成图片参考资产，不接入 Cocos/GDevelop 工程。
- 验证：
  - 已检查 `1万` 样稿为 `1024 x 1024` PNG。
  - 已检查三张新筒子单牌均为 `1024 x 1024` PNG。
  - 已人工查看 `7筒 / 8筒 / 9筒` 牌面数量和排布。

## T063：胡了卜 Cocos 首屏自动渲染

- 负责人：Codex / 开发 B
- 状态：已完成
- 进展：
  - 新增 Cocos 本地测试 scene model：`assets/scripts/bootstrap/HulebuSampleSceneModel.ts`。
  - `GameSceneController.start()` 默认加载测试 scene model，并自动补 `Canvas` / `UITransform`，降低手工配置出错概率。
  - `BoardLayerBinder` 可在 `TilePool` 下自动生成占位牌节点，绘制牌面、可点态和暗化态。
  - `SlotLayerBinder` 可自动绘制 8 格主槽。
  - `ComboBarBinder` 可自动绘制 `胡 / 杠 / 碰 / 吃` 占位按钮。
  - `HudBinder` 可按节点名自动查找或创建 HUD Label。
  - Cocos 工程 `tsconfig.json` 已补 `ignoreDeprecations` 和 `skipLibCheck`，便于本地脚本检查。
  - 根据 Creator 预览截图修正占位 UI 坐标系：测试首屏现在按 1280x720 左下角原点布局，牌山居中偏上、8 格槽居中靠下、按钮在槽下方、HUD 在顶部。
- 验证：
  - 已通过红灯测试：缺少 `HulebuSampleSceneModel.ts` 和自动建节点能力时 `mahjong-cocos-project` 测试失败。
  - 已通过 `npm run test -w packages/shared -- mahjong-cocos-project`。
  - 已通过 `npx tsc --noEmit -p apps/game/mahjong-roguelike/cocos/hulebu-cocos-3.8.8/tsconfig.json`。
  - 已通过 `npm run test -w packages/shared -- mahjong`。
  - 已通过 `npm run typecheck -w packages/shared`。
  - 已通过 `npm run docs:sync`，同步 30 个任务分片和 30 个领取分片。
  - 已通过 `git diff --check`。

## T065：胡了卜 Cocos 手机竖屏首屏适配

- 负责人：Codex / 开发 B
- 状态：已完成
- 进展：
  - 已把 Cocos 工程设计分辨率设置为 390x844。
  - 已把测试首屏从 1280x720 横屏基准改为手机竖屏基准。
  - 测试牌山改为中上区域，牌面尺寸调整为 52x70。
  - 8 格主槽改为底部居中，组合按钮放在槽下方。
  - HUD 压缩到顶部。
- 验证：
  - 已通过红灯测试：缺少 390x844 设计分辨率和移动布局常量时 `mahjong-cocos-project` 测试失败。
  - 已通过 `npm run test -w packages/shared -- mahjong-cocos-project`，1 个测试文件、4 个测试通过。
  - 已通过 `npx tsc --noEmit -p apps/game/mahjong-roguelike/cocos/hulebu-cocos-3.8.8/tsconfig.json`。
  - 已通过 `npm run test -w packages/shared -- mahjong`，5 个测试文件、35 个测试通过。
  - 已通过 `npm run typecheck -w packages/shared`。
  - 已通过 `npm run docs:sync`，同步 31 个任务分片和 31 个领取分片。
  - 已通过 `git diff --check`。

## T066：胡了卜 Cocos 真实可见尺寸自适应

- 负责人：Codex / 开发 B
- 状态：已完成
- 进展：
  - 已把 Cocos 首屏布局从固定手机常量调整为运行时可见尺寸口径。
  - `HulebuSampleSceneModel` 统一读取 canvas CSS 尺寸、Cocos 可见尺寸和 DPR，输出布局宽高、CSS 宽高和缩放值。
  - Board/Slot/Combo/HUD Binder 已统一按运行时缩放绘制占位牌、8 格槽、组合按钮和 HUD。
  - 已处理新增脚本目录导致的 Cocos 资产缓存残留问题，避免预览出现新的 `Missing class`。
  - 已在 Cocos Web Preview 的 iPhone 机型中确认 HUD、牌山、组合按钮和主槽位置可读。
- 验证：
  - 已通过 `npm run test -w packages/shared -- mahjong-cocos-project`。
  - 已通过 `npx tsc --noEmit -p apps/game/mahjong-roguelike/cocos/hulebu-cocos-3.8.8/tsconfig.json`。
  - 已通过浏览器预览截图检查，且浏览器错误日志未出现新的 `Missing class`。

## T067：胡了卜 Cocos 首屏目标图视觉壳

- 负责人：Codex / 开发 B
- 状态：待验收
- 进展：
  - 已在 Cocos 运行时绘制目标图方向的首屏视觉壳：绿色牌桌、顶部关卡/分数/进度牌、右侧洗牌/撤回/提示按钮、底部木质 8 格槽。
  - 已保留测试牌山、HUD 和 `胡 / 杠 / 碰 / 吃` 组合按钮，用于后续接真实玩法状态。
  - 已修复 Web Preview 只显示 Cocos 默认启动图的问题：运行时自动创建 `RuntimeCamera`，绑定到 `Canvas.cameraComponent`，并设置正交投影、清屏色和可见层。
  - 已在 Chrome 的 Cocos Web Preview 手机视口中确认首屏显示胡了卜牌山、绿色桌面、右侧工具按钮和 8 格卡槽。
- 验证：
  - 已通过 `npm run test -w packages/shared -- mahjong-cocos-project`。
  - 已通过 `npx tsc --noEmit -p apps/game/mahjong-roguelike/cocos/hulebu-cocos-3.8.8/tsconfig.json`。
  - 已通过 `npm run test -w packages/shared -- mahjong`。
  - 已通过 `npm run typecheck -w packages/shared`。

## T068：胡了卜麻将 UI 图片资源归档和切图

- 负责人：Codex / 开发 B
- 状态：已完成
- 进展：
  - 已新增 Cocos UI 图片资源目录：`apps/game/mahjong-roguelike/cocos/hulebu-cocos-3.8.8/assets/resources/ui/mahjong-tiles/`。
  - 已按 `牌面 / 字牌 / 参考图 / 中间稿` 分类归档现有图片。
  - 已复制 27 张数牌单图：万、条、筒各 1-9。
  - 已从字牌横向参考图切出 7 张字牌单图：东、南、西、北、中、发、白。
  - 已归档 2 张参考图和 7 张中间稿。
  - 已新增 `README.md` 和 `manifest.json`，记录每张图的来源、分类、尺寸和 `tileKey`。
- 验证：
  - 已生成 contact sheet 人工查看，确认数牌齐全、字牌切片干净。
  - 已检查 `manifest.json` 包含 27 张数牌、7 张字牌、2 张参考图和 7 张中间稿。
  - 已通过 `npm run docs:sync`。
  - 已通过 `git diff --check`。

## T069：胡了卜 Cocos 首条点击可玩链路

- 负责人：Codex / 开发 B
- 状态：待验收
- 进展：
  - 已为 `BoardLayerBinder` 增加牌面触摸回调，只有可点击测试牌会响应。
  - 已为 `ComboBarBinder` 增加组合按钮触摸回调，满足候选后可执行基础组合。
  - 已在 `GameSceneController` 中加入 8 格主槽测试状态，点击牌会进入槽位并刷新 HUD、组合按钮和分数。
  - 已支持测试首屏中的基础 `胡 / 杠 / 碰 / 吃` 候选检测与消除，其中 `胡` 使用 8 张 `3 + 3 + 2` 判定。
- 验证：
  - 已通过 `npm run test -w packages/shared -- mahjong-cocos-project`。
  - 已通过 `npx tsc --noEmit -p apps/game/mahjong-roguelike/cocos/hulebu-cocos-3.8.8/tsconfig.json`。
  - 已通过 `npm run test -w packages/shared -- mahjong`。
  - 已通过 `npm run typecheck -w packages/shared`。
  - 已通过 `npm run docs:sync`，同步 35 个任务分片和 35 个领取分片。
  - 已通过 `git diff --check`。
  - 已在 Cocos Web Preview 手机视口手动验证：点击 `1万` 后进入第 1 个卡槽；点击 `7条 / 8条 / 9条` 后 `吃 1` 亮起；点击 `吃` 后三张条子从卡槽消除。

## T070：胡了卜 Cocos 点击后遮挡解锁和槽位牌名显示

- 负责人：Codex / 开发 B
- 状态：待验收
- 进展：
  - 已补充 Cocos 工程结构测试，锁定点击后遮挡重算和槽位 Label 自动补齐两个回归点。
  - `GameSceneController` 已在测试首屏刷新时按剩余牌重新计算遮挡状态，继续使用 5% 遮挡阈值。
  - 点击移走上层牌后，下层牌会重新变亮并恢复可点态。
  - `SlotLayerBinder` 已为 8 格槽位稳定创建直接子级 `Label`，入槽牌会显示牌名。
  - 已修正 Cocos `resources.meta` 的 Asset Bundle 配置，避免 Web Preview 查询 settings 数据时报 `Cannot read properties of undefined (reading 'value')`。
- 验证：
  - 已通过 `npm run test -w packages/shared -- mahjong-cocos-project`。
  - 已通过 `npx tsc --noEmit -p apps/game/mahjong-roguelike/cocos/hulebu-cocos-3.8.8/tsconfig.json`。
  - 已通过 `npm run test -w packages/shared -- mahjong`，5 个测试文件、36 个测试通过。
  - 已通过 `npm run typecheck -w packages/shared`。
  - 已通过 `npm run docs:sync`，同步 36 个任务分片和 36 个领取分片。
  - 已通过 `git diff --check`。
  - 已在 Cocos Web Preview 手机视口手动验证：`7条 / 8条 / 9条` 入槽显示牌名，执行 `吃` 后中层 `西` 重新解锁并可继续入槽。
