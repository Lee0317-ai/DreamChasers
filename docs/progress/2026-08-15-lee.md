# 2026-08-15 Lee 进展

## T287 胡了卜完整 UI 全新视觉方向

- 配置 PPTOKEN 临时环境密钥完成 `gpt-image-2` 参考图编辑，密钥未写入仓库、技能或生成文档。
- 完成萌系麻将完整候选牌组：万/条/筒各 1–9、东南西北中发白、粉色胡萝卜牌背，共 35 张。
- 保留现有麻将牌义、数量和构图，统一改为暖白陶瓷牌面、薄荷绿牌壳、珊瑚粉边和圆润符号表现。
- 新增标准化工具，将不同尺寸和背景格式统一为 `1024×1024 RGBA PNG`、透明背景和中心锚点。
- 隔离并重做一筒网格残影与白牌多余方框两张异常图。
- 输出完整拼版、逐花色拼版、牌背预览、提示词和 `tile-pack-v3-manifest.json`。
- 当前素材仍位于 `output/hulebu-ui-design-v2/tile-pack-v1/generated-v3/`，未接入 Cocos 正式资源。

## 本机图片技能

- 为 `pptoken-imagegen` 增加生成请求和 CDN 下载的有限重试，覆盖 `RemoteDisconnected`、`IncompleteRead`、超时、429 和服务端错误。
- 技能结构校验通过，真实生成和中断恢复已验证。

## T287 独立动作按钮

- 新建 `output/hulebu-ui-design-v2/component-pack-v3/`，提示词、原始图、标准化图和预览继续隔离。
- 完成胡、杠、碰、吃、补杠五个独立动作按钮，材质与已确认麻将牌保持一致。
- 四个单字按钮统一为 `512×512 RGBA`，补杠统一为 `768×512 RGBA`，全部保留透明背景和中心锚点。
- 完成第二组局内组件：记牌器收起/展开面板、碰池面板、震落牌区和可复用单个手牌槽；统一输出透明 RGBA，手牌槽支持运行时重复拼成 8 格。
