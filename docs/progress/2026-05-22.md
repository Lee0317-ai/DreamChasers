# 2026-05-22 进展

## T013：添加部署文件

- 负责人：Codex / 开发 B
- 状态：已完成
- 完成内容：
  - 新增任务分片 `docs/tasks/items/T013-deployment-foundation.md`。
  - 新增领取分片 `docs/tasks/claims/T013-codex.md`。
  - 新增 `apps/web/Dockerfile`，使用 `STANDALONE_BUILD=1` 构建 Next.js standalone 运行产物，并在运行镜像中保留 Prisma 初始化所需文件和生产依赖。
  - 新增 `docker-compose.prod.yml`，包含 `postgres` 和 `web`，Web 只绑定 `127.0.0.1:3000`。
  - 新增 `.env.production.example`，用于服务器生产环境变量。
  - 新增 `.dockerignore`，减少镜像构建上下文并避免上传本地环境文件。
  - 新增 `deploy/nginx-host.conf`，用于宿主机 Nginx 反代。
  - 新增 `deploy/README.md`，记录本地构建镜像、上传服务器、启动 Compose、初始化数据库、配置 Nginx 和备份流程。
- 验证：
  - `docker compose -f docker-compose.prod.yml --env-file .env.production.example config`：通过。
  - `npm run build:standalone -w apps/web`：通过。
  - `docker build --platform linux/amd64 -f apps/web/Dockerfile -t dreamchasers-web:latest .`：未执行成功，本地 Docker daemon 未运行。
- 下一步：
  - 用户本地启动 Docker Desktop 后，按 `deploy/README.md` 构建镜像并上传服务器。

## T041：麻将 Roguelike 团队评审版玩法方案

- 负责人：Codex / 开发 B
- 状态：已完成
- 完成内容：
  - 新增变更卡 `IDEA-20260522-11`。
  - 新增任务分片 `docs/tasks/items/T041-mahjong-gameplay-review-docs.md`。
  - 新增领取分片 `docs/tasks/claims/T041-codex.md`。
  - 新增 `docs/modules/mahjong-roguelike/GAMEPLAY_REVIEW_PLAN.md`。
  - 新增 `docs/modules/mahjong-roguelike/GAMEPLAY_REVIEW.html`。
  - 评审稿覆盖游戏定位、核心循环、吃碰杠规则、槽位失败、孤张、记牌器、经济体力、Roguelike、永久成长、模式结构、牌局生成、MVP 范围和团队评审问题。
  - 更新麻将模块 `README.md`、`PROGRESS.md` 和 `HANDOFF.md`。
- 验证：
  - `npm run docs:sync`：通过，已同步 7 个任务分片和 7 个领取分片。
  - 文档自审：通过。
  - UTF-8 无 BOM 检查：通过。
- 下一步：
  - 团队评审后继续收敛 MVP 第一版范围，再进入开发计划拆分。

## T040：麻将 Roguelike 完整牌局规则、经济体力和失败救场规划

- 负责人：Codex / 开发 B
- 状态：已完成
- 完成内容：
  - 新增变更卡 `IDEA-20260522-10`。
  - 新增任务分片 `docs/tasks/items/T040-mahjong-complete-round-rules.md`。
  - 新增领取分片 `docs/tasks/claims/T040-codex.md`。
  - 在 `GAMEPLAY_PLAN.md` 中补齐完整牌局规则。
  - 明确只有未被上层牌覆盖的牌可点击，入槽动画期间锁定输入。
  - 明确入槽后不能手动撤销，只能通过 `撤回` 道具处理。
  - 明确多个 `吃 / 碰 / 杠` 候选由玩家自由选择，点击发动后不做二次确认。
  - 明确孤张只做内部判定，不直接在牌面标记给玩家。
  - 将收益拆为 `局内积分` 和 `铜钱` 两层。
  - 新增体力系统口径：每局 5 点体力，体力上限 100 点，按时间恢复，可用铜钱或付费购买。
  - 明确失败前救场顺序：组合检测、备用槽自动救场、满槽护符、救场道具窗口、首败保护、失败结算。
  - 将教学口径改为 `新手引导`。
- 验证：
  - `npm run docs:sync`：通过。
  - 文档自审：通过。
  - UTF-8 无 BOM 检查：通过。
- 下一步：
  - 继续单独展开牌堆生成约束，尤其是每关总牌数、组合包比例、干扰包上限和可解路径校验。

## T039：条件启用 Next.js standalone 自托管构建

- 负责人：Codex / 开发 A
- 状态：已完成
- 完成内容：
  - 新增任务分片 `docs/tasks/items/T039-conditional-standalone-build.md`。
  - 新增领取分片 `docs/tasks/claims/T039-codex.md`。
  - 在 `apps/web/next.config.ts` 中通过 `STANDALONE_BUILD=1` 条件启用 `output: "standalone"`。
  - 在 `apps/web/package.json` 中新增 `build:standalone` 脚本。
  - 保持默认 `npm run build -w apps/web` 不启用 standalone，避免影响 Vercel 自动部署。
- 验证：
  - `npm run build -w apps/web`：通过。
  - `npm run build:standalone -w apps/web`：通过。
  - `apps/web/.next/standalone`：已生成。
- 下一步：
  - 如要部署到 Alibaba Cloud Linux，可基于 `.next/standalone` 打包运行产物，不需要上传完整源码。

## T038：麻将 Roguelike 永久固化能力和卡槽系统规划

- 负责人：Codex / 开发 B
- 状态：已完成
- 完成内容：
  - 新增变更卡 `IDEA-20260522-09`。
  - 新增任务分片 `docs/tasks/items/T038-mahjong-permanent-abilities.md`。
  - 新增领取分片 `docs/tasks/claims/T038-codex.md`。
  - 在 `GAMEPLAY_PLAN.md` 中新增永久固化能力和能力卡槽规划。
  - 明确 `手牌槽位` 和 `能力卡槽` 分开管理。
  - 明确透视属于信息/道具强化，不等于扩槽。
  - 规划永久能力四层结构：基础成长、固化能力、起局能力、道具强化。
  - 初步确认东风场基线为固化能力槽 3、起局能力槽 1、道具强化槽 1。
- 验证：
  - 文档自审：通过。
  - UTF-8 无 BOM 检查：通过。
- 下一步：
  - 下一步可以继续细化首批可实现固化能力清单，或进入首批关卡/规则模型配置。

## T037：新增 docs:sync 自动汇总脚本

- 负责人：Codex / 两人协作
- 状态：已完成
- 完成内容：
  - 新增变更卡 `IDEA-20260522-08`。
  - 新增任务分片 `docs/tasks/items/T037-docs-sync-script.md`。
  - 新增领取分片 `docs/tasks/claims/T037-codex.md`。
  - 新增 `scripts/docs-sync.mjs`。
  - 在根目录 `package.json` 新增 `docs:sync` 脚本。
  - 更新文档同步策略，明确 `npm run docs:sync` 只维护自动生成摘要区。
  - 运行脚本生成 `TASK_BOARD.md`、`CLAIMS.md` 和 `CURRENT_STATUS.md` 的自动摘要区。
- 验证：
  - `npm run docs:sync`：通过。
  - 文档自审：通过。
  - UTF-8 无 BOM 检查：通过。
- 下一步：
  - 后续新增任务和领取记录时，优先写分片并运行 `npm run docs:sync`。

## T036：降低多人协作文档冲突的分片同步规范

- 负责人：Codex / 两人协作
- 状态：已完成
- 完成内容：
  - 新增变更卡 `IDEA-20260522-07`。
  - 新增任务 `T036`，并完成任务分片和领取分片。
  - 新增 `docs/workflow/doc-sync-policy.md`。
  - 新增 `docs/tasks/items/README.md` 和 `docs/tasks/claims/README.md`。
  - 更新 `AGENTS.md`、`CLAUDE.md`、`docs/PROJECT_CONTEXT.md` 和双人协作规范。
  - 明确分步操作优先写分片文件，完整任务完成后再汇总主文档。
- 验证：
  - 文档自审：通过。
  - UTF-8 无 BOM 检查：通过。
- 下一步：
  - 如后续需要进一步降低人工汇总成本，可新增 `docs:sync` 脚本，从任务分片生成主任务池和领取摘要。

## T035：麻将 Roguelike 局内能力池规划

- 负责人：Codex / 开发 B
- 状态：已完成
- 完成内容：
  - 新增变更卡 `IDEA-20260522-06`。
  - 新增任务 `T035`，并完成领取和状态同步。
  - 在 `GAMEPLAY_PLAN.md` 中新增局内 Roguelike 能力池。
  - 按孤张/补牌/换牌、杠流、吃流、碰流、花色流、槽位流、道具流、信息流 8 类规划 44 个局内能力。
  - 明确提示类、收益类、调度类、改牌类 4 档强度。
  - 明确直接移除牌、补牌、换牌属于强能力，需要稀有度和出现数量限制。
- 验证：
  - 文档自审：通过。
  - UTF-8 无 BOM 检查：通过。
- 下一步：
  - 继续讨论永久固化能力和能力卡槽的最终配置方式，或开始细化首批可实现能力。

## T034：麻将 Roguelike 牌谱记牌器规划

- 负责人：Codex / 开发 B
- 状态：已完成
- 完成内容：
  - 新增变更卡 `IDEA-20260522-05`。
  - 新增任务 `T034`，并完成领取和状态同步。
  - 在 `GAMEPLAY_PLAN.md` 中新增 `牌谱记牌器` 章节。
  - 确认顶部默认显示 `余牌  万/条/筒` 剩余总数。
  - 确认展开后显示 1-9 点数剩余数量。
  - 明确记牌器随消除实时减少。
  - 明确记牌器和透视的区别：记牌器给数量，透视给位置。
- 验证：
  - 文档自审：通过。
  - UTF-8 无 BOM 检查：通过。
- 下一步：
  - 进入局内 Roguelike 能力池规划，优先围绕孤张处理、补牌、换牌、杠流和记牌器联动能力。

## T033：麻将 Roguelike 组合提示和牌堆生成规则

- 负责人：Codex / 开发 B
- 状态：已完成
- 完成内容：
  - 新增变更卡 `IDEA-20260522-04`。
  - 新增任务 `T033`，并完成领取和状态同步。
  - 在 `GAMEPLAY_PLAN.md` 中补充组合提示和组合选择规则。
  - 确认 `吃 / 碰 / 杠` 只要出现合法组合就提示，并展示对应消除内容。
  - 确认多个组合时允许玩家选择具体组合。
  - 规划高阶挑战中的手动选牌出牌：玩家可手动选择 3-4 张再发动。
  - 明确牌堆使用组合包受控随机生成，不要求完整麻将牌组。
  - 补充杠包被碰后产生孤张的处理机制。
- 验证：
  - 文档自审：通过。
  - UTF-8 无 BOM 检查：通过。
- 下一步：
  - 继续细化局内 Roguelike 能力池，尤其是孤张处理、补牌、换牌和杠流奖励。

## T032：麻将 Roguelike 高阶挑战系统规划

- 负责人：Codex / 开发 B
- 状态：已完成
- 完成内容：
  - 新增变更卡 `IDEA-20260522-03`。
  - 新增任务 `T032`，并完成领取和状态同步。
  - 在 `GAMEPLAY_PLAN.md` 中新增高阶挑战系统。
  - 确认高阶挑战由 `牌山层数增长 + 词缀系统 + 随机事件 + 卡槽压缩 + Boss 试炼` 组成。
  - 新增第一批 12 个高阶词缀。
  - 新增第一批 12 个随机事件。
  - 新增第一批 5 个 Boss 试炼。
- 验证：
  - 文档自审：通过。
  - UTF-8 无 BOM 检查：通过。
- 下一步：
  - 继续细化局内 Roguelike 能力池、永久固化能力池和能力卡槽规则。

## T031：麻将 Roguelike 最终模式结构定稿

- 负责人：Codex / 开发 B
- 状态：已完成
- 完成内容：
  - 新增变更卡 `IDEA-20260522-02`。
  - 新增任务 `T031`，并完成领取和状态同步。
  - 在 `GAMEPLAY_PLAN.md` 中新增最终模式结构。
  - 确认最终结构为：`闯关模式`、`无尽牌山`、`高阶周目`、`每日牌局`、`成就图鉴`。
  - 将 20 关定义为首次完整通关目标，通关后解锁无尽牌山和高阶周目。
  - 将每日牌局定义为固定牌山种子，用于后续回访和复盘。
- 验证：
  - 文档自审：通过。
  - UTF-8 无 BOM 检查：通过。
- 下一步：
  - 继续细化无尽层数增长、高阶周目词缀、每日牌局成绩维度和成就奖励。

## T030：麻将 Roguelike 手动组合和成长系统规划

- 负责人：Codex / 开发 B
- 状态：已完成
- 完成内容：
  - 新增变更卡 `IDEA-20260522-01`。
  - 新增任务 `T030`，并完成领取和状态同步。
  - 新增 `docs/modules/mahjong-roguelike/GAMEPLAY_PLAN.md`。
  - 确认消除不自动触发，改为玩家手动选择组合并点击 `吃 / 碰 / 杠` 按钮发动。
  - 明确 `吃 / 碰 / 杠` 按钮默认灰色，满足条件后变成金红冒火状态。
  - 将槽位规划为可成长属性，包含槽位容量、备用槽、槽位净化等方向。
  - 重新拆分局内 Roguelike 奖励、局外槽位成长和主动道具。
- 验证：
  - 文档自审：通过。
  - UTF-8 无 BOM 检查：通过。
- 下一步：
  - 继续讨论按钮位置、多组合选择、槽位成长上限、铜钱消耗和 `胡牌目标` 是否进入 MVP。
