# 2026-06-01 Lee 进展

- 完成 T097 胡了卜教学关必须发动对应组合才通关。
- 默认玩家 Demo 前 4 关现在不能靠“全部点进卡槽”直接过关，必须分别点击 `碰 / 吃 / 杠 / 胡` 动作按钮完成教学目标。
- 教学关候选被限制为本关目标动作，前 3 关教学牌山缩为最小牌型包，避免新手关被额外干扰牌挤满 6 格槽。
- 完成 T094 胡了卜残局收官与试玩反馈设计。
- 将 Lee 试玩反馈登记为 IDEA-20260601-03，并创建 T094 任务分片与领取分片。
- 写入设计规格 `docs/superpowers/specs/2026-06-01-hulebu-endgame-settlement-design.md`。
- 设计结论：普通关不默认强制清槽；牌桌清空但槽内有残张时进入 `残局收官`。收官方向为 `弃牌通关 / 选作牌引 / 收入牌河`。
- 明确下一轮实现边界：前 4 关必须发动教学组合才过关；牌面和点击热区放大；`丢弃` 改为玩家选择槽位任意一张；玩家页补可见记牌器；Demo 第一阶段先实现弃牌通关和牌引，牌河兑换后置。
- 完成 T095 胡了卜混合窗口牌山生成器。
- 根据 Lee 反馈修正“顶层三张就是答案”的问题：密集牌山保留答案组，但释放窗口会混入不同答案组的牌，默认普通密集关首轮同一答案组最多露出 2 张。
- 运行态抽样确认第 1 关首轮 8 张可点、最大同组 2 张；Kimi WebBridge 打开真实页面第 5 关确认首轮 7 张可点、最大同组 2 张。
- 完成 T096 胡了卜玩家页布局、牌面放大和模板随机调参。
- 默认玩家页主栏调整为 `480px`、右侧道具栏 `72px`，牌桌、HUD、卡槽和道具栏在 1512x682 桌面视口无横向/纵向溢出。
- 密集牌山规则牌尺寸从 `45x60` 放大到 `52x70`，玩家页实测牌面约 `37x49`；主堆权重提升到 `2.45`，减少空旷感。
- 默认普通密集关 `auto` 模板按 seed/重开随机，并在候选失败或首轮可点超过 8 时回退到下一个安全模板；调牌器仍保留完整 8 模板手动指定。
- Kimi WebBridge 第 5 关 `seed=epsilon` 验证：生成 240 张、可见 43 张、首轮可点 8 张、显示 `环形` 模板，无横向/纵向溢出。
