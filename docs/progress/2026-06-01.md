# 2026-06-01 进展

## T093：胡了卜 10 关朋友试玩 Demo

- 负责人：Lee
- 状态：待验收
- 完成内容：默认玩家页已改为 10 关朋友试玩小 run。第 1-4 关分别教学 `碰 / 吃 / 杠 / 胡`；第 1-3 关使用 6 格卡槽，第 3 关后固定奖励 `卡槽 +2`，第 4 关起使用 8 格卡槽；第 5 关开始进入 240 张密集牌山高压模式。
- 道具调整：右侧道具改为 `洗牌 / 撤回 / 丢弃`。丢弃会移除主槽末尾牌并消耗次数；主槽已满且没有组合时，如果还有丢弃次数，会提示可用丢弃救场。
- 移动布局：390 CSS 像素下默认玩家页改为底部横排三道具，卡槽和工具栏完整可见，CDP 实测 `scrollWidth=390`、卡槽宽 `350`、底部工具栏宽 `370`。
- 修改文件：`apps/game/mahjong-roguelike/prototypes/config-playable/index.html`, `packages/shared/src/mahjong-config-playable-prototype.test.ts`, `packages/shared/src/mahjong-config.test.ts`, `docs/tasks/CHANGE_INTAKE.md`, `docs/tasks/items/T093-hulebu-friend-playtest-demo.md`, `docs/tasks/claims/T093-lee.md`, `docs/tasks/NEXT_ID.md`, `docs/superpowers/plans/2026-06-01-hulebu-friend-playtest-demo.md`, `docs/modules/mahjong-roguelike/README.md`, `docs/modules/mahjong-roguelike/PROGRESS.md`, `docs/modules/mahjong-roguelike/HANDOFF.md`, `docs/progress/2026-06-01.md`
- 验证结果：`npm run test -w packages/shared -- mahjong-config-playable-prototype mahjong-config` 通过；HTML 脚本 `node --check` 通过；Kimi WebBridge 桌面默认页验证 10 个关卡 tab、第一关 6 张教学牌/6 格卡槽、HUD `重点 碰`、道具 `洗牌 / 撤回 / 丢弃`；运行态验证第三关奖励只显示 `卡槽 +2`，丢弃会移除主槽牌并消耗次数。移动截图为 `tmp/hulebu-t093-mobile-cdp.png`。
- 遗留问题：本任务只改 HTML 配置试玩原型，未同步 Cocos 正式工程。丢弃当前默认移除主槽末尾牌；若朋友试玩反馈需要更强控制感，可另开任务改为选择主槽任意一张。

## T092：胡了卜玩家页正式一屏 HUD 重排

- 负责人：Lee
- 状态：待验收
- 完成内容：已把配置试玩原型默认玩家页重排为正式一屏 HUD。顶部新增 `play-hud`，显示关卡、目标、余牌、积分和铜钱；右侧完整信息侧栏在玩家页隐藏，只保留 `洗山 / 回手 / 看山` 道具栏；底部 8 格卡槽继续首屏可见。
- 桌面布局：Kimi WebBridge 实测 1512x682 视口无整页滚动，牌桌 `307x351`，HUD `460x61`，卡槽 `460x114`，右侧道具栏 `76x204`；第 1 关仍为 41 张可见、8 张可点，无横向溢出。
- 移动布局：390x844 受控移动检查页面无横向溢出且不滚动，牌桌 `276x315`，卡槽底部 `719px`，右侧道具栏 `64x204` 位于首屏；首轮仍为 8 张可点。
- 调牌器验证：`view=tuner&mode=mountain&level=1` 仍显示调参面板、余牌统计、已选奖励、工具、重开和下一关控制；顶部新 HUD 在调牌器视图隐藏。
- 验收调参：密集牌山轻微遮挡阈值统一为 `8%`，低于 8% 可点击，达到 8% 才阻塞；规则牌尺寸调整为 `45x60`，CSS 宽度调整为 `8.0357142857%`，桌面第 1 关实测牌面约 `24.5x32.7`。
- 牌山集中度：模板锚点向中心收拢，前 4 个主堆加权；第 1-8 关默认首轮可点稳定在 5-8 张，符合当前起手 3-8 张目标，同时继续覆盖 34 个具体牌面。
- 浏览器补充验证：Kimi WebBridge 默认页第 1 关 41 张可见、8 张可点，截图为 `/tmp/hulebu-t092-8pct-larger-tiles.png`；运行态遮挡探针验证 `0.9%` 视觉覆盖不阻塞、`11.1%` 视觉覆盖阻塞；390x844 受控移动检查牌桌 `254x290`、卡槽首屏可见。
- 修改文件：`apps/game/mahjong-roguelike/prototypes/config-playable/index.html`, `packages/shared/src/mahjong-config-playable-prototype.test.ts`, `docs/tasks/CHANGE_INTAKE.md`, `docs/tasks/items/T092-hulebu-one-screen-play-hud.md`, `docs/tasks/claims/T092-lee.md`, `docs/tasks/NEXT_ID.md`, `docs/modules/mahjong-roguelike/README.md`, `docs/modules/mahjong-roguelike/IMPLEMENTATION_PLAN.md`, `docs/modules/mahjong-roguelike/PROGRESS.md`, `docs/modules/mahjong-roguelike/HANDOFF.md`, `docs/progress/2026-06-01.md`
- 验证结果：`npm run test -w packages/shared -- mahjong-config-playable-prototype` 通过；`npm run test -w packages/shared -- mahjong-config` 通过；`node --check /tmp/hulebu-config-playable-script.js` 通过；Kimi WebBridge 桌面默认玩家页、调牌器检查和 8% 遮挡探针通过；390x844 受控移动检查通过。截图为 `/tmp/hulebu-t092-desktop.png`、`/tmp/hulebu-t092-mobile.png` 和 `/tmp/hulebu-t092-8pct-larger-tiles.png`。
- 遗留问题：当前只改 HTML 配置试玩原型，未同步 Cocos 正式工程；右侧道具栏暂时使用文字按钮，后续正式表现层应替换为更适合移动端的图标按钮和触控态。桥接堆现在允许单入口和多入口混合，若后续想让“移走一张后必然释放多个选择”成为硬规则，需要另开平衡任务。
