# 2026-08-10 Lee 进展

## T258：胡了卜正式 UI Batch D Cocos 接入

- 已把 `hulebu-formal-ui-v1` 的 80 项运行时资源复制到 Cocos `assets/resources/ui/formal-v1/`，包含背景、HUD、牌槽、动作/工具三态、卡片、弹层和 35 张麻将牌。
- 新增 `HulebuFormalUiCatalog`，集中维护正式 SpriteFrame 路径；背景、HUD、手槽、动作按钮、工具按钮、奖励卡和通用弹层已切换到 formal v1。
- `HulebuTileSpriteCatalog` 现在优先加载 formal v1 牌面，资源失败时才回退历史 v6 牌面。
- 正式资源清单核对为 80 个文件、80 个唯一 key、无缺失和额外 PNG。
- `mahjong-cocos-project` 当前 40 项测试通过；Cocos domain TypeScript 通过。
- Cocos Creator 3.8.8 已生成并提交 90 个 formal v1 权威 `.meta`；资源路径在精确提交快照内保持 clean。
- 精确提交 `1eb7f00e2b2e0fa764096c73b38235c82113fbb1` production build 通过，build ID 为 `1eb7f00e2b2e-20260809T164755Z`，5 个 smoke 路径均为 200。
- `390×844` 内置浏览器实测正式背景、HUD、按钮、槽位和牌面均加载；画布为 `390×844` CSS / `780×1688` 像素，控制台 0 warning/error。
- 点击一张顶层牌后余牌从 24 降到 23，牌正常进入手槽，核心交互未因视觉资源切换回归。

## T259：胡了卜 Cocos 竖屏正式 UI 布局重排

- 已登记并领取任务，问题基线为高 DPR 竖屏下顶部 HUD 裁切、牌阵过小、动作栏压住手牌槽和底部空间失衡。
- 初步定位到 `resolveHulebuRuntimeLayout` 将 Cocos 内部像素尺寸与 CSS 尺寸混用，且缩放上限固定为 1，导致正式 UI 在高 DPR 画布中没有按可见尺寸放大。
- 已完成首轮竖屏重排：顶部 HUD 不再裁切，动作栏、备用槽和 8 格手牌槽拆分为独立纵向带，空河牌格隐藏，稀疏首关牌阵放大。
- Cocos Creator `390×844` 预览已直接点击验证，牌可进入手槽且剩余牌数、分数随交互更新。
- 用户追加八条牌面、不可点击暗态和碰牌区/补杠识别反馈；T259 边界已扩展到牌面目录映射和副露状态回归测试，不修改玩法规则或关卡配置。
- T259 已完成：八条回退固定到 formal v1，锁牌透明度和色调加深，不可执行组合按钮使用 disabled 图，放大牌命中区同步 `visualScale`；碰后第四张同牌点亮补杠并升级四张副露的回归测试通过。
- 共享测试 40/40、Cocos TypeScript 和精确提交 production build 均通过；提交 `6a4a9cd3f5f50813d3677fa7480ae3b7f050f556` 的 build ID 为 `6a4a9cd3f5f5-20260810T145136Z`，5 个 smoke 路径均为 200。

## T260：胡了卜 Cocos 锁牌暗态与顶部 HUD 精修

- 已登记并领取任务。验收反馈为下层锁牌仍显亮、分数牌匾多行信息未完成、紧凑记牌器仍显示挤压长串。
- 本任务只处理表现和状态反馈，不修改玩法、关卡或正式图片原文件。
