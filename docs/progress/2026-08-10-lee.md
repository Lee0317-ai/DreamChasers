# 2026-08-10 Lee 进展

## T258：胡了卜正式 UI Batch D Cocos 接入

- 已把 `hulebu-formal-ui-v1` 的 80 项运行时资源复制到 Cocos `assets/resources/ui/formal-v1/`，包含背景、HUD、牌槽、动作/工具三态、卡片、弹层和 35 张麻将牌。
- 新增 `HulebuFormalUiCatalog`，集中维护正式 SpriteFrame 路径；背景、HUD、手槽、动作按钮、工具按钮、奖励卡和通用弹层已切换到 formal v1。
- `HulebuTileSpriteCatalog` 现在优先加载 formal v1 牌面，资源失败时才回退历史 v6 牌面。
- 正式资源清单核对为 80 个文件、80 个唯一 key、无缺失和额外 PNG。
- `mahjong-cocos-project` 当前 40 项测试通过；Cocos domain TypeScript 通过。
- Cocos Creator 3.8.8 已生成并提交 90 个 formal v1 权威 `.meta`；资源路径在精确提交快照内保持 clean。
- 精确提交 `1eb7f00e2b2e0fa764096c73b38235c82113fbb1` production build 通过，build ID 为 `1eb7f00e2b2e-20260809T164755Z`，5 个 smoke 路径均为 200。
- `390×844` 内置浏览器实测正式背景、HUD、按钮、槽位和牌面均加载；画布为 `390×844` CSS / `780×1688` 像素，控制台 0 warning/error。
- 点击一张顶层牌后余牌从 24 降到 23，牌正常进入手槽，核心交互未因视觉资源切换回归。
