# 2026-08-17 Lee 进展

## T289 侧视重力牌塔原型（已放弃）

- 与 Claude 逐项确认了侧视方案规则：重力垂直下落、仅列顶可点、深度只影响可见性、30-140 张牌量、震落飞侧边散牌区。
- 产出独立原型 `prototypes/side-view/`（assets 为符号链接，复用 v6 牌面）：第一版排排站立牌 + 前排坍塌，第二版改为参考图式实心金字塔叠塔 + 顶部条带可点判定 + 支撑消失下落。
- 两轮目检后确认画面与预期差距过大，决定放弃侧视图方向；主 demo、站内静态副本、Cocos 工程与共享包全程未改动。
- 教训：伪 3D 立牌堆叠靠 CSS 平面贴图难以达到参考图观感；后续同类视觉方向探索应先出静态视觉稿确认构图，再投入交互代码，或直接在 Cocos 里用真 3D 做。
- T289 关闭为"已放弃"，`side-view/` 目录保留作历史记录。

## T287 UI 全量盘点

- 按 Lee 要求暂停牌山选型，改为整理全部 UI 资产：生成 `component-pack-v3/previews/all-ui-inventory.png`（51 组件 + 35 牌 + 4 背景分类拼版）。
- 对照 `images/ui-states-v6/` 十个流程状态查漏，确认缺口：组合候选面板、弃牌救场面板、开局流派卡、Boss 血条、确认/取消/关闭按钮、Toast、教程遮罩、加载页、结算明细面板。
- 缺口清单已提交 Lee 确认优先级，未新增组件、未接入 Cocos。

## T287 缺口组件补齐

- Lee 确认"其他的继续补充"后，用 PPTOKEN 串行生成 12 张缺口组件图，原始稿进入 `component-pack-v3/images/batch-gap/`，提示词进入 `component-pack-v3/prompts/`。
- 11 个透明组件经 `tools/normalize_gap_components.py` 标准化进入 `normalized/`（清单 `gap-components-manifest.json`）；loading-screen 归档为 `backgrounds/images/loading.png` 全出血背景。
- 新增 `tools/build_gap_preview.py` 输出 12 格带标签拼版 `previews/gap-components-v3.png`，目检确认风格与既有组件一致；等待 Lee 确认后，再考虑接入 Cocos。

## T290 UI v3 正式接入 Cocos

- 将 63 张组件、5 张背景和 35 张麻将牌复制到正式 Cocos `assets/resources/ui/hulebu/`，并由 Creator 3.8.8 生成完整 `.meta`。
- 新增 `HulebuV3UiCatalog.ts`，formal UI、meta-flow 和麻将牌目录主路径统一切到 v3；旧牌面仍保留加载失败兜底。
- 动作按钮新增禁用态灰化；弃牌工具改用 v3 弃牌图标；手牌槽、记牌器展开板和已碰牌池底板改用 v3 组件。
- 局外大厅、地图、结算按 phase 使用对应 v3 背景，并移除旧深色遮罩造成的发灰问题；章节标题、节点数字和提示文案改为新粉彩底可读色。
- 浏览器 390×844 实测牌面点击、三牌点亮“碰”、记牌器展开、大厅与地图，无新增控制台 error。
- `mahjong-cocos-project.test.ts` 44/44 通过；联合测试 163/164，唯一失败为既有 `HulebuPortraitLayout.ts` 领域导入边界问题。
- 正式提交 `4cb1c029683bd476d5c0dbca723ddab397876b1c`；production build ID `4cb1c029683b-20260817T120429Z`，build 和 verify-only 均通过。
