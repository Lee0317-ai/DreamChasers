# 2026-08-03 Lee 进展

## T244 胡了卜 Cocos v1 M1 核心边界与状态机

- 在内置浏览器以 `390×844` 竖屏打开 production web-mobile，确认 Canvas、HUD、牌山、组合栏、槽位和工具栏完整显示，游戏控制台无 warn/error。
- 完成真实点击入槽、手动碰牌、刷新恢复、首层清台和通关弹窗。
- 第二层触发 `4筒 5筒 6筒` 与 `5筒 6筒 7筒` 两组吃牌候选；选择态刷新后候选原样恢复，选择右侧候选后只移除对应三张，槽中保留 `4筒`，exact candidate 结算正确。
- 验证通过：`npm run test -w packages/shared -- hulebu-cocos-domain mahjong-cocos-project`（`158/158`）、`npx tsc -p apps/game/mahjong-roguelike/cocos/hulebu-cocos-3.8.8/tsconfig.domain.json`、`npm run docs:sync`、`git diff --check`。
- 当前 HEAD `a17a37ee` 已生成 exact-commit production build `a17a37ee7c3b-20260803T042725Z`；`game:hulebu:build`、授权后的 `game:hulebu:verify-build` 和 5 条 HTTP smoke 均通过，T244 验收完成。
