# 2026-07-12 Lee Progress

## T243 胡了卜 Cocos 正式源码基线与构建溯源门禁

- 将当前正式 Cocos 运行时、绑定器、工程内山体生成器、安全工具和 v6 UI 资源收口到源码提交 `64824e47`，干净 checkout 不依赖 `profiles/**`。
- production wrapper 现在拒绝正式输入的已修改、删除、未跟踪、忽略、特殊索引、symlink、gitlink 和 tree 不一致状态，并只让 Creator 读取精确提交快照。
- 固定 Creator 3.8.8 realpath、二进制 SHA-256、bundle id、Info.plist 与唯一版本日志证据；全产物扫描禁止 `__HULEBU_DEBUG__`。
- 构建先写唯一 attempt，再经源码复核、Creator、TypeScript、产物校验和 HTTP smoke 后 promote；同步失败回滚，进程崩溃由持久 journal、原子 owner marker 和可重试 tombstone 在下次拿锁后恢复。
- schema 6 manifest 记录 `sourceState: clean`、源码/config 摘要、完整 `CocosCreator.app/Contents` SHA-256、`cocosTypecheckPassed: true`、artifact SHA-256 和 smoke 证据。
- 2026-07-15 最终干净 worktree 发布测试 `189/189`、Cocos 工程测试 `32/32`、真实 Creator build 与 verify-only 全部通过；正式包为 `566` 个文件、`135263593` 字节。
- 本任务只解决正式源码和可信构建基线；GameSession、RunStateMachine、UI、音效、站点切换与上线 QA 继续由 T244 和后续里程碑完成。
