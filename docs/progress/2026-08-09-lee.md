# 2026-08-09 Lee 进展

## T250：PPTOKEN 图片生成切换新站

- 把本机 `pptoken-imagegen` 技能的默认接口从旧站切换到 `https://api.pptoken.cc/v1`。
- 生成脚本支持新站直连 Bearer 请求和 `https://www.pptoken.cc/tool-api/images/generations` 网页代理封装。
- 修复返回 URL 下载未携带鉴权导致的 403，并兼容 `b64_json` 返回。
- 更新项目 `.env.example` 和 provider readiness 测试样例的新站域名。
- 技能本地 4 项测试、技能结构校验和项目 provider readiness 5 项测试通过。
- 全程未把真实 API Key 写入文件。

## T249/T251：胡了卜正式 UI 样稿确认与 Batch A+B

- Lee 已确认 T249 三张视觉样稿通过，正式进入分层资源生产。
- 使用 PPTOKEN 新站和已授权原始参考图生成无牌山、无 HUD、无按钮的青绿干净背景母版。
- 新增 `build_formal_ui_batch_ab.py`，从已通过状态母版输出 HUD、牌槽、动作和工具按钮三态。
- 正式资源包当前包含 36 个 manifest 资源；设计视口 `390×844`，资源倍率 2x。
- 主场景组合预览和透明棋盘三态预览已人工检查；机器校验确认 key 唯一、RGBA、路径存在和三态画布一致。
- 本批未接入 Cocos；下一步是 Batch C 卡片、弹窗和麻将牌面。

## T252：胡了卜正式 UI 资源 Batch C

- 通过 PPTOKEN 新站生成 4 张无文字奖励/风场卡和 5 个无文字流程弹窗母版。
- 本地完成洋红隔离底抠图、溢色抑制、统一画布和透明边缘校验。
- 复用 v7 牌体输出 34 张正面和 1 张背面，并把旧八条重排为 2×4 标准竹节。
- formal v1 manifest 从 36 项扩展到 80 项；Batch A+B key 全部保留。
- 卡片/弹窗预览和 35 张牌面预览已人工检查；机器校验状态为 `passed`。
- 本批未接入 Cocos；正式资源类别已经齐备，下一步进入 Batch D。

## T253：胡了卜正式字牌与背面透明底修正

- 修正东南西北、中发白和背面的浅色底边与隐藏白色 RGB。
- 8 张目标牌统一使用标准牌体 alpha bbox `(27, 23, 246, 354)`。
- alpha=0 像素同步清零 RGB，避免 Cocos 双线性采样出现白边。
- 深色棋盘预览已人工检查；formal v1 仍保持 80 个唯一 key 和 35 张统一牌面。
- validation report 新增两项门禁并通过；未修改其他 27 张数字牌或 Cocos 工程。

## T254：胡了卜正式字牌与背面底唇绿色化

- 根据深色棋盘截图继续定位到底座内部仍有不透明的白色、米色和棕色侧壁，并非透明区隐藏 RGB。
- 仅重建东南西北、中发白和背面 8 张资源，从 `y=312` 起按颜色识别残留并映射为连续绿色过渡。
- 牌背最下方米白投影和字牌底部浅色角已清除，原有绿色底座明暗保留。
- validation report 新增 `honorBackGreenLowerLip` 门禁并通过；formal v1 仍为 35 张牌、80 个唯一 key。
- 其他 27 张数字牌、Cocos 工程和临时产物未修改或提交。

## T255：胡了卜正式字牌与背面单层底座统一

- 根据整套牌面对比截图确认 T254 的绿色渐变形成了第二层底座，问题从颜色修正升级为牌体结构不一致。
- 删除额外底唇渐变，从 `y=288` 起直接复制标准空白牌体，保留上方文字、白板边框和牌背图案。
- 东南西北、中发白和背面的下半部与 `wan-01` 逐像素完全一致，统一为米白侧壁加单层绿色底座。
- validation report 用 `honorBackStandardLowerBody` 替换颜色比例门禁并通过；formal v1 仍为 35 张牌、80 个唯一 key。
- 其他 27 张数字牌、Cocos 工程和临时产物未修改或提交。

## T256：胡了卜正式字牌标准白色牌体统一

- 根据 Lee 反馈确认旧字牌整张牌体仍叠在标准下半部上，造成白色区域双层边框和贴图感。
- 7 张字牌改用完整 `standard-body-blank.png` 作为底板，只按墨色、红色和绿色 mask 提取字形或白板边框。
- 中、发、白的 mask 增加最低 alpha 阈值，清除内容矩形内残留的低透明度旧米白背景。
- 内容区之外与标准牌体逐像素一致，双层白色牌体已消除。
- validation report 新增 `honorStandardWhiteBody` 门禁并通过；formal v1 仍为 35 张牌、80 个唯一 key。
- 牌背、其他 27 张数字牌、Cocos 工程和临时产物未修改或提交。
