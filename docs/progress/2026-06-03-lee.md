# 2026-06-03 Lee 进展

- 完成 T105 胡了卜震落牌平铺和遮挡点击修复。
- 修复根因：连续开山之前只按本次震落的局部序号计算落点，导致多次 `杠 / 胡` 震落牌复用坐标；遮挡判定跳过震落牌，导致被震落牌盖住的普通牌仍可点击。
- 修复后震落牌使用全局递增平铺序号，连续开山不再互相叠起；震落牌会清理旧堆叠、桥接和 blocker 元数据，并保持自身可点击。
- 普通下层牌现在会被震落牌的视觉覆盖阻塞，恢复“被盖住就不能点”的核心规则。
- 同步 `/games/hulebu-demo/index.html` 静态发布副本，并保留站内绝对配置路径。
- 验证通过：`npm run test -w packages/shared -- mahjong-config`；`npm run test -w packages/shared -- mahjong-config-playable-prototype`；`npm run test -w apps/web -- hulebu`；HTML 内联脚本语法检查；Kimi WebBridge 行为检查；390px Chrome 截图和 DOM 检查。
- 新增 T106 AI 面试助手规划任务，完成需求变更卡、任务分片、领取分片、独立模块文档和评估设计稿。
- T106 规划结论：该功能适合作为网页工具频道下的 AI 能力工具；第一版建议做双入口 HTML 面试作战包和文本虚拟面试，实时语音、账号历史、企业筛选和 ATS 接入后置。
- T106 补充后续增强：简历优化建议，以及在面试题纲中补充专业领域简单实战例子、行业用法说明和可迁移回答素材。
- 新增 T107 AI 面试助手 MVP 作为后续待领取开发任务，便于后续确认负责人后进入实现。
- 新增 T108 统一账号中心、产品型工具入口和 AI Gateway 规划任务，完成需求变更卡、任务分片、领取分片和平台规划设计稿。
- T108 规划结论：拾光和镜界归属工具站下的独立产品型工具；账号采用多处登录入口但统一身份；AI Gateway 统一承接平台额度、用户临时 Key、外部 Gateway BYOK、自建加密 Key Vault 和本地连接器五类模型来源。
- 修复 T015 PDF 工具箱 PDF 转 Word Beta 导出格式问题。
- 修复根因：旧实现生成 HTML 后用 `.doc` 和 `application/msword` 下载，部分编辑器会直接显示 HTML 内容，不能作为真实 Word 文档包打开。
- 修复后输出真实 `.docx` OOXML ZIP 包，下载文件名改为 `dreamchasers-pdf-to-word-beta.docx`，MIME 改为 `application/vnd.openxmlformats-officedocument.wordprocessingml.document`。
- 新增 PDF 文本导出回归测试，验证生成结果以 DOCX ZIP 文件头 `PK` 开头，避免再次回退成 HTML。
- 验证通过：`npm run test -w apps/web -- pdf`；`npm run typecheck -w apps/web`；`npm run lint -w apps/web`；`npm run build -w apps/web`。
- 新增 T109 Supabase PostgreSQL 切换到自托管 PostgreSQL 记录。
- 验证 Supabase 源库可连接但 `public` 无业务表，Auth / Storage 无实际用户或文件数据；目标 PostgreSQL `47.90.180.92:5432/postgres` 可连接。
- 已对目标库执行 Prisma schema 同步，创建当前项目业务表，并将本地 `.env` 与 `apps/web/.env` 的数据库连接切到目标 PostgreSQL。
- 新增并完成 T110 账号中心邮箱验证登录和账户基础能力。
- 实现 Auth.js + Prisma adapter 邮箱验证登录，新邮箱验证成功后自动注册并进入 `/account`。
- 新增账号中心页面、登录页、检查邮箱页、错误页、安全页、权益页、AI 模型来源页和 API Key 管理页。
- 新增平台 API Key 创建/停用接口，明文只展示一次，数据库只保存 hash 和 hint。
- 扩展 Prisma schema，加入 User、Account、Session、VerificationToken、UserProfile、Product、CreditWallet、CreditLedger、PlatformApiKey、ProductSession、CredentialReference 和 AccountAuditLog 等账号中心模型。
- 验证通过：`npm exec prisma validate -w apps/web`；`npm run test -w apps/web -- account email-login`；`npm run typecheck -w apps/web`；`npm run lint -w apps/web`；`npm run build -w apps/web`；Kimi WebBridge 检查 `/login` 邮箱登录、开发链接回调到 `/account`、`/account/api-keys` 创建 API Key。
- 新增并完成 T111 账号中心生产化配置和登录安全补强。
- 补充 `.env.example` 和 `.env.production.example` 中的 `NEXTAUTH_URL`、`AUTH_SECRET`、`AUTH_EMAIL_SERVER` 和 `SMTP_*` 配置说明。
- 新增 `EmailLoginRequest` 表和邮箱登录发送冷却服务，同一邮箱 60 秒内重复请求会跳转到 `/login/error?reason=rate-limited` 并显示剩余秒数。
- 验证通过：`npm exec prisma validate -w apps/web`；`npm exec prisma db push -w apps/web`；`npm run test -w apps/web -- email-login account login-rate-limit`；`npm run typecheck -w apps/web`；`npm run lint -w apps/web`；`npm run build -w apps/web`；Kimi WebBridge 检查 `/login` 连续提交同一邮箱后出现冷却提示。
- 新增并完成 T112 产品型工具 token exchange 骨架。
- 建立默认产品定义 `timepick` / `wonderland`，新增受保护 API `/api/account/products/[productSlug]/sessions`，已登录用户可生成 10 分钟有效的一次性产品 token。
- 账号中心产品区新增生成 token 操作，token 明文只展示一次，数据库只保存 hash，并写入 `product_session_created` 审计记录。
- 验证通过：`npm run test -w apps/web -- product-session account`；`npm run typecheck -w apps/web`；`npm run lint -w apps/web`；`npm run build -w apps/web`；Kimi WebBridge 检查 `/account` 为拾光 TimePick 生成产品 token。
- 新增并完成 T113 产品型工具 token 消费接口。
- 新增公开 API `POST /api/account/products/[productSlug]/sessions/consume`，产品后端可用一次性 token 换取最小用户身份声明，不依赖浏览器登录 cookie。
- 消费规则校验 token hash、产品归属、过期时间和消费状态；成功后写入 `consumedAt`，二次消费统一返回 400，避免 token 复用。
- 验证通过：`npm run test -w apps/web -- product-session account`；`npm run typecheck -w apps/web`；`npm run lint -w apps/web`；`npm run build -w apps/web`；手动生成拾光 TimePick token 后首次 consume 成功、二次 consume 返回 400；`git diff --check`。
- 新增 T114 TimePick 同账号 PostgreSQL 迁移基座任务。
- 已确认 TimePick 本地路径为 `/Users/lee/Desktop/Lee/TimePick/`，现有项目是 Vite React + Supabase Auth/RLS；Lee 决定不做 Supabase Auth 桥接，改为直接迁移到 DreamChasers PostgreSQL，并使用同一套 DreamChasers 账号作为 TimePick 数据 owner。
- 完成 T114 迁移基座：新增 `TimePick*` Prisma 模型，私有表 owner 均关联 DreamChasers `User.id`，新增默认 section 规则和同账号访问规则测试。
- 新增 `GET /api/timepick/bootstrap`，登录用户可初始化 TimePick profile 并确保 `网页 / 文档 / 图片 / 视频` 四个默认 section 存在；已通过 `prisma db push` 同步到目标 PostgreSQL。
- 验证通过：`npm exec prisma validate -w apps/web`；`npm exec prisma db push -w apps/web`；`npm run test -w apps/web -- timepick account`；`npm run typecheck -w apps/web`；`npm run lint -w apps/web`；`npm run build -w apps/web`。
- 新增 T115 TimePick 前端同账号登录壳任务，目标是先把 TimePick 登录入口切到 DreamChasers 账号中心，并通过 `/api/timepick/bootstrap` 获取平台用户。
- 完成 T115 TimePick 前端同账号登录壳：新增 DreamChasers auth client，TimePick `AuthContext` 改为读取 DreamChasers bootstrap，登录弹窗、登录页、注册页改为统一账号中心入口。
- T115 验证通过：TimePick 触碰文件定向 ESLint 退出码 0（`AuthContext` 仍有既有 Fast Refresh warning）；`npm run build` 通过。TimePick 全量 lint 仍因多个既有未触碰文件解析错误失败，未在本任务内修复。
- 新增 T116 TimePick 文件夹和资源列表 API 切换任务，优先替换首页文件夹树和资源列表的数据读取。
- 完成 T116 TimePick 文件夹和资源列表 API 切换。
- DreamChasers 新增 TimePick folders/resources API，登录用户只能读取和操作自己的文件夹与资源；资源移动会校验资源 owner 和目标文件夹 owner。
- DreamChasers TimePick API 补充 CORS/OPTIONS 支持，允许 TimePick 本地开发源携带 cookie 调用平台 API；后续线上域名可通过 `TIMEPICK_ALLOWED_ORIGINS` 配置。
- TimePick 新增 DreamChasers TimePick API client，`FolderTree` 的文件夹读取、删除文件夹和拖拽移动资源已不再直接调用 Supabase。
- `ResourceList` 的资源列表、子文件夹和面包屑读取已切到 DreamChasers API；标签过滤仍保留在前端应用层。
- T116 保持范围边界：新增/编辑资源、上传、灵感、待办、抽签、标签管理、AI 识别和历史数据导入仍留后续任务。
- T116 真实浏览器联调：使用 Kimi WebBridge 在 TimePick `http://localhost:8080` 登录 DreamChasers 测试账号后进入 `/home`；网络请求显示 `GET /api/timepick/bootstrap`、`GET /api/timepick/folders`、`GET /api/timepick/resources?displayMode=folder-and-resource&selectedType=all` 均为 200，相关 OPTIONS preflight 为 204。
- T116 验证通过：`npm run test -w apps/web -- timepick account`；`npm run typecheck -w apps/web`；`npm run build -w apps/web`；TimePick 定向 ESLint；TimePick `npm run build`；Kimi WebBridge 真实浏览器联调。
