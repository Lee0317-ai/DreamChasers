# 2026-06-15 Lee 进展

## T171：胡了卜每日牌局第一版

- 状态：待验收
- 完成内容：
  - 已新增任务分片 `docs/tasks/items/T171-hulebu-daily-match.md`。
  - 已新增领取分片 `docs/tasks/claims/T171-lee.md`。
  - 已记录 `IDEA-20260615-01`，把胡了卜每日牌局第一版纳入任务池。
  - `/games/hulebu` 局外 `每日` 面板已开放 `开始每日`。
  - 外层 iframe URL 已支持 `mode=daily&dailySeed=YYYY-MM-DD`，并继续传入三项局外升级 bonus。
  - 外层本地存档新增 `dailyBestLevels`，会按日期记录当日本地最好关数。
  - HTML 原型已新增 `runMode: "daily"`、`isDailyMode()`、`getDailyLevelIndex()` 和 `getDailyDifficultyProfile()`。
  - 每日牌局生成 seed 已绑定到 `dailySeed`，同一天重开仍保持同一桌骨架。
  - 站内静态 Demo 已同步，并保留站内绝对配置路径。
  - 已把失败态收回为纯失败弹框：内层原型失败时只显示通用失败提示和重开按钮，外层 `/games/hulebu` 失败结算不再展开高阶 / Boss / 事件复盘卡片。
- 验证：
  - `npm run test -w packages/shared -- mahjong-config-playable-prototype`：通过。
  - `npm run test -w apps/web -- hulebu`：通过。
  - `npm run typecheck -w apps/web`：通过。
  - `npm run build -w apps/web`：通过。
  - HTML 内联脚本语法检查：通过，源原型和站内静态副本均可解析。
  - `npm run docs:sync`：通过。
  - 占位符扫描：通过，目标文档未发现占位残留。
  - `git diff --check`：通过。
  - 浏览器验收：通过。桌面端进入 `/games/hulebu` 后可切到 `每日` 面板并启动今日牌局，iframe URL 含 `mode=daily&dailySeed=2026-06-15`；390x844 下 `scrollWidth=390`，无横向溢出。
- 遗留：
  - 本任务不做成就、高阶周目、云存档、排行榜、付费、广告或 Cocos 正式工程追平。

## T172：胡了卜成就图鉴第一版

- 状态：待验收
- 完成内容：
  - 已新增任务分片 `docs/tasks/items/T172-hulebu-achievement-codex.md`。
  - 已新增领取分片 `docs/tasks/claims/T172-lee.md`。
  - 已记录 `IDEA-20260615-02`，把胡了卜成就图鉴第一版纳入任务池。
  - `/games/hulebu` 局外 `图鉴` 面板已从占位改为成就图鉴第一版。
  - 外层本地存档新增 `achievements`，会记录首批成就的首次解锁时间。
  - 图鉴第一版已承接主线、无尽、每日和局外升级四类已有进度。
  - 旧存档 hydration 时会按 `bestEndlessLayer`、`dailyBestLevels`、`upgrades` 和历史结算补算可解锁成就。
- 验证：
  - `npm run test -w apps/web -- hulebu`：通过。
  - `npm run typecheck -w apps/web`：通过。
  - `npm run build -w apps/web`：通过。
  - `npm run docs:sync`：通过。
  - 占位符扫描：通过，目标文档未发现占位残留。
  - `git diff --check`：通过。
  - 浏览器验收：通过。桌面端和 390x844 下都可进入 `/games/hulebu` 的 `图鉴` 面板，能看到 `0/8` 总览、下一目标和成就卡；移动端 `scrollWidth=390`，无横向溢出。
- 遗留：
  - 本任务不做完整事件词条库、Boss 详情页、奖励路线收藏页、云同步、排行榜、付费、广告或 Cocos 正式工程追平。

## T173：胡了卜高阶周目第一版

- 状态：待验收
- 完成内容：
  - 已新增任务分片 `docs/tasks/items/T173-hulebu-ascension-first-pass.md`。
  - 已新增领取分片 `docs/tasks/claims/T173-lee.md`。
  - 已记录 `IDEA-20260615-03`，把胡了卜高阶周目第一版纳入任务池。
  - `/games/hulebu` 局外 `高阶` 面板已开放 `东风场 / 南风场` 两档周目入口，并显示当前解锁、下一档提示和进行中摘要。
  - 外层本地存档新增 `bestAscensionLevel`，会保存已解锁最高周目，并在高阶 run 通关后推进下一档。
  - HTML 原型与站内静态 Demo 已新增 `ascensionLevel / ascensionName` 解析、周目 HUD、结算摘要、重开承接和第一版固定压力口径。
  - `东风场` 会提高牌山压力并带周期性高压词缀；`南风场` 进一步减少起始工具并禁用洗牌。
- 验证：
  - `npm run test -w packages/shared -- mahjong-config-playable-prototype`：通过。
  - `npm run test -w apps/web -- hulebu`：通过。
  - `npm run typecheck -w apps/web`：通过。
  - `npm run build -w apps/web`：通过。
  - HTML 内联脚本语法检查：通过，源原型和站内静态副本均可解析。
  - `npm run docs:sync`：通过。
  - 占位符扫描：通过，目标文档未发现占位残留。
  - `git diff --check`：通过。
  - 浏览器验收：通过。桌面端和 390x844 下都可切到 `/games/hulebu` 的 `高阶` 面板并启动高阶 run，iframe URL 带 `mode=ascension&ascensionLevel=...&ascensionName=...`；移动端 `scrollWidth=390`，无横向溢出。
- 遗留：
  - 本任务不做完整高阶事件扩容、完整 Boss 试炼第二版、云同步、排行榜、付费、广告或 Cocos 正式工程追平。

## T174：胡了卜账号进度续层

- 状态：待验收
- 完成内容：
  - 已新增任务分片 `docs/tasks/items/T174-hulebu-account-progress-sync.md`。
  - 已新增领取分片 `docs/tasks/claims/T174-lee.md`。
  - 已记录 `IDEA-20260615-04`，把胡了卜账号进度续层纳入任务池。
  - `apps/web/prisma/schema.prisma` 已新增 `HulebuProgress` 数据模型，用于保存登录账号的局外长期进度。
  - `/api/games/hulebu/progress` 已开放登录态读取与合并写回，当前承接局外铜钱、无尽最高层、高阶解锁、每日最佳和成就。
  - `/games/hulebu` 壳层已在 hydration 后尝试读取账号进度，并把本地旧存档与账号记录做首次合并；未登录时继续保留本地兜底。
  - 局外升级、结算和长期进度变化现在会在本地持久化的同时回写账号记录，避免重新打开页面后又从 0 开始。
  - `hulebu-publish` 测试已补到账户进度 API 与同步失败文案的发布保护。
- 验证：
  - `npm run test -w apps/web -- hulebu`：通过。
  - `npm run typecheck -w apps/web`：通过。
  - `npm run build -w apps/web`：通过。
  - `npm run docs:sync`：通过。
  - `git diff --check`：通过。
  - 浏览器验收：通过。当前登录态刷新 `/games/hulebu` 后会回到局外壳层，长期进度接口与页面壳层已可正常联通；本轮未再出现构建后页面报错。
- 遗留：
  - 本任务不做跨账号冲突解决、排行榜、多人同步、跨游戏成就中心或 Cocos 正式工程追平。
