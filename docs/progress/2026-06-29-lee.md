# 2026-06-29 Lee 进展

## T166：AI 修图剩余 AI 功能补全

- 已新增通用单图 AI 编辑任务 store，覆盖 `repair`、`enhance`、`prompt-edit`。
- 已新增 `/api/tools/photo/repair`、`/api/tools/photo/enhance`、`/api/tools/photo/prompt-edit` 及任务查询/结果读取路由。
- 已开放页面剩余 AI 工具：`AI 细节修复`、`高清增强` 和右侧 `AI 对话修图`。
- 三类能力均复用当前画布图片，走平台 AI Gateway `image_edit`，任务完成后替换当前画布。
- 验证结果：
  - `npm run test -w apps/web -- photo-edit repair enhance prompt-edit openai-compatible` 通过。
  - `npm run lint -w apps/web` 通过，但有既有 generated Prisma warning。
  - `git diff --check` 通过。
  - 浏览器冒烟通过：AI 工具入口不再 disabled，页面无 `未开放/暂未开放` 文案。
  - `npm run typecheck -w apps/web` 仍受既有依赖缺失和认证类型问题阻塞。

## T165：AI 修图批量品牌填充和 AI 溶图实现

- 已同步到最新 `main`，当前 `main` 与 `origin/main` 一致。
- 已读取项目入口文档、AI 修图模块文档和 `NEXT_ID.md`。
- 已使用 `T165` 登记新增需求，并把 `NEXT_ID.md` 递增到 `166`。
- 已新增任务分片 `docs/tasks/items/T165-ai-photo-batch-branding-and-scene-blend.md` 和领取分片 `docs/tasks/claims/T165-lee.md`。
- 已在 AI 修图模块文档中记录两个新增方向：
  - 批量品牌填充：左上角 10 个以内短字、可选前置 logo、右下角 LOGO、批量上传、批量预览和单张微调。
  - AI 溶图/场景融合：产品图与雪山等背景图自然融合，重点改善边缘、接触阴影、环境光、反光、色温和透视一致性。
- 已将 T165 从规划任务升级为实现任务，允许修改 AI 修图前端、图片工具库和 `/api/tools/photo/**`。
- 已完成 T165 第一版实现：
  - 批量品牌填充支持最多 12 张图片、左上角 10 字以内短字、前置 Logo、右下角 Logo、批量预览、单张载入画布和 Canvas 批量导出。
  - AI 溶图支持产品图、背景图和场景描述，走平台 AI Gateway 任务化接口，轮询后获取结果并替换当前画布。
  - OpenAI-compatible provider 已支持产品图和背景图双图 image edit 表单提交。
- 已修复 961-1280px 宽度下参数面板被隐藏的问题，桌面 1280px 下批量品牌和 AI 溶图面板可正常显示。
- 已调整批量品牌单张预览：点击预览会先生成品牌填充结果，再载入主画布继续单独修改。
- 验证结果：
  - `npm run test -w apps/web -- photo ai-gateway openai-compatible-provider` 通过，4 个测试文件、11 个用例通过。
  - `npm run lint -w apps/web` 通过，但有 generated Prisma unused eslint-disable warning。
  - 浏览器冒烟：桌面 1280x720 和移动 390x844 均可展开批量品牌与 AI 溶图控件。
  - `git diff --check` 通过。
  - `npm run typecheck -w apps/web` 受既有依赖/类型问题阻塞。
  - `npm run build -w apps/web` 受 Prisma/zeptomatch ESM 兼容问题阻塞。
## 胡了卜

- 创建并完成 T205：Cocos 正式工程已接入高阶周目入口基础。
- Cocos 配置层新增 `advanced` run mode、`HulebuAdvancedRunTier` 和东风场、南风场、西风场、北风场四档 profile。
- Cocos 大厅新增 `高阶` 入口，点击后可选择四档风场；选择后进入本局流派选择，再启动对应高阶 run。
- 当前高阶先复用主线后半段关卡压力，后续再接高阶词缀、能力槽压缩、账号解锁和专属奖励。
- 创建并完成 T206：Cocos 正式工程已接入局外铜钱和升级成本闭环。
- Cocos Controller 新增本地局外铜钱；通关回大厅前会发放铜钱，局外成长面板会显示铜钱、等级和价格。
- 六轴成长升级现在会校验价格、扣除铜钱并受上限约束，满级后显示满级。
- 创建并完成 T207：Cocos 正式工程已接入高阶风场压力基础。
- 高阶东/南/西/北四档现在会在每关开局自动合并压力修饰器；南风及以上会体现禁洗牌、禁看山或扣减撤回/打牌次数。
- 创建并完成 T208：Cocos 正式工程已接入高阶专属奖励基础。
- 高阶奖励节点现在会优先给出当前风场专属奖励，并把奖励效果带入 runtime 的工具、护符、备用槽、铜钱或组合分数。
- 创建并完成 T209：Cocos 正式工程已接入高阶能力槽基础。
- 高阶 run 现在会在风场选择后先进行能力 3 选 1，再进入本局流派选择；能力效果会通过奖励状态和本关修饰器进入 runtime。
- 创建并完成 T210：Cocos 正式工程已接入高阶事件池基础。
- 高阶事件节点现在会优先展示当前风场专属事件，并把事件效果带入本关 runtime。
- 创建并完成 T211：Cocos 正式工程已接入事件元信息基础。
- 普通事件和高阶事件现在都带 `rarity / tags / dangerLevel`，关前事件弹层会展示稀有度、风险和标签。
- 创建并完成 T212：Cocos 正式工程已接入无尽和每日事件变体。
- 无尽 run 会优先展示长线补给和尾盘容错事件；每日 run 会优先展示今日手气和今日变招事件，再用普通事件补足 3 个选项。
- 创建并完成 T213：Cocos 正式工程已接入本局流派事件偏置。
- 事件节点现在会优先展示当前本局流派事件，再展示高阶/无尽/每日等模式事件，最后用普通事件补足 3 个选项。
- 创建并完成 T214：Cocos 正式工程已接入 Boss 变体基础。
- 主线第 10/20 关会区分中段试炼和胡了卜王；高阶、无尽、每日 run 会按 profile 获得对应 Boss 变体，HUD 会显示变体名称，并可合入轻量 Boss 目标补丁。
- 创建并完成 T215：Cocos 正式工程已接入当前本轮继续基础。
- 大厅在存在本地 active run 快照时会显示“继续本轮”；恢复会带回 run mode、当前关序、本局流派、本轮奖励、局外成长、局外铜钱和高阶能力，并从当前关开局继续。
- 创建并完成 T216：Cocos 正式工程已接入最近一轮结算摘要基础。
- run 通关回大厅时会把上一轮模式、关序、铜钱奖励和奖励数写入本地结算摘要；大厅在无 active run 时会优先显示最近一轮状态。
- 创建并完成 T217：Cocos 正式工程已接入本地长期进度基础。
- Cocos Controller 新增 `metaProgress` 本地快照；无尽 run 会记录最高层，每日 run 会按 seed 记录今日最佳，高阶 run 会记录最高已完成风场。
- 大厅 `主线 / 无尽 / 每日 / 高阶` 入口现会展示长期进度副文案，方便直接读到局外累计状态。
- 创建并完成 T218：Cocos 正式工程已接入局外生涯总览基础。
- 大厅新增 `生涯` 入口；Cocos `collection` 弹层可集中查看当前本轮、最近一轮、局外铜钱、六轴成长等级，以及主线、无尽、每日和高阶本地累计状态。
- 创建并完成 T219：Cocos 正式工程已接入成就图鉴最小版基础。
- Cocos Controller 新增本地成就快照和首批 8 项成就；`生涯` 面板现可查看图鉴总览、下一项目标和首批图鉴摘要。
- 创建并完成 T220：Cocos 正式工程已接入主线独立长期进度基础。
- `metaProgress` 新增 `bestMainlineLevel`；大厅主线按钮和生涯面板现在会优先展示主线最高已到关序。
- 创建并完成 T221：Cocos 正式工程已接入局外档案本地持久化基础。
- `GameSceneController` 新增独立 `meta profile` 本地快照；局外铜钱和六轴成长会在大厅启动、回大厅、局外升级和通关发钱后稳定读写。
- 创建并完成 T222：Cocos 正式工程已接入当前关中局恢复基础。
- `HulebuRuntimeState` 新增运行态快照导出/恢复接口；`activeRun` 现在会携带当前关 runtime 快照，继续本轮会优先恢复中局牌桌、卡槽、牌河、明牌区、震落牌、分数和工具次数。
- 创建并完成 T223：Cocos 正式工程已接入关间 phase 恢复基础。
- `activeRun` 新增 `resumablePhase`；继续本轮现在也可恢复 `cleared / reward / event` 三类关间节点，而不是只恢复到 `playing`。
- 创建并完成 T224：Cocos 正式工程已接入开局前 flow 恢复基础。
- `activeRun` 新增 `pendingRunProfile` 并把 `resumablePhase` 扩展到 `advancedAbility / archetype`；继续本轮现在可恢复高阶能力选择和本局流派选择节点。
- 创建并完成 T225：Cocos 正式工程已接入每日牌局第二版信号基础。
- Cocos 配置层新增每日词缀 helper 和 8 档每日主题；每日事件与奖励会按当天 seed 稳定偏置，`metaProgress` 新增 `dailyStreak / lastDailySeed`，大厅和生涯可展示今日词缀、今日奖励、今日最佳与连续参与。
- 创建并完成 T226：Cocos 正式工程已接入账号进度桥接基础。
- `GameSceneController` 现可在浏览器环境下尝试 GET/POST `/api/games/hulebu/progress`，并桥接局外铜钱、六轴成长、无尽最高层、每日最佳/连续参与、成就和当前本轮快照。
- 大厅启动和回到局外时会尝试合并账号档；本地状态变化时会做轻量推送；未登录或接口失败时会回退本地档，不阻断继续本轮或局外成长。
- 创建 T227 胡了卜 Cocos 明牌区与槽位真实牌面补齐。
- `MeldRiverLayerBinder` 现在会优先把明牌区和牌河渲染成真实麻将牌面，不再只显示文字 chip；`碰 / 杠 / 补杠` 会展示 3-4 张同牌，牌河会展示单张弃牌。
- `SlotLayerBinder` 现在会优先把主槽和备用槽渲染成真实麻将牌面，同时保留现有槽位背板和文字 fallback。
- T227 已收尾验证完成：顺手修掉了 `GameSceneController` 的高阶风场 pressure 调用类型问题，当前 Cocos TypeScript 编译、共享静态测试和空白检查已通过。
- 创建并完成 T228：Cocos 正式工程已接入候选组合选择弹层基础。
- `HulebuRuntimeState` 现在可按组合类型返回全部候选；`GameSceneController` 在同类候选超过 1 组时会先弹出选择面板，候选项优先显示真实小牌面并保留文字 fallback，点选后只执行用户选中的候选。
- 创建并完成 T229：修复 Cocos Web Preview 因跨工作区导入 `packages/shared/src/mahjong-mountain-generator` 导致的启动报错。
- Cocos 工程内新增 `config/HulebuMountainGenerator.ts`，`HulebuLevelConfig.ts` 改为导入本地模块；共享测试新增扫描，防止 Cocos 脚本再次直接 import `packages/shared/src/`。
- 创建并完成 T230：修正 Cocos 移动端/高 DPR 设备下整体画面过大的问题。
- runtime 布局缩放不再被 `devicePixelRatio` 额外放大，小屏视口现在允许收缩牌山、HUD 和操作按钮；共享测试已新增缩放口径静态约束。
- 创建并完成 T231：按 `game-studio` 路由继续补齐 Cocos UI 资源接入。
- Cocos `ui/v6` 资源树已补齐奖励卡、场景皮肤卡、弹层底板和面板资源；顶部牌匾、奖励选择卡和流程弹层会优先加载真实 Sprite，顶部 HUD 不再额外叠一排小 badge。
- 创建并完成 T232：继续对齐目标概念图，给 Cocos 正式工程补入茶室桌面整屏背景。
- 新增 1024x1536 背景资源，中心留白承载牌山和 UI，边缘提供灯笼、茶具、木桌、绿植和玉坠氛围；`GameSceneController` 会优先加载 `SceneBackgroundArt`，失败时保留原程序化绿桌底板兜底。
- 创建并完成 T233：继续把 Cocos 奖励三选一往目标概念图的大卡呈现靠。
- 奖励弹层从小按钮弹窗扩大为更适合三张奖励卡的面板，单张奖励卡尺寸提升到 `106x120`，继续复用 v6 静态奖励卡 Sprite 和文字 fallback。
- 创建并完成 T234：Cocos 右侧工具按钮角标改为随 runtime HUD 动态同步。
- `GameSceneController` 现在会从 `hud.toolText` 解析 `洗 / 撤 / 打` 剩余次数，并写回 `洗牌 / 撤回 / 打牌` 三个工具按钮角标，不再固定显示 `3`。

## 验证

- 通过：`npm run test -w packages/shared -- mahjong-cocos-project`
- 通过：`npx tsc --noEmit -p apps/game/mahjong-roguelike/cocos/hulebu-cocos-3.8.8/tsconfig.json`
