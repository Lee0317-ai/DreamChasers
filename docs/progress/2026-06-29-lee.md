# 2026-06-29 Lee 进展

## T166：AI 修图剩余 AI 功能补全

- 已新增通用单图 AI 编辑任务 store，覆盖 `repair`、`enhance`、`prompt-edit`。
- 已新增 `/api/tools/photo/repair`、`/api/tools/photo/enhance`、`/api/tools/photo/prompt-edit` 及任务查询/结果读取路由。
- 已开放页面剩余 AI 工具：`AI 细节修复`、`高清增强` 和右侧 `AI 对话修图`。
- 三类能力均复用当前画布图片，走平台 AI Gateway `image_edit`，任务完成后替换当前画布。
- 验证结果：
  - `npm run test -w apps/web -- photo-edit repair enhance prompt-edit openai-compatible` 通过。
  - `npm run lint -w apps/web` 通过，但有既有 generated Prisma warning。
  - `git diff --check` 通过。
  - 浏览器冒烟通过：AI 工具入口不再 disabled，页面无 `未开放/暂未开放` 文案。
  - `npm run typecheck -w apps/web` 仍受既有依赖缺失和认证类型问题阻塞。

## T165：AI 修图批量品牌填充和 AI 溶图实现

- 已同步到最新 `main`，当前 `main` 与 `origin/main` 一致。
- 已读取项目入口文档、AI 修图模块文档和 `NEXT_ID.md`。
- 已使用 `T165` 登记新增需求，并把 `NEXT_ID.md` 递增到 `166`。
- 已新增任务分片 `docs/tasks/items/T165-ai-photo-batch-branding-and-scene-blend.md` 和领取分片 `docs/tasks/claims/T165-lee.md`。
- 已在 AI 修图模块文档中记录两个新增方向：
  - 批量品牌填充：左上角 10 个以内短字、可选前置 logo、右下角 LOGO、批量上传、批量预览和单张微调。
  - AI 溶图/场景融合：产品图与雪山等背景图自然融合，重点改善边缘、接触阴影、环境光、反光、色温和透视一致性。
- 已将 T165 从规划任务升级为实现任务，允许修改 AI 修图前端、图片工具库和 `/api/tools/photo/**`。
- 已完成 T165 第一版实现：
  - 批量品牌填充支持最多 12 张图片、左上角 10 字以内短字、前置 Logo、右下角 Logo、批量预览、单张载入画布和 Canvas 批量导出。
  - AI 溶图支持产品图、背景图和场景描述，走平台 AI Gateway 任务化接口，轮询后获取结果并替换当前画布。
  - OpenAI-compatible provider 已支持产品图和背景图双图 image edit 表单提交。
- 已修复 961-1280px 宽度下参数面板被隐藏的问题，桌面 1280px 下批量品牌和 AI 溶图面板可正常显示。
- 已调整批量品牌单张预览：点击预览会先生成品牌填充结果，再载入主画布继续单独修改。
- 验证结果：
  - `npm run test -w apps/web -- photo ai-gateway openai-compatible-provider` 通过，4 个测试文件、11 个用例通过。
  - `npm run lint -w apps/web` 通过，但有 generated Prisma unused eslint-disable warning。
  - 浏览器冒烟：桌面 1280x720 和移动 390x844 均可展开批量品牌与 AI 溶图控件。
  - `git diff --check` 通过。
  - `npm run typecheck -w apps/web` 受既有依赖/类型问题阻塞。
  - `npm run build -w apps/web` 受 Prisma/zeptomatch ESM 兼容问题阻塞。
