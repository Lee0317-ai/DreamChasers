# 2026-06-22 Lee 进展

## T181 胡了卜 HUD 和按钮透明切图包

- 按用户要求调用 `game-studio:game-studio`，并路由到游戏 UI 方向处理：本轮不再只修裁切，而是按参考样式重新做一套 UI kit。
- 已新增 v4 重设计包：`output/hulebu-ui-assets/hulebu-ui-component-pack-v4-redesigned-style/`。
- v4 使用青绿金边、米色纸底、深木槽、橙色火焰态等参考图材质语言，重绘 HUD、右侧工具按钮、底部组合按钮、弃牌/手牌槽、Buff 抽屉、奖励卡、场景皮肤卡和组合选择弹层。
- 场景皮肤卡已从“半截裁图”改为完整重绘卡片；圆形场景预览只借用参考图缩略图，卡片框体和状态标签重新绘制为透明 PNG。
- 组合选择弹层继续使用 T051 v7 麻将牌面小图替代文字牌名。
- v4 已通过资源包校验，当前等待用户查看 `preview/contact-sheet.png`。
- 用户反馈 v4 不如原图质感；判断 v4 的问题是程序化重绘丢失了原图纹理、厚金边和手绘光影。
- 已新增 v5 原图质感忠实修正版：`output/hulebu-ui-assets/hulebu-ui-component-pack-v5-source-faithful/`。v5 保留 v3 的原图裁切修正，并用正确坐标补回完整 `tool_counter` 和四张场景皮肤卡。
- v5 已通过资源包校验，当前推荐查看 `output/hulebu-ui-assets/hulebu-ui-component-pack-v5-source-faithful/preview/contact-sheet.png`。
- 根据用户补充“麻将牌面也要透明底”，已新增 v6 增量包：`output/hulebu-ui-assets/hulebu-ui-component-pack-v6-source-faithful-transparent-tiles/`。
- v6 保留 v5 原图质感 UI 组件，并新增 `tiles/mahjong/` 透明牌面目录，包含 34 张麻将正面和 1 张背面；新增 `preview/mahjong-tile-contact-sheet.png` 供直接目检。
- 已更新校验脚本，v6 会同时检查 UI 组件和 `tileComponents` 的透明边缘；抽样确认单张牌边缘 100% 透明。

## T182 胡了卜国风人物操作演出概念稿

- 根据用户确认的“国风克制、像精致棋牌游戏”方向，新增 T182 做人物 cut-in 和大操作贴图概念稿。
- 当前环境未设置 `PPTOKEN_API_KEY`，暂不直接调用图像生成 API；第一版先用现有透明 UI / 牌面资源和程序化国风剪影输出可视化概念板。
- 已生成 `output/hulebu-ui-assets/hulebu-action-fx-character-concept-v1/preview/action-fx-character-concept-board.png` 和 `preview/transparent-sticker-sheet.png`。
- 输出包含四风场人物 cut-in 占位和 `杠 / 补杠 / 胡了` 透明操作字牌，用于先判断人物出现位置、强弱层级和是否挡牌。
- 尝试使用用户提供的临时 `PPTOKEN_API_KEY` 生成真实 `东风场掌局人` 透明立绘；默认端点返回 `HTTP 401 INVALID_API_KEY`，切到用户补充的 PPTOKEN `.cc` 端点后透明底参数不被模型支持，普通背景生成超时断开；未生成真实人物图，未把 key 写入文件。
- 根据用户提供的 APIMART 示例新增 `output/hulebu-ui-assets/scripts/generate_apimart_image.py`，用于从环境变量读取 token、提交任务、轮询并下载 PNG；当前本机到 `api.apimart.ai:443` 连接超时，后续网络恢复后可直接复用该脚本补真实人物立绘。
- 按用户追问检查系统 `$imagegen` 技能：当前会话没有内置 `image_gen` 工具，fallback CLI 需要 `OPENAI_API_KEY` 但环境未设置，因此本轮不能直接通过系统 `$imagegen` 出图。

## T183 胡了卜 UI 资产 Cocos 接入说明文档

- 根据用户要求“把 UI 这些记录成之后 Cocos 接入可用的文档”，新增 T183 文档任务。
- 已创建 `docs/modules/mahjong-roguelike/UI_ASSETS_COCOS_INTEGRATION.md`，记录 T181 v6 透明 UI / 牌面资源包和 T182 v1 操作演出概念包的路径、预览图、Cocos `resources` 目录建议、SpriteFrame key 建议、牌面逻辑 key 映射、导入设置、接入顺序和验收清单。
- 已在模块 `README.md`、`PROGRESS.md`、`HANDOFF.md` 挂入口；文档明确 T182 人物 cut-in 仍是占位，不应直接进入正式发布包，T181 v6 也暂缺正式 `胡` 按钮。

## 胡了卜失败弹框收口

- 根据用户最新反馈，`/games/hulebu` 的失败反馈继续压回最短路径：失败时只保留 iframe 内的 `失败 / 重开` 弹框。
- 补充 `apps/web/src/modules/games/hulebu/__tests__/hulebu-publish.test.ts` 断言，锁定失败态继续保持最小文案，避免后续又把说明性内容塞回去。
- 内层原型和站内静态 Demo 的按钮文案已从 `重开本关` 收成 `重开`，外层壳在失败后也不再显示“继续本轮 / 进行中”。
- 外层壳的失败消息处理也继续保持短路：`hulebu:run-failed` 不写失败结算，不写失败说明，失败就留在牌桌里处理；桌面 1440 宽和移动 390 宽检查都确认未出现额外失败面板。
- 外层壳现在进一步只认“完成结算”：旧失败存档会在读取时直接丢弃，壳层 `SettlementState` 也不再保留失败分支，后续更难把失败复盘页误接回来。
- 这轮又补了一刀旧存档兜底：如果浏览器 localStorage 里还残留历史 `failed` settlement，`/games/hulebu` 外层现在也不会在 hydration 时把它强行当成完成结算恢复出来，刷新后仍只保留牌桌内的简短失败弹框。
- 继续补了旧调试链接兜底：如果浏览器还停在更早版本留下的 `?debugSettlement=...failure...` 地址，外层壳加载时会直接清掉这个参数并回到正常局外态，不再把旧失败样例页当成现在线上页面。
- 这轮又继续把 Boss 结算 payload 本身收成最小集：原型、站内静态 Demo 和外层类型现在只保留 `bossVariant` 这种成就确实会用到的标记，不再顺带带着 `phaseTarget / keyGoal / keyMiss / nextAdvice / mismatch` 这类解释性字段到壳层。
- 最新补丁把 iframe 内失败层也从复用 `reward-panel` 的大结算壳改成独立 `failure-panel` 小弹框，视觉上只剩 `失败` 标题和 `重开` 按钮，不再像奖励结算页。
- 这轮又补了一刀：失败弹框切换时会同步清空 `runComplete` 结算区，避免旧奖励或通关文案残留在失败层背后。
- 现在连失败文案钩子也一起收掉了：原型和站内静态 Demo 不再单独挂 `GENERIC_FAILURE_MESSAGE` / `getBossGoalMissingText`，共享原型测试和站内发布测试都改成只认最短失败口径。
- 这轮继续把外层首页的“解释感”也削掉：`/games/hulebu` 的主线 / 无尽 / 每日页签不再展开大块导向卡、路线建议、节奏提醒和“最后一刀”式长文案，只保留开局按钮、核心状态和简短预览，页面回到更像游戏入口而不是说明页。

## T184 胡了卜路线奖励和长期成长深化

- 把原先只写在 T175 路线里的“路线奖励和局外能力深化”正式落成 T184，避免代码、任务和验收口径继续错位。
- 当前长期成长已正式按六轴口径收口：`备用槽 / 满槽护符 / 初始道具 / 河道扩容 / 开局铜钱 / 看山预置`。
- `/games/hulebu` 升级页新增路线 `当前收益 / 下一步` 两张状态卡，同时在右侧预览里补了 `路线当前收益 / 路线下一步`，让玩家能直接看懂当前挂载到底给了什么、下一手该补哪一轴。
- 本轮继续保持“失败只弹框”的口径，不把路线成长说明混进失败反馈。

## T185 胡了卜 Web 数值平衡和内容冻结第一轮

- 已把特殊事件第一轮冻结的第一刀落进 Web 原型和站内静态 Demo：主线触发从 `6 / 8 / 10 / 13 / 16 / 19` 扩成 `6 / 8 / 10 / 12 / 14 / 16 / 18 / 20`，后半程不再只剩三次固定露面。
- 每日牌局触发同步扩成 `4 / 7 / 10 / 12 / 14 / 16 / 18 / 20`，让中后段也会持续看到事件，而不是只在前半段定调、尾段收一下。
- 高阶周目触发同步扩成 `5 / 8 / 11 / 13 / 15 / 17 / 19 / 20` 对应的关前节点，终章前后都还能继续接事件变化。
- 主线后半段新增分段事件偏置：`11` 关后偏信息和护路、`13` 关后偏顺手追胡和风险抬注、`15` 关后偏挡墙续压、`17` 关后偏牌尾收束、`19` 关后偏北风封尾和试炼探口。
- 无尽长跑新增 `chapterIndex >= 5 && chapterStep === 3` 的额外事件点，后段章节从每章 3 次事件抬到 4 次，长 run 不会在后半程突然变空。
- 共享原型测试已新增后半程触发和主线后段偏置断言；原型与站内静态副本 inline script `node --check` 均通过，`npm run test -w apps/web -- hulebu` 继续通过。
- 继续收主线终章门前两档硬压：第 `18 / 19` 关牌量从 `340 / 348` 收到 `336 / 344`，字牌权重从 `70 / 76` 收到 `68 / 72`，孤张预算从 `9 / 10` 收到 `8 / 9`，让第 18 关和 `胡了卜王` 之间还保留层差，但不再像连着两关一起压满。
- 继续收每日终局的词缀附加题：第 20 关只再挂 `1` 个今日词缀重点组合，不再默认叠 `2` 个额外目标；当天主题还在，但不会把终局写成双考卷。
- 继续收无尽后段 Boss 的机械补题：第 `7 / 8` 相位在章节主题已经自带双目标时，不再额外挂通用 `碰 1`；章节守层记分线也从 `168 + 章节 * 18` 再收成 `166 + 章节 * 17`。
- 这轮继续把后半程奖励从“拖命”往“落成 build”推：无尽第 4 章主题奖励从 `胡分 / 胡钱 / 备用槽` 收成 `胡分 / 胡钱 / 工具包`，避免牌尾收束章节还在发纯容错位子。
- 路线型后段奖励也继续去防守件：无尽和每日的 `河控流` 晚段奖励从 `杠分 / 吃道具 / 护符` 收成 `杠分 / 吃道具 / 胡钱`，主线 `救场流` 晚段奖励也从 `备用槽 / 护符 / 吃道具` 收成 `备用槽 / 护符 / 丢弃`，让后段救场更像“补最后一刀”而不是只加厚血条。
- 继续把基础路线池本身也拉回后段口味：主线固定第 `12 / 15` 档奖励从 `护符 / 备用槽` 口味改成 `胡钱 / 工具包`，即使不吃晚段偏置，主线中后段也更像“继续成线、准备收官”。
- 路线基础池和路线熟练度奖励也同步收口：`河控流` 不再默认塞 `护符`，而是改成 `杠分 / 吃道具 / 胡钱`；`救场流` 把第三个固定奖励从 `吃道具` 改成 `丢弃`，让后段救场更偏整理手牌和补最后一刀。
- 本轮 scoped 验证继续通过：`npm run test -w packages/shared -- mahjong-config-playable-prototype`、`npm run test -w apps/web -- hulebu`、两份内联脚本 `node --check`、`git diff --check`。
- 本轮新增回归已通过：`npm run test -w packages/shared -- mahjong-config-playable-prototype`、`npm run test -w apps/web -- hulebu`、两份内联脚本 `node --check`。
- 已把这轮冻结口径重新钉回代码和回归：朋友试玩主线第 `17 / 18 / 19` 关现正式锁成 `328 / 334 / 340` 张、字牌权重 `62 / 66 / 70`、孤张预算 `7 / 8 / 8`；无尽后段 Boss 分数线锁成 `164 + 章节 * 16`；共享原型测试和站内发布测试已同步到这组新值。
- 继续把 T186 从规划推到运行时代码：外层 `/games/hulebu` 现在把 `局内流派` 明确成每局开局前的主轴，把 `路线挂载` 收成 `局外偏好`，避免玩家再把长期成长和本局打法读成同一套系统。
- 内层 HTML 原型与站内静态 Demo 已开始真正由 `runArchetype` 驱动：开局加成、奖励偏置、事件偏置和终章 Boss 口径都先看本局流派，`preferredRoute / routeMasteryLevel` 只保留轻协同。当前 scoped 验证通过；`npm run typecheck -w apps/web` 与 `npm run build -w apps/web` 也通过。
- 这轮又继续把局外偏好的实际影响压回“轻协同”：当前 `preferredRoute / routeMasteryLevel` 只再轻补一点起手、事件和奖励，避免玩家重新读成“跨很多关养一条路线”；真正决定这一局打法的入口已经收回到开局前的 `runArchetype` 选择。
