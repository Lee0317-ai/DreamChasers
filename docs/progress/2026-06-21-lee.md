# 2026-06-21 Lee 进展

## T051 胡了卜麻将牌面 UI 参考图

- 用户继续指出 `1-9筒 / 1-9条` 的绿底和其他牌不一致。
- 已新增 `v4` 标准底板修正版：`output/hulebu-ui-assets/hulebu-master-tile-pack-v4-standard-body/`。
- `1-9筒 / 1-9条` 现已改为共用 `tile_wan_01` 标准底板并只转描符号。
- 已通过 `--require-green-base --require-standard-body` 校验，当前预览以 `preview/contact-sheet.png` 为准。
- 根据用户继续反馈 `1-9筒` 有贴片感，新增 `v7` 清底融合版：`output/hulebu-ui-assets/hulebu-master-tile-pack-v7-clean-template-dots/`。
- `v7` 对筒子使用严格颜色蒙版、羽化融合、底缘碎片清理，并修正标准空底板残留 `萬` 字下沿的问题。
- `v7` 已通过 `--require-green-base --require-standard-body --require-fused-dots --require-clean-dot-edges` 校验。
- 用户已确认 `v7` “可以”，T051 以 `output/hulebu-ui-assets/hulebu-master-tile-pack-v7-clean-template-dots/` 作为当前可交付版收口。

## T181 胡了卜 HUD 和按钮透明切图包

- 用户指出 HUD、右侧工具按钮、底部组合按钮、组合选择弹层、槽位、奖励卡和场景皮肤卡仍没有单独抠成透明 PNG。
- 已新增 T181，并保存两张 UI 参考图到 `output/hulebu-ui-assets/hulebu-ui-component-sources/`。
- 已生成 v1 透明组件包：`output/hulebu-ui-assets/hulebu-ui-component-pack-v1-transparent/`。
- 组件包包含顶部 HUD、记牌器、组合按钮普通/火焰态、右侧工具按钮、弃牌/手牌槽、Buff 抽屉、奖励卡、场景卡和组合选择弹层。
- 组合选择弹层已按用户确认改为 `panel_bg.png` 空底板 + T051 v7 真实麻将小图预览，不再使用文字牌名。
- 用户指出 v1 中很多玩法整图来源组件是半截图，已判断不可作为正式资源。
- 已生成 v2 可用包：`output/hulebu-ui-assets/hulebu-ui-component-pack-v2-usable-only/`，剔除玩法整图中半截或贴边的独立 `tool_counter` 与场景皮肤卡，只保留完整来源组件。
- v2 已通过资源包校验，当前等待用户查看 `preview/contact-sheet.png`。
- 复核 v2 后确认右侧部分工具按钮仍裁切偏紧，且 `score_badge` 和组合选择横条被旧算法抠空底色。
- 已生成当前推荐包 `output/hulebu-ui-assets/hulebu-ui-component-pack-v3-usable-clean/`：修正四个右侧工具按钮完整裁切、保留浅色牌匾底色，并用 T051 v7 牌面生成组合选择麻将图预览；等待用户验收。

## T176 胡了卜高阶周目后续加深

- 继续补齐高阶新分支的结算归因。
- `东风试路·定风 / 南风挡墙·回潮 / 西风试锋·收注 / 北风锁火·熄潮` 已补成功收益线、下一轮建议、失败构筑回顾、关键散点和下一轮第一手建议。
- `/games/hulebu` 外层高阶配置卡同步补了四条分支的事件聚光和奖励主骨。
- 高阶结算新增 `nextDecision` 路线决策层，东/南/西/北四档会分别提示下一局更适合深追前段开线、补中段容错墙、压后段收账或确认终局封口顺序。
- 高阶结算继续新增 `tempoRead` 节奏读法层，四档会分别提示三拍开线、呼吸墙、读账收账和封火落锁的操作节拍。
- 高阶失败结算新增 `failureAxisVerdict` 失败轴判定层，把四类失败归因为“目标线先塌 / 节奏先断拍 / 容错先见底 / 构筑先散线”，并同步到原型、站内静态 Demo 和 `/games/hulebu` 结算页。
- 高阶失败结算继续拆成四段式判卷，新增 `failureGoalBreakdown / failureTempoBreakdown / failureBufferBreakdown / failureBossMismatchVerdict`，让目标、节奏、容错和构筑 / Boss 错位分开看。
- 高阶失败复盘又加了一层 `本轮高阶身份`，把奖励链、事件链、手感和 Boss 压力并排摆出来，输了也能一眼看出这套 build 更像哪一档。
- 继续把 `东风探手·压口` 单独从东风通用前段里拎出来，补了专属的事件身份 / 构筑手感 / 奖励链 / Boss 压力判定，让前段压口不再只吃通用东风文案。
- 继续把 `西风收口·留门 / 北风锁火·终煤 / 北风封尾·闷锁` 这些更偏终局的分支往专属手感和奖励骨上补厚，让后段收口、闷燃封火和闷锁封尾更像不同构筑，而不是同一档的换皮句。
- 继续把失败反馈往更清楚的方向拆，补了 `北风锁火·终煤 / 北风封尾·闷锁` 的专属构筑手感、Boss 压力和奖励链判定，让终局失败也能更像不同死法而不是统一高压失败。
- 继续把 `北风锁火·终煤 / 北风封尾·闷锁` 的失败拆得更细，目标完成度、节奏和容错现在能更明确地分出火线没闷到最后一拍、尾门没焊死、以及 Boss 终局逼锁错位这几类卡点。
- 把失败页卡片顺序也往高阶专属复盘页推了一步，现在会先讲本轮高阶身份和失败拆解，再讲 Boss / 构筑错位，最后才提还活下来的收益，不再像普通结算页那样先看收益卡。
- 高阶成功结算新增 `rewardChainVerdict` 奖励链判定层，把四档收益身份区分为“东风开线链 / 南风筑墙链 / 西风收账链 / 北风封刀链”，让成功复盘不只罗列奖励，而能直接读出这套 build 立住了哪条收益链。
- 高阶事件结算新增 `eventIdentityVerdict` 事件身份判定层，把四档事件身份区分为“东风试路局 / 南风换气局 / 西风读账局 / 北风落锁局”，让事件线也能直接拉开构筑手感。
- 高阶构筑结算新增 `buildIdentityVerdict` 构筑手感判定层，把四档 build 手感区分为“前段开线 / 中段呼吸墙 / 后段读账收口 / 终局封刀”，让成功后的手感身份更直给。
- 高阶 Boss 结算新增 `bossPressureVerdict` Boss 压力判定层，把四档 Boss 压力区分为“前段催线 / 中段压墙 / 后段查账 / 终局逼锁”，让结算更清楚这一档到底在逼哪条线。
- 高阶局内奖励池继续补厚，新增 `东风构筑：三拍开线 / 南风构筑：呼吸墙核 / 西风构筑：读账封口 / 北风构筑：终局封刀` 四张 A1-A4 专属奖励，并接进对应档位池和构筑加权池。
- 这轮继续把高阶奖励池和能力池加厚，补进更多东/南/西/北分支奖励、事件锚点和构筑联动项，让四档高阶的抽奖、事件和结算复盘不再只靠少数固定词条撑场。
- 高阶失败复盘又补了一层“本轮高阶身份”读法，把失败时的奖励链、事件链、手感和 Boss 压力并排摆出来，让输了也能看出这套 build 更像哪一档、卡在哪一类转换链。
- 继续把高阶新路线的事件身份从四档大类里拆出来，`河杠流 / 收官流 / 顺手胡流 / 救场流 / 护河续押 / 牌尾流 / 杠流 / 河控流 / 试炼流 / 稳压流` 现在各自有更专属的事件读法。
- 高阶装配回顾也同步加厚，新路线不再只说“当前 build 收束”，而会点名牌河控制、河道缓冲、试炼接口、自然成胡、临门补洞、终局定线等具体转换链。
- 原型和站内静态 Demo 的 iframe payload 也同步补齐新路线 identity，不再只由 `/games/hulebu` 外壳兜底；`rewardChainVerdict / eventIdentityVerdict / buildIdentityVerdict / bossPressureVerdict / tempoRead` 现在会直接输出河杠、收官、顺手胡、救场、护河、牌尾、杠流、河控、试炼和稳压的专属读法。
- 高阶失败三轴继续路线化，原型和站内静态 Demo 现在会按 `河杠流 / 收官流 / 顺手胡流 / 救场流 / 护河续押 / 牌尾流 / 杠流 / 河控流 / 试炼流 / 稳压流` 输出专属的目标完成度、节奏断档、容错见底和 Boss 错位拆分。
- 高阶结算新增 `routeChainChecklist` 路线链路复盘，原型和站内静态 Demo 会按新路线输出事件差异、奖励链检查和下一手抓点；`/games/hulebu` 外层结算页同步显示“路线链路复盘”卡，进一步说明这套构筑是怎么从事件、奖励、手感和 Boss 压力接起来的。
- 高阶奖励池继续补 8 张路线补强奖励：`高阶构筑：顺手摸牌 / 临门补洞`、`南风构筑：护河留手 / 稳河控口`、`西风构筑：试炼审计 / 牌尾留门`、`北风构筑：杠潮压顶 / 稳压续命`，并接入 A1-A4 档位池和对应构筑加权池，让顺手胡、救场、护河、河控、试炼、牌尾、杠流和稳压不只停留在结算文案里。
- 高阶失败结算新增 `failureRouteChecklist` 失败链路复盘，原型和站内静态 Demo 会按新路线输出“哪里断、Boss 哪里错口、下一把先补什么”，`/games/hulebu` 外层失败页同步显示“失败链路复盘”卡。
- 站内静态 Demo 已与原型同步。
- 当前验证通过：`npm run test -w packages/shared -- mahjong-config-playable-prototype`、`npm run test -w apps/web -- hulebu`、`npm run typecheck -w apps/web`、原型 HTML 内联脚本 `node --check`、站内静态 Demo 内联脚本 `node --check`。
