# 2026-07-29 Lee Progress

## T244 胡了卜 Cocos v1 M1 核心架构

- 修复恢复态多次撤回计数回弹，恢复后道具数按执行后的剩余次数保留。
- 为 `HulebuRuntimeSnapshot` 增加深层结构与语义校验，覆盖完整牌面多重集、位置与阻塞拓扑、松动牌落点、槽位/牌河/明牌区引用、工具与历史快照。
- 控制器恢复链路会校验奖励、局外成长、特殊事件、流派、能力、runtime 与 coordinator；v0 存档只归一化一次，奖励、事件和组合选择弹层按保存时上下文精确恢复。
- 聚焦测试通过 `158/158`，Cocos domain TypeScript 检查通过；两轮独立评审最终为 `0 Critical / 0 Important`。
- 精确提交 `1bc4867cf56919b1230307297d9d4600b4f6bb4f` 的 Creator 3.8.8 production build 与 verify-only 通过；build id 为 `1bc4867cf569-20260729T150509Z`，5 条 HTTP smoke 均为 `200`。
- 内置浏览器确认桌面与 `390x844` 正式包非黑屏、无 warning/error；点击牌后刷新恢复相同局内状态。Lee 已确认微信小程序方向按竖屏验收，横屏不再属于目标，不登记横屏适配任务。
- 本轮内置浏览器拒绝重新访问本地 production 地址，未新增组合、多候选 exact choice 和一次清关的端到端证据；T244 调整为待验收，不提前标记完成。

## T245 胡了卜 Cocos 快照真实路径

- production 构建对 source snapshot 的校验与物化统一使用 `realpath`，symlink worktree 不再错误拼接快照根目录。
- 发布测试通过 `189/189`；最新精确提交 production build、verify-only 和正式包加载共同验证该路径修复。
- T245 已完成，无自身遗留阻塞。
