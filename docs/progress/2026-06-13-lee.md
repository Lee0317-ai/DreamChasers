# 2026-06-13 Lee 进展

## T162：胡了卜残局收官和悬台窄腰高压池

- 状态：已完成
- 完成内容：
  - 已新增任务分片 `docs/tasks/items/T162-hulebu-endgame-settlement-demo.md`。
  - 已新增领取分片 `docs/tasks/claims/T162-lee.md`。
  - 已记录 `IDEA-20260612-04`，把残局收官和高压窄腰池纳入任务池。
  - 默认玩家 Demo 第 8-10 关 auto 随机池已加入 `suspended-waist / 悬台窄腰`，第 5-7 关仍保留基础模板池。
  - 普通关牌桌清空但主槽有残张时会进入 `残局收官`，不再直接无成本通关。
  - `残局收官` 第一阶段已实现 `弃牌通关` 和 `选作牌引`；牌引会把选中的主槽牌带到下一关开局，并在 HUD 中提示。
  - 已同步站内静态 Demo `/games/hulebu-demo/index.html`，并保留静态配置 fetch 路径。
- 验证：
  - `npm run test -w packages/shared -- mahjong-config-playable-prototype`：通过。
  - `npm run test -w packages/shared -- mahjong-config`：通过。
  - HTML 内联脚本语法检查：通过，源原型和站内静态副本均可解析。
  - `npm run test -w apps/web -- hulebu`：通过。
  - 右侧内置浏览器桌面端检查 `/games/hulebu` 与 `/games/hulebu-demo/index.html`：通过，iframe 和静态 Demo 正常渲染，记牌器、动作栏、卡槽、底部道具无重叠。
  - 右侧内置浏览器 390px 移动端检查 `/games/hulebu-demo/index.html`：通过，卡槽在首屏可见，底部道具栏不遮挡卡槽。
  - `npm run docs:sync`：通过。
  - `git diff --check`：通过。
- 遗留：
  - `收入牌河` 仍是后续正式构筑方向，本轮不做。
  - 特殊事件、高阶词缀和 Boss 试炼需要另开任务规划，不能直接混入 T162。

## T163：胡了卜特殊事件 Demo 第一版

- 状态：已完成
- 完成内容：
  - 已新增任务分片 `docs/tasks/items/T163-hulebu-special-events-demo.md`。
  - 已新增领取分片 `docs/tasks/claims/T163-lee.md`。
  - 已记录 `IDEA-20260613-01`，把特殊事件第一版纳入任务池。
  - 默认朋友试玩 Demo 第 6、8、10 关前会弹出一次特殊事件选择。
  - 事件池包含 `路遇老雀`、`旧牌匣`、`加注一局`、`暗灯牌局`，选项覆盖立即铜钱、道具补给和主动加压。
  - 第一版词缀支持 `禁洗牌`、`禁透视`、`高压牌山`；`禁洗牌` 会禁用洗牌按钮，`高压牌山` 会提高当前关牌量压力并纳入高压模板池。
  - HUD 和状态栏会提示当前事件/词缀；事件通关奖励会结算到铜钱，词缀不会串到下一关。
  - 已同步站内静态 Demo `/games/hulebu-demo/index.html`，并保留静态配置 fetch 路径。
- 验证：
  - `npm run test -w packages/shared -- mahjong-config-playable-prototype`：通过。
  - `npm run test -w packages/shared -- mahjong-config`：通过。
  - HTML 内联脚本语法检查：通过，源原型和站内静态副本均可解析。
  - `npm run test -w apps/web -- hulebu`：通过。
  - 右侧内置浏览器桌面端检查：通过，第 6 关前事件弹层和高压入局 HUD 正常。
  - 右侧内置浏览器 390px 移动端检查：通过，事件卡片不溢出，牌面约 `40x54`，记牌器上下两层可读，卡槽和底部工具栏无重叠。
- 遗留：
  - `禁透视` 已保留词缀状态和 HUD 提示；默认朋友 Demo 当前没有独立透视按钮，后续恢复透视工具时再接实际禁用。
  - Boss 试炼、事件池扩容和完整高阶周目需要另开任务。

## T164：胡了卜 Boss 试炼 Demo 第一版

- 状态：已完成
- 完成内容：
  - 已新增任务分片 `docs/tasks/items/T164-hulebu-boss-trial-demo.md`。
  - 已新增领取分片 `docs/tasks/claims/T164-lee.md`。
  - 已记录 `IDEA-20260613-02`，把 Boss 试炼 Demo 第一版纳入任务池。
  - 默认朋友试玩 Demo 第 10 关启用 `终局试炼`，目标为 `杠 1 / 胡 1 / 积分 180`。
  - HUD 显示 `试炼 x/3`；玩家页新增紧凑试炼目标条，移动端可直接看到三项目标。
  - 清空牌山但试炼目标未完成时进入失败弹层，状态提示 `目标未完成`，详情列出 `杠 / 胡 / 积分` 缺口。
  - 击破试炼后一次性发放 `试炼奖励 +180 铜钱`，重复结算不会重复发奖。
  - 第 10 关密集牌山生成器已承接试炼目标包，并启用起手窗口平衡，避免起手直接露出完整答案组。
  - 已同步站内静态 Demo `/games/hulebu-demo/index.html`，并保留静态配置 fetch 路径。
- 验证：
  - `npm run test -w packages/shared -- mahjong-config-playable-prototype`：通过。
  - `npm run test -w packages/shared -- mahjong-config`：通过。
  - `npm run test -w apps/web -- hulebu`：通过。
  - HTML 内联脚本语法检查：通过，源原型和站内静态副本均可解析。
  - 右侧内置浏览器桌面端检查：通过，第 10 关事件后进入 `终局试炼`，HUD 和目标条正常。
  - 右侧内置浏览器 390px 移动端检查：通过，目标条可见，牌宽约 `37.5px`，卡槽与底部工具栏间距约 `9.6px`，无重叠。
- 遗留：
  - 当前为 Boss 试炼第一版，只覆盖朋友 Demo 第 10 关；完整高阶周目、更多 Boss 主题和局外奖励后续另开任务。
