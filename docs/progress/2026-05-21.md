# 2026-05-21 进展

## T029：麻将 Roguelike 消除框架调研和规划

- 负责人：Codex / 开发 B
- 状态：已完成
- 完成内容：
  - 新增变更卡 `IDEA-20260521-07`，记录麻将 Roguelike 消除框架调研和规划需求。
  - 新增任务 `T029`，并完成领取和状态同步。
  - 补齐 `docs/modules/mahjong-roguelike/` 模块文档目录。
  - 新增 `FRAMEWORK_PLAN.md`，基于官方文档整理 Cocos Creator、GDevelop、Next.js iframe 嵌入和 `postMessage` 通信的框架做法。
  - 推荐路线为：共享规则与配置优先，GDevelop 可做 Web H5 原型，Cocos Creator 作为正式小游戏主线，Next.js 只做站内嵌入和事件接收。
- 验证：
  - 文档自审：通过。
  - UTF-8 无 BOM 检查：通过。
- 下一步：
  - 讨论玩法细节，确认默认槽位数、前 10 个奖励、胡牌目标是否进入第一版，以及原型路线选 GDevelop 还是 Cocos 直做。

## T028：将独立模块归档规则写入整体架构

- 负责人：Codex / 两人协作
- 状态：已完成
- 完成内容：
  - 新增变更卡 `IDEA-20260521-05`，记录需要把独立模块规则上升到整体架构。
  - 新增任务 `T028`，并完成领取和状态同步。
  - 更新 `AGENTS.md`、`CLAUDE.md`、`README.md`，让所有 AI 和新开发者从入口文档就看到模块目录规则。
  - 更新 `docs/PROJECT_CONTEXT.md`、整体设计稿、实施计划和双人协作规范，明确文档目录和代码目录结构。
- 验证：
  - 文档自审。
  - 旧路径扫描。
  - UTF-8 编码检查。

## T027：补充 AGENTS.md 文档输出格式规则

- 负责人：Codex / 开发 A
- 状态：已完成
- 完成内容：
  - 已新增变更卡 `IDEA-20260521-04`。
  - 已新增并领取 `T027`。
  - 已在根目录 `AGENTS.md` 增加“文档输出格式”章节。
  - 规则明确：需要人类手写编辑或版本控制 diff 的文档用 Markdown；需要信息可视化、交互或一次性阅读的内容用 HTML。
- 验证：
  - 文档自审：通过。
  - UTF-8 无 BOM 检查：通过。

## T026：建立工具/游戏独立模块归档规范

- 负责人：Codex / 两人协作
- 状态：已完成
- 完成内容：
  - 新增变更卡 `IDEA-20260521-03`，记录每个小工具和游戏都需要独立文档文件夹和独立代码模块的要求。
  - 新增任务 `T026`，并完成领取和状态同步。
  - 更新 `docs/modules/README.md`，明确模块文档目录结构和代码目录规则。
  - 将 PDF 工具箱文档迁移到 `docs/modules/pdf-toolbox/`，后续全过程都在该目录下记录。
  - 将 T015 代码范围调整为 `apps/web/src/modules/tools/pdf-toolbox/**`，路由 `apps/web/src/app/tools/pdf-toolbox/**` 只保留页面入口。
- 验证：
  - 文档自审。
  - UTF-8 编码检查。

## T015：实现 PDF 工具箱 MVP

- 负责人：Codex / 开发 A
- 状态：进行中
- 完成内容：
  - 已在 `docs/tasks/CLAIMS.md` 领取 T015。
  - 已将 T015 依赖调整为当前可用的页面基础 `T025`。
  - 新增 `docs/modules/pdf-toolbox/README.md`，明确 PDF 工具箱目标、免费/付费边界、MVP 分阶段范围、技术路线、主要文件和验证方式。
  - 新增 `docs/modules/pdf-toolbox/IMPLEMENTATION_PLAN.md`，把后续实现拆为依赖确认、纯逻辑和测试、页面渲染、工具台交互、入口联动、扩展能力六个阶段。
  - 已安装 `pdf-lib` 和 `pdfjs-dist`。
  - 已新增 `/tools/pdf-toolbox` 真实工具页。
  - 已新增独立代码模块 `apps/web/src/modules/tools/pdf-toolbox/`。
  - 已实现上传 PDF、读取页数、缩略图预览、选择页面、旋转、排序、删除、拆分下载和下载当前 PDF。
  - 已实现文字水印、签名图片和 PDF 转 Word Beta。
  - 已实现区域遮盖式 PDF 去水印：仅用于处理自己 PDF 中不需要的水印、遮挡或局部元素，文案明确不用于移除他人版权标识。
  - 已实现拍照/扫描图片合成 PDF：支持 PNG/JPG 图片按 A4 页面输出 PDF。
  - 已将工具站 PDF 卡片和搜索结果指向真实工具页。
- 验证：
  - `npm run test -w apps/web -- pdf`：通过，11 个测试通过。
  - `npm run lint -w apps/web`：通过，存在 Prisma 生成文件既有 warning。
  - `npm run typecheck -w apps/web`：通过。
  - `npm run build -w apps/web`：通过，`/tools/pdf-toolbox` 已静态生成。
  - 浏览器检查：内置浏览器可打开 `http://127.0.0.1:3018/tools/pdf-toolbox`，页面已显示“遮盖区域”和“图片扫描成 PDF”，当前视窗无横向溢出；本轮内置浏览器接口未暴露文件选择器上传能力，下载动作由 PDF 单测覆盖。
- 下一步：
  - 继续补 PDF 转图片和基础压缩。

## T025：拆分独立工具站和游戏站入口体验

- 负责人：Codex / 开发 A
- 状态：已完成
- 参考设计：`docs/网站UI/index.html`, `docs/网站UI/tools.html`, `docs/网站UI/games.html`
- 完成内容：
  - 首页改为左右分流入口：`效率工具箱` 和 `休闲游戏馆`。
  - `/tools` 改为独立工具站视觉，保留顶部唯一 `去游戏馆` 入口。
  - `/games` 改为独立游戏站视觉，保留顶部唯一 `去工具箱` 入口。
  - 工具站和游戏站使用不同配色、导航、圆角、卡片密度和首屏氛围。
  - 保留现有筛选、搜索和详情弹窗交互。
- 验证：
  - `npm run lint -w apps/web`：通过，存在 Prisma 生成文件既有 warning。
  - `npm run typecheck -w apps/web`：通过。
  - `npm run build -w apps/web`：通过。
  - 浏览器检查：`/`, `/tools`, `/games` 在 390x844 和 1440x900 下无横向溢出；PDF 和麻将卡片弹窗可打开。
- 截图：
  - `.codex-screenshots/t025-entry.png`
  - `.codex-screenshots/t025-tools.png`
  - `.codex-screenshots/t025-games.png`
