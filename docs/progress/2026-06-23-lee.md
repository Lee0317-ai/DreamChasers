# 2026-06-23 Lee 进展

## T186 胡了卜局外成长与局内流派开局重构规划

- 根据用户最新确认，胡了卜新的主线方向不再是“跨多关慢慢 build 一条流派”，而是 `长期成长在局外，每一局开局前主动选流派`。
- 已按用户要求调用并遵循 `game-studio:game-studio` 规划路径，同时结合 `web-game-foundations` 明确：当前阶段不切引擎，继续保留 `/games/hulebu` Web shell + `config-playable` HTML 原型双层结构。
- 已新增变更卡 `IDEA-20260623-01`，把这次方向重构正式登记到 `docs/tasks/CHANGE_INTAKE.md`。
- 已新增规划任务 `T186`，避免后续继续把产品结构问题挤进 T184 的长期成长深化或 T185 的纯数值冻结里。
- 已产出完整实现计划：`docs/superpowers/plans/2026-06-23-hulebu-meta-progression-and-run-archetype-selection.md`。
- 该计划明确了后续六块核心工作：
  - 局外成长改为负责资源、升级、解锁与流派携带能力。
  - 每次开局前新增 `局内流派` 选择，定义这一局的收益方向。
  - 旧 `路线挂载` 降级成局外偏好与轻协同，不再定义整局身份。
  - 奖励、事件和 Boss 压力改为围绕“本局已选流派”做偏置。
  - 前 20 关承担渐进解锁和教学，20 关后以及长期模式开放自由流派开局。
  - 继续通过现有 Web shell、原型、静态 Demo 和回归测试分层推进，不并入 Cocos 和音画资源。
- 后续实现已完成到待验收：`/games/hulebu` 外层新增开局 `局内流派` 选择和渐进解锁，HTML 原型与静态 Demo 已接入 `runArchetype` 并让它主导开局加成、奖励/事件偏置和终章 Boss 口径。
- 最终术语扫尾已完成：旧 `路线挂载` 已收成 `局外偏好`，自动候选改为 `自动偏好`，Boss 阶段目标改为 `本局流派主骨`；后续扩内容时不要把 `preferredRoute` 重新作为本局 build 身份。
