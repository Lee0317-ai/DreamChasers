# 2026-08-11 Lee 进展

## T260：胡了卜 Cocos 锁牌暗态与顶部 HUD 精修

- 完成下层锁牌状态修复：覆盖判定同时驱动点击和牌面暗态，异步 Sprite 加载不会再恢复错误亮色。
- 完成顶部 HUD 动态值精修：分数三位数保持清楚；记牌器紧凑态只显示动态余牌总数，展开详情逻辑保留。
- Cocos `390×844` 预览实测暗牌点击 no-op；亮牌点击后进入手牌，余牌从 `23` 更新为 `22`。
- 共享测试 `40/40`、Cocos TypeScript、精确提交 production build 与 verify-only 均通过；build ID `9f423dd1fb0c-20260810T160923Z`，5 个 smoke 路径均返回 `200`。

## T261：胡了卜 Cocos 八条、牌体底层与动作按钮底色修复

- 已登记并领取任务。验收问题为八条牌面仍不正确、正式整牌下方透出重复程序化牌体、吃碰杠胡按钮后方透出错误底色。
- 本任务只修正式 Cocos 表现与资源映射，不修改玩法、关卡、存档或其他模块。
- 八条已恢复为此前确认的上下 `W/M` 形构图，并以 v7 已确认源图作为 formal v1 唯一输入。
- 正式牌面与动作按钮成功加载后会清空各自程序化 fallback；动作按钮三态已重新裁出透明异形边界，不再带整块错误矩形底色。
- Cocos `390×844` 预览确认八条、牌体和按钮暗态正确；共享测试 `40/40`、Cocos TypeScript、精确提交 production build 和 verify-only 均通过。
- production build ID：`f35f07e3cd44-20260811T005150Z`；5 个 smoke 路径均返回 `200`，最新构建已在 `http://127.0.0.1:4173/` 提供验收。

## T262：胡了卜 Cocos 分数、记牌器与锁牌牌背精修

- 已登记并领取任务。验收口径为移除分数数值后的浅色底块、把紧凑记牌器改为纯中文入口并保留 34 牌展开详情、把所有不可点击牌改为正式牌背。
- 本任务只修改正式 Cocos 竖屏表现，不改玩法、关卡、点击判定、存档或其他模块。
- 分数数字已直接叠在正式分数牌匾上，不再带额外浅色底块；紧凑记牌器只显示“记牌器”，展开后按万、条、筒、字展示全部 34 种牌面和剩余数。
- 不可点击牌已统一显示 formal v1 正式绿色牌背；可点击牌仍显示真实牌面，原有最高层命中规则保持不变。
- Chrome `390×844` production 实测记牌器可展开/收起，锁牌牌背和分数区域正确；共享测试 `40/40`、Cocos TypeScript、精确提交 production build、verify-only 和 5 条 smoke 均通过。
- production build ID：`11b6581eb2e2-20260811T035723Z`，当前验收地址为 `http://127.0.0.1:4173/`。
