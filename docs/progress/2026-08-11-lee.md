# 2026-08-11 Lee 进展

## T260：胡了卜 Cocos 锁牌暗态与顶部 HUD 精修

- 完成下层锁牌状态修复：覆盖判定同时驱动点击和牌面暗态，异步 Sprite 加载不会再恢复错误亮色。
- 完成顶部 HUD 动态值精修：分数三位数保持清楚；记牌器紧凑态只显示动态余牌总数，展开详情逻辑保留。
- Cocos `390×844` 预览实测暗牌点击 no-op；亮牌点击后进入手牌，余牌从 `23` 更新为 `22`。
- 共享测试 `40/40`、Cocos TypeScript、精确提交 production build 与 verify-only 均通过；build ID `9f423dd1fb0c-20260810T160923Z`，5 个 smoke 路径均返回 `200`。

## T261：胡了卜 Cocos 八条、牌体底层与动作按钮底色修复

- 已登记并领取任务。验收问题为八条牌面仍不正确、正式整牌下方透出重复程序化牌体、吃碰杠胡按钮后方透出错误底色。
- 本任务只修正式 Cocos 表现与资源映射，不修改玩法、关卡、存档或其他模块。
- 八条已恢复为此前确认的上下 `W/M` 形构图，并以 v7 已确认源图作为 formal v1 唯一输入。
- 正式牌面与动作按钮成功加载后会清空各自程序化 fallback；动作按钮三态已重新裁出透明异形边界，不再带整块错误矩形底色。
- Cocos `390×844` 预览确认八条、牌体和按钮暗态正确；共享测试 `40/40`、Cocos TypeScript、精确提交 production build 和 verify-only 均通过。
- production build ID：`f35f07e3cd44-20260811T005150Z`；5 个 smoke 路径均返回 `200`，最新构建已在 `http://127.0.0.1:4173/` 提供验收。

## T262：胡了卜 Cocos 分数、记牌器与锁牌牌背精修

- 已登记并领取任务。验收口径为移除分数数值后的浅色底块、把紧凑记牌器改为纯中文入口并保留 34 牌展开详情、把所有不可点击牌改为正式牌背。
- 本任务只修改正式 Cocos 竖屏表现，不改玩法、关卡、点击判定、存档或其他模块。
- 分数数字已直接叠在正式分数牌匾上，不再带额外浅色底块；紧凑记牌器只显示“记牌器”，展开后按万、条、筒、字展示全部 34 种牌面和剩余数。
- 不可点击牌已统一显示 formal v1 正式绿色牌背；可点击牌仍显示真实牌面，原有最高层命中规则保持不变。
- Chrome `390×844` production 实测记牌器可展开/收起，锁牌牌背和分数区域正确；共享测试 `40/40`、Cocos TypeScript、精确提交 production build、verify-only 和 5 条 smoke 均通过。
- production build ID：`11b6581eb2e2-20260811T035723Z`，当前验收地址为 `http://127.0.0.1:4173/`。

## T263：胡了卜 Cocos 牌背点击穿透修复

- 已登记并领取任务。问题根因是旧命中链路先过滤锁牌，再从可点牌中找命中，导致点击牌背时可能穿透选择附近或上层正面牌。
- 本任务只修 `BoardLayerBinder` 的命中优先级：先找光标下视觉层级最高的实际牌，命中牌背时直接消费并 no-op。
- 命中排序最终以 Cocos UI 的 sibling 绘制顺序为第一依据，`zIndex` 只作兜底；锁牌不会被过滤后再穿透到其他可点牌。
- Chrome `390×844` production 实测正面牌使余牌 `14 -> 13`，同坐标露出牌背后再次点击余牌保持 `13`；共享测试 `40/40`、Cocos TypeScript、精确提交 build、verify-only 和 5 条 smoke 均通过。
- production build ID：`05237d301ceb-20260811T044247Z`，当前验收地址为 `http://127.0.0.1:4173/`。

## T264：胡了卜 Cocos 通关弹层视觉优化

- 已登记并领取任务。当前问题是通关弹层误用三选一底板，标题和说明与空卡位重叠，且右侧工具层浮在遮罩之上。
- 本任务只调整竖屏通关反馈的底板、信息层级和遮罩层级，不修改通关流程、奖励逻辑或计分规则。
- 通关状态已切换到 formal v1 `settlement` 单面板底图，正式 Sprite 加载后清空程序化 fallback；标题、本层得分、关卡说明和继续按钮改为清楚的单列排版。
- 全屏遮罩会阻断底层输入，并在清台后的 HUD 刷新完成后重新置顶，右侧工具按钮不会再浮在遮罩上。
- 共享测试 `40/40`、Cocos TypeScript、精确提交 production build、verify-only 和 5 条 smoke 均通过；build ID `14696e097fd7-20260811T052502Z`。内置浏览器受本地 URL 安全策略限制，未完成自动化截图目检，当前服务已在 `http://127.0.0.1:4173/` 启动供直接验收。

## T265：胡了卜 Cocos 顶部 HUD、记牌器与已碰牌池优化

- 已登记并领取任务。验收问题包括记牌器展开位置不贴入口、顶部正式图片烘焙文字与动态值重叠、余牌缺少正式 UI、缺少右上角退出入口，以及副露区缺少清楚的“已碰牌池”语义。
- 本任务只调整竖屏 HUD 和既有 `openMeldNodes` 的表现，不改补杠识别、副露数据或玩法规则。
- 关卡、分数和余牌 HUD 已统一为正式金边底图加动态内容面，加载正式 Sprite 后清空程序化底板，烘焙值不会再与运行时值重叠。
- 记牌器展开层固定在入口正下方左对齐；右上角新增 `×` 退出按钮并回到局外。
- 既有 `openMeldNodes` 已进入“已碰牌池”视觉容器，空态显示暂无，最多展示四组碰/杠/补杠，补杠识别链路未变。
- 共享测试 `40/40`、Cocos TypeScript、精确提交 production build、verify-only 和 5 条 smoke 均通过；build ID `e1733c74fff7-20260811T064949Z`，当前版本已更新到 `http://127.0.0.1:4173/`。

## T266：胡了卜 Cocos 通关弹层标题避让

- 已登记并领取任务。T265 production 截图确认结算底图顶部莲花装饰仍压住通关标题。
- 本任务只扩大通关面板纵向空间并下移正文层级，不改通关流程或其他 UI。
- 结算底板高度已从 `200` 墠至 `220`，标题/得分/说明/按钮坐标调整为 `0 / -28 / -51 / -84`，正文完全离开顶部莲花与流苏区域。
- 共享测试 `40/40`、Cocos TypeScript、精确提交 production build、verify-only 和 5 条 smoke 均通过；build ID `7469fe97fa60-20260811T071148Z`。

## T267：胡了卜 Cocos 已碰牌池挂载与轻遮挡容差

- 已登记并领取任务。定位到正式场景缺少 `MeldRiverRoot`，因此 T265 虽有已碰牌池绘制代码，但运行时 binder 始终为 `null`。
- 当前生成层与点击层的覆盖阈值均为 `0.001`，本任务统一改为 `0.08`：覆盖不足 8% 可点击，达到 8% 才锁牌。

## T268：胡了卜完整局外与引导 UI 体系设计

- 完成 `390×844` 高保真交互稿，统一 formal-v1 的青玉、金红、深木和象牙纸视觉语言。
- 页面覆盖登录、大厅、五种模式、主线地图、关卡准备、教程、提示、暂停设置、奖励三选一和胜负结算。
- 九个核心页面边界检查无溢出；登录至结算八步关键交互全部通过；浏览器控制台无错误。
- 本任务只交付体验设计与交互原型，没有修改 T267 Cocos 运行时、玩法、关卡、存档或正式原图。

## T271：胡了卜完整局外流程正式视觉母稿

- 基于 T268 与 formal-v1，生成登录/标题、局外大厅、五模式、主线地图、胜利和失败六张正式竖屏视觉母稿。
- 新增六宫格总览、资源 manifest、中文生成说明和 Cocos SpriteFrame key 清单。
- 母稿统一青玉、旧金、象牙纸、深胡桃木和克制朱砂色；所有运行时文字继续由 Cocos Label 叠加。
- 六张母稿和总览均为有效 PNG；文件完整性和 manifest 解析通过；API Key 未写入资源包或文档。
- 本任务没有修改 Cocos runtime、玩法、关卡、存档、Web Demo 或既有 formal-v1 原图。
- 控制器已动态创建并挂载缺失的 `MeldRiverRoot`，放在 `BoardRoot` 之上；已碰牌池空态和副露态会进入真实渲染链路。
- 生成层与点击层均改为 `>= 0.08` 才锁牌；`boardRevision` 已升级，旧 `0.001` active run 会自动失效。
- 共享测试 `40/40`、Cocos TypeScript、精确提交 production build、verify-only 和 5 条 smoke 均通过；build ID `3ba7aabd81a4-20260811T075426Z`。

## T269：胡了卜 Cocos 多组合按钮上方候选菜单

- 已登记并领取任务。Lee 明确多组“碰”应从“碰”按钮上方展开两个候选，不应使用全屏遮罩、结算式大底板或返回按钮。
- 本任务只替换多候选表现，保留 `playing.comboChoosing`、精确 candidate key 执行和刷新恢复。
- 候选已改为锚定对应动作按钮并向上纵向排列；再次点击同一动作按钮会取消并回到 `playing.idle`。
- 共享测试 `40/40`、Cocos TypeScript、精确提交 production build、verify-only 和 5 条 smoke 均通过；build ID `c83d581d3274-20260811T085237Z`。内置浏览器访问本地 4173 被安全策略拒绝，需 Lee 刷新后目视确认。

## T270：胡了卜 Cocos 已碰牌池、记牌器与组合候选精修

- 已登记并领取任务。目标是把已碰牌池改成左下角按钮后展开、提高记牌器展开面板对比度，并把组合按钮上方候选限制为最多 3 个。
- 本任务只调整既有表现层，不改副露/补杠数据、组合判定、计分、关卡或牌山。
- 已碰牌池入口固定左下并默认收起，面板从入口上方向上展开；记牌器改为深绿高对比；组合候选截取最多 3 项。
- 共享测试 `40/40`、Cocos TypeScript、精确提交 production build、verify-only 和 5 条 smoke 均通过；build ID `13e50a661257-20260811T092740Z`。

## T272：胡了卜 Cocos 震落牌独立区域与静态圆点清理

- 已登记并领取任务。余牌下方四个点确认只是静态装饰，将直接移除。
- 杠、补杠、胡震落的 blocker 仍保持可点击，但从原牌山布局中分离，进入独立“震落牌区”。
- 四个静态圆点及其布局字段已删除；震落牌使用 `displayZone: "loose"`，以 5 列网格进入深绿金边托盘，普通牌山拟合只计算 `mountainTiles`。
- 共享测试 `40/40`、Cocos TypeScript、精确提交 production build、verify-only 和 5 条 smoke 均通过；build ID `fd6d1e39c26f-20260811T105137Z`。

## T273：胡了卜标题与大厅透明组件资源包

- 从 T271 已确认的局外视觉方向生成标题/通用、大厅面板和大厅徽章三张无文字源表。
- 裁出标题牌匾、玉印、主/次按钮、说明底板、头像框、资产牌匾、继续面板、进度轨、四个入口徽章和底部导航，共 14 个透明 RGBA PNG。
- 新增可重复构建脚本、Cocos 导入说明、SpriteFrame manifest 和透明棋盘联系表；九宫格、锚点、状态派生策略和来源裁切框均可追溯。
- 14/14 组件通过透明角、至少 17px alpha 边距、强品红残留、正方形画布、key/路径唯一性和九宫格合法性检查；API Key 未写入资源或文档。

## T274：胡了卜模式与主线地图透明组件资源包

- 从 T271 已确认的模式选择和主线地图方向生成四张无文字品红键控源表。
- 裁出模式长卡、状态牌、五枚模式徽章，以及章节牌匾、星级牌匾、章节切换、曲线路径、四类关卡节点和空/实星，共 17 个透明 RGBA PNG。
- 新增可重复构建脚本、Cocos 导入说明、SpriteFrame manifest 和透明棋盘联系表；运行时状态、锚点、九宫格和来源裁切框均可追溯。
- 17/17 组件通过透明角、至少 17px alpha 边距、强品红残留、正方形画布、key/路径唯一性和九宫格合法性检查；API Key 未写入资源或文档。

## T275：胡了卜胜负结算透明组件资源包

- 从 T271 已确认的胜利/失败方向生成胜负印章、结算信息板和结算按钮三张无文字品红键控源表。
- 裁出胜败印章、双结果标题牌、统计牌匾、失败建议面板、解锁横幅和结算主/次按钮，共 9 个透明 RGBA PNG。
- 新增可重复构建脚本、Cocos 导入说明、SpriteFrame manifest 和透明棋盘联系表；胜败态、按钮态、领取态、锚点、九宫格和来源裁切框均可追溯。
- 9/9 组件通过透明角、至少 17px alpha 边距、强品红残留、正方形画布、key/路径唯一性和九宫格合法性检查；API Key 未写入资源或文档。
- T273-T275 三批合计交付 40 个透明运行时候选组件，覆盖标题/登录、大厅、模式选择、主线地图和胜负结算；教程、提示、暂停、设置和奖励继续复用 formal-v1 已有正式资源。

## T276：胡了卜 Cocos 新手引导正式接入

- 前五关新增不遮挡操作的深绿金边教学条，按当前槽位数量和可执行 `碰 / 吃 / 杠` 动态切换文案。
- 大厅新增“新手教学”入口，从 `1-1` 临时重玩至 `1-5`；教学过程不写 active run，中途退出或完成后会恢复原进行中存档。
- `1-5` 教学完成弹层显示“新手教学完成 / 回到大厅”，普通主线通关按钮仍保持原行为。
- 共享测试 `40/40`、Cocos TypeScript、精确提交 production build 和 verify-only 均通过；浏览器实测 `1-3` 教学条、`1-1` 入口、选牌后文案更新和无控制台错误。build ID `dec5351f0057-20260811T144015Z`。
