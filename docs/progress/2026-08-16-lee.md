# 2026-08-16 Lee 进展

## T287 胡了卜独立 UI 素材

- 完成 component-pack-v3 第二组：记牌器收起/展开、碰池、震落区、单个可复用手牌槽。
- 完成 component-pack-v3 第三组：洗牌、撤回、弃牌、看山工具按钮；关卡、分数、剩余牌、Boss 空白 HUD。
- 工具按钮与 HUD 已统一为透明 RGBA 成品，动态文字、数字和次数不写死在图片中。
- 预览分别输出到 `output/hulebu-ui-design-v2/component-pack-v3/previews/playfield-components-v3.png`、`tools-v3.png`、`hud-v3.png`。
- 当前仍未接入 Cocos，下一阶段继续登入、模式/关卡选择、结算和奖励的独立组件。
- 工具按钮后续修正：以洗牌按钮为统一母版重做撤回、弃牌、看山，四个按钮现在共享同一外壳，只替换中心图标。
- 完成局外流程组件：登入标题牌、模式入口卡、关卡节点、奖励卡、胜利印章、失败印章；预览输出到 `meta-entry-v3.png` 与 `meta-result-v3.png`。
- 完成大厅/地图骨架：头像框、货币牌、底部导航、章节牌、可连接路径段、星星进度牌；预览输出到 `lobby-components-v3.png` 与 `map-components-v3.png`。
- `gpt-image-2` 在星星进度牌请求中两次返回模型路由异常，Grok 随后返回 429；最终使用章节牌母版本地派生三槽星星进度牌，未继续消耗失败请求。
- 完成模式与地图节点：主线、每日、无尽、进阶四个模式图标；普通、事件、奖励、Boss 四种节点；预览输出到 `mode-icons-v3.png` 与 `map-node-states-v3.png`。
- 完成事件与中断组件：事件卡、升级卡、暂停面板、设置面板、风险标签、奖励标签；事件卡与升级卡以同一母版重做统一外壳，预览输出到 `event-upgrade-v3.png` 与 `pause-settings-v3.png`。
- 完成吉祥物与通用图标：待机、引导、开心、思考、失败五个粉色胡萝卜姿态；货币、护符、星星三类图标；失败态粗黑描边已隔离并重做，预览输出到 `mascot-poses-v3.png` 与 `shared-icons-v3.png`。
- 完成四张独立竖屏背景：登入大厅、模式/地图、局内牌桌、结算奖励；统一 `1024×1536`，预览输出到 `component-pack-v3/backgrounds/backgrounds-v3.png`。
- 按 Lee 反馈重做局内牌桌背景，移除底部烘焙 8 格手牌槽，后续由独立 `hand-slot` 组件运行时叠加。
- 完成四张页面级合成预览：登入大厅、模式/选关地图、局内主玩法、结算奖励；仅用于检查独立组件在页面中的尺寸与层级，未接入 Cocos。
- 新增程序化牌山验证：用 35 张独立麻将素材拼出 7/5/3/1 菱形坡面，少量正面牌露头，上层压住下层；预览输出到 `page-gameplay-v3-mountain.png`。
- 按 Lee 反馈改为侧视图牌山：生成 `tile-side-back` 侧视盖牌并用 7/6/5/4/3 层程序化堆叠验证山体厚度，预览输出到 `page-gameplay-v3-mountain-side.png`。
- 侧视牌山二次迭代：改为 7/6/5/4/3/2 六层金字塔，正面牌沿坡面 ±20° 斜置；像素校验确认山体不与 HUD 和手牌槽重叠，等待 Lee 对整体轮廓确认。
- 方向变更回归俯视图：放弃侧视方案，改为调用正式 `generateHulebuMountain` 生成列柱堆叠坐标，再用独立麻将素材合成俯视牌山；已输出 center-tower 主预览与 two-wings/cross/ring 对照预览。
