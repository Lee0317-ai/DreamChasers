# 2026-07-08 Lee Progress

## 麻将 Roguelike 消除 T239/T240 入口和点击链路修复

- 按 Lee 反馈修正 Cocos 发布包启动流程：`GameSceneController` 默认不再停在 lobby；无有效 active run 时直接进入教程首关 `startLevel(1)`。
- `HULEBU_BOARD_REVISION` bump 为 `tutorial-pointer-2026-07-08`，使旧首关/旧入口 active run 自动失效。
- `BoardLayerBinder` 在 Cocos `TOUCH_END / MOUSE_UP` 之外新增 canvas DOM `pointerup / mouseup / touchend` 兜底，修复桌面浏览器点击牌面无效。
- DOM pointer 坐标已按现有 hit-test UI 口径换算，包含 y 轴同口径修正，避免点中视觉牌但命中测试落空。
- 验证通过：`npm run test -w packages/shared -- mahjong-cocos-project mahjong-mountain-generator`、Cocos TypeScript、Cocos web-mobile 非 debug 构建。
- Kimi WebBridge 真实 Chrome 启动态验证为 `playing / 15 总牌 / 3 可点 / 12 锁牌`，截图 `.codex-tmp/kimi-tutorial-pointer-final-start.png`。
- Playwright 鼠标点击中间顶层牌后 slot0 写入 `graph_long-wall_node-010`，总牌从 15 变 14。
