# 2026-06-30 Lee 进展

- 完成 T235 胡了卜 Cocos 局内布局截图 QA 和修正。
- 对照目标概念图，用 Cocos Web Mobile 构建产物和 Playwright 进入真实 `playing` 状态截图，确认问题集中在运行时坐标换算、背景锚点、牌山尺度和组合动作条位置。
- Cocos runtime layout 改为以 `visibleSize` 作为世界尺寸，避免 DOM CSS 尺寸二次换算造成桌面/竖屏错位。
- 茶室背景 Sprite 已锚到居中坐标原点，牌山缩放上限已收紧，组合动作条改为跟随底部槽位上方。
- 验证通过：`npm run test -w packages/shared -- mahjong-cocos-project`；`npx tsc --noEmit -p apps/game/mahjong-roguelike/cocos/hulebu-cocos-3.8.8/tsconfig.json`；Playwright 截图 `tmp-hulebu-cocos-final2-ingame-portrait.png` 和 `tmp-hulebu-cocos-final2-ingame-mobile.png`。
- 备注：`git pull origin main` 仍因本地与远端分支分叉且未配置 pull 策略失败；Cocos CLI 构建日志显示 `build Task (web-mobile) Finished`，但进程退出码仍为 36。

- 完成 T236 胡了卜 Cocos HUD 比例和层级精修。
- 按反馈处理 4 项：麻将牌缩小为 `44x59`；隐藏/移除底部重复托盘层；右侧三个工具按钮挂到 `ToolOverlayRoot` 顶层；记牌器移动到关卡牌匾下方。
- 验证通过：`npm run test -w packages/shared -- mahjong-cocos-project`；`npx tsc --noEmit -p apps/game/mahjong-roguelike/cocos/hulebu-cocos-3.8.8/tsconfig.json`；Cocos Web Mobile 构建产物 + Playwright 截图 `tmp-hulebu-cocos-t236-portrait.png`、`tmp-hulebu-cocos-t236-mobile.png`。

- 完成 T237 胡了卜 Cocos 局内可玩性和观感重排。
- 麻将牌进一步缩小到 `38x51`，牌山缩放收紧；组合按钮缩小到 `64x34` 并下移；底部槽位压进背景木托盘；右侧工具按钮缩小并保持顶层。
- 使用 `debug=false` Cocos Web Mobile 构建重新截图，输出 `tmp-hulebu-cocos-t237b-portrait.png` 和 `tmp-hulebu-cocos-t237b-mobile.png`，去掉 profiler 干扰后局内画面更干净。

- 完成 T238 胡了卜 Cocos 中央堆叠牌山模板。
- 新增 `pyramid` 牌山模板，前 5 关优先使用中央三角堆叠牌山，后续关卡继续使用原有模板轮换。
- HUD 顶部进一步清理：记牌器独立放在关卡牌匾下方，余牌进度拆成普通牌匾，避免“余牌 42”压在记牌器贴图上。
- 验证通过：`npm run test -w packages/shared -- mahjong-cocos-project`；`npx tsc --noEmit -p apps/game/mahjong-roguelike/cocos/hulebu-cocos-3.8.8/tsconfig.json`；Cocos Web Mobile 构建产物 + Playwright 截图 `tmp-hulebu-cocos-t238c-portrait.png`、`tmp-hulebu-cocos-t238c-mobile.png`。
