# 2026-07-05 Lee 进展

## T239 / T240 胡了卜 Cocos 牌山点击错位修复

- 复现并定位 Lee 反馈的“点击牌出现另一张牌 / 上下层仍乱”问题：上一轮调试中把 Cocos `UITransform.getBoundingBoxToWorld()` 当作玩家触摸坐标系使用，但 web-mobile 发布包里该 world rect 与 `EventTouch.getUILocation()` 并不在同一坐标系，导致视觉牌、命中框和 tileId 产生偏移。
- `BoardLayerBinder` 已将命中矩形收口为 `getTileEventRect()`：用当前 `HulebuBoardNodeModel.position` 转换到 `EventTouch.getUILocation()` 的 UI 坐标系。最初复测仍沿用 5% 覆盖率口径，随后已在下节按 demo 要求收紧为 `0.001` 顶层-only。
- 保留 BoardRoot 统一 `TOUCH_END` 入口，继续绕开 Cocos 3.8.8 中 `Button.enabled` / 单牌 Button 状态不可靠的问题；方法命名从旧的 screen/world 口径改为 event rect，避免后续再混淆坐标系。
- 早期 `ring` 模板复测 3 张代表性可点牌：`graph_ring_node-005 / tile.tiao.1`、`graph_ring_node-015 / tile.tong.3`、`graph_ring_node-030 / tile.tong.5`，点击后主槽中的 `tileId` 和 `prefabKey` 均与被点牌一致，余牌均从 30 变 29；该模板随后已被 `long-wall` 取代。
- 早期灰化被压牌入口复测：直接调用 `handleTileClick(graph_ring_node-001)` 后主槽数量保持 0；最终验收已更新为 `graph_long-wall_node-*`。
- Cocos Web Mobile 非 debug 构建通过，CLI 返回既有成功码 36，日志显示 `build Task (web-mobile) Finished`。
- 验证通过：`npm run test -w packages/shared -- mahjong-cocos-project`，31 个测试通过。
- 验证通过：`npx tsc --noEmit -p apps/game/mahjong-roguelike/cocos/hulebu-cocos-3.8.8/tsconfig.json`。

## T239 / T240 胡了卜 Cocos 顶层-only 与 demo 横向牌山修正

- 按 Lee 最新反馈重新收口验收口径：不是“点中几张可点牌即可”，而是被更高层压住的牌必须灰化且不可点击，只允许最上层牌作为入口。
- 覆盖阈值从 5% 收紧到 `0.001`：Cocos 生成 blocker、`GameSceneController.refreshBoardInteractivity()` 和 `BoardLayerBinder` 触摸命中都按“只要更高层实际覆盖就锁住”处理。
- 被锁牌不再只靠透明度；`BoardLayerBinder` 会把被压牌 Sprite 染成灰色，保留下层露边的厚度感，但不再像亮牌一样可选。
- 首关 `validation_intro_peng` 从分散 `ring` 模板改成 `long-wall` 横向压叠模板，更接近 demo 中一条横向牌山的观感；首关仍保持 30 张、6 张可点。
- Kimi WebBridge daemon 已启动，但浏览器扩展未连接；本轮实际验收使用系统 Chrome + Playwright 通过 Node REPL 完成。
- 系统 Chrome 手机视口验收：首关 30 张、6 张亮牌、24 张灰牌，覆盖规则检查 `violations: []`；灰牌 `graph_long-wall_node-001 / tile.tiao.8` 点击后槽位保持空；亮牌 `graph_long-wall_node-005 / tile.tiao.4` 点击后入槽 tileId/prefabKey 与目标一致。
- 截图证据：`.codex-tmp/hulebu-long-wall-final.png`，画面为横向压叠牌山、顶层亮牌、下层灰化露边。

### 2026-07-05 demo 横墙二次收口

- 根据最终截图复盘，`long-wall` 不能再呈现为 6 个孤立小塔，也不能缩成一团；已把模板列距调整到中间值，让 6 张顶层亮牌横向连续展开，下层灰化牌只作为压叠厚度露出。
- 增加浏览器只读调试出口 `globalThis.__HULEBU_DEBUG__`，用于自动验收当前 scene model、强制进入主线首关、调用指定 tile 点击，避免只能靠截图猜测。
- 最终系统 Chrome 手机视口验收：首关 30 张、6 张顶层亮牌、24 张灰化锁牌，牌山运行时 `xSpan: 152 / ySpan: 16`，覆盖规则 `violations: []`。
- 点击矩阵：灰牌 `graph_long-wall_node-001 / tile.tiao.8` 调用点击后槽位仍为空；亮牌 `graph_long-wall_node-005 / tile.tong.8` 点击后入槽为同一 `tileId / prefabKey / label`。
- 最终截图证据：`.codex-tmp/hulebu-final-wall-before-click.png`；点击后截图：`.codex-tmp/hulebu-final-wall-after-click.png`。

### 2026-07-05 旧 active run 自动作废

- 复查 Lee 截图后定位到普通入口仍可能恢复旧局：旧 `hulebu-cocos-active-run` 和账号 `cocosSnapshot` 没有牌山版本字段，入口会继续显示旧的 cross/高塔式牌山与 89 张余牌。
- `GameSceneController` 增加 `HULEBU_BOARD_REVISION = "top-only-long-wall-2026-07-05"`，新 active run 持久化时写入 `boardRevision`；本地或账号侧 snapshot 缺失/不匹配该版本时直接视为不可恢复，旧局不会再出现在“继续本轮”。
- 共享测试 `mahjong-cocos-project.test.ts` 增加 active run 版本字段和本地/账号 snapshot 版本门禁断言，防止后续删掉这层迁移保护。
- 系统 Chrome 发布包复测：预置一份旧 active run 后普通打开，启动阶段 `phase=lobby` 且 `localStorage["hulebu-cocos-active-run"] === null`；进入首关后 `余牌 30`、30 张牌、6 张可点、24 张锁牌、`hasCrossIds=false`、`hasLongWallIds=true`。
- BoardLayer 运行时命中复测：灰色底牌边缘 UI 点不会选牌、不会阻止事件；顶层中心 UI 点只移走 `graph_long-wall_node-005` 并放入槽位，余牌从 30 变 29。
- 截图证据：`.codex-tmp/hulebu-stale-run-cleared-level-1.png`。

### 2026-07-05 Kimi 真浏览器三牌首关收口

- 按 Lee 反馈安装并启动 Kimi WebBridge，真实 Chrome 扩展已连接；用 Kimi 打开 Cocos web-mobile 构建复测当前问题。
- 复现旧构建问题：真实 Chrome 中首关仍是 `余牌 30`、总牌 30、6 张可点、24 张锁牌，且横屏超宽视口下牌面过大。
- 按新口径改为首关 3 张顶层牌：`validation_intro_peng.targetTileCount` 从 30 改为 15，并为第 1 关增加 `resolveCocosOpeningFreeRange()` 特例，opening free 固定为 `{ min: 3, max: 3 }`；后续关仍保持原 5+ opening free 和 36+ 牌量。
- 牌面视觉收小：BoardLayer 牌面尺寸从 `38x51` 收为 `32x43`，顶层缩放从 `1.14` 收为 `1.04`；锁层透明度和锁色下调，增强上下层区分。
- Active run 版本 bump 到 `top-only-three-card-2026-07-05`，避免旧 30/6 首关快照继续恢复。
- Kimi 新构建复测：启动回 lobby 且 active run 为空；进入首关为 `余牌 15`、总牌 15、3 张可点、12 张锁牌、版本 `top-only-three-card-2026-07-05`。
- Kimi BoardLayer 点击矩阵：底牌边缘点 no-op；三张顶层牌分别点击后，slot0 的 `tileId` 均与目标一致，余牌从 15 变 14。
- 截图证据：`.codex-tmp/kimi-current-hulebu-before-fix.png`、`.codex-tmp/kimi-three-card-after-fix.png`。
