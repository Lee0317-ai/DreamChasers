# 2026-06-28 Lee 进展

## 胡了卜

- 修正 Web v6 资源中的 `6条` 牌面误用问题，避免显示成 `4条`。
- 完成 T189 立项：确认旧 Cocos Creator 3.8.8 工程可复用，并记录 Web 冻结后 Cocos 追平顺序。
- 复用结论：保留现有 Cocos 项目壳、场景 Binder、runtime 起点、资源目录和共享测试；后续先同步 v6 UI 与全量牌面映射，再追平牌河、明牌组、补杠、震落、满槽救场、Boss、事件、无尽、每日、高阶和局外成长。
- 完成 T190：将 Web v6 牌面、组合按钮、工具按钮、HUD 和槽位资源接入 Cocos `assets/resources/ui/v6/`；Cocos 牌面 catalog 已切到 v6 路径，组合按钮已接 v6 图片 fallback。
- 完成 T191：Cocos 主槽背板、HUD 背板和右侧工具按钮已接 v6 Sprite，保留程序化 fallback；下一步可开始追有限牌河、明牌组、补杠、震落和满槽救场。
- 完成 T192：Cocos scene model/runtime 已接 `riverNodes / openMeldNodes / bugang`，新增 `MeldRiverLayerBinder` 渲染牌河和明牌组；后续可接丢弃按钮选择态、震落和满槽救场。
- 完成 T193：Cocos 右侧 `打牌` 工具已接入丢弃选择态，选择主槽牌后会进入牌河并刷新场景。
- 完成 T194：Cocos `杠 / 补杠 / 胡` 已接基础震落开山，主槽满且牌河可用时会提示可打牌入河。
- 完成 T195：Cocos `洗牌 / 撤回` 已接真实 runtime 方法，工具链基础形成 `洗牌 / 撤回 / 打牌入河` 三件套。
- 完成 T196：Cocos 第 10/20 关已接 Boss 多目标配置，runtime 会记录组合次数、花色集合和积分目标进度，HUD 会显示 Boss 摘要；通关判断已改为牌山清空且 Boss 目标完成。
- 完成 T197：Cocos 奖励节点不再只是流程推进，三选一奖励会累计到本轮状态并影响后续关卡的备用槽、护符、首败保护、开局铜钱、工具次数和吃/碰/杠分数。
- 完成 T198：Cocos 已接关前特殊事件基础流程，第 `6 / 8 / 10 / 14 / 18` 关前会弹出三选一事件；事件可给本关铜钱、补撤回、禁看山或禁洗牌，效果只作用当前关。
- 完成 T199：Cocos 已新增 `mainline / endless / daily` run mode 基础层；主线仍 20 关结束，无尽从第 21 层循环第 11-20 关配置，每日按 seed 稳定轮换配置。
- 完成 T200：Cocos 已接局外成长基础状态，备用槽、护符、初始工具、河道扩容、开局铜钱和看山预置六轴可以在 run 启动时合入 runtime。
- 完成 T201：Cocos 已接本局流派基础状态，`顺吃流 / 碰开流 / 开杠流 / 追胡流 / 道具流 / 信息流` 可在 run 启动前选定并影响工具、铜钱和组合得分；Controller 新增 `selectRunArchetype()`，后续可接开局选择 UI。
- 完成 T202：Cocos 新 run 启动前会先弹出开局流派选择 UI，六个流派按钮可选择本局打法并进入对应模式首关。
- 完成 T203：Cocos 默认启动先显示局外模式入口，提供 `主线 / 无尽 / 每日` 三个按钮；选择模式后再进入本局流派选择。
- 完成 T204：Cocos 大厅新增 `升级` 入口，局外成长面板可点击升级备用槽、护符、工具、牌河、铜钱、看山六轴，并让下一局 runtime 生效。

## 验证

- 通过：`npm run test -w packages/shared -- mahjong-cocos-project`
- 通过：`npx tsc --noEmit -p apps/game/mahjong-roguelike/cocos/hulebu-cocos-3.8.8/tsconfig.json`
- 通过：`npm run docs:sync`
- 通过：`git diff --check`
