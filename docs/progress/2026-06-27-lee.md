# 2026-06-27 Lee 进展

## T188 胡了卜账号级当前本轮存档

- 根据用户确认，新增 T188，把 T187 的本地 `activeRun` 恢复快照同步到账号进度。
- `HulebuProgress` 已新增 nullable `activeRun` JSON 字段，并补对应 migration。
- 账号进度 GET/POST 现在会读写 `activeRun`。
- `/games/hulebu` 初始同步会比较本地和账号 `activeRun.updatedAt`，选择更新的一份恢复继续本轮。
- 本轮仍只恢复当前关或当前层开局，不恢复牌桌、卡槽、牌河、事件弹窗或奖励选择弹窗。
