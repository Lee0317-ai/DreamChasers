# 2026-06-14 Lee 进展

## T170：胡了卜无尽牌山第一版

- 状态：已完成
- 完成内容：
  - 已新增任务分片 `docs/tasks/items/T170-hulebu-endless-mountain.md`。
  - 已新增领取分片 `docs/tasks/claims/T170-lee.md`。
  - 已记录 `IDEA-20260614-04`，把胡了卜无尽牌山第一版纳入任务池。
  - `/games/hulebu` 局外 `无尽` 面板已开放 `开始无尽`，无尽 run 从第 21 层开始。
  - 外层 iframe URL 已支持 `mode=endless&startLayer=21`，并继续传入三项局外升级 bonus。
  - 外层本地存档新增 `bestEndlessLayer`，局外首页、进行中摘要、结算面板和最近一轮记录都能显示无尽层数。
  - HTML 原型已新增 `runMode` 和无尽层 helper，支持按层数循环后半段压力 profile、每 3 层奖励、每 5 层 Boss 压力。
  - 站内静态 Demo 已同步，保留静态副本的绝对配置路径。
- 验证：
  - `npm run test -w packages/shared -- mahjong-config-playable-prototype`：通过。
  - `npm run test -w apps/web -- hulebu`：通过。
  - `npm run typecheck -w apps/web`：通过。
  - `npm run build -w apps/web`：通过。
  - HTML 内联脚本语法检查：通过，源原型和站内静态副本均可解析。
  - `npm run docs:sync`：通过。
  - 占位符扫描和 `git diff --check`：通过。
  - 桌面端和 390px 移动端 `/games/hulebu` 检查：通过，均可看到无尽入口并进入 `mode=endless&startLayer=21` 牌桌。
- 遗留：
  - 本任务不做每日、成就、高阶周目、云存档、排行榜、付费、广告或 Cocos 正式工程追平。
  - 无尽第一版复用现有密集牌山、Boss goal 和路线型奖励池；后续高阶词缀、专属奖励和层数经济应单独拆任务。
  - 下一步建议开 T171，补每日牌局入口和固定日 seed。

## T169：胡了卜路线型奖励池扩展

- 状态：已完成
- 完成内容：
  - 已新增任务分片 `docs/tasks/items/T169-hulebu-route-reward-pool.md`。
  - 已新增领取分片 `docs/tasks/claims/T169-lee.md`。
  - 已记录 `IDEA-20260614-03`，把胡了卜路线型奖励池扩展纳入任务池。
  - `apps/game/mahjong-roguelike/config/rewards.json` 已从 10 个基础奖励扩展到 16 个奖励。
  - 默认 20 关主线奖励节点已覆盖 `吃流 / 碰流 / 杠流 / 胡流 / 道具流 / 信息流`。
  - 奖励弹层已新增路线标签；已选奖励区会展示 `奖励名 · 路线`。
  - 本轮只复用既有 effect 类型，不新增复杂被动状态机，不修改 `levels.json`。
- 验证：
  - `npm run test -w packages/shared -- mahjong-config-playable-prototype`：通过。
  - `npm run test -w packages/shared -- mahjong-config`：通过。
  - `npm run test -w apps/web -- hulebu`：通过。
  - HTML 内联脚本语法检查：通过，源原型和站内静态副本均可解析。
  - `npm run docs:sync`：通过。
  - 占位符扫描和 `git diff --check`：通过。
  - 桌面端和 390px 移动端 `/games/hulebu` 检查：通过。
- 遗留：
  - 下一步建议开 T170，补无尽牌山入口和 20 关主线后的长期挑战承接。

## T168：胡了卜铜钱资产和 3 项局外升级

- 状态：已完成
- 完成内容：
  - 已新增任务分片 `docs/tasks/items/T168-hulebu-meta-coins-upgrades.md`。
  - 已新增领取分片 `docs/tasks/claims/T168-lee.md`。
  - 已记录 `IDEA-20260614-02`，把胡了卜局外铜钱真实消费和 3 项升级纳入任务池。
  - `/games/hulebu` 局外页的 `备用槽 / 满槽护符 / 初始道具` 已从说明文案升级为真实可购买升级卡。
  - 浏览器本地存档已扩展为“局外铜钱 + 最近结算 + 三项升级等级”。
  - 外层壳开始新一轮主线时，会把 `reserveBonus / shieldBonus / toolBonus` 作为 URL 参数传给 iframe Demo。
  - 原型源文件和站内静态副本都已读取三项 bonus，并把它们接入 `reserveLimit / shields / tools` 开局状态。
- 验证：
  - `npm run test -w apps/web -- hulebu`：通过。
  - `npm run typecheck -w apps/web`：通过。
  - `npm run build -w apps/web`：通过。
  - HTML 内联脚本语法检查：通过，源原型和站内静态副本均可解析。
  - 页面行为检查：通过。预置 `999` 铜钱后，购买首档 `备用槽` 会扣到 `839`，新开 run 的 iframe URL 已带 `reserveBonus=1`。
  - 390px 页面检查：通过，升级页可显示 3 张升级卡和局外铜钱读数。
- 遗留：
  - 下一步建议开 T169，把路线型奖励池和 build 分叉接上，避免 20 关主线只靠基础成长推进。

## T167：胡了卜局外首页和结算面板

- 状态：已完成
- 完成内容：
  - 已新增任务分片 `docs/tasks/items/T167-hulebu-outside-shell-settlement.md`。
  - 已新增领取分片 `docs/tasks/claims/T167-lee.md`。
  - 已记录 `IDEA-20260614-01`，把胡了卜局外首页和结算壳纳入任务池。
  - `/games/hulebu` 已从“直接打开 iframe”改为先显示局外首页，再进入主线 run。
  - 局外页已提供 `开始挑战 / 继续本轮 / 升级 / 图鉴 / 无尽 / 每日` 壳层入口，并补局外铜钱累计预览。
  - 局外页已预留 `备用槽 / 满槽护符 / 初始道具` 三项外置升级说明，为 T168 铺路。
  - 站内壳层已支持 `返回局外`、`继续本轮`，并在通关或失败后展示站内结算面板。
  - 静态 Demo 与原型源文件已增加 `embed=shell` 模式和父页面消息桥接，能向外层壳同步运行中、通关和失败事件。
- 验证：
  - `npm run test -w apps/web -- hulebu`：通过。
  - `npm run typecheck -w apps/web`：通过。
  - `npm run build -w apps/web`：通过。
  - HTML 内联脚本语法检查：通过，源原型和站内静态副本均可解析。
  - 右侧内置浏览器桌面端检查：通过，局外首页、开始挑战、返回局外和继续本轮入口可用。
  - 右侧内置浏览器 390px 移动端检查：通过，局外首页与进入牌桌外层壳无横向溢出。
- 遗留：
  - T167 只补壳层和结算，不开放升级消费。
  - 下一步建议开 T168，把铜钱预览升级为真实可消费资产，并补 3 项外置升级。
