# 2026-07-02 Lee 进展

## T240：胡了卜 Cocos 概念图 UI 架构落地遗留收口

- 修复被压牌仍可点击：`BoardLayerBinder` 对不可交互牌启用 `BlockInputEvents`，并在 `GameSceneController.handleTileClick()` 入口按最新 scene model 再做一次不可点击兜底。
- 收紧首关初始可点牌：首关初始可点数锁定到 5-6 张，避免起手入口过多。
- 修正 `enforceCocosInitialFreeMaximum()` 的边界方向：不再把目标牌自身抬层偏移，而是用不承担压牌职责的自由牌盖到目标牌同坐标上一层。

## 验证

- `npm run test -w packages/shared -- mahjong-cocos-project`：通过，31 tests passed。
- `npx tsc --noEmit -p apps/game/mahjong-roguelike/cocos/hulebu-cocos-3.8.8/tsconfig.json`：通过。

## 备注

- `git pull origin main` 仍因本地分支与远端 main 分叉且未指定 merge/rebase 策略失败，本次未强行处理。
