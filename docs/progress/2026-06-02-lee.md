# 2026-06-02 Lee 进展

- 完成 T098 胡了卜朋友 Demo 第 5-10 关渐进难度曲线。
- 默认玩家 Demo 第 5-10 关新增 difficulty profile：牌量 `72 / 96 / 132 / 168 / 210 / 240`，堆叠深度 `3 / 3 / 4 / 5 / 6 / 6`，字牌权重 `12 / 18 / 24 / 30 / 35 / 40`。
- 第 5 关标题改为“正式入门”，不再从教学关直接跳到 240 张高压；第 10 关在朋友 Demo 中定位为“综合高压”，不叠正式 Boss 目标和胡包，调牌器与正式配置仍保留 Boss 配置。
- 完成 T099 胡了卜试玩页卡槽满槽显示修复和记牌器。
- 默认朋友 Demo `reserveLimit` 为 0，第 8 张入槽后保留在可见主槽，不再静默进入隐藏备用槽；满槽且无组合时提示先发动组合或使用丢弃救场。
- 玩家页记牌器移到牌桌和卡槽之间，默认首屏可见，按 `万 / 条 / 筒 / 字` 展示剩余未移除牌总数和点数数量，并随状态刷新。
- Kimi WebBridge 验证：第 5 关 72 张、首轮可点 8、最大同组 2；第 10 关 240 张、首轮可点 7、最大同组 2；满槽采样 `slotLength=8`、`reserveLength=0`、第 8 张仍在 `slot`，截图保存到 `/tmp/hulebu-t098-t099-after.png`。
- 验证通过：`npm run test -w packages/shared -- mahjong-config-playable-prototype`；`npm run test -w packages/shared -- mahjong-config`；HTML 脚本 `node --check`。
- 完成 T100 胡了卜有限牌河、补杠和胡牌奖励核心玩法设计。
- 玩法规格明确：牌河是有限容量容错，不是无限丢弃；明碰区显示已碰组合，补杠只低收益处理第 4 张；明杠才触发震山开牌；胡牌作为最强局内奖励，清槽、强开山并处理牌河；生成器需要增加孤张预算和恢复路线校验。
- 后续建议拆实现任务，先在 HTML Demo 验证 `牌河 3 格 + 选槽打牌 + 明碰区/补杠 + 明杠开山 + 胡后清河`，确认体验成立后再沉淀到 Cocos 正式工程。
- 完成 T101 胡了卜有限牌河、补杠和胡牌奖励试玩 Demo。
- 默认玩家 Demo 新增 `明牌` 和 `牌河 0/3` 面板；右侧 `丢弃` 进入选择模式，玩家可点击任意槽位牌打入有限牌河。
- `碰` 会进入明牌区，后续第 4 张同牌出现 `补杠` 候选；补杠只升级明牌并清掉孤张，直接 `杠` 才开山，`胡` 会清槽、强开山并清理牌河 1 张。
- 满槽判定改为牌河未满时继续提示打牌，牌河满且无组合/救场时才失败。
- 验证通过：`npm run test -w packages/shared -- mahjong-config-playable-prototype`；`npm run test -w packages/shared -- mahjong-config`；HTML 脚本 `node --check`；Kimi WebBridge 默认玩家页 smoke test，截图保存到 `/tmp/hulebu-t101-smoke.png`。
- 根据 Lee 试玩第 5 关反馈继续修正 T101 生成器：第 5-10 关自然明杠包提高到每关 2 个；明杠目标牌面从普通填充组中排除，避免同一牌面被多个 3 张组稀释。Kimi WebBridge 第 5 关验证：生成 72 张、起手 7 张可点、存在 2 条干净明杠路线，按释放步骤推进到第 3 步出现 `杠 wan-6` 候选；截图保存到 `/tmp/hulebu-t101-level5-kong-routes.png`。
- 补齐 T101 试玩 Demo 的简化听牌提示和孤张预算检查：槽内距离 `3 + 3 + 2` 差 1 张时显示 `听`，差 2 张时显示 `差 ... 可胡`，记牌器高亮牌山仍有的目标牌；第 5-10 关生成器新增 `orphanBudget` 并在生成后检查风险。Kimi WebBridge 验证 `听：9筒`、`差：2条 / 9筒 可胡`，第 5 关仍有 2 条干净明杠路线且孤张风险 `1/1`；截图保存到 `/tmp/hulebu-t101-hu-hint-scrolled.png`。
- 按 Lee 对 `杠开山` 的反馈修正 T101 开山行为：明杠和胡牌开山不再直接移除压顶牌，而是把压顶牌震落到桌面平铺层，保留为可点击入槽的 `board` 牌，并解除其对下层牌的阻挡。Kimi WebBridge 验证震落牌按钮未禁用，点击后进入卡槽；后续发布前反馈已把直接 `杠` 调为震落 1 张、`胡` 调为震落 3 张。
- 根据 Lee 继续试玩反馈修正 T101 记牌器和动作栏：记牌器只统计牌山 `board` 牌，卡槽、牌河、明牌区和移除区不再计入；`吃 / 碰 / 杠 / 补杠 / 胡` 拆到卡槽上方独立动作栏，移动端补底部留白，避免固定道具栏遮盖卡槽。Kimi WebBridge 验证入槽前 `万 1`、入槽后 `万 0`；390px headless Chrome 验证无横向溢出、按钮文字不挤出、底部道具栏不遮盖卡槽；截图保存到 `/tmp/hulebu-t101-counter-layout-v2.png` 和 `/tmp/hulebu-t101-counter-layout-mobile-v2.png`。
- 根据 Lee 发布前反馈继续修正 T101/T102：直接 `杠` 震落 1 张压顶牌，`胡` 震落 3 张压顶牌；默认玩家页隐藏内部标题栏，修正旧 grid 行撑高记牌器的问题，牌面回到 390px 下约 `40x54`，记牌器改为上方牌面、下方余牌数量。T102 静态发布副本已同步，390px Playwright 截图保存到 `/tmp/hulebu-t102-mobile-counter-split-final.png`。
- 根据 Lee 提供的高堆叠参考图创建 T103 胡了卜立体窄腰牌山模板设计。当前结论为只借鉴“上层大平台、中段窄腰、底部支撑柱、侧向散牌”的结构，不复制参考图美术、牌面、颜色、文案或具体布局；推荐后续新增 `悬台窄腰 / suspended-waist` 模板，先接入调牌器验证，再决定是否进入第 8-10 关随机池和发布副本。
- 完成 T104 胡了卜悬台窄腰模板调牌器实现：`suspended-waist` 已接入 HTML 原型和 `/games/hulebu-demo/tuner.html`，但不进入默认玩家页 auto 池。Kimi WebBridge 验证 `template=suspended-waist` 下生成 240 张牌、首轮可点 8 张、同组最多露出 2 张；390px 截图保存到 `/tmp/hulebu-t104-suspended-waist-mobile.png`。
