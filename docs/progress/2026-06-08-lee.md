# 2026-06-08 Lee 进展

## T145：弹珠机福利玩法规划落档

- 状态：待验收
- 完成内容：
  - 登记 IDEA-20260608-01：弹珠机福利玩法和 AI 积分奖励边界。
  - 新增任务分片 `docs/tasks/items/T145-marble-pachinko-reward-planning.md`。
  - 新增领取分片 `docs/tasks/claims/T145-lee.md`。
  - 新增模块文档目录 `docs/modules/marble-pachinko/`。
  - 明确 `paidCredits`、`bonusCredits`、`playTickets`、`gameCoins` 四类资产边界。
  - 明确优先考虑“充值赠送游戏次数”，不建议第一阶段直接售卖概率游戏次数。
- 验证：
  - `npm run docs:sync`：通过。
  - 占位符扫描：通过。
  - `git diff --check`：通过。
- 遗留：
  - 后续如果进入实现，先做无奖励物理原型，再单独评审平台福利和概率奖励规则。

## T146：AI Gateway MVP 运行时与模型 API

- 状态：已完成
- 完成内容：
  - 新增 Prisma 模型 `AiGatewayRequestLog` 和迁移 `20260608221000_add_ai_gateway_request_log`。
  - 新增 `apps/web/src/lib/ai/**` 运行时骨架，覆盖 capability/credential source、模型目录、gateway 校验、provider 适配器、mock provider、OpenAI-compatible 壳和请求日志写入。
  - 新增 `GET /api/ai/capabilities/[capability]/models` 与 `POST /api/ai/tasks`。
  - 复用现有 `CreditWallet` / `CreditLedger`，新增平台积分扣减函数 `chargePlatformCreditsForUser()` 供 AI Gateway 调用。
  - `/account/ai` 已统一重定向到 `/account/ai/credits`，与账号中心第一阶段收口保持一致。
  - `积分管理` 页面升级为 AI Gateway 控制面，展示平台积分余额、可用 capability 数、模型目录、凭据来源策略、最近 Gateway 请求和最近积分账本。
  - 账号概览页 `积分管理` 快捷入口文案已同步为“查看余额、模型目录、凭据模式和 AI 使用账本”。
  - 清理 Prisma 生成文件尾随空格，恢复 `git diff --check` 可用状态。
- 验证：
  - `npm run test -w apps/web -- account-ai-overview model-catalog ai-gateway account-ai-config`：通过。
  - `npm run test -w apps/web -- account-ai-overview model-catalog ai-gateway account-ai-config mock-provider timepick-fortune-chat timepick-recognition`：通过。
  - `npm run typecheck -w apps/web`：通过。
  - `npm run build -w apps/web`：通过。
  - `npm run docs:sync`：通过。
  - `git diff --check`：通过。
  - HTTP 烟测：`GET /api/ai/capabilities/structured_extraction/models` 返回 `200`；未登录 `POST /api/ai/tasks` 返回 `401`；`HEAD /account/ai` 返回 `307 -> /account/ai/credits`。
- 遗留：
  - PDF 工具箱 / AI 修图还没有真实接到 Gateway。

## T147：TimePick 运势聊天接入 AI Gateway 首条真实产品链路

- 状态：已完成
- 完成内容：
  - 登记 IDEA-20260608-02，新增 T147 任务分片和领取分片。
  - 新增 `apps/web/src/lib/timepick/timepick-fortune-chat.ts`，统一构造 TimePick 运势聊天的 AI Gateway 请求和响应映射。
  - `apps/web/src/app/api/timepick/fortune/chat/route.ts` 已从本地占位文案切到 `runAiGatewayTask()`。
  - `apps/web/src/lib/ai/providers/mock-provider.ts` 已补 `text_generation` mock 输出，能根据输入返回稳定中文建议。
  - `apps/web/src/lib/ai/ai-gateway.ts` 已把“平台积分不足”映射为结构化 `AiGatewayError`，便于产品侧按 `402` 处理。
  - 登录事件已接入 `ensureStarterPlatformCreditsForUser()`，首次登录会幂等赠送 `20` 平台积分。
  - AI Gateway 已补扣费后失败退款补偿，provider/内部链路失败时会写入 `refund` 账本回滚。
  - 当前数据库已手工执行 `20260608221000_add_ai_gateway_request_log` SQL，补齐本地真实链路缺失的表。
- 验证：
  - `npm run test -w apps/web -- account-security ai-gateway mock-provider timepick-fortune-chat`：通过。
  - `npm run typecheck -w apps/web`：通过。
  - `npm run build -w apps/web`：通过。
  - HTTP 烟测：未登录 `POST /api/timepick/fortune/chat` 返回 `401`。
  - 真实成功烟测：临时测试账号真实登录后调用 `POST /api/timepick/fortune/chat` 成功；数据库验证平台钱包余额 `19`，最近账本为 `grant 20 / usage -1`，并写入 `AiGatewayRequestLog(text_generation, mock-structured-fast, timepick, timepick-fortune-chat, succeeded)`；测试数据已清理。
- 遗留：
  - 当前仍使用 mock provider，真实 provider 和更细的 prompt/结果约束后续继续迭代。
  - 同样链路后续可继续扩展到 PDF / AI 修图中的下一条能力。

## T148：TimePick URL 自动识别接入平台 AI Gateway

- 状态：已完成
- 完成内容：
  - 新增 `apps/web/src/lib/timepick/timepick-recognition.ts`，统一构造 URL 自动识别的 Gateway 请求与输出映射。
  - 新增 `POST /api/timepick/recognition/url`，复用登录态校验、TimePick CORS 和 `AiGatewayError` 返回结构。
  - `mock-provider` 已补 URL 识别场景的 `structured_extraction` 输出，可稳定返回基于 hostname 的标题和摘要。
  - 新增 `apps/web/src/lib/timepick/__tests__/timepick-recognition.test.ts`，覆盖识别输入契约、输出映射和缺字段回退。
  - TimePick 前端已新增 `recognizeTimePickUrl()`，并把 `ResourceDialog` / `ResourceCard` 的“自动识别”从本地占位切到 DreamChasers API。
- 验证：
  - `npm run test -w apps/web -- ai-gateway mock-provider timepick-recognition timepick-fortune-chat`：通过。
  - `npm run test -w apps/web -- account-ai-overview model-catalog ai-gateway account-ai-config mock-provider timepick-fortune-chat timepick-recognition`：通过。
  - `npm run typecheck -w apps/web`：通过。
  - `npm run build -w apps/web`：通过。
  - `npm run build`（TimePick）：通过。
  - `npm run docs:sync`：通过。
  - `git diff --check`（DreamChasers / TimePick）：通过。
  - HTTP 烟测：未登录 `POST /api/timepick/recognition/url` 返回 `401`。
  - 真实浏览器验证：桌面端 `ResourceDialog` 与移动端 `ResourceDialog` 点击“自动识别”均会回填结果；移动端 `ResourceCard` 点击“识别”会出现成功 toast。
  - 数据库验证：最近三条 `AiGatewayRequestLog(timepick-url-recognition)` 均为成功记录，平台积分账本余额按账本汇总为 `17`。
- 遗留：
  - 当前仍使用 mock provider，后续如需更接近真实效果，再补网页元信息抓取或真实 provider。
