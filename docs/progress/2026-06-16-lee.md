# 2026-06-16 Lee 进展

## T175 胡了卜网页版完整版路线重排和缺口拆分

- 记录 Lee 的新路线：先完成 `/games/hulebu` 网页版完整版，再补 Cocos、音乐、美术、动效和发布资源。
- 新增 T175 任务分片和领取分片，确认本轮只做文档和任务体系，不改 Web Demo、Cocos 工程、共享包或资源。
- 新增 Web 完整版优先路线规格，盘点当前 Web 已完成内容和相对完整版缺口。
- 更新胡了卜 README、PROGRESS、HANDOFF、IMPLEMENTATION_PLAN 和 DECISIONS，移除“下一步优先 Cocos 追平”的路线。
- 后续建议顺序为：高阶周目完整版、Boss 试炼第二版、特殊事件池扩容、成就图鉴扩容、无尽/每日深度化、路线奖励和局外能力深化、Web 数值平衡和内容冻结；Cocos 和音画资源后置。

## T176 胡了卜高阶周目完整版

- 已完成高阶周目第一大块：`/games/hulebu` 高阶入口扩展为 `东风场 / 南风场 / 西风场 / 北风场` 四档，局外新增 `高阶配置` 面板。
- 内层试玩页已接入高阶能力槽、专属奖励、档位事件、构筑识别、失败复盘和结算复盘。
- 新增二级高阶构筑线：`封尾流`、`试火流`、`迟尾流`，并补了对应专属事件和奖励链。
- 高阶失败反馈已拆成 `目标完成度 / 容错耗尽 / 节奏断档 / 构筑失配` 四类，结算页也会显示结构化复盘。
- 站内静态 Demo 已同步，验证命令、类型检查、构建和脚本检查继续通过。

## T177 胡了卜 Boss 试炼第二版

- 已完成 Boss 试炼第二版：HTML 原型新增 `BOSS_TRIAL_PHASES` 和 `BOSS_TRIAL_VARIANTS`。
- 第 10 关中段试炼、第 20 关胡了卜王、高阶 Boss 和无尽 Boss 现在有可区分变体、阶段目标和 `Boss 奖励品质`。
- 内层通关/失败消息新增 `bossReview` payload，包含 Boss 变体、阶段、奖励品质、关键目标、缺口和下一轮建议。
- `/games/hulebu` 外层结算页新增 `Boss 复盘` 卡片，并兼容旧本地存档。
- 桌面端和 390px 移动端已检查高阶 Boss：`Boss 奖励品质 / 阶段目标 / 高阶 Boss 变体` 可见，高阶 Boss 不出现普通教程目标，移动端无页面级横向溢出。

## T178 胡了卜特殊事件池扩容

- 已完成特殊事件池第二版第一轮扩容：HTML 原型新增 `SPECIAL_EVENT_RARITIES`、`SPECIAL_EVENT_TAGS` 和 `EVENT_BUILD_LINKS`。
- 普通 run 新增 `河灯旧约 / 封盘押后 / 险招翻倍` 三个第二版事件；高阶 run 会按 `河控流 / 稳压流 / 封盘流` 等构筑线优先出现更贴近的事件。
- 事件弹层现在会显示 `稀有事件 / 构筑联动 / 高阶事件` 等提示；事件结果仍复用现有 `coins / tool / comboBonus / shield / reserve / river / modifier` 效果口径。
- `/games/hulebu` 外层结算页新增 `最近事件` 卡片，能跟着最近一轮一起显示事件摘要。
- 当前测试、类型检查、构建和原型/静态 Demo 内联脚本检查已通过。

## T179 胡了卜成就图鉴扩容

- 已完成图鉴第二版第一轮扩容：`/games/hulebu` 的 `图鉴` 面板从 8 张平铺卡扩成 `图鉴总览 / 分类进度 / 分组列表` 结构。
- 成就表新增 Boss、事件、高阶和路线目标，当前图鉴覆盖主线碑记、Boss 纪录、无尽牌山、每日留痕、局外积累、高阶征途、事件见闻和路线收束 8 个分类。
- 新增隐藏目标态：未解锁的高阶 / Boss / 事件成就会先以 `未揭示目标` 和模糊提示出现，解锁后再显示真实标题与说明。
- 旧存档 hydration 继续复用 `bestEndlessLayer / dailyBestLevels / bestAscensionLevel / upgrades / lastSettlement / bossReview / specialEventReview` 自动补算新增成就，不新增后端字段。
- 当前 `npm run test -w apps/web -- hulebu`、`npm run typecheck -w apps/web`、`npm run build -w apps/web`、`npm run docs:sync` 已通过；接下来补 `git diff --check` 和浏览器布局确认后收尾提交。

## T180 胡了卜无尽和每日深度化

- 已完成无尽和每日深度化：HTML 原型新增 `getEndlessChapterProfile()` 和 `getDailyMutatorProfile()`。
- 无尽第 21 层后开始按每 5 层一个章节推进，章节尾部会抛出 `章节 Boss` 信号，并同步给外层壳层结算与局外页。
- 每日牌局第二版新增 `今日词缀 / 今日奖励 / 连续参与` 三个信号，原型通关文案和 `/games/hulebu` 局外页、结算页都会显示。
- `/games/hulebu` 的 `无尽` 面板现已显示 `当前章节 / 章节 Boss / 无尽最高`，`每日` 面板现已显示 `今日词缀 / 今日奖励 / 连续参与 / 今日最佳`。
- 站内静态 Demo 已同步为 `/games/hulebu-demo/config/*.json` 绝对资源路径，`apps/web` 发布测试与共享原型测试都已通过。
- 当前验证通过：`npm run test -w packages/shared -- mahjong-config-playable-prototype`、`npm run test -w apps/web -- hulebu`、`npm run typecheck -w apps/web`、`npm run build -w apps/web`、`npm run docs:sync`、`git diff --check`。
