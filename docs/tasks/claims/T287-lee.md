# T287 领取记录

- 任务：T287 胡了卜完整 UI 全新视觉方向概念图
- 负责人：Lee
- 状态：进行中
- 领取时间：2026-08-14
- 修改范围：`output/hulebu-ui-design-v2/**`、T287 任务/领取分片和相关进展文档；不覆盖 T287 v1 概念资源。
- 下一步：先生成并确认粉色球形胡萝卜透明母稿，再用统一角色标准重绘首轮 8 张 UI 概念图；确认前不接入 Cocos。
- 当前：粉色球形胡萝卜母稿已确认，正在生成 8 张 mascot-v2 流程图。
- 结果：mascot-v2 8 张流程图已生成并完成视觉检查，等待 Lee 对文字误差和下一轮重绘范围确认。
- 当前：主玩法牌山方向已按 v5 和遮挡可见性规则确认，正在补齐其他 UI 状态图；确认前不接入 Cocos。
- 结果：ui-states-v6 10 张其他 UI 状态图已生成并完成检查，等待 Lee 对可用画面和需重绘画面确认。
- 当前：已按 Lee 要求转向组件级切图生产，第一批生成核心局内透明 PNG 组件。
- 下一步：放弃低可用率的第一批组件母版作为主要素材来源，按 generate2dmap 重做 foundation-only 与空白面板优先的 UI 素材包。
- 结果：asset-pack-v2 已完成 92 个独立组件和 manifest，等待 Lee 验收后进入正式资源映射。
- 当前：正在生成独立麻将牌面和牌背素材包。
- 当前：正在按“新牌壳 + 现有牌面内容”规则生成正式 tile-pack-v1 全套麻将牌。
- 当前：正在生成六个可爱风格牌面样张，确认后再批量迁移全套牌面。
- 结果：六张样牌方向已确认，并完成 35 张完整麻将候选素材、标准化成品、提示词、资源清单和完整拼版；等待 Lee 对全套牌面与牌背确认，确认前不接入 Cocos。
- 追加修正：按 Lee 反馈重做 5条、7条、8条，使竹子独立分离且数量一眼可数；旧稿已隔离，完整拼版已更新。
- 当前：component-pack-v3 第一组五个独立动作按钮已生成并完成尺寸/透明通道标准化，等待 Lee 确认材质与配色。
- 结果：第二组记牌器、碰池、震落区和单个手牌槽组件已生成并完成尺寸/透明通道标准化，等待 Lee 确认后继续工具按钮和 HUD。
- 结果：第三组工具按钮和 HUD 已生成并完成尺寸/透明通道标准化，等待 Lee 确认后继续登入、关卡选择和结算流程独立组件。
- 追加修正：按 Lee 反馈统一四个工具按钮的外壳、底座、边框、光照和尺寸，只保留中心图标差异。
- 当前：局外流程六个独立组件已生成并完成标准化，包含登入标题、模式卡、关卡节点、奖励卡、胜利印章和失败印章。
- 结果：大厅与地图六个骨架组件已生成/派生并完成标准化，包含头像框、货币牌、底部导航、章节牌、路径段和星星进度牌。
- 结果：模式选择和地图状态八个组件已生成并完成标准化，包含四个模式图标和四种关卡节点。
- 结果：事件/中断六个组件已生成并完成标准化，事件卡与升级卡已统一外壳，包含暂停和设置两种模态面板及风险/奖励标签。
- 结果：吉祥物五个姿态和三类通用图标已生成并完成标准化，失败态已修正为无粗黑外轮廓版本。
- 结果：四张独立竖屏背景已生成并完成尺寸检查，包含大厅、地图、牌局和结算；仍未接入正式页面。
- 追加修正：局内牌桌背景已去除烘焙手牌槽，改由独立手牌槽组件运行时叠加。
- 结果：已完成四张页面级合成预览，用已确认组件检查登入、地图、局内和结算的布局关系，仍未接入正式页面。
- 结果：新增程序化堆叠牌山预览，验证独立牌面素材按菱形坡面和层级压盖组合，后续接入时保持遮挡规则可计算。
- 当前：按 Lee 要求转向侧视图牌山，已生成侧视盖牌并完成一版山体预览；下一步需要为侧向可点正面牌单独制作牌壳。
- 迭代：侧视山体改为六层金字塔并让正面牌按坡面斜置，预览已通过像素校验，等待 Lee 确认后再合成侧视正面牌壳。
- 方向变更：Lee 否定侧视方案，已回归俯视图并接入正式牌山生成器坐标；center-tower 主预览与 two-wings/cross/ring 对照预览已生成，等待 Lee 选择默认模板。
