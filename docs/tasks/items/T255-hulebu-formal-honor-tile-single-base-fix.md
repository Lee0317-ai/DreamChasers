# T255：胡了卜正式字牌与背面单层底座统一

- 优先级：P0
- 负责人：Lee
- 状态：已完成
- 依赖：T254 字牌与背面底唇绿色化
- 阻塞：无
- 允许修改文件：Batch C 构建脚本、formal v1 的 7 张字牌与 1 张背面、牌面预览、manifest/validation report、T248/T255 分片、麻将模块文档、当天进展/完成记录及 `npm run docs:sync` 自动生成主文档
- 禁止修改范围：其他 27 张数字牌、卡片/弹窗、Batch A+B、Cocos 工程、玩法规则、Web 试玩版、横屏、微信小游戏 SDK 和其他模块
- 验证方式：8 张目标资源底座与 `wan-01` 标准下半部逐像素一致；T253 alpha/RGB 门禁回归；35 张牌、80 key 和预览检查；`npm run docs:sync`；UTF-8 无 BOM；`git diff --check`

## 目标

消除 `东 / 南 / 西 / 北 / 中 / 发 / 白 / 背面` 牌底部的双层绿色结构，使其与万筒条保持同样的单层绿色底座。

## 实现方式

- 删除 T254 的额外底唇渐变重绘。
- 以 `wan-01` 为标准牌体参考，只复制不涉及牌面内容的下半部牌体像素到 8 张目标资源。
- 保留目标牌的文字、白板边框、牌背图案和透明区清理。

## 不做

- 不修改万筒条数字牌。
- 不重新生成美术母版。
- 不接入 Cocos。

## 完成结果

- 删除 T254 的额外绿色渐变层，目标 8 张牌恢复为与数字牌一致的单层绿色底座。
- 从 `y=288` 起复制标准空白牌体；该区域与 `wan-01` 标准下半部逐像素完全一致。
- 东南西北、中发白的文字与白板边框保留，牌背图案保留。
- formal v1 保持 35 张麻将牌、80 个唯一资源 key；validation report 状态为 `passed`。
- 其他 27 张数字牌和 Cocos 工程未修改。
