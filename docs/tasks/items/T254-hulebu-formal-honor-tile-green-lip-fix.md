# T254：胡了卜正式字牌与背面底唇绿色化

- 优先级：P0
- 负责人：Lee
- 状态：已完成
- 依赖：T253 字牌与背面透明底修正
- 阻塞：无
- 允许修改文件：Batch C 构建脚本、formal v1 的 7 张字牌与 1 张背面、牌面预览、manifest/validation report、T248/T254 分片、麻将模块文档、当天进展/完成记录及 `npm run docs:sync` 自动生成主文档
- 禁止修改范围：其他 27 张数字牌、卡片/弹窗、Batch A+B、Cocos 工程、玩法规则、Web 试玩版、横屏、微信小游戏 SDK 和其他模块
- 验证方式：8 张目标资源底唇区域浅色像素检查；T253 alpha/RGB 门禁回归；35 张牌、80 key 和预览检查；`npm run docs:sync`；UTF-8 无 BOM；`git diff --check`

## 目标

把 `东 / 南 / 西 / 北 / 中 / 发 / 白 / 背面` 牌体底部仍可见的浅米色侧壁改为与绿色底座连续的绿色过渡。

## 实现方式

- 只处理标准牌体底部侧壁区域，不修改牌面、字形、金色边线和绿色底座轮廓。
- 将区域内的浅米色像素按纵向位置映射为深浅绿色，并保留原有明暗纹理。
- 在 Batch C 构建脚本和 validation report 中固化底唇颜色门禁。

## 不做

- 不裁短牌体。
- 不修改万/筒/条数字牌。
- 不接入 Cocos。

## 完成结果

- 仅重建 `东 / 南 / 西 / 北 / 中 / 发 / 白 / 背面` 8 张目标资源及牌面预览。
- 从牌体底部 `y=312` 起识别并替换白色、米色和棕色残留；原有绿色像素保留，最下方统一为连续绿色过渡。
- 8 张目标资源继续共享标准 alpha bbox `(27, 23, 246, 354)`，完全透明像素 RGB 保持清零。
- formal v1 保持 35 张麻将牌、80 个唯一资源 key；validation report 状态为 `passed`。
- 其他 27 张数字牌和 Cocos 工程未修改。
