# T281 通关结算提示优化

- 负责人：Lee
- 范围：Cocos 麻将 Roguelike 通关结算层渲染、相关回归断言。
- 目标：通关提示在 390×844 设计尺寸内完整居中；宽窗口下遮罩覆盖整个视口；标题、统计、奖励和继续按钮不互相遮挡；继续按钮保持可点击。
- 不在范围：关卡规则、得分计算、奖励内容、存档、牌局布局、原始 UI 图片资源。
- 验证：共享包测试、TypeScript 检查、production 构建，并在本地浏览器检查通关层。

## 结果

- 已完成清屏、统一结算底板、三列统计与通关按钮输入修复。
- 共享测试 43/43 通过，Cocos TypeScript 检查通过，`git diff --check` 通过。
- exact-commit 构建暂未执行：提交前构建脚本按设计拒绝 dirty formal input；提交后可直接运行 build 与 verify-only。
