# T287：胡了卜完整 UI 全新视觉方向概念图

- 任务编号：T287
- 负责人：Lee
- 状态：进行中
- 优先级：P0
- 目标：在不继承现有青瓷茶馆视觉的前提下，生成覆盖登入、模式/关卡选择、竖屏牌局和结算流程的完整 UI 概念图，用于选择胡了卜完整 UI 的全新美术方向。
- 允许修改文件：`output/hulebu-complete-ui-redesign-v1/**`、`output/hulebu-ui-design-v2/**`、本任务分片、领取分片、`docs/tasks/CHANGE_INTAKE.md`、当天进展、麻将模块进展、完成记录及 docs:sync 主文档。
- 禁止修改文件：`apps/**`、`packages/**`、现有 `output/hulebu-ui-assets/**`、现有 Cocos 正式资源、玩法规则、关卡、存档、Web Demo。
- 隔离规则：本轮生成图片进入独立的 `output/hulebu-ui-design-v2/images/`，人工提示词进入对应的 `prompts/`，预览进入 `previews/`；不复制到 Cocos 或既有 UI 资源目录，也不覆盖 T287 v1 概念资源。
- 当前产出：首轮已生成 8 张统一 `864×1152` 的 PNG，覆盖登入/模式/地图、局内主玩法、组合候选、弃牌救场、记牌器与碰池、通关奖励、Boss 地图、失败与暂停；8 份提示词独立存放，等待 Lee 评审后再定向重绘。
- 验证：8 张 PNG 存在且尺寸一致；8 份提示词独立存在；视觉检查；敏感文字与赌博暗示检查；API Key 扫描；UTF-8 无 BOM；`git diff --check`。
- 追加视觉要求：粉色胡萝卜吉祥物参考用户提供的粉色球形角色，保留圆润粉色体块、蓝色大椭圆眼睛和简洁黑色弯嘴，同时加入绿色胡萝卜叶冠与小手小脚；先生成透明背景母稿，再用于整套 UI 重绘。
- 追加产出：已确认粉色球形胡萝卜母稿，后续 8 张流程图重绘版本进入独立的 output/hulebu-ui-design-v2/images/mascot-v2/，对应提示词进入 output/hulebu-ui-design-v2/prompts/mascot-v2/，不覆盖首轮图。
- 当前产出：mascot-v2 已生成 8 张统一 864×1152 PNG，覆盖登入/模式/地图、局内主玩法、组合候选、弃牌救场、记牌器与碰池、通关奖励、Boss 地图、失败与暂停；等待 Lee 评审文字误差和是否需要定向重绘。
- 追加玩法图规则：主玩法 v3 使用锥形实体堆叠牌山；不做数字层数标注；顶层可点，下层只要关键牌面没有被盖住也可点；真正被覆盖的牌显示盖牌；粉色球形胡萝卜不遮挡牌山。
- 追加玩法图修正：v5 牌山减少正面牌比例，只有少量关键可见牌显示真实牌面；组合按钮必须位于手牌槽上方独立一排，底部必须保留独立 8 格手牌槽。
- 当前玩法规则确认：采用遮挡可见性规则；看得到牌面就正面可点，看不到牌面就牌背不可点；不做翻面动画，不做数字层数标注；v5 为主玩法牌山方向基准。后续继续补齐其他 UI 状态，全部确认后再合并到主页面。
- 当前产出：ui-states-v6 已生成 10 张统一 864×1152 PNG，覆盖登入/模式/地图、组合候选、弃牌救场、记牌器与碰池、通关奖励、Boss 地图、失败暂停、开局流派、局外升级、关前事件；文字和局内局部布局仍待 Lee 评审后定向修正，确认前不接入 Cocos。
- 追加交付方式：Lee 确认所有 UI 需要拆成独立组件，整屏图只作为布局参考；后续开始生成透明 PNG 组件库，组件、提示词和拼版预览继续隔离在 output/hulebu-ui-design-v2/components/，确认前不接入 Cocos。
- 追加生产方式：上一批组件母版可用率不足，后续按 generate2dmap 的生产原则重做为 foundation-only 与空白面板优先的 UI 素材包；先生成不带文字、不带标题、不带占位物的基础层，再生成按钮、牌面、记牌器、槽位、地图节点等可裁切组件，并输出 manifest 与拼版预览。
- 当前产出：asset-pack-v2 已生成 11 张生产母版和 92 个独立组件，包含 foundation 面板、动作按钮、工具按钮、麻将牌、槽位河牌、记牌器、地图节点、奖励事件升级、HUD、震落/碰池和吉祥物动作；输出 asset-pack-v2-manifest.json 与拼版预览，等待 Lee 验收后进入 Cocos 命名/九宫格映射。
- 追加专项：Lee 要求先生成麻将牌面和牌背；后续在 output/hulebu-ui-design-v2/tile-pack-v1/ 单独生成可裁切麻将牌素材，不混入其他 UI 组件。
- 追加合成规则：麻将牌面内容以现有资源为准，不让 Grok 重写点数、花色或字牌；新风格只负责牌壳、厚度、牌背和状态，正式输出使用本地合成生成完整牌面预览。
- 追加牌面方向：Lee 确认现有牌面语义保留，但需要评估可爱风格；后续先生成 1万、2条、3筒、东、中、发六个可爱风格样张，确认后再批量迁移全套牌面。
- 麻将牌阶段结果：已通过 PPTOKEN `gpt-image-2` 参考图编辑完成 35 张候选素材，包含万/条/筒各 1–9、东南西北中发白和粉色胡萝卜牌背；原立牌面负责牌义、数量与构图，新图负责萌系陶瓷牌壳和符号表现。
- 生产规格：原始生图进入 `tile-pack-v1/generated-v3/images/`，提示词进入 `prompts/`，标准化成品进入 `normalized/`；所有成品统一为 `1024×1024 RGBA PNG`、透明背景、中心锚点和一致主体高度。
- 质量修正：一筒网格残影和白牌多余方框已隔离至 `images/rejected/` 并定向重生成；最终完整拼版为 `previews/full-tile-pack-v3-contact-sheet.png`，资源清单为 `tile-pack-v3-manifest.json`。
- 当前边界：麻将候选素材包已完成，尚未复制到 Cocos 正式资源，也未合并到主玩法页面；等待 Lee 对完整拼版确认后继续其他独立 UI 组件。
- 定向修正：Lee 指出 5条、7条、8条在小尺寸下不够容易数清；已将三张重生成成 5/7/8 根独立竹子、明确留白和可数结构，旧稿隔离到 `images/rejected/`，完整拼版与 manifest 已更新。
- 二次修正：Lee 明确 8条需要延续原始 W/M 组合，而不是独立柱子；已再次重生成，上方保留 W、下方保留 M，仅收窄整体宽度并细化竹节，误解稿继续隔离在 `images/rejected/`。
- 下一阶段：新增 `component-pack-v3/`，先按已确认麻将牌材质重做胡/杠/碰/吃/补杠五个独立动作按钮；四个单字按钮标准化为 `512×512 RGBA`，补杠标准化为 `768×512 RGBA`，等待 Lee 确认后继续记牌器、碰池/震落区和 8 格手牌槽。
- 第二组组件：已完成记牌器收起挂件、记牌器展开空白面板、碰池面板、震落牌区和单个可复用手牌槽；面板类不写死文字/数字，手牌槽标准化为 `150×220 RGBA`，可由运行时复用成 8 格。
- 第三组组件：已完成洗牌、撤回、弃牌、看山四个独立工具按钮，以及关卡、分数、剩余牌、Boss 四个空白 HUD 牌体；工具按钮和 HUD 均为透明 RGBA，运行时文字/次数留空。
- 工具按钮修正：Lee 指出洗牌、撤回、弃牌、看山四个按钮外壳差异过大；已统一使用洗牌按钮作为母版，只替换中心图标，三张旧版本已隔离到 `images/rejected/`，工具预览已更新。
- 局外流程组件：已完成登入标题牌、模式入口卡、关卡节点、奖励卡、胜利印章和失败印章六个独立透明组件；标题牌保留“胡了卜”，模式/奖励内容区域留给运行时，节点和印章不依赖整屏背景。
- 大厅/地图骨架：已完成头像框、货币牌、底部导航框、章节牌、可连接路径段和三槽星星进度牌；星星进度牌在上游模型路由异常与 Grok 限流时由章节牌母版本地确定性派生，保持同材质并避免额外失败请求。
- 模式/节点状态：已完成主线、每日、无尽、进阶四个模式图标，以及普通、事件、奖励、Boss 四种地图节点状态；模式图标统一画布，普通/事件/奖励节点统一尺寸，Boss 节点单独放大。
- 事件/中断组件：已完成事件卡、升级卡、暂停面板、设置面板、风险标签和奖励标签；事件卡与升级卡已二次统一为同一母版外壳，内容区域全部交给运行时。
- 吉祥物/通用图标：已完成粉色胡萝卜吉祥物的待机、引导、开心、思考、失败五个状态，以及货币、护符、星星三类通用图标；失败态曾产生粗黑描边，已以待机态母版重做并隔离旧稿。
- 背景层：已完成登入大厅、模式/地图、局内牌桌、结算奖励四张独立竖屏背景，统一为 `1024×1536`；中心区域分别预留标题、地图节点、牌山/HUD、结算印章与奖励卡的摆放空间，不烘焙任何 UI 文字或按钮。
- 背景修正：Lee 指出局内背景底部不应烘焙 8 格手牌槽，因为独立手牌槽组件已完成；已重做局内牌桌背景，只保留中央牌山区和上下留白，旧版隔离到 `backgrounds/images/rejected/`。
- 页面合成预览：已用独立组件拼出登入大厅、模式/选关地图、局内主玩法、结算奖励四张 `1024×1536` 布局稿，输出到 `component-pack-v3/previews/page-*-v3.png`；仅作为页面级尺寸与层级检查，不接入 Cocos。
- 牌山方案：已按 Lee 反馈改用独立麻将素材程序化堆叠，不再把牌山作为单张生成图；新增 `build_gameplay_mountain.py` 验证 7/5/3/1 菱形坡面、层级压盖和少量正面露头，预览输出到 `page-gameplay-v3-mountain.png`。
- 牌山视角修正：Lee 明确主玩法牌山采用侧视图而不是俯视图；已新增 `tile-side-back` 侧视盖牌和 `build_gameplay_mountain_side.py` 验证 7/6/5/4/3 层侧向山体，正面牌暂时仍用正视牌嵌入外圈/下层，预览输出到 `page-gameplay-v3-mountain-side.png`。
- 侧视牌山迭代：山体调整为 7/6/5/4/3/2 六层金字塔，底层最宽、向上逐层收窄；可点正面牌沿左右坡面按 ±20° 斜置，略小于盖牌以呈现“躺在坡面上”的观感；像素校验显示山体范围约 y=412..1151、x=200..824，与顶部 HUD 区和 y=1330 手牌槽均无重叠。
- 俯视排山回归：Lee 否定侧视方案，要求回到俯视图并参考正式生成器的堆叠结构；已直接调用 `packages/shared` 的 `generateHulebuMountain`（center-tower 种子 t287-preview，48 张牌、6 层、7 个初始可点），把真实节点坐标导出后用独立麻将素材合成俯视预览；列柱堆叠、顶层露牌面、下层盖牌，牌阵约占牌桌 640×540，与 HUD 和手牌槽无重叠。
- 俯视模板对照：另按同一管线输出 two-wings、cross、ring 三种模板预览，文件名后缀对应模板，等待 Lee 选定主玩法默认轮廓。
- 牌山方向暂停：Lee 决定暂不考虑牌山，转向全量 UI 盘点；四种俯视模板与侧视方案全部保留在 previews 中，后续需要时再回来选型。
- UI 全量盘点：已生成 `component-pack-v3/previews/all-ui-inventory.png` 总览拼版，覆盖 51 个 UI 组件、35 张麻将牌和 4 张背景；对照 `images/ui-states-v6/` 十个流程状态完成查漏，缺口清单（组合候选面板、弃牌救场面板、开局流派卡、Boss 血条、通用确认/取消/关闭按钮、Toast、教程遮罩、加载页、结算明细面板）已提交 Lee 确认优先级。
- 缺口组件补齐：Lee 确认全部缺口继续补充后，已用 PPTOKEN 串行生成 12 张组件图（组合候选面板、弃牌救场面板、开局流派卡、Boss 血条、确认/取消/关闭/返回按钮、Toast 横幅、教程高亮框、加载页、结算明细面板），原始稿进入 `images/batch-gap/`，提示词进入 `prompts/`。
- 标准化结果：11 个透明组件经 `tools/normalize_gap_components.py` 裁边、缩放、居中后进入 `normalized/`，清单为 `gap-components-manifest.json`；loading-screen 属于全出血不透明背景，归档为 `backgrounds/images/loading.png`（1024×1536，与现有 4 张背景并列），不进入 normalized。
- 缺口预览：新增 `tools/build_gap_preview.py`，输出 12 格带标签拼版 `previews/gap-components-v3.png`，已完成目检；全部组件延续暖象牙陶瓷 + 薄荷绿厚度 + 珊瑚粉描边的既定风格，内容区不烘焙文字数字。等待 Lee 确认风格后，再考虑接入 Cocos。
