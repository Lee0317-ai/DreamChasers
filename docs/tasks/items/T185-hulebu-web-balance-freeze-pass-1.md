# T185：胡了卜 Web 数值平衡和内容冻结第一轮

- 任务编号：T185
- 优先级：P1
- 任务名称：胡了卜 Web 数值平衡和内容冻结第一轮
- 默认负责人：Lee
- 负责人：Lee
- 状态：进行中
- 依赖：T177, T178, T179, T180, T184
- 背景：T177-T184 已把 Boss 第二版、特殊事件第二版、成就图鉴扩容、无尽/每日深化和长期成长第二轮接进 Web 版，但这些系统当前仍更像“都已上线”，还没进入真正的内容冻结。最明显的节奏缺口是特殊事件第二版虽然已经有分层池、构筑联动和高阶专属事件，主线触发仍主要集中在前半段，导致后半程和长跑的变化感不够连续。
- 目标：
  - 把 Web 数值平衡和内容冻结正式开成任务，避免继续只停在路线图里。
  - 先做第一轮最小收口：扩展特殊事件触发节奏，让主线后半程、每日和高阶的中后段也能稳定看到事件变化。
  - 保持原型、站内静态 Demo、测试和文档口径一致。
  - 明确这只是 `数值冻结第一轮`，不把它误写成最终完整版平衡。
- 不做：
  - 不改 Cocos 正式工程。
  - 不补音乐、美术、动效和发布资源。
  - 不重做奖励 effect 底座或共享 JSON 配置。
  - 不改账号、Prisma、PDF、AI 修图、TimePick、部署或其他模块。
  - 不进入第二轮全量数值冻结。
- 允许修改文件：
  - `apps/game/mahjong-roguelike/prototypes/config-playable/index.html`
  - `apps/game/mahjong-roguelike/prototypes/config-playable/assets/ui-v6/**`
  - `apps/web/public/games/hulebu-demo/index.html`
  - `apps/web/public/games/hulebu-demo/assets/ui-v6/**`
  - `packages/shared/src/mahjong-config-playable-prototype.test.ts`
  - `apps/web/src/modules/games/hulebu/HulebuGamePage.tsx`
  - `apps/web/src/modules/games/hulebu/__tests__/hulebu-publish.test.ts`
  - `docs/tasks/CHANGE_INTAKE.md`
  - `docs/tasks/NEXT_ID.md`
  - `docs/tasks/items/T185-hulebu-web-balance-freeze-pass-1.md`
  - `docs/tasks/claims/T185-lee.md`
  - `docs/tasks/TASK_BOARD.md`
  - `docs/tasks/CLAIMS.md`
  - `docs/status/CURRENT_STATUS.md`
  - `docs/modules/mahjong-roguelike/**`
  - `docs/progress/2026-06-22-lee.md`
  - `docs/completion/**`
- 禁止修改文件：
  - `apps/game/mahjong-roguelike/cocos/**`
  - `apps/game/mahjong-roguelike/config/levels.json`
  - `apps/game/mahjong-roguelike/config/rewards.json`
  - `apps/web/prisma/**`
  - `apps/web/src/app/account/**`
  - `apps/web/src/lib/ai/**`
  - `apps/web/src/modules/tools/**`
  - `/Users/lee/Desktop/Lee/TimePick/**`
  - `deploy/**`
  - `docker-compose.yml`
  - `docker-compose.prod.yml`
- 验证方式：
  - `npm run test -w packages/shared -- mahjong-config-playable-prototype`
  - `npm run test -w apps/web -- hulebu`
  - `npm run typecheck -w apps/web`
  - `npm run build -w apps/web`
  - `perl -0ne 'print $1 if /<script>([\\s\\S]*?)<\\/script>/' apps/game/mahjong-roguelike/prototypes/config-playable/index.html > /tmp/hulebu-config-playable-inline.js && node --check /tmp/hulebu-config-playable-inline.js`
  - `perl -0ne 'print $1 if /<script>([\\s\\S]*?)<\\/script>/' apps/web/public/games/hulebu-demo/index.html > /tmp/hulebu-static-inline.js && node --check /tmp/hulebu-static-inline.js`
  - `npm run docs:sync`
  - `rg -n "T[B]D|T[O]DO|待[补]" docs/tasks/items/T185-hulebu-web-balance-freeze-pass-1.md docs/tasks/claims/T185-lee.md docs/modules/mahjong-roguelike/README.md docs/modules/mahjong-roguelike/PROGRESS.md docs/modules/mahjong-roguelike/HANDOFF.md docs/progress/2026-06-22-lee.md`
  - `git diff --check`
  - 浏览器桌面端和 390px 移动端检查 `/games/hulebu`
- 拆分子任务：
  - [x] 写测试锁定特殊事件触发会延伸到主线后半程。
  - [x] 改造原型与站内静态 Demo，同步新的事件触发节奏。
  - [x] 同步模块文档和当天进展，标记数值冻结第一轮已开工。
  - [x] 跑自动化验证、脚本检查、构建和浏览器检查。

- 当前进展：
  - 已把主线事件触发从 `6 / 8 / 10 / 13 / 16 / 19` 扩成 `6 / 8 / 10 / 12 / 14 / 16 / 18 / 20`，让后半程不再只剩三次固定露脸。
  - 已把每日事件触发同步扩成 `4 / 7 / 10 / 12 / 14 / 16 / 18 / 20`，避免每日后半程只在末尾再补一跳。
  - 已把高阶触发扩成 `5 / 8 / 11 / 13 / 15 / 17 / 19 / 20` 对应的关前节点，让中后段和终章前都还能继续接事件。
  - 已为主线后半段新增 `11 / 13 / 15 / 17 / 19` 五档事件偏置，分别把信息、顺手追胡、挡墙续压、牌尾收束和北风封尾感往后段推进。
  - 已继续把每日和高阶后半段事件口味拉开：每日新增 `12 / 14 / 16 / 18 / 20` 五档后段偏置，高阶新增 `11 / 13 / 15 / 17 / 19 / 20` 对应的后段偏置，让这两条线不只是“触发更多”，而是越往后越能读出信息线、续压线、封口线和终局封尾线。
  - 已把无尽长跑追加 `chapterIndex >= 5 && chapterStep === 3` 的额外事件点，让长期 run 后段从每章 3 次事件抬到 4 次。
  - 已继续细化无尽章内事件拍点：在保留章节主题偏置的基础上，再按 `chapterStep` 把事件压成 `起手稳河 -> 中段点火 -> 牌尾封口 -> 长章封尾` 四段节奏，避免长跑里“事件次数够了但脸谱还不够清楚”。
  - 已修正特殊事件触发位索引：无尽和每日不再偷用主线的 `SPECIAL_EVENT_TRIGGER_LEVELS` 索引，`第 23 层 / 第 25 层` 这类无尽事件位和每日前段事件位现在都会真正选出事件，不会再出现“该触发但拿不到事件”的空拍。
  - 已继续细化无尽奖励池：章节奖励不再只有 `chapter.theme` 一层偏置，而是按章内步点再压成 `稳河补台 -> 换气整手 -> 点火涨分 -> 牌尾收束 -> 终局封口` 的奖励节奏，让无尽里同章不同奖励节点也能读出拍点差异。
  - 已把终章前最后一档主线奖励从纯容错口味改成 `胡分 / 看山 / 工具` 的收官包，让第 19 关后的最后一次选奖更像“补最后一刀”，而不只是再拿一层拖命资源。
  - 已把每日和高阶第 20 档奖励偏置同步改成 `胡分 / 看山 / 工具`，减少终局前继续只发 `护符 / 备用槽` 这类纯缓冲项，让当天词缀和高阶 build 更容易在终局前真正落成。
  - 已把无尽后段奖励再往收官推一格：长跑 `chapterStep === 3` 的奖励从 `胡分 / 备用槽 / 铜钱` 改成 `胡分 / 看山 / 铜钱`，高章节第 `6 / 7` 相位的章节偏置也从单纯续命改成 `杠分 / 胡钱 / 看山` 与 `胡分 / 护符 / 整手`，后段不会一直重复“给盾给位子”。
  - 已继续把无尽第 `7` 章后的事件口味往终局封口推：章节第 `7` 相位不再回到 `补河 / 缓封`，而是改成 `尾灯留门 / 西风收口·锁门 / 北风封尾·断门`；同时新增 `chapter >= 7 && step === 4` 的长跑末拍偏置，让高章节每章最后一拍更稳定落到 `锁门 / 断门 / 封火` 这条终章线。
  - 已继续把后段奖励口味从“拖命”往“整手/兑现”推：无尽第 `4` 相位不再夹 `备用槽`，改成 `胡分 / 胡钱 / 工具包`；主线第 `13` 档从 `看山 / 护符 / 备用槽` 改成 `看山 / 工具包 / 胡钱`；每日第 `12` 档从 `看山 / 洗牌 / 护符` 改成 `看山 / 洗牌 / 工具包`；高阶第 `14` 档从 `备用槽 / 护符 / 工具补给` 改成 `胡钱 / 工具包 / 洗牌`，让后半段更像整手和收口，而不是继续补前段容错件。
  - 已继续把后段路线奖励的味道拉开：`胡流` 后段更偏终口兑现，`信息流` 更偏读口转化，`河控流` 更偏控河转压，`道具流` 更偏整理节奏，`救场流` 则回到补位续命；无尽 / 每日 / 高阶三条线各自的后段奖励不再只是同一锅料换顺序。
  - 已继续收主线后半程硬压：第 `17 / 18 / 19` 关牌量从 `336 / 348 / 360` 收到 `330 / 340 / 348`，同时把字牌权重和孤张预算各回落一格，让终章前的压力更像“持续绷紧”而不是最后三关一起爆表。
  - 已继续收终章 Boss 的“全都要”负担：`胡了卜王` 仍保留复合目标，但把 `碰 2` 收到 `碰 1`、积分目标从 `180` 收到 `170`，保留终章构筑检查，不再把最后一口硬挤成纯罚站。
  - 已继续收无尽后段 Boss 的层层叠压：后段章节 Boss 记分线再从 `170 + 章节 * 20` 收到 `168 + 章节 * 18`，并且第 8 章这类自带 `胡 / 杠` 章节不再额外叠第二层 `杠 2`；`吃` 额外检查也只留在第 7 相位，不再把第 8 相位终章感做成 `碰 / 吃 / 杠 / 胡 + 高分` 一起上。
  - 已继续收每日终局 Boss 的附加负担：每日第 20 关现在仍保留词缀口味和终章复合目标，但 `碰` 目标压回 `1`，得分目标上限再从 `194` 压到 `190`，避免“今日词缀”把终章 Boss 直接再抬成比主线终章更硬一档。
  - 已继续收主线终章前两档硬压：第 `18 / 19` 关牌量再从 `340 / 348` 收到 `336 / 344`，字牌权重从 `70 / 76` 收到 `68 / 72`，孤张预算从 `9 / 10` 收到 `8 / 9`，让终章门前和终章本体之间仍有压迫差，但不再像两档一起顶满。
  - 已继续收每日终局的“词缀又加题”问题：第 20 关现在只会再挂 `1` 个每日词缀重点组合，不再默认叠到 `2` 个，让当天主题仍存在，但不会把终局变成“基础 Boss + 词缀双考卷”。
  - 已继续收无尽第 `7 / 8` 相位的机械补题：章节主题已经自带双目标时，不再额外挂通用 `碰 1`；后段守层改成章节主题自己发力，而不是每档都再往上叠一层通用检查。
  - 已继续收无尽后段分数线：章节 Boss 记分线再从 `168 + 章节 * 18` 收到 `166 + 章节 * 17`，后段仍然逼分，但不会和章节主题、胡/杠目标一起把终局压得太死。
  - 已再收主线终章前三关硬压：第 `17 / 18 / 19` 关当前改成 `328 / 334 / 340` 张，字牌权重收成 `62 / 66 / 70`，孤张预算收成 `7 / 8 / 8`，让终章门前仍紧，但不会连续三关一起顶满。
  - 已再收主线和每日终局 Boss：主线 `胡了卜王` 现在把 `杠` 目标也压回 `1`，得分线改到 `166`；每日第 20 关的最终得分线再压到 `186` 以内，减少“当天词缀 + 终章考卷”叠压。
  - 已继续收每日终局的词缀附加题口径：第 20 关现在只会优先把词缀里的 `胡 / 杠` 终局型组合带进 Boss，加不出终局型组合时就不再额外挂题，避免 `碰 / 吃` 这种偏前中段的日词缀把终章最后一拍重新拉散。
  - 已把每日第 `16` 档后段奖励从纯容错包改成 `胡钱 / 整手 / 工具` 过渡包，让每日后段先进入整手收口，而不是又回去补位补盾。
  - 已再收无尽高章节守层分数：后段章节 Boss 记分线改成 `164 + 章节 * 16`，保留长跑逼分，但减掉第 `7 / 8` 章那种主题目标之外的机械加码。
  - 已把朋友试玩后半程主线压力正式对齐到当前冻结口径：第 `17 / 18 / 19` 关现为 `328 / 334 / 340` 张、字牌权重 `62 / 66 / 70`、孤张预算 `7 / 8 / 8`；原型、站内静态 Demo 与共享回归断言已同步。
  - 已继续把无尽第 `7 / 8` 章节主题奖励改成更像回账/封口包：第 `7` 章从 `备用槽 / 护符 / 胡钱 / 整手` 改成 `胡钱 / 杠分 / 整手 / 丢弃`，第 `8` 章从 `胡分 / 杠分 / 护符 / 看山` 改成 `胡分 / 杠分 / 胡钱 / 看山`，减少高章节里继续发纯续命件的比例。
  - 已把主线第 `19` 关固定路线奖励也往终章兑现再推一格：最后一档不再混 `碰分`，改成 `杠分 / 胡钱 / 看山`，让终章前最后一次选奖更明确服务收官而不是半途补贴。
  - 已继续把晚段奖励从“拖命”往“落成 build”推：无尽第 `4` 章主题奖励从 `胡分 / 胡钱 / 备用槽` 收成 `胡分 / 胡钱 / 工具包`，让牌尾收束章节也开始更像整手收口而不是继续补位。
  - 已继续收路线型后段奖励里的防守件：无尽和每日的 `河控流` 晚段奖励从 `杠分 / 吃道具 / 护符` 收成 `杠分 / 吃道具 / 胡钱`，主线 `救场流` 晚段奖励从 `备用槽 / 护符 / 吃道具` 收成 `备用槽 / 护符 / 丢弃`，让后段救场也更像补最后一刀。
  - 已把终章前后的晚段拍点继续往“锁门收口”推：主线第 `19` 关、每日第 `20` 关和高阶第 `19` 档的后段事件从 `探牌 / 试口` 收成 `锁门 / 封尾`；无尽第 `7` 章的回账主题奖励也把 `洗牌` 收成 `工具包`，让长跑后段更像回账落成，不再继续发信息过渡件。
  - 已把每日后段两档主题词缀也收成更明确的终局 build：`挡墙续押` 从 `碰/杠 + 信息件` 收成 `胡/杠 + 胡钱/杠分/工具包`，`封尾落锁` 从 `胡/碰 + 丢弃补刀` 收成 `胡/杠 + 胡分/杠分/工具包`，并把两档事件都压向 `锁门 / 封尾 / 终煤` 这条终局线。
  - 已继续把终章前最后一档与无尽最高章节最后一拍的奖励都从“继续给信息”收成“直接帮你落成”：主线第 `19` 档从 `杠分 / 胡钱 / 看山` 改成 `杠分 / 胡钱 / 工具包`，无尽第 `8` 相位从 `胡分 / 杠分 / 看山` 改成 `胡分 / 杠分 / 工具包`，后段最后一次选奖更像整理手牌、把 build 兑现出来，而不是再补观察件。
  - 已继续把主线和高阶的晚段奖励往“兑现 build”推一格：主线 `18+` 新增 `胡分 / 杠分 / 工具包` 的收官偏置，高阶第 `16 / 18` 档也从 `看山` 改成 `工具包`，让后段更像整理手牌和封口，而不是继续补信息件。
  - 已继续把每日终章前一拍也收进同一口径：每日第 `18` 档从 `胡分 / 杠分 / 看山` 改成 `胡分 / 杠分 / 工具包`，这样第 `18 / 20` 两档都会更像整理手牌和封口，不再在终章前再补一口信息件。
  - 失败链路维持最小口径：失败不进入 `/games/hulebu` 外层结算，只保留 iframe 内的 `失败 / 重开` 弹框；原型、静态 Demo 和共享测试都按这个口径锁定。
  - 已把 iframe 内失败弹框的视觉也继续收紧成小尺寸单按钮口径，避免后续又在失败态里塞回解释文案或复盘段落。
  - 已继续把“解释感”从结算层剥掉：原型和站内静态 Demo 的通关弹层不再追加 `Boss 复盘` 卡，`/games/hulebu` 外层结算也只保留核心结果指标与摘要，不再顺手讲路线、词缀、Boss 或周目说明。
  - 已继续把局外首页也收回游戏入口口径：`/games/hulebu` 的主线 / 无尽 / 每日面板已删除长段“导向 / 节奏 / 最后一刀”说明，只保留开局按钮、核心状态和简短预览，避免用户一回外层就被解释性内容淹住。
  - 已补旧存档兜底：`/games/hulebu` 外层现在不会再把历史 `failed` settlement 强行归一成完成结算；浏览器里即使还留着旧失败记录，刷新后也只会回到局外或牌桌内的简短失败弹框。
  - 已补旧失败调试参数兜底：如果本地还开着更早版本留下的 `?debugSettlement=...failure...` 链接，`/games/hulebu` 外层加载时会主动清掉该参数并回到正常局外态，不再把历史失败样例误当成现在的真实页面。
  - 已把 Boss 结算 payload 继续收成最小集：原型、站内静态 Demo 和外层类型现在只再保留 `bossVariant` 这类成就解锁真正会用到的标记，不再顺带携带 `phaseTarget / keyGoal / keyMiss / nextAdvice / mismatch` 这些解释性字段。
  - 已把 `hulebu-publish` 里的两条过期断言同步到当前内容口径：一条不再要求结果层保留 `本轮路线`，另一条把高阶后段事件偏置阈值从旧的 `10` 校正到当前实际使用的 `11`，现在 `npm run test -w apps/web -- hulebu` 已重新通过。
  - 已把原型和静态 Demo 的通关摘要也压成结果读数口径：主线 / 每日 / 高阶现在统一显示成 `关数 + 铜钱 + 奖励` 的短摘要，不再写成长句叙述；对应发布测试已改为校验 `dailyParticipationStreak / dailyStreak` 信号而不是旧的 `连续参与` 文案。
  - 已同步修正共享原型测试中的过期快照：朋友试玩后半程牌量/字牌权重断言、每日终局 `186` 封顶、无尽后段 `164 + 章节 * 16` 守层分数线都已对齐到当前代码。
  - 已完成本轮 scoped 验证：`npm run test -w packages/shared -- mahjong-config-playable-prototype`、`npm run test -w apps/web -- hulebu`、两份内联脚本 `node --check`、`git diff --check` 通过；桌面 1440 宽和移动 390 宽页面检查中均未出现外层失败结算，`继续本轮` 在未开局时保持禁用。
  - 已继续把 T186 的主线结构同步进运行时代码：外层首页把 `局内流派` 抬成每局开局前的主轴，把 `路线挂载` 收成 `局外偏好`；内层原型和站内静态 Demo 也开始由 `runArchetype` 主导开局加成、奖励/事件偏置和终章 Boss 的本局打法口径，`preferredRoute / routeMasteryLevel` 只保留轻协同。
  - 已继续压低 `局外偏好` 的运行时权重：当前局外偏好只再轻补一点起手、事件和奖励，不再像旧版本那样把本局主轴重新拉回跨关路线；`runArchetype` 现在才是主线、无尽、每日和高阶开局前真正决定这局打法的入口。
  - 已把 T181 v6 透明 UI 资源嵌入 Web Demo 预览链路：原型源文件和站内静态副本都新增 `assets/ui-v6/`，牌桌、槽位、牌河、组合按钮和工具按钮开始加载真实麻将牌面与透明按钮切图；文字牌面保留为图片加载失败兜底，方便在 `/games/hulebu` 直接看 UI 效果。
  - 已根据右侧浏览器截图继续修 UI 接入问题：`胡` 不再误用红色 `杠` 图，改成独立金色文字按钮；记牌器改成每格上方真实麻将小牌、下方数量；候选组合改成牌面组合预览；卡槽改为单格槽位并让牌面居中进入槽格，避免继续显示裸数字或牌面压在槽外。
  - 已按现有组合按钮风格补齐 `胡` 的普通态和点燃态 UI 资源：`action_hu_normal.png` / `action_hu_fire.png`；运行时 `COMBO_BUTTON_ASSETS.hu` 已改为 `action_hu`，右侧浏览器检查确认胡按钮加载独立资源，不再使用文字兜底或误用杠按钮图。
  - 已继续清理 v6 UI 接入后的视觉残留：图片按钮不再叠 DOM 文字和数字角标，右侧工具按钮改成无文字无红圈的纯图标，记牌器和候选组合去掉额外底色边框，v6 麻将牌关闭额外阴影和堆叠伪元素，减少牌下灰杠与右上角圆点干扰。
  - 已修正牌山模式下旧 `.board[data-mode="mountain"] .tile` 阴影规则盖过 v6 透明牌的问题，开局牌和震落牌不再带黄色/灰色底影；同时把 play-view 记牌器压成 18px 小牌格，整体高度约减半。
  - 已继续补牌山特殊态兜底：`stack-preview / stack-bridge / loose-mountain` 在 v6 牌面下也强制透明无阴影，避免桥牌、震落牌或散牌继续漏出旧黄色底。
  - 已按右侧浏览器反馈把洗牌、撤回、提示工具按钮恢复为 v6 原包完整按钮图，保留原来的牌匾质感；页面仍隐藏 DOM 叠加文字，避免重复显示。
  - 已修正 v6 Web 牌面资源里的 `6条` 错牌：原 `tile_bamboo_06.png` 实际只显示 4 根条，现已用同风格牌体和条子元件重合成 6 根条，并同步到 Web 静态 Demo 和原型资源目录。
