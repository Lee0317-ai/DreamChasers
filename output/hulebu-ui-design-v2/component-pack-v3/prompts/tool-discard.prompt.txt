Use the supplied shuffle button as the exact visual shell reference. Change only the center icon to a simple jade-green discard symbol: one small Mahjong tile moving downward into a shallow tray. Preserve the exact outer shell, dimensions, corner radius, ivory face, coral-pink rim, mint-green side thickness, lighting, camera angle, padding, and transparent background. Do not add decorative corner ornaments, inner frames, badges, or extra objects. The final asset must look like the same button family with only the icon replaced.
