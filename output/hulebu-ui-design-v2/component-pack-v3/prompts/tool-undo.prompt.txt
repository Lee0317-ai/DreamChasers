Use the supplied shuffle button as the exact visual shell reference. Change only the center icon to one thick curved counterclockwise undo arrow. Preserve the exact outer shell, dimensions, corner radius, ivory face, coral-pink rim, mint-green side thickness, lighting, camera angle, padding, and transparent background. Do not add any decorative corners, frames, badges, shadows, or extra objects. The final asset must look like the same button family with only the icon replaced.
